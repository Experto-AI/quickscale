name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  lint-frontend:
    name: Lint Rendered Frontend (SA103)
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Node.js
      uses: actions/setup-node@v6
      with:
        node-version: "24"

    - name: Install pnpm
      uses: pnpm/action-setup@v5
      with:
        version: 11.0.9

    - name: Get pnpm store directory
      shell: bash
      run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

    - name: Setup pnpm cache
      uses: actions/cache@v5
      with:
        path: ${{ env.STORE_PATH }}
        key: ${{ runner.os }}-pnpm-store-${{ hashFiles('quickscale_core/src/quickscale_core/generator/templates/themes/showcase_react/package.json.j2') }}
        restore-keys: |
          ${{ runner.os }}-pnpm-store-

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Run rendered frontend lint
      run: |
        make lint-frontend

  backups-validation:
    name: Fail Fast Backups PG18 Contract
    runs-on: ubuntu-24.04
    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_HOST_AUTH_METHOD: trust
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Provision hosted PostgreSQL backups profile
      run: scripts/provision_ci_postgres.sh hosted-setup --profile backups

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Run backups module validation tests
      run: |
        PYTHONPATH="quickscale_modules/backups:quickscale_modules/backups/src" \
          poetry run pytest quickscale_modules/backups/tests -q --no-cov \
            -p pytest_django --ds=tests.settings

  module-manifest-contract:
    name: Fail Fast Ready Module Contract
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Run ready-module manifest contract tests
      run: |
        poetry run pytest quickscale_cli/tests/test_module_manifest_contract.py -q

  # BEGIN GENERATED: hosted-gate-jobs
  module-core-compat:
    name: Module-vs-Oldest-Core Compatibility
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Run module-vs-core compatibility check (static analysis + install/import probe)
      run: |
        make check-core-compat

  module-core-import-linter:
    name: Module-Core Import Linter
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Run module-core import linter
      run: |
        make check-module-core-imports

  manifest-sync-gate:
    name: Manifest Sync Gate (SA16.1)
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Verify module manifests match core snapshots
      run: |
        make check-manifest-sync

  module-app-declaration-gate:
    name: Module App Declaration Gate (SA167c)
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Verify evidence-bearing modules declare Django apps wiring
      run: |
        make check-module-app-declaration

  org-context-primitives-gate:
    name: Org-Context Primitives Gate (SA13.4)
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Verify no external use of privatized org-context primitives
      run: |
        make check-org-context-primitives

  csrf-exempt-gate:
    name: CSRF-Exempt Gate (SA46)
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Verify every csrf_exempt callsite pairs with _enforce_csrf or signature verification
      run: |
        make check-csrf-exempt

  commit-testimony-gate:
    name: Behavioural Commit Testimony Gate (SA166)
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      with:
        fetch-depth: 0

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Verify behavioural commits carry a vNN roadmap reference
      run: |
        make check-commit-testimony

  check-gate-suites:
    name: Registered Script Gate Suites
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Run registered scripts test suites
      run: |
        make check-gate-suites

  dependency-vulnerabilities-gate:
    name: Dependency Vulnerability Gate (SA123)
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Run dependency vulnerability gate
      run: |
        make check-dependency-vulnerabilities

  security-static-analysis-gate:
    name: Security Static Analysis Gate (SA123)
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Run security static-analysis gate
      run: |
        make check-security-static-analysis
  # END GENERATED: hosted-gate-jobs

  test:
    name: Run Tests (Unit + Integration)
    # BEGIN GENERATED: needs-test
    needs: [backups-validation, module-manifest-contract, module-core-compat, module-core-import-linter, manifest-sync-gate, module-app-declaration-gate, org-context-primitives-gate, csrf-exempt-gate, commit-testimony-gate, check-gate-suites, dependency-vulnerabilities-gate, security-static-analysis-gate]
    # END GENERATED: needs-test
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_HOST_AUTH_METHOD: trust
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    strategy:
      matrix:
        python-version: ["3.14"]

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install Poetry
      id: install-poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true
      # Retry on SSL/network failures
      continue-on-error: true

    - name: Retry Poetry install (if failed)
      if: steps.install-poetry.outcome == 'failure'
      run: |
        for i in {1..3}; do
          curl -sSL https://install.python-poetry.org | python3 - && break || sleep 5
        done
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Cache Poetry dependencies
      uses: actions/cache@v5
      with:
        path: |
          ~/.cache/pypoetry
          .venv
        key: ${{ runner.os }}-poetry-${{ hashFiles('poetry.lock') }}
        restore-keys: |
          ${{ runner.os }}-poetry-

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Run gate parity check
      run: |
        make check-gate-parity

    - name: Check for stale sub-package venvs
      run: |
        # Fail if any sub-package has a local .venv (should use root venv)
        # This prevents version drift between packages
        STALE_VENVS=$(find quickscale_core quickscale_cli quickscale quickscale_modules -maxdepth 2 -type d -name ".venv" 2>/dev/null || true)
        if [ -n "$STALE_VENVS" ]; then
          echo "ERROR: Found stale .venv directories in sub-packages:"
          echo "$STALE_VENVS"
          echo ""
          echo "Sub-packages must use the root monorepo venv to prevent version drift."
          echo "Delete these directories and use 'poetry install' from root instead."
          exit 1
        fi
        echo "✓ No stale .venv directories found"

    - name: Run linting (ruff)
      run: |
        make lint -- --core --cli --modules --devtools

    - name: Run worker pool harness tests (no PostgreSQL needed)
      run: |
        make test-integration-worker-pool

    - name: Run local CI parallelism regression tests (no PostgreSQL needed)
      run: |
        make test-ci-local-parallel

    - name: Run type checking (mypy)
      run: |
        make typecheck -- --core --cli --modules --devtools

    - name: Provision hosted PostgreSQL restricted profile
      run: scripts/provision_ci_postgres.sh hosted-setup --profile restricted

    - name: Run unit tests (core + CLI, DB-free)
      run: |
        make test-unit SECTION="core cli"

    - name: Run integration tests (modules, PostgreSQL NOBYPASSRLS gate)
      run: |
        ./scripts/test_integration.sh

    - name: Upload coverage reports
      uses: codecov/codecov-action@v5
      if: always() && github.ref == 'refs/heads/main'
      with:
        files: ./quickscale_core/coverage.xml,./quickscale_cli/coverage.xml,./quickscale_modules/*/coverage.xml
        flags: unittests,integration
        name: codecov-umbrella
      env:
        CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
      continue-on-error: true

  isolation-conformance:
    name: Isolation Conformance (AF10)
    # BEGIN GENERATED: needs-isolation-conformance
    needs: [backups-validation, module-manifest-contract, manifest-sync-gate, org-context-primitives-gate, csrf-exempt-gate, check-gate-suites]
    # END GENERATED: needs-isolation-conformance
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_HOST_AUTH_METHOD: trust
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      id: install-poetry-iso
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true
      continue-on-error: true

    - name: Retry Poetry install (if failed)
      if: steps.install-poetry-iso.outcome == 'failure'
      run: |
        for i in {1..3}; do
          curl -sSL https://install.python-poetry.org | python3 - && break || sleep 5
        done
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Cache Poetry dependencies
      uses: actions/cache@v5
      with:
        path: |
          ~/.cache/pypoetry
          .venv
        key: ${{ runner.os }}-poetry-${{ hashFiles('poetry.lock') }}
        restore-keys: |
          ${{ runner.os }}-poetry-

    - name: Install dependencies
      run: |
        poetry install --with dev

    - name: Provision hosted PostgreSQL isolation profile
      run: scripts/provision_ci_postgres.sh hosted-setup --profile isolation

    - name: Run isolation-conformance suite
      run: |
        make isolation-conformance

  lint-cli:
    name: Lint CLI Package
    # BEGIN GENERATED: needs-lint-cli
    needs: [backups-validation, module-manifest-contract, manifest-sync-gate, org-context-primitives-gate, csrf-exempt-gate, check-gate-suites]
    # END GENERATED: needs-lint-cli
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python 3.14
      uses: actions/setup-python@v6
      with:
        python-version: "3.14"

    - name: Install Poetry
      id: install-poetry-cli
      uses: snok/install-poetry@v1
      # Retry on SSL/network failures
      continue-on-error: true

    - name: Retry Poetry install (if failed)
      if: steps.install-poetry-cli.outcome == 'failure'
      run: |
        for i in {1..3}; do
          curl -sSL https://install.python-poetry.org | python3 - && break || sleep 5
        done
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Install dependencies
      run: poetry install --with dev

    - name: Run linting
      run: |
        make lint -- --cli --devtools

    - name: Run type checking
      run: |
        make typecheck -- --cli --devtools
