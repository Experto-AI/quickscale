platform: opencode
supports:
  directives:
    invoke_skill: reference
    invoke_agent: reference
    invoke_workflow: reference
  contract_fields:
    mode: false
    inputs: false
    outputs: false
    success_when: false
  commands: markdown
