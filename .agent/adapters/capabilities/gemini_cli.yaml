platform: gemini_cli
supports:
  directives:
    invoke_skill: import
    invoke_agent: import
    invoke_workflow: import
  contract_fields:
    mode: false
    inputs: false
    outputs: false
    success_when: false
  commands: toml
  agents: markdown_imports
