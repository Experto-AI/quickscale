platform: gemini_cli
verified_on: "2026-02-07"
tier: verified
supports:
  directives:
    invoke_skill: import
    invoke_agent: import
    invoke_workflow: import
  contract_fields:
    mode: false
    inputs: false
    outputs: false
    success_when: false
  commands: toml_prompt
  agents: markdown_local_agents
  settings: json
