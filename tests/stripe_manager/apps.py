from django.apps import AppConfig


class StripeManagerConfig(AppConfig):
    """Configuration for stripe_manager app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'stripe_manager' 