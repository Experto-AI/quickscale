# Poetry local configuration for quickscale-module-orgs
# Standalone lock/install is unsupported — use root monorepo venv instead.
# See docs/technical/development.md for architecture details.

[virtualenvs]
create = false
