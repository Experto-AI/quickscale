# Poetry local configuration for quickscale_cli
# This prevents creation of local .venv - use root monorepo venv instead
# See docs/technical/development.md for architecture details

[virtualenvs]
create = false
