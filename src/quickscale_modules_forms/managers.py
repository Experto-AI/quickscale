"""Forms module manager contract.

T1.7: delegates to the shared ``quickscale_modules_orgs.managers.TenantManager``.
Module-local ``TenantScopedManager``/``OperatorManager`` classes are removed.
See ``quickscale_modules_orgs.managers.TenantManager`` for the canonical
implementation.
"""
