# Changelog

`CHANGELOG.md` is the canonical QuickScale release history index. Published releases pair each version entry with a single official release note in `docs/releases/` linked from the GitHub tag and release PR. When a release note is prepared before the maintainer completes the manual tag/publish step, the changelog entry and note must say so explicitly and must not imply publication. Use `docs/technical/roadmap.md` for active or unpublished release status. Entries are version-ordered.

- v0.87.0 — unreleased / not yet tagged — Hardening release. Consolidates a large hardening pass across tenant isolation, module configuration, the generator, and disaster recovery.
  - **SA117b plan-review-cap checkpoint (2026-07-28; documentation only; task remains open).** **Done:** Track 3 was clean and synced to `v87` at `76462b13`; the Poetry environment, Docker daemon, and GitHub origin were ready; focused discovery mapped the publication, verifier, Compose-owner, shared-Git, and caller-parity seams. Two bounded review-driven re-plan cycles resolved `SA117B-PLAN-001` through `SA117B-PLAN-004` at plan-detail level: exclusive verifier-owned Compose identity with signal/timeout cleanup, publication-only trusted Git/NUL controls, five-consumer caller parity, and exact reviewed base/tip merge identity. **Pending / blocking:** `SA117B-PLAN-005` (**medium**, completeness) requires Ruff check, Ruff format-check, and repository type-check gates before executable review, after resynchronization, and post-merge. The task findings `SA117-CR-003`, `SA117-CONT-PR-003`, `SA117-CONT-PR-006`, and absorbed `SA117A-CR-002` remain unimplemented. **Decisions needed:** none. The plan-review cap triggered recorded partial delivery; no source/test file changed, no task validation or executable review ran, SA117b remains unchecked, SA117c remains blocked, and no split push or publication is authorized.
  - **SA117a third plan-cap checkpoint (2026-07-28; documentation only; task remains open).** **Done:** Track 3 was clean and synced to `v87` at `fc8a0474`; Poetry 2.4.1, Python 3.13.14, GNU Make 4.3, and the worktree `.venv` were ready; focused discovery confirmed the sole source target; and two bounded planner/reviewer cycles made the normal implementation, validation, staging, commit, rollback-ref, abort, reset, and disarm paths literal. `SA117A-PLAN-014` is resolved at plan-detail level, and the proposed `SA117A-CR-002` spy target is correct. **Pending / blocking:** `SA117A-PLAN-010` (high) requires the immutable accepted post-sync Track 3 tip to be bound to the current full-scope Review B and consumed unchanged by merge-back; `SA117A-PLAN-013` (medium) requires a copyable shared-document conflict sequence that verifies the recorded unmerged set before staging, emptiness after staging, and reruns affected roadmap/changelog invariants; `SA117A-CR-002` (medium) remains unimplemented. **Decisions needed:** none. The plan-review cap triggered recorded partial delivery; no source or test file changed, no task validation ran, SA117a remains unchecked, and SA117b remains blocked.
  - **`SA117-DEC-005` — SA117a closed, residue folded into SA117b, gates matched to change size (2026-07-28; documentation only).** **Why.** SA117a's load-bearing content — the exact canonical lockstep comparison closing `SA117-CR-001` — was verified present in code: the private lockstep-only canonical parser in `quickscale_core/src/quickscale_core/manifest/loader.py`, and all twelve source plus all twelve bundled `module.yml` versions equal to `VERSION` `0.87.0`. That is the entirety of what SA112b's evidence validity depends on. After `SA126-DEC-001` removed its complexity share, SA117a's remaining scope was **one `assert_not_called()` spy in one existing test** (`SA117A-CR-002`) — which nonetheless consumed **one change-review cap, three plan-review caps, and a full-scope documentation review certifying a two-document checkpoint that implemented nothing: five review artifacts, zero lines.** **Actions.** (1) **SA117a closed**; the SA117 chain is now SA117b → SA117c → SA117e, and **SA117b is Track 3's next action** with `deps: none`. (2) **`SA117A-CR-002` absorbed into SA117b with its literal diff pre-written** — target `quickscale_cli/tests/commands/test_module_commands.py`, test `test_update_rolls_back_noncanonical_manifest_version` (`:1560`), inserting a third `patch(...) as spy_sync_deps` after the existing `spy_commit` patch (`:1632-1634`) and `spy_sync_deps.assert_not_called()` after `spy_sync_state.assert_not_called()` (`:1651`), in the identical shape as the two spies that block already uses. The assertion holds because `_read_embedded_module_version` (`module_commands.py:1302`) raises before `_sync_module_dependencies` (`:1318`); a non-tautology check is specified. This makes the remaining work transcription rather than planning. (3) **`SA117A-PLAN-010` and `SA117A-PLAN-013` retired as ticket blockers** — they described reviewed-tip identity and shared-document conflict reconciliation, generic merge ceremony applying to every child rather than to SA117a's content, and are restated once as standing merge-procedure guidance in the handoff contract. (4) **Proportionality rule added**, binding SA117b–SA117e: a child whose delta is confined to test assertions or documentation, with no production-behavior change, **requires no plan-review cycle of its own** — implementation plus one change review; scoped plan review is reserved for production behavior, security boundaries, and external/public surfaces. **Splitting SA117a further was explicitly rejected** as the wrong remedy — the ticket was already two lines, and additional handoffs were the cost rather than the cure; this is the inverse of `SA126-DEC-001`, where splitting was correct because it separated two genuinely different concerns. **Net effect on the critical path:** the chain loses a node and its head becomes real production work (`SA117-CR-003` security, `SA117-CONT-PR-003` resilience, `SA117-CONT-PR-006` completeness) instead of a stalled test assertion. Every reference re-pointed: open-work list and entry 6, the SA117 umbrella and its split note, the handoff contract, SA117b's tag/deps/body, SA117e's deps, the dependency diagram, the critical-path chains, the parallelism and cross-track paragraphs, the Track 3 readiness bullets and detail bullet, the Net, and Standing decisions. No source files touched; SA117b remains unchecked.
  - **Roadmap cleaning pass (2026-07-28, fifth pass of the day; documentation only).** **Closeout transfer:** the completed **SA126** block was removed from `docs/technical/roadmap.md` — it was checked `[x]` with full completion evidence and its closeout entry already exists below, so per the roadmap's own rule it no longer belongs in the open-work file. Every reference was re-pointed: the `SA117-QG-REMED` retirement note, the Track 1 header and ordering list (now **SA121 → SA122a**, with the now-obsolete single-owner exception note deleted), the green-gate status block, the dependency diagram, the parallelism bullets, the cross-track-edge paragraph, the track-readiness bullets, and the Net. **No audit finding became closeable:** arch Findings 11, 12, 7, 2, 4 remain open (12 stays open precisely because SA121 has not landed), and `tech-audit.md` still carries zero open findings. **Track assignment complete; no gaps.** **Two verified state changes.** (1) `make quality` was **rerun directly and exits 0** with 0 warning and 0 critical baseline regressions; `radon cc` confirms SA126's closeout at `_perform_module_embed` **18** (ceiling 20) and `_update_single_module` **13** (ceiling 17); `QUARANTINE_TICKETS` verified **empty**. This cleared `SA121-QG-001`, making **Track 1 truly green for the first time in this release train** (start/finish/merge all yes). (2) **SA117a took a fourth review cap with still zero source delta** — one change-review plus three plan-review caps — for a ticket now scoped to a single `assert_not_called()` spy in one test file, with both surviving findings (`SA117A-PLAN-010`, `SA117A-PLAN-013`) concerning merge/staging procedure rather than the test. A process warning was added to the Track 3 readiness bullet: the three states are all YES, so nothing blocks it, but a fifth cap should be read as the plan-gate process rather than engineering being the critical-path constraint. **Green-gate milestone remains unclaimed, and the roadmap now says why precisely:** the remaining gap is *evidence, not defects* — only `make quality` was rerun; `make ci` and `make ci-e2e` need exclusive PostgreSQL/Docker, are Track 3's to schedule, and **no open ticket owns the four-command rerun** (it is SA112f's closeout duty and SA96-PUBLISH's precondition). **Rebalancing: one open decision, `SA112A-TRACK-002`** — move SA112a onto Track 1, which needs **no fourth worktree** and so differs materially from the permanently-declined `SA112A-TRACK-001`. It is newly worth asking because SA126 closed and SA117a stalled again: Track 1's entire remaining queue is off the critical path while Track 3 is stuck at the critical-path head. Independence checks pass (deps satisfied, no SA117e bound before SA112b, three-file allowlist disjoint from SA121/SA122a and the whole SA117 chain, service-free validation, self-contained seam extraction). **Not applied, because it delays truly-green SA121** — which closes arch Finding 12 — so it is the maintainer's call, with the honest counter-argument recorded that SA117a's four caps were all plan-review failures, so a second plan-gated ticket may yield two stalled tickets rather than one. Conflict surface if taken is the shared closeout files only, covered by the Merge procedure. **Tier census: no open ticket is Tier 3.** No source files touched.
  - **SA126 complete — module-command complexity remediation (2026-07-28).** Functional commit `60c5d4f3` reduced `_perform_module_embed` to CC **18** (from 22; ceiling 20) and `_update_single_module` to CC **13** (from 18; ceiling 17). **102 focused tests passed**; `make quality`, `make lint`, `make typecheck`, and `git diff --check` all exited 0, with no tests or ceilings changed. Independent functional review returned **STATUS: ok** with exact behavior/message/caller parity and **no new findings/blockers**. Existing repository-wide quality warnings and MyPy informational notes were non-regressing and non-blocking. The roadmap checkbox is checked; **SA121 is now the next pending Track 1 task**. The final docs-inclusive review runs after this edit and is not claimed here.
  - **SA117a second plan-cap checkpoint (2026-07-28; documentation only; task remains open).** **Done:** Track 3 was clean, fast-forwarded to `v87` at `24919792`, and its Poetry environment was ready; two bounded planner/reviewer cycles resolved the original synchronization-transaction design gap (`SA117A-CONT-PR-008` / `SA117A-PLAN-011`) and specified the eight-case cleanup parity matrix (`SA117A-PLAN-012`). **Pending / blocking:** final review staging remains incomplete (`SA117A-PLAN-010`, high), validation/final-stage commands are not uniformly fail-fast and literal (`SA117A-PLAN-013`, medium), and merge-back lacks an armed rollback transaction (`SA117A-PLAN-014`, medium); source blockers `SA117A-CR-002` and `_perform_module_embed` CC 22 > 20 therefore remain untouched. **Decisions needed:** none — the maintainer selected recorded partial delivery at the cap. No source or test file changed, no implementation or completion evidence landed, SA117a remains unchecked, and SA117b remains blocked.
  - **Ticket split for parallelism — SA126 created, `SA112A-TRACK-001` resolved (2026-07-28, fifth pass of the day; documentation only).** **Tier census: no open ticket is Tier 3.** SA126, SA112e, and SA96-PUBLISH are Tier 1; every other open ticket is Tier 2. Both historical Tier 3s were already split (SA117 → SA117a/b/c/e by `SA117-DEC-003`; SA122 → SA122a/SA122b). **`SA112A-TRACK-001` resolved: the repository holds at three worktrees.** No Track 4 is created and SA112a stays on Track 3 — a second concurrent review lane is not worth one critical-path node while SA117a is still converging. Parallelism was obtained by splitting a ticket instead. **`SA126-DEC-001` — new Tier 1 ticket `SA126` on Track 1**, holding the two `make quality` blockers extracted from SA117a and SA117b: `_perform_module_embed` (CC 22 > 20) and `_update_single_module` (CC 18 > 17). The split was made on empirical rather than declared-Tier grounds — SA117a was declared Tier 2 but had produced three review caps (one change-review, two plan-review) with zero source delta while bundling a test-only assertion with a production refactor in a different file. **Both blocking functions live in the same file**, `quickscale_cli/src/quickscale_cli/commands/module_commands.py`, so assigning one to SA117a and the other to SA117b was itself the sole cause of that file being shared between two serial critical-path children; one owner **removes** a conflict surface rather than adding one. **Effects:** Track 1 moves from *can-finish: NO* to *can-finish: YES*, because `SA121-QG-001` (SA121's only external blocker) is now cleared by SA126 immediately upstream on the same track instead of waiting on the SA117 chain's plan gates; SA117a shrinks to a single test-file assertion with a correspondingly smaller staging allowlist; and the green-gate milestone stops sitting behind Track 3. Track 1 order becomes **SA126 → SA121 → SA122a**, with SA122b still merge-gated behind SA112e. SA126 is behaviour-preserving only, is forbidden from raising any `scripts/quality_baseline.json` ceiling (SA121 shrink-only rule), and its refactor falls inside SA117e's mandatory full-scope review as part of `v87`'s delta — a review-scope note, not a merge hazard, since SA126 merges well before SA117e runs. **`SA117-QG-REMED` retired**; neither SA117a nor SA117b carries a complexity criterion or touches `module_commands.py`. Every reference was re-pointed in the same pass: open-work summary and list numbering, the Track 1 section header and its new single-owner exception note, `SA117-QG-REMED`, SA117a's and SA117b's verify lists, SA117a's checkpoint and open-findings block, the green-gate current-status block, SA121's blocker list, the dependency diagram, the Track 1 ordering list, four parallelism bullets, the conflict-surface paragraph, the track-readiness three-state bullets, the Net paragraph, and Standing decisions. **Two tracks are now truly green and can run in parallel today** — Track 3 (SA117a) on the critical path and Track 1 (SA126) feeding it, with disjoint files, no ordering constraint between them, and no shared infra. **No open decision remains anywhere on the board.** No source files touched.
  - **Roadmap rebalancing pass (2026-07-28, fourth pass of the day; documentation only).** **No closeout transfer owed:** all seven tickets remain genuinely open, and no `arch-audit.md` or `tech-audit.md` finding became closeable (arch 11/12/7/2/4 open; tech-audit at zero open findings). **Track assignment complete**, no gaps. **One rebalancing move found, surfaced as open decision `SA112A-TRACK-001`** — SA112a is the only open ticket that is simultaneously on the critical path, dependency-satisfied today (it does not carry the SA117e bound, which begins at SA112b), file-disjoint from the entire SA117a–SA117c allowlist, and service-free to validate via `make smoke-install`. Running it concurrently removes one node from the critical path, but no existing track can host it (Track 3 is one-child-at-a-time, Track 1 holds runnable SA121, Track 2 is a closed worktree), so it costs a fourth worktree and a second review lane. Because that is a real cost rather than a pure speedup, the move was **not applied** — it is recorded for the maintainer, with SA112a staying on Track 3 by default. This **corrects a self-inconsistency**: the prior pass asserted "no rebalancing move is available" while simultaneously recording that SA112a is technically unblocked. The Parallelism bullet on SA112's serial chain was rewritten to scope its no-move claim to SA112b–SA112f, and SA112a's ticket tag now names the open decision. **Track states restated as three independent verdicts** (can start / can finish / can merge) per track, replacing single-verdict prose: **Track 3 is the only truly green track and is on the critical path** — SA117a can start, finish, and merge entirely on its own work. Track 1 can start and merge but **cannot finish** — SA121's acceptance needs `make quality` green, hard-blocked by SA117a and SA117b's complexity findings, unclearable by any maintainer decision. Track 2 can start but **cannot finish** (blocked by SA112f) **and cannot merge** (order-gated behind SA112e); both hard dependencies. **Verified against code, not prose:** `radon cc` re-measured `_perform_module_embed` 22 > 20 and `_update_single_module` 18 > 17; `scripts/quality_baseline.json` top-level keys confirmed as `schema_version`/`dead_code`/`complexity`/`duplication` with zero `large_files`; `scripts/quality_waivers.json` empty; `SA117-DEC-001` step 1 (stamp) confirmed landed with all twelve source **and** all twelve bundled `module.yml` versions at `0.87.0` and the canonical lockstep parser present in `manifest/loader.py`; `wt-track1`/`wt-track3` confirmed fully merged into `v87` and `wt-track2` still carrying `5193f198` + `5b5de830`. Conflict surface unchanged and covered by the Merge procedure. No source files touched, no ticket state or dependency changed.
  - **Roadmap verification pass (2026-07-28, third pass of the day; documentation only).** **No closeout transfer was owed:** every ticket in `docs/technical/roadmap.md` is genuinely open (SA117a/b/c/e, SA112a–f, SA96-PUBLISH, SA115, SA121, SA122a/b), and no `arch-audit.md` or `tech-audit.md` finding became closeable — arch Findings 11, 12, 7, 2, and 4 all remain open, and `tech-audit.md` already carries zero open findings with `TA62` reduced to a reconciliation line. **No track assignment was missing and no rebalancing move was available:** the critical path `SA117a → SA117b → SA117c → SA117e → SA112a → … → SA112f → SA96-PUBLISH` is entirely Track 3 and causally serial (evidence validity, not file overlap), so no off-path ticket can be moved onto it to shorten it; Track 2 remains closed to new work while merge-gated SA115 commits sit on its branch, and Track 1's two file-disjoint tickets stay on one worktree because neither feeds the critical path. **Verification against code rather than prose:** `radon cc` re-measured both green-gate blockers live (`_perform_module_embed` 22 > 20, `_update_single_module` 18 > 17); `SA125-DEC-001` was confirmed implemented (zero `large_files` entries in `scripts/quality_baseline.json`, that data retained as report-only in `scripts/check_quality.sh:587`); and SA121's merged partial delivery was confirmed present and inert (`scripts/quality_waivers.json` empty). A dated re-verification note recording those measurements was added above the Track readiness bullets; no other roadmap surface changed, because none was found stale or self-inconsistent.
  - **Roadmap closeout/rebalancing pass (2026-07-28, second pass of the day; documentation only).** **Closeout transfer:** the completed **SA125** block was removed from `docs/technical/roadmap.md` (455 → 425 lines); its decision rationale, implementation steps, completion evidence, sequencing, and SA121 file-overlap record are already held verbatim in the two SA125 entries below, so nothing was lost. Every surface that referenced it was re-pointed in the same pass — the open-work summary, the `SA117-QG-REMED` block, SA117a's and SA121's ticket tags (`deps: SA125 ✓` → `deps: none (SA125 ✓ closed)`), SA117a's now-obsolete deps note, the green-gate current-status block (rewritten to state **zero line-count blockers** and exactly two complexity blockers), the Track 1 ordering list, the dependency diagram, the "completed temporary upstream node" paragraph (deleted — the critical path no longer runs through Track 1), the parallelism and conflict-surface bullets, the track-readiness bullets, the Net paragraph, and the standing-decisions entry for `SA125-DEC-001` (kept as policy, restated as implemented and closed). Also corrected a stale code anchor: SA121's problem statement cited `scripts/check_quality.sh:792-814,824-846`, whose second range was the deleted `max_lines` block; it now reads `818-831`. `START_HERE.md`'s roadmap length refreshed to 425 lines. **Audits:** `tech-audit.md` stands at **zero open findings** and needed no transfer. `arch-audit.md` Finding 12 was **narrowed, not closed** — its evidence cited the `max_lines` half of the gate that SA125 deleted, so the anchor was corrected to the surviving `max_complexity` comparison and a dated Reconciliation-log line records that two of the three census increases (`module_commands.py` 1596→1608, `config_schema.py` 605→611) no longer have a gate to be self-authorized against, while `_validate_modules_section` 11→12 remains the live instance and SA121 still owns the fix. Finding 11 stays fully live for SA122a/SA122b; Findings 7/2/4 stay deliberately unscheduled. **Track assignment: complete, no gaps.** All six open entries (SA117a–e, SA112a–f, SA96-PUBLISH, SA115, SA121, SA122a/SA122b) carry a track; v88 backlog items correctly carry the `v88` scope tag under the assign-at-kickoff rule. **Rebalancing verdict: no move available, and none would help.** The critical path is unchanged at `SA117a → SA117b → SA117c → SA117e → SA112a…f → SA96-PUBLISH`, entirely on Track 3 and causally serial (evidence validity, not file overlap), so it cannot fan out. SA125's closure removed the last cross-track dependency, so Track 1 no longer feeds the critical path at all. The one theoretically splittable pair — SA121's continuation and SA122a, which are file-disjoint with no ordering constraint — was **left serial on Track 1**: Track 2 remains closed to new tickets because `wt-track2` still carries the merge-gated SA115 commits (`5193f198`, `5b5de830`, verified still unmerged this pass) and a track merges as a branch rather than as a ticket, and a fourth worktree would buy no release time because neither ticket is on or feeds the critical path. The parallelism bullet was rewritten to say this explicitly — the pair is serial by choice, not by dependency. No ticket changed track, so no ticket tag, per-module gate list, dependency diagram, ordering prose, or track-readiness bullet needed re-pointing on that account. **Conflict surface unchanged:** `CHANGELOG.md`, `docs/technical/roadmap.md`, and conditionally `docs/technical/decisions.md`, all covered by the Merge procedure's `git merge v87`-before-merge-back step; `.github/workflows/e2e.yml`'s path list stays serialized by SA115's `merge after SA112` bound; `scripts/quality_baseline.json` is a declared non-conflict now that its `large_files` section is gone — SA117a/SA117b do not touch it and SA121 is contractually forbidden from doing so (`SA121-DEC-001`). **Track review: all three tracks clean to continue, with no open maintainer decision anywhere.** Track 1 — SA121's parser continuation under `SA121-DEC-002`, then SA122a. Track 2 — SA115 heavy validation authorized under `SA115-DEC-001` step 3, yielding infra to Track 3, merge still queued behind SA112. Track 3 — a fresh scoped SA117a continuation closing `SA117A-CR-002` plus its single-item quality share (`_perform_module_embed` CC 22 > 20). `make quality` remains RED at `v87` HEAD, owned by Track 3, so the green-gate milestone stays unclaimed. No ticket state, dependency, or decision changed; no source files touched.
  - **SA125 — file line-ceiling quality gate retired (2026-07-28; complete).** Functional commit `05d0f2b8` removed blocking `large_files` enforcement, narrowed the monotonicity helper/tests to the surviving sections, and retained advisory large-file diagnostics as non-blocking output; they no longer affect the baseline, monotonicity checks, waivers, or exit status. Validation from the functional closeout: focused pytest **149 passed**; the SA125 subset **4 passed / 145 deselected**; Bash syntax, `make lint`, `make typecheck`, and `git diff --check` passed. `make quality` exited 2 with exactly the two pre-existing Track 3 complexity findings — `_perform_module_embed` CC **22 > 20** and `_update_single_module` CC **18 > 17** — and zero line-count blockers; these are not SA125 failures. `SA125-CR-ADV-001` remains a low/advisory consistency finding for stale comments/docstrings in the monotonicity helper/tests; behavioral semantics are correct, so it is non-blocking and does not reopen SA125. Functional independent review returned **STATUS: ok** and safe to commit. SA121 is now Track 1's next pending task, and SA117a's SA125 dependency is satisfied and unblocked.
  - **`SA125-DEC-001` — file line ceilings retired, complexity ceilings kept (2026-07-28; documentation only, SA125 complete).** The maintainer retired per-file `max_lines` enforcement from the quality gate and kept per-function `max_complexity`. Rationale: the ceilings are **measurement fossils** — `manifest/loader.py`'s 562 is exactly its size at `43f49350`, the commit that happened to be HEAD when `76c5cc55` captured the baseline, so an accident of timing had been promoted into a binding target (the mirror image of arch Finding 12, which names the same artifact acting as its own waiver authority). The gate also **taxes documentation**: `loader.py` is 686 lines — 348 code, 171 docstring, 134 blank, 33 comment — so the cheapest legal route to compliance was deleting ~124 of 204 documentation lines, including the docstring specifying SA117a's fail-hard rejection contract. It is **aimed against the project's own direction**, since `loader.py` grew 187 → 686 absorbing the declarative-manifest migration that `decisions.md` §module-derivation-schema mandates. And it **cannot distinguish mandated growth from decay** — the 124-line overage is almost exactly the 123-line region SA117a added for the reviewed exact-lockstep correction. Complexity ceilings are kept because they are local, actionable, ignore docstrings, and name a specific function. **SA125 implemented it** (`Tier 1 · Track 1 · deps: none`): drop `large_files` (41 entries) from `scripts/quality_baseline.json` and its blocking enforcement in `scripts/check_quality.sh`, narrow the SA121 monotonicity helper and its tests to the surviving sections, update `quality_tools.md`, and record the rule in [decisions.md §file-size-metric-policy](docs/technical/decisions.md#file-size-metric-policy); `complexity` (151 entries), `dead_code`, and `duplication` are untouched. **Consequences recorded across the roadmap:** `SA117-QG-REMED` collapses from five findings to two — `_perform_module_embed` CC 22 > 20 (SA117a) and `_update_single_module` CC 18 > 17 (SA117b) — while `module_commands.py` 1642 > 1608, `manifest/loader.py` 686 > 562, and unbaselined `git_utils.py` at 509 **dissolve with the gate rather than being remediated**, and SA117a/SA117b no longer touch `quality_baseline.json` at all. **Sequencing (maintainer decision):** SA125 runs first on Track 1 and Track 3's SA117a continuation waits for it, trading a short pause at the head of the critical path for the deletion of three of SA117a's five blockers and a 124-line `loader.py` refactor that would have been discarded; the rejected alternative was SA117a proceeding CC-only in parallel. SA125 also precedes SA121's continuation — they share `check_quality.sh`, both monotonicity scripts, and `quality_tools.md`, and are one coherent unit of gate-policy work — and it narrows `SA121-CR-003`'s remaining matrix, since the large-file separator-key cases stop existing. `SA121-DEC-001`'s prohibition on SA121 touching `quality_baseline.json` is unaffected; SA125 owns that file. Track 1's order is now **SA125 → SA121 → SA122a**, with SA122b still merge-gated behind SA112e. No source file was touched and no ticket completed.
  - **Roadmap closeout/rebalancing pass (2026-07-27, second pass of the day; documentation only).** No ticket completed since the previous pass, so nothing moved from `docs/technical/roadmap.md` to this file — the same seven tickets (SA117a–e, SA112a–f, SA96-PUBLISH, SA115, SA121, SA122a, SA122b) remain open, three at recorded partial checkpoints. Both audits were re-checked and need no closeout transfer: `tech-audit.md` already stands at **zero open findings** (its last closure, `TA62`, exists only as a Reconciliation-log line), and `arch-audit.md`'s open Findings 11 and 12 are still live context for SA122a/SA122b and SA121 respectively, while Findings 7/2/4 stay deliberately unscheduled. **Track assignment:** one gap fixed — `SA96-PUBLISH`'s ticket tag read `Tier 1 · v87` while the open-work list homed it on Track 3; the tag now reads `Tier 1 · Track 3 · v87`. Every other open ticket already carried a track, and v88 backlog items correctly carry the `v88` scope tag under the assign-at-kickoff rule. **Rebalancing verdict: no move available, and none needed.** Track 3 owns the entire critical path (`SA117a → SA117b → SA117c → SA117e → SA112a…f → SA96-PUBLISH`) as a causally serial chain that cannot fan out; Track 2 stays **closed to new tickets** while `wt-track2` carries merge-gated SA115 commits (`5193f198`, `5b5de830`); Track 1's SA121 and SA122a are file-disjoint but share one worktree, and splitting them onto a fourth worktree would buy no release time because neither is on the critical path. No ticket changed track, so no ticket tag, per-module gate list, dependency diagram, ordering prose, or track-readiness bullet needed re-pointing beyond the SA96-PUBLISH tag. Conflict surface unchanged: `CHANGELOG.md`, `docs/technical/roadmap.md`, and conditionally `docs/technical/decisions.md`, all covered by the Merge procedure's `git merge v87`-before-merge-back step; `scripts/quality_baseline.json` remains a declared non-conflict; after SA125's closeout, SA117a/SA117b do not modify it because their file-line findings are retired, while SA121's continuation remains contractually forbidden from touching it (`SA121-DEC-001`). **Roadmap purge:** superseded narrative was removed from `docs/technical/roadmap.md` (457 → 417 lines) — the SA117 Tier-3 sizing rationale and measured candidate breakdown, the SA117/SA117a/SA121 checkpoint "Done" recitals, SA117's byte-level manifest measurements and its duplicate "two independent gaps" restatement, SA112's pre-SA113 gap-analysis prose, SA115's pre-implementation design narrative and `SA115-DEC-002` load arithmetic, and duplicated Track 1/Track 2 parallelism bullets, two overlapping conflict-surface paragraphs, and a Net paragraph that restated the track-readiness bullets. All of it remains in this file; every open blocker, decision, verify criterion, allowlist, and ordering bound was preserved verbatim. Also refreshed `START_HERE.md`'s roadmap length. **Track review: all three tracks clean to continue, with no open maintainer decision anywhere for the first time in this release train.** Track 1 — SA121's parser continuation under `SA121-DEC-002`, then SA122a. Track 2 — SA115 heavy validation authorized under `SA115-DEC-001` step 3, yielding infra to Track 3, merge still queued behind SA112. Track 3 — a fresh scoped SA117a continuation closing `SA117A-CR-002` plus its `SA117-QG-REMED` quality share. `make quality` remains RED at `v87` HEAD, owned by Track 3, so the green-gate milestone stays unclaimed. No ticket state, dependency, or decision changed; no source files touched.
  - **SA117a exact-lockstep recorded partial delivery (2026-07-27; task remains open).** **Done:** the Track 3 correction now validates manifest and running-core versions as exact canonical three-component ASCII-decimal strings before tuple comparison, preserving the dedicated mismatch error while rejecting padded, zero-padded, signed, Unicode-digit, empty-component, malformed, and unequal values. Four-file regression coverage spans managed-wiring regeneration, standalone/apply embed fail-stop behavior, update rollback, and the real all-twelve apply-wrapper → step → manager path. Accepted task-scoped evidence is green: 42 targeted tests, a separate passing all-twelve JUnit proof, 4,606 core/CLI unit tests with 1 skip, 12/12 version and manifest parity, lint, typecheck, and diff checks. Independent review explicitly resolved `SA117-CR-001` and found production correctness, caller parity, breaking-change safety, and scope sound. **Pending / blocking:** `SA117A-CR-002` (medium, test gap) remains because the update rollback regression lacks an explicit `_sync_module_dependencies` spy and `assert_not_called()`; production sequencing is correct, but this non-continuation claim is not regression-proven. The synchronized `v87` state initially assigned SA117a its `SA117-QG-REMED` share across `module_commands.py` 1642 > 1608 lines, `manifest/loader.py` 686 > 562 lines (the incoming pre-SA117a checkpoint measured 624), and `_perform_module_embed` CC 22 > 20. `SA125-DEC-001` subsequently retired the line-ceiling gate, so the current SA117a quality-remediation share is only `_perform_module_embed` CC 22 > 20; together with SA117b's `_update_single_module` CC 18 > 17, these are the only remaining `make quality` blockers. No baseline ceiling may be raised as the remedy. The second review-fix cycle reduced the medium review ledger from two findings to one and hit the review cap, so SA117a remains unchecked and SA117b cannot start. **Decisions needed:** none; the next continuation must add the missing spy, clear SA117a's quality-remediation share, rerun the full validation/quality chain, and obtain independent review. This entry supersedes the earlier checkpoint's statement that `SA117-CR-001` itself remains open; it does not approve split publication or close SA117a/SA117.
  - **SA121 focused continuation recorded partial delivery (2026-07-27; task remains open).** Track 1 corrected the `quality_tools.md` account of policy-artifact `waiver_evaluations`, expanded strict-key/canonical-diagnostic tests, and isolated shell success tests from live-repository line-count drift; no runtime helper, baseline, waiver ledger, threshold, or Make target changed. Evidence: 156/156 focused tests, the helper against `v87`, Bash syntax, and `make check` passed. Full review remained `STATUS: partial`: `SA121-CR-003` is high/blocking because the triple-colon key is accepted as an orphan and the strict malformed-envelope matrix is incomplete; `SA121-CR-006` is medium/blocking because broad/subset assertions remain; `SA121-QG-001` is high/blocking because `make quality` exits 2 on the two pre-existing Track 3 complexity findings. SA125-DEC-001 retired the three file line-ceiling blockers; SA121 therefore remains unchecked pending its review findings and the two Track 3 complexity fixes. By explicit maintainer instruction this checkpoint is merged despite those findings; SA121 stays unchecked, and the merge is not a waiver, review approval, completion claim, or release-ready evidence.
  - **SA117 recorded partial delivery merged to `v87` (2026-07-27; task remains open and unapproved).** The Track 3 lockstep candidate — previously preserved as checkpoint `43d9b8fc` (69 files changed, 8 new `scripts/` helpers) — is merged to `v87` by explicit maintainer instruction as blocked preservation only. It stamps all twelve module metadata sets and bundled manifests to `0.87.0`, adds embed/managed-wiring lockstep enforcement, and adds lease-safe split-publication tooling; none of that executable content received independent approval in this continuation. The continuation made no source edit and ran no test because its mandatory plan gate remained `STATUS: partial`. Four executable blockers remain: `SA117-CR-001` (high, correctness — padded versions pass literal lockstep), `SA117-CR-003` (high, security — blank direct-CLI origins bypass the pre-mutation public-source gate), `SA117-CR-005` (medium, breaking — scope CLI/help/Make caller contracts disagree), and `SA117-CR-006` (high, completeness — lock-drift omits complete baseline semantics). Plan review resolved `SA117-CONT-PR-001`, `-002`, `-004`, and `-005` at plan-detail level but remains blocked by `SA117-CONT-PR-003` (high, resilience — validation-container cleanup is not armed throughout creation) and `SA117-CONT-PR-006` (medium, completeness — external Git controls lack a secure literal bootstrap and a filename-safe final staged-index check). Before clean continuation, a maintainer must authorize another focused plan/review cycle for those two findings or supply an accepted literal plan; plan-review `STATUS: ok` is required before source edits, and full-scope executable review `STATUS: ok` is still required before checking SA117, pushing splits, starting SA112, or treating this merge as release-ready. SA115 heavy validation remains **frozen** under `SA115-DEC-001` step 3 **Keep unauthorized**.
  - **Three maintainer decisions recorded (2026-07-27; documentation only).** **`SA115-DEC-001` step 3 — AUTHORIZED**, reversing the 2026-07-26 "Keep unauthorized" hold: SA115's heavy Docker/PostgreSQL validation may proceed, and the ratified `SA115-DEC-002` guard clamp (a fired memory guard must also clamp `E2E_XDIST_WORKERS=1`, visibly when it overrides an explicit value) implements as part of that phase. Two constraints attach — exclusive PG/Docker capacity is contended with SA117e and every SA112 heavy leg, so **Track 3 holds priority** and an SA115 run is abandoned/restarted rather than made to block a critical-path leg; and validation authorization is **not** merge authorization, since the `merge after SA112` bound exists for `.github/workflows/e2e.yml` path-list coordination with SA112e/`SA115-CI-001`, not for validation risk. Track 2 moves from validation-paused to runnable while staying closed to new tickets. **`SA117-DEC-004` — DEFER (option b)**: the SA117 scope meta-tooling and its `SA117-CR-005` caller-contract inconsistency are dropped from the SA117 acceptance set and tracked as new v88 backlog item **SA124**; the merged code at `43d9b8fc` stays in place but unadvertised, reviewed by SA117e as inert merged code rather than as a contract to prove. SA117 therefore has four children (SA117a, SA117b, SA117c, SA117e), not five, and Track 3 carries no open decision. **`SA122-DEC-001` — publish is a full-coverage gate context**: every repository conformance gate required locally and in hosted CI is also required in `.github/workflows/publish.yml`. Rationale is asymmetry of cost — publish is the last step and the only irreversible one, so a gap there costs a release while a gap in any editable-state context costs a fix; the last step re-verifies rather than trusts. The five conformance gates absent from `publish.yml` are consequently a **declared-open gap, not an accepted narrowing**: SA122a's parity checker is expected to fail on it and must not silence it with an exclusion, and SA122b closes it by adding the gates. Rule text added to `decisions.md` as §publish-path-gate-coverage; the disposition that previously sat inside SA122a step 3 is now pre-resolved, so Track 1 has no mid-ticket decision either. Release ordering (tag → push splits → PyPI) was reconfirmed unchanged. No ticket completed and no source file touched.
  - **SA121 focused continuation authorized (2026-07-27; `SA121-DEC-001`).** The maintainer granted the tests/documentation-only cycle that SA121's recorded partial checkpoint required, scoped to `SA121-CR-003` (strict-parser coverage), `SA121-CR-005` (`quality_tools.md` contradicting code and `decisions.md` on raw `waiver_evaluations`), and `SA121-CR-006` (broad assertions that must become exact ordered records/counts/exits). The cycle must preserve the reviewed runtime/source delta unchanged, leave `scripts/quality_baseline.json` untouched, run the full focused and quality gates, and obtain full-scope independent `STATUS: ok` before SA121 is checked or a completion entry is added here; if closing a finding turns out to require a runtime change, it stops and re-opens the decision rather than widening scope. Track 1 now has **two runnable tickets and no open pre-start decision** — SA121 and SA122a are file-disjoint with no ordering constraint between them, so the track takes them serially in either order, with SA121 first as the nearer to closure. SA121 remains unchecked and open.
  - **SA117 resized from Tier 3 into five serial children (2026-07-27; `SA117-DEC-003`; documentation only).** The roadmap requires every phase to be Adaptive Tier 1–2, and SA117 failed that gate on three counts: its objective could not be stated in one sentence (a product lockstep contract, a release-operations publication toolchain, and a scope-control checker built to police this ticket's own review), its candidate `43d9b8fc` is 77 files / 7,424 changed lines against an EDIT budget of ≲2,000 across ≲20 sites, and its coupling is ≥6 interdependent elements against a limit of 3. The failure was empirical: two plan-review cycles hit the non-convergence cap without reaching `STATUS: ok`, because each pass had to hold all three objectives at once. Measured split of the candidate: 625 lines product lockstep · 4,156 publication tooling · 2,586 scope meta-tooling · 200 docs/Make/lock. SA117 is now umbrella-acceptance only, with the six open findings distributed across **SA117a** (`CR-001`, exact/canonical lockstep comparison — critical path), **SA117b** (`CR-003` security, `CONT-PR-003` resilience, `CONT-PR-006` completeness — publish-path safety, critical path), **SA117c** (`CR-006`, lock-drift completeness), **SA117d** (`CR-005`, scope-tooling caller contract — **off the critical path**), and **SA117e** (full-scope review over the complete delta including `43d9b8fc`, then the human-confirmed refreshed split push). Because the executable content is already merged as unapproved recorded partial delivery, the children are correction-and-review handoffs over existing code, not greenfield builds; each carries its own scoped plan review, and SA117e still requires one full-scope review before SA117 closes. Critical path becomes `SA117a → SA117b → SA117c → SA117e → SA112a…f → SA96-PUBLISH` — longer in node count but not in work, and SA117d leaves the path entirely since the meta-tooling gates no release property. **Track 3's next action is SA117a, which no longer needs a maintainer decision to start.** New open decision `SA117-DEC-004`: defer the SA117d scope meta-tooling to v88, reconcile it in v87, or revert it. `decisions.md` §module-version-lockstep gains **Rule 2a** — version equality is decided on a canonical parse, and an unparseable version is a hard error rather than a fallback to literal string comparison. Track 1 sizing was reviewed in the same pass and needs no split: SA121's remaining scope is tests/documentation-only against three named findings, and SA122a/SA122b are each a single coherent objective already sized Tier 2. No ticket was completed, no source file touched, and no other track assignment or dependency changed.
  - **Roadmap/audit rebalancing pass (2026-07-27; documentation only).** No ticket completed this pass, so nothing moved from `docs/technical/roadmap.md` to this file — the seven open items (SA117, SA112a–f, SA96-PUBLISH, SA115, SA121, SA122a, SA122b) all remain open, three of them at recorded partial checkpoints. `tech-audit.md`'s closed `TA62` finding body and summary-table row were removed, adopting arch-audit's convention that closed findings exist only as dated Reconciliation-log lines; the closure record stays in the SA120 entries below and the structural cause stays open as arch Finding 11. **Rebalancing verdict: no safe move exists.** The one candidate — re-homing SA122a (`deps: none`, purely additive) from Track 1 onto the idle Track 2 — was rejected because `wt-track2` carries two committed but unmerged commits (`5193f198` SA115 phases 1–2, `5b5de830` the `v87` reconciliation merge) and a track merges as a branch, not as a ticket; landing SA122a from there would drag SA115 onto `v87` in violation of its `merge after SA112` bound. Track 2 is now documented as **closed to new tickets** until SA115 merges. Track 1 needs no relief regardless: SA121's remaining tests/documentation-only cycle and SA122a's new registry/checker are file-disjoint with no ordering constraint, so SA122a runs now while SA121 waits on authorization. Also fixed: eleven stale `../others/arch-audit.md` / `../others/tech-audit.md` links in `docs/technical/decisions.md` and one in `arch-audit.md`'s reconciliation log (both audits moved to the repository root on 2026-07-26); `START_HERE.md` roadmap length refreshed to 375 lines; v88 backlog items given an explicit track-assignment rule (assigned at v88 kickoff, deliberately not homed on a v87 release-train worktree). Conflict surface unchanged and covered by the Merge procedure: `CHANGELOG.md`, `docs/technical/roadmap.md`, and `docs/technical/decisions.md` (SA122a writes its publish-context disposition there). Track review: **Track 1 clean to continue on SA122a**, SA121 authorization-blocked; **Track 2 blocked** — validation-paused under `SA115-DEC-001` step 3 **Keep unauthorized**, merge-gated behind SA112, closed to new work; **Track 3 blocked** at SA117 with four executable and two plan findings awaiting maintainer authorization. No ticket state, track assignment, dependency, or decision changed; no source files touched.
  - **Roadmap/audit closeout pass (2026-07-27; documentation only).** `docs/technical/roadmap.md` no longer carries the completed SA120 block — its implementation detail, resolved review IDs, and validation evidence live in the SA120 entries below. Track 1 intake prose, the Finding-7/tooling-gap paragraph, the dependency-diagram note, the parallelism bullets, and the track-readiness/Net lines were reconciled to the three remaining governance tickets (SA121, SA122a, SA122b); `TA62` survives only as named drift evidence for arch Finding 11. `tech-audit.md`'s `TA62` finding body was compacted to a closed-status summary pointing at this changelog, since no open work depends on its pre-fix narrative (the structural cause stays open as arch Finding 11). `START_HERE.md`'s roadmap length was refreshed to 365 lines. No ticket state, track assignment, dependency, or decision changed; no source files touched. Track review: Track 1 clean to continue (next: SA121); Track 2 implementation-ready but validation-paused under `SA115-DEC-001` step 3 **Keep unauthorized** and merge-gated behind SA112; Track 3 blocked at SA117 independent review with four carried findings and no maintainer decision pending.
  - **SA120 complete — quiet-check frontend-lint parity (2026-07-26).** Full-scope independent review returned `STATUS: ok` with no new findings; SA120-REV-002, SA120-REV-003, and SA120-REV-006 resolved. Normal and quiet `make check` now share the same guarded `lint-frontend` target; quiet mode uses collision-safe temporary capture, is silent on success, and replays diagnostics exactly once on failure with nonzero exit status. Validation: 79/79 policy tests passed; 4,542 unit tests passed; `make check QUIET=1` completed green on first run; normal dry-run exit 0 contained `lint-frontend`. No unresolved blockers or decisions. SA120 roadmap item marked `- [x]`. SA121 is now Track 1's next pending task. The prior partial checkpoint entry below documents implementation history.
  - **SA120 recorded partial checkpoint (2026-07-26; task remains open).** Track 1 added normal/quiet `make check` frontend-lint parity after the shared repository gates, explicit failure propagation, and recursive-target policy coverage. Validation passed 29 focused and 79 full policy tests; the final quality-gate run also proved dry-run order and reversible rendered-TypeScript failures in both modes. The two-cycle independent review cap ended `STATUS: partial`: **SA120-REV-002** (medium, blocking) still requires opposite-tool-present fixtures for independent Node/pnpm absence; **SA120-REV-003** (medium, blocking) still requires exact quiet silence/single replay and collision-safe capture for overlapping runs; **SA120-REV-006** is low/advisory unreachable test-helper cleanup. No product decision is open. SA120 remains unchecked; this checkpoint is not a waiver or completion claim.
  - **SA112 plan-review non-convergence recorded; umbrella split into six serial handoffs (2026-07-26; no implementation landed).** Two focused plan-review cycles over the former monolithic SA112 A–E artifact confirmed the exact five-path GitHub Actions trigger tuple and the deterministic `yaml.BaseLoader` contract (resolving `SA112-CR-005` at plan-detail level) and corrected the installed `apply` stdin to the current three confirmations (`"n\ny\ny\n"`). Highest unresolved severity stayed high across both cycles (`SA112-PR-002`, completeness), so the plan loop hit its non-convergence cap. An unexpected uncommitted SA112 prototype was preserved as evidence in `45e37a86` and fully neutralized by `56b27045` — its 13 paths have zero net delta from the synced `v87` baseline, so the mergeable tree carries no SA112 executable delta. The maintainer chose recorded partial delivery and replaced the all-at-once handoff with the serial children **SA112a–SA112f** (provisioner extraction, traceback capture, root fix, permanent lifecycle E2E, workflow-trigger contract, ordered acceptance/closeout), each with its own narrow literal plan, scoped plan review, independent change review, and merge-back before the next begins. The superseded monolithic artifact must not be reconstructed or executed. `SA112-PR-002` remains open and is discharged by the children's per-task literal plans.
  - **arch-audit Finding 10 bookkeeping completed (2026-07-26; documentation only).** The closed-status stub left in `docs/others/arch-audit.md`'s live findings section for `frontend-source-generation-specialized` was removed, restoring that document's convention that closed findings exist only as dated Reconciliation-log lines. Closure detail remains in this file (SA104/SA105/SA106/SA107/SA108) and in the arch-audit log entries dated 2026-07-21 and 2026-07-23. No finding status changed and no audit was re-run.
  - **SA114/SA116 complete — Track 1 re-verification closed (2026-07-26).** Patch-fed full-scope review returned `STATUS: ok` with confidence 98 and zero findings. **SA114:** ordered chain `make check && make quality && make ci && make ci-e2e` exited 0 on 2026-07-25; QUARANTINE_TICKETS empty; coverage 92.88%/94.41%; Core 35/CLI 29 E2E; no source fix required. **SA116:** 1761→1749 comment/docstring-only compaction, `scripts/quality_baseline.json` unchanged at `max_lines` 1749, `make quality` exit 0, independent source review `STATUS: ok`. **SA114-CR-001** (consistency) resolved by SA115-DEC-001 **Keep unauthorized** maintainer decision — all roadmap authority surfaces now consistently state SA115 step 3 is not authorized. **SA116-CR-002** (completeness) resolved by exact patch-fed review: `git show a58f07e0 -- quickscale_core/src/quickscale_core/contracts/resolvers.py` confirmed 2 insertions / 14 deletions, comments/docstring/blank only, no executable or API-surface change, baseline 1749 unchanged. **SA115-DEC-001** step 3 **Keep unauthorized** remains in effect — SA115 heavy validation remains unauthorized pending explicit future approval; human-authorization-gated, not SA112-gated. SA115 remains open, implementation-ready but validation-paused, merge after SA112. Track 1 removed from open-work roadmap. Tasks completed 2026-07-26.
  - **SA114/SA116 partial re-review — SA115 authority resolved (2026-07-26; SA115-DEC-001 step 3 decision recorded; SA114-CR-001 resolved; SA116-CR-002 awaits patch-fed review).** The full-scope re-review authorized by SA114-DEC-002 completed and returned `STATUS: partial` as previously recorded. SA115-DEC-001 step 3 was resolved on 2026-07-26: the maintainer chose **Keep unauthorized** — SA115 heavy Docker/PostgreSQL validation remains unauthorized pending explicit future approval; it is human-authorization-gated, not SA112-gated. **SA114-CR-001** (medium, consistency) is **resolved** by this decision; all roadmap authority surfaces now consistently state step 3 is not authorized. **SA116-CR-002** (medium, blocking, completeness) remains open; the maintainer authorized one fresh Track 1 patch-fed full-scope review that supplies the plaintext SA116 patch (or a git-show-capable reviewer). SA114 and SA116 remain unchecked/open pending that review. No source changes were made or authorized.
  - **SA115 committed and reconciled with `v87` (2026-07-25; `SA115-DEC-001` steps 1–2; still open, merge still queued behind SA112).** The four previously parked SA115 files were committed on `wt-track2` (`5193f198`) and `v87` was merged in (`5b5de830`), ending the uncommitted-work hazard and clearing the accumulated `scripts/test_e2e.sh` drift. Three conflicts — Environment header, `--help` list, and the worker-count resolution block against the landed memory preflight — were all additive on both sides and resolved by keeping both, adopting `v87`'s column alignment. SA115's banner and `pytest_cmd` hunks merged without conflict. Post-merge (combined pytest exit 0): `bash -n` clean, 31 focused tests pass (16 fixture + 15 harness), Ruff check/format clean. The merge was verified non-destructive — `git diff v87...HEAD` is 274 insertions / 0 deletions across exactly the four SA115 files, so nothing landed on `v87` was dropped. Two items recorded for the validation phase: (a) the memory guard demoted to serial *lanes* under low RAM while `E2E_XDIST_WORKERS` resolved independently, so a demoted run still fanned out N workers per lane and could exceed the load the guard existed to prevent — **resolved by decision `SA115-DEC-002` (2026-07-25): the guard wins and clamps workers to serial when it fires**, visibly, with the existing `QS_E2E_NO_MEMORY_GUARD=1` as the sole opt-out; implementation lands in SA115's validation phase; (b) `SA115-CI-001` — neither `test_e2e_xdist_fixtures.py` nor `quickscale_core/tests/conftest.py` appears in `.github/workflows/e2e.yml`'s trigger paths, the same defect class as `SA112-CR-005`. Heavy validation (container lifecycle, teardown, `make ci-e2e`) is not yet run.
  - **SA114-CR-001 documentation reconciliation (2026-07-25; roadmap cleanup pass).** The two same-fact scheduling sites the SA114 final review named were reconciled in `docs/technical/roadmap.md`: the "only open work is shown" dependency diagram no longer carries completed Track 1/Track 2 rows, and the SA115 concurrency bound, parallelism bullets, infra-serialization prose, and track-readiness bullets no longer treat SA114's heavy `ci`/`ci-e2e` legs as future contention. Completed Track 2 (SA104→SA108) and Track 3 (prior) narrative was compacted to CHANGELOG pointers, retaining only the standing AF7 discovery reference. A follow-up deep-review pass the same day found three further stale-fact sites the first pass missed — SA114's own intro paragraph still asserting the gates had not been re-run, SA116's present-tense "`make quality` exits 2" problem statement, and SA115's "parked" label after the work was committed — plus a stale `START_HERE.md` roadmap length (550 → 280 lines). All corrected. Source unchanged; SA114-CR-001 stays open on its remaining half — a full-scope independent re-review returning `STATUS: ok` — and SA114/SA116 remain unchecked.
  - **SA114/SA116 recorded partial checkpoint (2026-07-25; tasks remain open).** **Done:** the exact ordered chain `make check && make quality && make ci && make ci-e2e` exited 0 from a clean wt-track1 synced to latest v87; QUARANTINE_TICKETS empty; core+CLI equal-weight coverage 92.88%; integration module mean 94.41%; Core E2E 35 passed, CLI E2E 29 passed; no source fix required. SA114-ORDER-001 is satisfied. Non-gating Django/pytest warnings were observed. **Pending / blocking:** `SA114-CR-001` (medium, blocking, consistency) remains open after the second/final review continuation found stale roadmap diagram and SA115 scheduling/infra references that treated completed SA114 work as active; final review returned `STATUS: partial`. SA114 and SA116 remain unchecked, and this checkpoint is not a waiver. **Decisions needed:** none; a post-cap fix/re-review requires explicit user approval or a fresh scoped continuation.
  - **SA114/SA116 blocked checkpoint (2026-07-23; implementation and compaction complete — task remains open on SA114-ORDER-001).** Prior SA114 gate-drift checkpoint (gate-drift fixes, fail-closed donor preflight, normal/quiet `make check` dispatch, runtime-branding/leakage proof) independent review returned `STATUS: ok` with checkpoint merge-approved. SA116 resolved `SA114-QUALITY-001` via policy-compliant `contracts/resolvers.py` comment/docstring-only compaction (1761→1749 lines; `scripts/quality_baseline.json` unchanged at `max_lines` 1749; `make quality` exit 0 with 0 warning/critical regressions). Independent source review `STATUS: ok` with no findings. **SA114-ORDER-001** (medium, blocking for completion, completeness) — the exact ordered gate chain (`make check && make quality && make ci && make ci-e2e`) lacks a final `make ci-e2e` exit status on the literal ordered run. Second/final QG ran exact prefix `make check` → `make quality` → `make ci` successfully; `make ci-e2e` exceeded the 30-minute agent runtime window and returned no final exit code. Prior identical-tree E2E 30/30 is supporting evidence but does not resolve ORDER-001. Continuation: from clean Track 1 worktree synced to latest v87, rerun exact `make check && make quality && make ci && make ci-e2e` with sufficient runtime; all exit 0 and quarantine empty; obtain independent full-scope change review `STATUS: ok`; only then mark SA114/SA116 `[x]`. User authorized checkpoint merge despite incomplete completion criterion — this is not waiver of ORDER-001. SA114 and SA116 both remain unchecked/open.
  - **E2E/unit test-infra hardening (2026-07-23; landed on `v87`).** `scripts/test_e2e.sh` gained a low-memory preflight guard — it falls back to serial Core/CLI lanes when resting RAM/swap headroom is low, guarding against `systemd-oomd` reaping a long run (thresholds `QS_E2E_MIN_AVAIL_MB` default 4096 / `QS_E2E_MIN_SWAP_MB` default 3072; bypass `QS_E2E_NO_MEMORY_GUARD=1`) — plus a `QS_E2E_HEARTBEAT_INTERVAL` (default 15s) "still running" progress heartbeat during the concurrent-lane phase. `scripts/test_unit.sh` gained parallel-execution and coverage-handling enhancements. Both documented in `docs/technical/development.md`. This directly mitigates the **SA114-ORDER-001** failure mode — a long `make ci-e2e` hanging/returning no final exit code within the runtime window — and is available for that ticket's clean-continuation run. No product code, CLI behavior, or gate threshold changed.
  - **Generated-Dockerfile `pnpm install` network resilience (2026-07-24; landed on `v87`) — targets SA114-ORDER-001.** The generated project's `Dockerfile.j2` gave the Python/pip install step retry + timeout resilience but left the frontend `pnpm install` with none, so slow `registry.npmjs.org` throughput failed the Docker build on a single timed-out download — the exact SA114 CLI E2E failure mode. The frontend install now mirrors the pip house pattern: a 3-attempt retry loop plus raised pnpm fetch timeouts (`--config.fetch-retries=5`, `--config.fetch-retry-maxtimeout=120000`, `--config.fetch-timeout=300000`). `test_react_theme_integration` updated to assert on `pnpm install` (the literal `RUN pnpm install` line is now inside the retry loop); the SA90 emission-parity fixture rebaselined for the 3 identical react-variant Dockerfile hashes. Fast validation: SA90 parity green; 257 generator/template/theme tests pass. Hardens every generated project's build; full `ci-e2e` proof still pending Track 1 continuation.
  - **Installed-context managed-wiring traceback surface (2026-07-24; landed on `v87`) — unblocks SA112 Phase-1 diagnosis.** The `apply` managed-wiring regeneration wrapper (`apply_command.py`, `_regenerate_managed_wiring_for_apply._wiring_fn`) caught every exception and reduced it to `str(exc)`, discarding the traceback — the reason SA112's installed-context `NameError: QUICKSCALE_BILLING_ENABLED` has stayed pre-diagnostic across cycles (static passes confirmed the billing adapter uses a dict access, not a bare-name site, so the raising frame genuinely needs a runtime traceback). The wrapper now prints the full traceback to stderr when `QUICKSCALE_DEBUG` is set; default behavior and the user-facing failure banner are unchanged. The SA112 Phase-1 capture procedure is now `QUICKSCALE_DEBUG=1` on the installed-apply run. Validation: ruff clean; apply-wiring regeneration suites 65 passed.
  - **E2E memory-guard swap-veto correction (2026-07-25; landed on `v87`).** The low-memory preflight in `scripts/test_e2e.sh` treated low `SwapFree` as an independent veto on concurrent lanes, so a RAM-rich host demoted every `make ci-e2e` run to serial lanes purely because idle desktop processes had accumulated swapped-out pages — a condition that is normal under `vm.swappiness` defaults, does not clear on its own, and is not evidence of memory pressure (observed: 14.7 GB `MemAvailable` alongside 1.7 GB `SwapFree`, forcing serial and roughly doubling E2E wall time). The swap check is now gated on `MemAvailable` also sitting below a new comfort threshold `QS_E2E_COMFORT_AVAIL_MB` (default 8192); `MemAvailable` under `QS_E2E_MIN_AVAIL_MB` still forces serial on its own, and comfort is clamped up to the hard floor if configured inverted. Related fail-hard fix: a non-numeric threshold override previously made each `[ x -lt $threshold ]` error out and evaluate false, silently disabling the guard the operator was tuning; overrides are now validated and abort with a clear message, per [decisions.md §fail-hard-principle](docs/technical/decisions.md#fail-hard-principle). Validation: 9 threshold scenarios exercised against the extracted guard (current host state, mediocre-RAM with low and healthy swap, below-floor RAM, swapless host, garbage override, empty override, inverted comfort, bypass) — all correct; `bash -n` clean. Documented in `docs/technical/development.md`. No product code, CLI behavior, or gate threshold changed.
  - **E2E runner provenance banner and serial-cause attribution.** The runner prints a start timestamp and provenance banner (checkout path, HEAD, local changes, and an out-of-date warning against `QS_E2E_INTEGRATION_REF`) before any lane starts, so a long run's revision is identifiable; serial runs now report their real cause instead of always blaming `QS_E2E_PARALLEL=0`.
  - **E2E heartbeat stuck-lane detection.** The concurrent-lane heartbeat ticks every 60s and reports per-lane silence (`running` vs `running, quiet Nm`) derived from lane-log growth, so a wedged lane is distinguishable from one doing quiet work.
  - **SA114 Dockerfile QG1 hardening and fixture parity (2026-07-25; landed on `v87`).** The generated `Dockerfile.j2` now carries the QG1-corrected pnpm/poetry install resilience: `timeout 600` wrapper kills stalled (dead-socket) installs, `--reporter=append-only` emits timestamped progress under buildkit (no TTY spinner), `rc=$?` capture reports failure codes, `rc=124` identifies `timeout`-killed attempts explicitly. Two Node 24 incompatible pnpm config flags (`--config.fetch-retry-maxtimeout`, `--config.fetch-timeout`) removed per QG1 finding — Node 24's `AbortSignal.timeout` rejects string-typed timeout configuration at startup. Three SA90 emission-manifest Dockerfile SHA-256 hashes refreshed (identical across all three react variants). QG1 outcomes — all on the 2026-07-25 run from the Track 1 worktree synced to `v87` `76f36c83`: `make check` exit 0 (core 2527 passed / 1 skipped; CLI 1998 passed); `make quality` exit 0 with zero baseline regressions; `make ci` exit 0 (92.88% core/CLI equal-weight coverage mean; 94.41% module integration mean); `make ci-e2e` exit 1 (Core 35 passed; CLI 28 passed + 1 Docker build timeout at the unchanged 300-second caller timeout). Quarantine remained empty. File-scope distinction: `Dockerfile.j2` is the sole executable delta (35 lines changed); SA90 fixture is the sole data delta (3 sha256-only lines); `docs/technical/roadmap.md` and this changelog are checkpoint bookkeeping only; E2E test has no net diff — the QG2 timeout expansion was discarded. QG2 was canceled and returned no valid result; its partial unvalidated mutations (e2e test timeout expansion, non-minimal fixture rewrite) have been discarded. SA114 remains unchecked/open on **SA114-ORDER-001**. No completion claim or ORDER-001 waiver.
  - **SA112 pre-diagnostic plan cycles (2026-07-24; no implementation landed).** Four Track 3 continuations produced no product change. A disposable prototype proved installed-wheel `plan` for all 12 modules and reached installed `apply` through all 12 module embeds before failing with `NameError: name 'QUICKSCALE_BILLING_ENABLED' is not defined` during managed-wiring regeneration; `up`/`ps`/`manage migrate`/`down` were never reached. Independent change review rejected the prototype for merge and every executable, test, workflow, and changelog change from it was reverted to `v87` — no SA112 implementation exists on any branch. Two static discovery passes confirmed no bare-name evaluation of that symbol in the traced managed-wiring path (the billing adapter at `adapter.py:35` uses `settings["QUICKSCALE_BILLING_ENABLED"]`, a dict access that would raise `KeyError`) and no billing source/bundled/package version skew, establishing that the raising frame requires a runtime traceback — the obstacle since removed by the `QUICKSCALE_DEBUG` traceback surface above. Discovery produced validated topology snapshots `sa112-topology-v1` and `sa112-topology-v2`. Successive plan/plan-review cycles resolved `SA112-PR-001`, `SA112-PR-003`, `SA112-PR-004`, `SA112-PR-005`, `SA112-CR-001`..`SA112-CR-004`, `SA112-CR-006`, `SA112-CR-007`, `SA112-CR-008`, and `SA112-CONT-001` at plan level; the maintainer-authorized extra blocking-only cycle still returned `STATUS: partial`, leaving `SA112-PR-002` (high) and `SA112-CR-005` (medium). The maintainer then authored the literal continuation artifact (wheel-provision helper, lifecycle-lane argv/stdin registry, Phase-1 traceback procedure, fully-expanded ordered CI trigger tuple, validation order), which addresses both at plan-detail level; it is retained in `docs/technical/roadmap.md` as the specification for the next continuation. No diagnostic instrumentation, product code, runner, or workflow change was made, and no heavy Docker/PostgreSQL lane ran.
  - **SA112 plan-cycle status supersession (2026-07-26; task remains open).** The preceding SA112 historical entry's statement that the monolithic artifact is retained as the next executable specification is superseded. Two later focused plan-review cycles both returned `STATUS: partial`: `SA112-PR-002` remains high/blocking, while `SA112-CR-005` is resolved at plan-detail level. The unexpected prototype checkpoint `45e37a86` was fully neutralized by `56b27045`, leaving its 13 paths with zero net delta from synced `v87`; no SA112 implementation is present in the mergeable tree. Active work is now the serial SA112a–SA112f handoff chain in `docs/technical/roadmap.md`, beginning with SA112a's scoped plan-review gate. This is status clarification, not completion language.
  - **SA115 parked implementation checkpoint (2026-07-23; merge-order-blocked, uncommitted).** On `wt-track2` synced to `v87` at `c09e2093`: **Phase 1** added a session-scoped override of pytest-docker's `docker_compose_project_name` in `quickscale_core/tests/conftest.py`, deriving a worker-unique Compose project name from the lane's `QS_E2E_COMPOSE_PROJECT_NAME` plus `PYTEST_XDIST_WORKER` (single name when not under xdist), keeping the lane prefix intact so `cleanup_scoped_containers` still matches; `postgres_service` is reused unchanged. **Phase 2** added `QS_E2E_XDIST_WORKERS` to `scripts/test_e2e.sh` — RAM-derived default cap mirroring the Makefile `PYTEST_XDIST_WORKERS` heuristic, `0`/`1` degrading to the serial no-`-n` path, `>=2` appending `-n <N> --dist loadscope`, documented in `--help` and the `# Environment:` header, with the chosen worker count printed in the lane banner. Harness tests live in `scripts/test_e2e_parallel.py`. Evidence: 16 fixture tests, 13 runner-harness tests, `bash -n`, focused `ruff check` and `ruff format --check` — all pass. The fixture tests are deterministic naming/isolation tests and exercise no container lifecycle. The four changed files remain **uncommitted on `wt-track2`**; SA115 stays open and cannot merge until SA112 lands, since both edit `scripts/test_e2e.sh` and the shared CI surface.
  - **Frontend-source de-specialization stage 2a (SA105) — complete (2026-07-20).** Checkpoint content: removed `selected_modules`-driven frontend source omission (`REACT_THEME_OPTIONAL_FILES`, Pass 6 mapping filter, `_should_skip_optional_react_file`); converted `App.tsx`, `main.tsx`, `App.test.tsx`, and `PublicSocialPages.test.tsx` to static verbatim-copy files; `useModules.ts`, `Sidebar.tsx`, `Dashboard.tsx`, and `templates/index.html` always emit every module entry regardless of generation-time selection. Module and public-social routes use React.lazy/Suspense with fail-closed runtime flag checks via `window.__QUICKSCALE__`. Checkpoint SA90 exact-manifest parity 6/6 green. **Completion closure:** CR-002 resolved via side-effect-free `renderQuickScaleRoot` dispatcher seam with direct Vitest cases for `modules.social=true` (both social surfaces), `modules.social=false` (ordinary app root), and absent `modules.social` (fallback to app root) — all green. SYNC-001 resolved with static explanatory banners on all 10 dormant module surfaces: BlogPage, CrmPage, FormsPage, ListingsPage, FormRenderer, FormFieldRenderer, FormSuccess, useFormSchema, SocialLinkTreePublicPage, SocialEmbedsPublicPage; all banners hardcoded (no generation-time interpolation), preserving byte identity across projects. SA90 final delta: 11 approved hashes changed + 2 entries added, 0 removed/unexpected, provenance/.venv preserved. Validation: targeted pytest 21 passed; generated Vitest 20 passed; typecheck/build passed; SA90 6 passed; lint-frontend/frontend-proof passed; core unit 2,482 passed at 90.47%; core E2E 35 passed; policy checks passed. CLI E2E 8 passes with two pre-existing apply-confirmation failures and one infrastructure timeout (judged non-causal by review). **Independent change-review STATUS ok (confidence 94).** Advisory SA105-ADV-001 (non-blocking): `.venv` fixture records symmetrically excluded from parity. SA106 (identity de-spec) depends only on SA104 ✓ and remains independently assistant-executable.
  - **Frontend-source de-specialization stage 2b (SA106) — identity de-spec complete (2026-07-20).** Converted three identity-bearing `showcase_react` frontend source templates from Jinja-specialized `.j2` files to static verbatim-copy files: `useModules.ts` (was `useModules.ts.j2`), `Dashboard.tsx` (was `Dashboard.tsx.j2`), `Sidebar.tsx` (was `Sidebar.tsx.j2`). The `useModules.ts` `defaultConfig.projectName` is set to empty string — runtime value is injected via `window.__QUICKSCALE__.projectName` (already present in `templates/index.html.j2`). Dashboard and Sidebar read project identity at runtime via `useProjectConfig().projectName` instead of Jinja-rendered `{{ project_name }}`. All three files are now project-agnostic and byte-identical across generated projects on the same theme version. The generator's existing non-Jinja copy path (`_theme_non_jinja_emitted_paths`) handles emission automatically — no generator.py logic changes needed. SA90 emission fixture: 3 hashes updated (useModules.ts, Dashboard.tsx, Sidebar.tsx) across all 3 variants. **Quality-gate validation:** contract test stale references to `useModules.ts.j2` fixed (two occurrences in `test_module_manifest_contract.py`); beta-migration taxonomy updated — `renderQuickScaleRoot.tsx` and `RootDispatch.test.tsx` classified as `INTENTIONALLY_UNMANAGED` (user-owned frontend source, following `main.tsx`/`App.test.tsx` precedent). Both fix groups verified by targeted pytest. **Two-project identity proof:** `frontend/src` diff between two independently generated projects confirmed zero identity-bearing differences — `useModules.ts`, `Dashboard.tsx`, and `Sidebar.tsx` are byte-identical across projects; project name renders correctly from `window.__QUICKSCALE__.projectName` at runtime. **Evidence:** targeted pytest (contract-fix tests, taxonomy conformance checked); SA90 emission-parity fixture green across all 3 variants (3 hashes updated, 0 removed/unexpected, provenance/.venv preserved); frontend proof (`make frontend-proof`) passed; `make check` gate green. **Resolved findings:** QG-SA106-001 (consistency — `useModules.ts` correctly attributed to SA106, not SA105), QG-SA106-002 (consistency — taxonomy rationale for `renderQuickScaleRoot.tsx` and `RootDispatch.test.tsx` recorded in decisions.md U7). **Advisory (non-blocking):** CR-SA106-002 (test-gap — no durable focused test pins all three non-Jinja mappings and runtime rendering in a single test; deferred, not implemented).
   - **Fail-hard runtime-seam validation (SA107) — complete (2026-07-20).** Added `src/lib/validateQuickScaleSeam.ts` — a shared fail-hard validation gateway that every load-bearing `window.__QUICKSCALE__` read must pass through. The `validateQuickScaleConfig()` function requires the seam to exist, `projectName` to be a non-empty string, and `modules` to be an object whose present values are strict booleans; optional `publicPage` is validated when present. Replaced the silent fallback/optional-chaining in three consumers: `resolveProjectConfig()` (useModules.ts now calls `validateQuickScaleConfig()` instead of merging with a default config), `renderQuickScaleRoot()` (reads config through validation instead of `config?.modules?.social ?? false`), and `main.tsx` (reads `publicPage?.surface` from the validated config instead of `window.__QUICKSCALE__?.publicPage?.surface`). Removed the `defaultConfig` constant from `useModules.ts` — runtime validation is authoritative; no silent defaults remain. **Public-social contract resolved:** social embeds/link_tree templates inject `owner` and complete `modulePaths`; validation enforces `modulePaths` shape (`crm`, `social`, `analytics` as non-empty strings), `owner` (mode `solo`|`saas`, `currentOrgSlug` string|null), and `publicPage.module`/`publicPage.surface` enum checks. **Strict own-boolean module validation:** every module flag in `modules` is validated as a strict boolean; no `?? false` fallback or optional-chaining remains. **Production-shaped/inherited-boolean tests:** 16 seam validation tests plus 3 production-shaped public-social boot tests cover missing/malformed/well-formed seams, inherited-boolean module flags, and public-social boot smoke. **Fixture parity:** SA90 emission fixture updated with 2 new entries (validateQuickScaleSeam.ts, validateQuickScaleSeam.test.ts) + 3 updated hashes (useModules.ts, main.tsx, renderQuickScaleRoot.tsx) across all 3 variants, 0 removed/unexpected. **Validation (pre-sync independent QG):** `make lint-frontend` ✓, `make frontend-proof` ✓, `make test-unit -- --core` 2,482/0 at 90.47% ✓, Vitest 35/35 ✓. **Post-sync phase:** `make lint-frontend` ✓; direct isolated `ProjectGenerator.generate` render + generated `pnpm install`/`type-check`/`build` ✓; `make test-unit -- --core` 2,512/0 at 90.46% ✓. Post-sync `make frontend-proof` timed out twice at the render step (identical isolated generator succeeded; remaining commands passed) — the wrapper timeout is noted transparently and does not indicate a product regression. **Independent change-review STATUS ok.** SA107-CR-001, SA107-CR-002, SA107-TEST-001, SA107-DOC-001 all resolved; no remaining blocker or advisory.
  - **Tenant isolation unified.** Every tenant-scoped model inherits one shared isolation base instead of hand-copied boilerplate; a project-wide check requires each model to be explicitly tenant-scoped or excluded and is wired into generated projects' own CI.
  - **Fail-closed database access.** The app refuses to boot under a database role that bypasses row-level security (`BYPASSRLS` or `SUPERUSER`) unless explicitly overridden; production requires a restricted tenant-safe role, with the elevated role reserved for migrations.
  - **Database-level parent/child isolation.** Parent/child records (e.g. a CRM contact and its notes) are kept in the same organization via `NOT DEFERRABLE` composite foreign-key constraints, replacing the fragile trigger-based check; a conformance gate verifies every composite FK.
  - **Fixed a row-level-security bug affecting pooled database connections**, and removed redundant per-statement tenant-context setup within a transaction.
  - **Public `org_scope` API.** Internal org-context primitives are retired in favor of one public `org_scope()` seam used everywhere, with a canonical `PublicSystemOrgReadMixin` for anonymous public reads and a lint gate enforcing the boundary.
  - **Operator tooling.** Admins can "view as" a specific organization from the admin panel (visible banner, one-click exit); a single audited `operator_access()` path gates cross-tenant reads and sensitive maintenance commands, logging who ran what and why.
  - **`TenantModelAdmin` base.** An orgs-owned admin base resolves the active org and scopes every admin view through `org_scope()`, fail-closed; every module admin is ported onto it, removing hand-copied scoping and inline formsets.
  - **Fail-hard configuration sweep.** Missing or invalid module settings (page sizes, rate limits, feature flags, provider/enabled flags, proxy config, storage backend, orgs mode) now raise at boot or apply time instead of silently defaulting or coercing; validators are wired into the apply path and the dead coercions they masked are removed.
  - **`QUICKSCALE_MODE` fails hard at boot** instead of defaulting to `solo`, and legacy `quickscale.yml` keys are rejected with a named-replacement error instead of being silently translated.
  - **Public-content sanitization.** Markdown-rendered links on public blog/listing pages are run through an allowlist URI-scheme sanitizer (blocking `javascript:` and obfuscated schemes per the WHATWG URL spec); the analytics template-tag payload is escaped before `mark_safe`; and orgs debug-view `next` redirects are validated same-host/scheme.
  - **Non-string public form submissions** return validation errors instead of 500ing.
  - **Client-IP and cache infrastructure.** Generated projects gain a canonical `get_client_ip()` helper with trusted-proxy config (matching DRF's proxy-count semantics) and a shared `CACHES` backend (Redis when available, else `DatabaseCache` with a deploy-script `createcachetable` step); forms/blog throttles and IP logging point at the shared seam.
  - **Cross-org account-deletion safety.** User FKs on org content are `SET_NULL`/`PROTECT` (never `CASCADE`) so deleting a user no longer destroys content across organizations, enforced by a conformance gate.
  - **Last-owner invariant.** One canonical last-owner deletion-blocking check (with `select_for_update` concurrency protection and a `pre_delete` cascade backstop) replaces three divergent copies and is enforced at the account-deletion boundary.
  - **Secrets kept out of logs and argv.** The deploy script prints per-variable set/`MISSING` status instead of raw values; Railway/DR adapter secrets are piped via stdin instead of command-line arguments, with a hard-stop rather than an insecure fallback.
  - **Billing ownership and integrity.** Subscriptions are authoritatively owned by the organization; a partial unique constraint enforces credit-ledger idempotency at the database level; account-deletion cleanup distinguishes a benign "nothing to cancel" from an anomalous subscription missing its Stripe id; and billing's state-changing endpoints move onto the DRF baseline (automatic CSRF) with a decisions-doc rule naming the two sanctioned JSON-API bases.
  - **Backups and disaster recovery.** Admin backup create/prune/restore run off the synchronous request path via background subprocess with atomic claiming; the uploaded-restore copy is crash-safe; stranded `STATUS_RESTORING` artifacts are detected and recoverable; DR media-backend resolution fails hard on real misconfiguration instead of silently falling back to local storage; and a `csrf_exempt` CI gate requires every callsite to pair with verification.
  - **Core/module boundary.** A single `quickscale_core.runtime` facade is the only public core import surface (import-linter enforced); a Django-free persistence protocol lets backups/DR inject model access; managed adapters register explicitly instead of at import time; and a bidirectional import ban keeps core from importing modules.
  - **Resilient project generation.** `apply` checkpoints progress after each step and resumes on failure; destructive or remote actions require explicit confirmation; `apply --force` stages to a backup directory with full rollback; module `pyproject` splices route through a validated TOML writer; and generator internals are split into smaller focused modules.
  - **Declarative module configuration.** The `module.yml` derivation loader is proven by round-trip tests, listings is fully migrated off imperative wiring, and a guardrail blocks any module from reintroducing it; a manifest-sync gate fails on drift between each module's `module.yml` and its core snapshot.
  - **Contract-version tracking.** Generated projects record the contract version they were built against, a compatibility gate probes each module against its claimed minimum core version, and `quickscale status` flags modules ahead of the project's contract.
  - **Retired the `showcase_html` generator theme** in favor of React-only output; existing generated projects keep their user-owned files.
  - **Generator emission mapping** is exported as a single authoritative function shared by production and its beta-migration conformance gate.
  - **Cross-module signal decoupling.** Orgs no longer reverse-imports CRM/auth: a `organization_created` signal drives CRM's default-stage seeding (self-sufficient under restricted roles), the auth adapter fails hard when the module is absent, and version-floor constraints on `required_modules` are enforced at apply time.
  - **PostgreSQL-only test infrastructure.** All modules test exclusively against PostgreSQL (SQLite removed everywhere) under a shared pre-provisioned restricted (`NOBYPASSRLS`) role; a dedicated integration gate proves tenant isolation under that role and is structured so it cannot pass by silently skipping.
  - **Parallel integration execution.** The per-module integration loop runs as a configurable parallel worker pool with isolated coverage/log state, joined failure propagation, deterministic replay, and signal-safe cleanup, matching sequential verdicts and coverage.
  - **Untracked accreting build/test artifacts** (coverage/log files, blog test-upload media) and pointed test `MEDIA_ROOT` at a temp directory.
  - **Migration squash (SA92):** All nine modules squashed to final-schema `0001_initial` (`organization_id NOT NULL` from row zero); fresh-only posture ratified with pg_policies/catalog/data hash parity against v87; Finding 8 (module-RLS-context-procedural) closed.
  - **DR persistence port (SA89a+SA89b):** Backups ORM lifecycle moved behind a fail-hard provider registry in `dr_engine/persistence.py`; bidirectional core↔module import ban enforced; Finding 1 (circular lattice) closed.
  - **Parallel integration worker pool (SA91):** Per-module integration loop runs as a configurable parallel worker pool (`QS_INTEGRATION_JOBS`) with isolated coverage/log state, joined failure propagation, and signal-safe cleanup.
  - **Per-module restricted-role gates closed under SA82 baseline:** CRM (SA84, 263 pass/21 skip/0 fail), listings (SA86), blog (SA95, non-reproducible post-SA92), forms (SA85), orgs (SA77), notifications (SA79). `make test-integration` green with empty quarantine.
  - **Pre-publish module sweeps (SA96-T1/T2):** All 12 modules re-verified green in isolation on post-SA92 v87 — no regression, empty quarantine.
  - **React-only theme retirement (SA94):** `showcase_html` generator theme retired; fail-closed `theme_validation` preflight wired into all 8 CLI callsites; generated project output is now React-only.
  - **Quality baseline established (GATE-quality):** Makefile `quality` target gates complexity/dead-code against `quality_baseline.json`; baseline reset for v87.
  - **SA97 — shared tenant test-state reset fixture.** Consolidated six divergent private copies (CRM `_reset_crm_test_state`, forms `_reset_test_state`, blog `_reset_current_org_context`, orgs `_reset_test_state`, listings `_reset_current_org_context`, social `_reset_test_state`) into one conftest-importable autouse fixture in `tests_shared/reset_state.py`. The shared fixture resets the org ContextVar, PostgreSQL GUCs (`app.current_org_id`, `app.operator_access`, `ROLE`), AF9 per-transaction priming memo, and Django cache before and after every test — preserving the full CRM/forms contract as the canonical superset (blog/orgs/listings/social reconciled upward). All Django imports are deferred until fixture execution, so the fixture is safe for both pytest-django-managed (CRM, orgs) and self-managed (forms, blog, listings, social) conftests. The commons ownership rule is recorded in [`decisions.md §SA97`](./docs/technical/decisions.md#test-commons-ownership): orgs owns org-context runtime helpers, `tests_shared/` owns cross-module test plumbing, citing `apply_force_rls`/`revert_force_rls` (SA92) as house precedent. No private copy of the reset contract remains in any module conftest. **Independent review (Adaptive-change-review pass 2) STATUS ok — CR-SA97-001 resolved.** `make test-integration`: all 12 modules pass, empty quarantine, 94.39% mean coverage. Remaining non-blocking advisories: CR-SA97-002 (low — no direct lifecycle edge-case test) and CR-SA97-003 (low — docstring at `tests_shared/reset_state.py:4-6` states three copies unified while six were migrated).
  - <a id="sa98"></a>**SA98 — shared rendered-link sanitizer closeout.** Moved the `_sanitize_href`/`_sanitize_rendered_html` implementation into the orgs-owned shared seam and switched blog/listings consumers to it; no second runtime copy remains. Evidence: orgs **874 passed / 11 skipped**, blog **206 passed**, listings **132 passed**; scoped Ruff/MyPy, sanitizer uniqueness/caller proof, diff check, and manifest sync passed. Independent review returned **STATUS ok with no findings**. No SA98-specific blockers/findings were identified. Broad `make check`/`make quality` baseline failures (coverage/dead-code/complexity) remain unrelated and outside this ticket; they were not fixed, and this closeout makes no E2E claim.
  - **SA99 — `quickscale_devtools` brought under repo ruff/mypy coverage.** arch-audit [Finding 7](docs/others/arch-audit.md) cheap sub-item. Added `quickscale_devtools` to `ruff.toml` known-first-party and to Makefile `lint`, `lint-fix`, `format`, and `typecheck` section loops (new `devtools` section with `--devtools`/`-d` flag, added to default `ACTIVE_SECTIONS` and `SRC_DIRS`). No mypy section override needed — devtools source passes strict global defaults. `make lint` and `make typecheck` cover devtools and exit 0. Finding 7 tuple-derivation remainder stays unscheduled (gated on third consumer / public update command). `DEFAULT_SECTIONS` caller-parity preservation resolved CR-SA99-001 (stale default test dispatch); CR-SA99-002 and CR-SA99-003 also resolved. **Independent change-review pass 2 STATUS ok.**
  - **SA102 — CI lint/typecheck devtools coverage (closes tech-audit TA61).** Added explicit `--devtools` to all ten scoped invocations across `ci.yml`, `publish.yml`, and `scripts/check_ci_locally.sh`; corrected the original five-site inventory discrepancy. Aggregate and CLI-scoped lint/typecheck checks passed, and a temporary devtools Ruff failure probe was rejected and removed.
  - **SA103 — blocking rendered-frontend proof gate (2026-07-19; complete).** Added the blocking `lint-frontend` workflow job with Node/pnpm/cache setup, wired the local `make ci` / `scripts/check_ci_locally.sh` frontend-lint fan-out with an absent-Node/pnpm guard that does not mask real failures, and made `publish.yml` run `frontend-proof` before downstream build/publish. A deliberate injected TypeScript error made `make ci` exit 2 with `TS2322`/`TS6133`; `Dashboard.tsx.j2` was restored byte-identically; `make frontend-proof` passed. QG1's 15 focused tests and bounded `PYTEST_XDIST_WORKERS=2 make ci` passed (4,536 core/CLI tests, 323 backups tests, 2 expected skips, 92.88% mean coverage with every file ≥80%, integrations green); QG2's 19 focused tests plus six delta tests and Ruff/MyPy/YAML/workflow-order/Bash/Make/diff checks passed. Independent change-review pass 2 returned `STATUS: ok` and resolved **SA103-REV-001**, **SA103-REV-002**, and **SA103-ADV-001**; no blocker or open decision remains. Hosted GitHub Actions were not remotely observed; this is a non-blocking residual caveat.
  - **SA104 — Frontend static-source de-specialization stage 1 (2026-07-19; complete).** 57 byte-static theme files across the `showcase_react` frontend theme/verbatim-copy surface (including the converted theme-root `frontend/index.html`) moved onto the verbatim-copy path — `.j2` suffix and `{% raw %}` wrappers removed, now routed through the generator's existing non-Jinja copy path (`_theme_non_jinja_emitted_paths`). 13 genuine active-Jinja files remain. Emitted byte/path/mode unchanged: SA90 emission-parity fixture stayed green; two-project diff of `frontend/src` for the now-static files showed zero differences; `test_generated_project_runtime.py` passed. Added `test_non_jinja_theme_root_index_html_routed_correctly` regression test. QG run 2/2: Ruff/MyPy; SA90 20/20; React 51/51; zero unexpected differences across 61 verbatim-copied frontend files and identical `frontend/index.html` SHA-256; runtime 5/5; frontend proof/lint; `make test-unit` 4,482. Independent change-review pass 2 returned **STATUS ok**; **SA104-CR-001** (blocking completeness — root `index.html.j2` was raw-only static) fixed and resolved; Caller-Parity PASS. No blocker or open decision remains. User-approved project-intake setup moved `docs/technical/project_intake.yml` to root `adaptive.intake.yml` and rewired `adaptive.rules.md` with no profile semantic changes. SA105 and SA106 are now unblocked by SA104; SA105 remains blocked by its own dormant-files maintainer decision; SA106 is assistant-executable. Root `check-json` hook exclude (`^quickscale_core/src/quickscale_core/generator/templates/themes/showcase_react/tsconfig\.json$` JSONC) reviewed `STATUS ok`; all 10 hooks pass; SHA-256 `9aaae682d5e2df06d1375bc66e738ab372a9cd42106353752f42cdda2d875d99` unchanged. **Advisory SA104-ADV-001** (severity: medium, disposition: advisory (non-blocking), category: consistency): generated `quickscale_core/src/quickscale_core/generator/templates/.pre-commit-config.yaml.j2:10` still runs strict `check-json` against emitted `frontend/tsconfig.json`; pre-existing, deferred to separate follow-up.
  - **SA100 — recovery-ledger theme exemption narrowed (complete).** TA58 now uses an explicit validator opt-in that permits only the recovery ledger's `__checkpoint__` placeholder; stale/retired recovery themes retain fail-closed remediation, all other callers remain unchanged, and TA59's dead `_RECOVERY_PROBE_PATHS` constant plus adjacent `.quickscale` comment typos were removed. Adaptive-change-review pass 1: **STATUS ok**, no findings, caller parity passed. Validation: 53 targeted tests passed; Ruff check/format and `git diff --check` passed; numeric coverage not generated. Blockers: none.
  - **TP2 — unit-suite xdist (2026-07-18; independent change-review pass 1 STATUS ok, no findings).** Added root `pytest-xdist==3.8.0`, refreshed `poetry.lock`, and installed the dev environment. `make test-unit` uses `-n auto --dist loadfile` by default; `PYTEST_XDIST_WORKERS=0` runs serially and `PYTEST_XDIST_WORKERS=N` pins workers. Serial/xdist collection sets matched exactly (core 2,354; CLI 1,993). CLI had 1,993 passed and 90.36% XML coverage in both modes; wall clock improved 56.36s → 33.44s. Core had 2,353 passed + 1 skipped and unchanged 80.39% XML coverage; wall clock improved 176.03s → 68.40s. The existing core 90% package floor remains below the unchanged 80.39% measurement, but TP2 requires parity and unchanged coverage—not that pre-existing floor to become green—so this is a waived, non-blocking baseline finding. No coverage floor or test was changed.
  - **TP2b — xdist-safe coverage phases (2026-07-19; complete).** `make test-cov` uses isolated phase `COVERAGE_FILE` paths, one explicit `coverage combine` pipeline, guarded xdist defaults, and a serial override while preserving the backups requirement behavior. Both exact commands accepted: `QS_BACKUPS_DB_USER=quickscale_test_role make test-cov` exit 0 and `QS_BACKUPS_DB_USER=quickscale_test_role make test-cov REQUIRE_BACKUPS_COVERAGE=1` exit 0 — **4,536 core/CLI passed, 323 backups passed, 2 skipped; 92.88% equal-weight package mean** (core 95.27%, CLI 90.49%); all 90 files ≥80%; default 24 auto workers, no stall. **CR-TP2B-001**, **CR-TP2B-002**, and **QG-TP2B-001** resolved. Final change-review **STATUS ok** (confidence 94). No blocking findings remain. The prior `auto`-worker Poetry-lock contention is a non-blocking advisory — it did not recur in the accepted runs. Evidence recorded in the roadmap completed task block.
  - <a id="sa93-continuation"></a>**SA93 — E2E green-gate closeout (2026-07-18).** Canonical local evidence: exact `make ci-e2e` is green (all 12 stages, Core 35, CLI 29, all 12 integration modules, 91.90% mean coverage (combined), every file at least 80%, empty per-file quarantine). The initial hosted run [29641299861](https://github.com/Experto-AI/quickscale/actions/runs/29641299861) aborted its generated-project Docker job because the workflow supplied one apply confirmation while the current CLI required two. The workflow prompt-parity fix was published in commit `667396cc` (pipe `y\ny\n`); hosted rerun [SA93-EVID-001](https://github.com/Experto-AI/quickscale/actions/runs/29641954573) verified `headBranch=v87`, `headSha=667396cc44ac7c1737c0b7825963c72963bc9fd2`, `event=workflow_dispatch`, `status=completed`, and `conclusion=success`; **Test Generated Project Docker Build** and **Full E2E Tests (PostgreSQL + Playwright)** both succeeded. Hosted evidence owns only those two green jobs and the generated-project Docker contract; it does not imply that the hosted workflow executed `make ci-e2e`. All 11 prerequisites and open decisions (zero) were resolved; SA93-REV-005 remains an unresolved advisory/out-of-scope test gap and SA93-ADV-001..004 remain advisory/non-gating.
  - **TP1 — parallel local-CI static gates.** `scripts/check_ci_locally.sh` now launches independent stages 2–9 concurrently, buffers and replays output in declaration order, attributes every failed gate, and preserves `QS_CI_PARALLEL=0` for serial debugging; database-dependent and E2E stages remain serial. The maintained `test-ci-local-parallel` target covers concurrency, lint/typecheck/manifest failure attribution, serial parity, and ownership-safe TERM/INT/HUP cleanup without stale-PID signaling, and runs in both `make ci` and GitHub CI. Same-host full-gate timing was 539.70s parallel versus 550.96s serial (2.04% faster). Final `make ci` passed with 14 TP1 tests, 4,395 core/CLI tests, 323 backups tests, all PostgreSQL integration suites, and unchanged coverage floors (91.90% equal-weight policy mean). CR-TP1-REV-001/002/003 are resolved; no blocker remains. **Independent change-review pass 3 STATUS ok.**
  - **SA96-GATE blocked checkpoint (2026-07-19; not complete):** Authored implementation was limited to five new test modules — `quickscale_core/tests/test_dr_engine_orchestration.py`, `quickscale_core/tests/test_dr_engine_orchestration_a2a.py`, `quickscale_core/tests/test_dr_engine_orchestration_a2b.py`, `quickscale_core/tests/test_dr_engine_orchestration_a3.py`, and `quickscale_core/tests/test_manifest_resolver_phase2_coverage.py`. Canonical coverage recorded **2,480 passed, 1 skipped, 90 deselected; 6,026/6,670 = 90.34%**, including orchestration **745/1,011 = 73.69%** (**+35 from 5,991**); focused authored tests passed (127), Ruff/MyPy passed, and `make check` exited 0 in **3m35.303s** with a **94.41%** integration-policy mean and empty quarantine. This supersedes/resolves the prior coverage blocker **SA96-GATE-BLK-001**. `make quality` exited 2 with **9 critical + 10 warning baseline regressions** at `.quickscale/quality_report.md` lines 32–52, so **SA96-GATE-BLK-002** remains blocking. `make ci` and `make ci-e2e` were not run; independent change-review pass 2 returned **STATUS ok** with **SA96-CR-001 (blocking)** and **SA96-CR-002 (advisory)** resolved and no new findings. This blocked checkpoint is safe to merge; SA96-GATE remains unchecked. No source, config, baseline, or threshold changes were made.

  - **SA96-GATE — green-gate join complete (2026-07-20).** All four commands exit 0 on a clean rerun at the prior synced code baseline (post-SA92 squash) with `QUARANTINE_TICKETS` **empty**: `make check` exit 0; `make quality` exit 0 with zero baseline regressions; `make ci` exit 0 (coverage mean 92.89%, all 90 files ≥80%); `make ci-e2e` exit 0 (64 E2E passed); integration: 2,262 passed, mean 94.41%. **Resolved operational finding:** initial aborted attempts left an orphan `test_test_quickscale_orgs` Django mirror DB (double-prefixed). No repository defect or threshold change was needed — no stale test workers remained, and only the disposable double-prefixed mirror database was dropped before the clean rerun passed. No open product blocker at that checkpoint. **Superseded release-path status:** later installed-context coverage work added SA111a/SA112; SA111a is complete, SA112 is next, and **SA96-PUBLISH** remains human-only and held until SA112 closes.

  - <a id="sa109"></a>**SA109 — Fix installed-wheel module discovery (2026-07-20; complete).** Module discovery previously only consulted the maintainer monorepo `quickscale_modules/` workspace and raised `ImproperlyConfigured` for any other context — the installed wheel crashed on import. Phase 1: added `discover_bundled_module_names()`, `get_bundled_manifests_path()`, `get_resolution_source()`, and the `ModuleResolutionSource` enum to `module_discovery.py`; added bundled-manifest access/discovery/fallback over pre-existing synchronized snapshots under `quickscale_core/data/manifests/*/module.yml` for all 12 shipped modules. Phase 2: `get_discovered_module_names()` in `module_catalog.py` now resolves source inventory first, then falls back to bundled inventory, then fails hard — the three-tier precedence that powers lazy discovery in `config_schema.py` (via `_validate_modules_section`) and `module_commands.py` (via `_LazyModuleChoice`), replacing the two previous import-time eager discovery sites. Phase 3 (this entry): AF7 decision recorded in `decisions.md` §Bundled Module Inventory and Source-Required Paths (AF7); SA109 roadmap item marked `- [x]`; all same-fact summary, diagram, and readiness statements updated. Source-required operations (`get_modules_base_path`, `discover_shipped_module_paths`, `load_module_manifest`, `refresh_managed_adapters`) remain fail-hard in the installed-wheel context — the AF7 fail-hard intent is preserved where it is genuinely load-bearing. **Validation:** 327 focused AF7 tests passed; 2,505 core unit tests passed; 1,997 CLI tests passed (with one pre-existing unrelated Track 2 taxonomy failure). Corrected `make install` installed-wheel smoke exit 0 from outside the source tree — `quickscale --version`, `quickscale version`, `quickscale --help`, `quickscale status` all exit 0 with exactly 12 bundled module names. Existing synchronized snapshots are verified by `make check-manifest-sync` (pre-existing manifest snapshots, not manifests added by SA109 — Phase 1 created the bundled manifest directory as a new snapshot target, but the per-module `module.yml` files were already present in `quickscale_modules/` before this phase). AF7 decision is new in the policy hub (was previously docstring-only). The next critical-path step is **SA110** (installed-artifact smoke gate, deps SA109 ✓).

  - <a id="sa110"></a>**SA110 — Installed-artifact smoke gate (2026-07-20; complete).** QG proofs confirmed SA110-REV-003 (negative SA109 regression — disposable core copy with manifests removed, gate red with ImproperlyConfigured (not ImportError)) and SA110-REV-004 (deterministic candidate ordering — 7/7 shim tests pass). Advisory SA110-ADV-001: actual 3.14/3.15 binaries unavailable; boundary tests use version-reporting shims; hosted evidence deferred. Adds `scripts/smoke_install.sh` — a gate that builds all three QuickScale wheels (quickscale_core, quickscale_cli, quickscale) from per-run staged copies (never mutates source pyproject.toml), installs them into a throwaway venv **outside the source tree** (so the monorepo `parents[4]/quickscale_modules` cannot resolve), and runs 19 probes from an external workdir with sanitized PYTHONPATH/PYTHONHOME. The gate is designed to catch the class of regression SA109 fixed — a wheel that crashes on import — before it can reach PyPI. Eighteen probes (version/help commands) assert exit 0 and no traceback: `quickscale --version`, `quickscale version`, `quickscale --help`, `--help` for every registered subcommand (version, up, down, shell, manage, logs, ps, deploy, dr, update, push, plan, apply, status, remove). The 19th probe runs `quickscale status` from outside a project and asserts exit 1, the expected "Not in a QuickScale project directory" diagnostic, and no traceback. The `quickscale status` outside-project exit-1 contract is preserved by explicit user decision — the CLI was not changed. Uses existing project conventions: `set -euo pipefail`, `_python_requirement.sh` sourcing, Poetry build with per-run staging and isolated build venvs, throwaway venv in `/tmp`, trap-based cleanup. Applies the full `>=3.13,<3.15` requires-python constraint during interpreter candidate selection (iterates past invalid candidates). **Design decisions:** staged copies eliminate the race-unsafe in-place pyproject.toml backup/restore pattern; `run_smoke_probe` sanitizes PYTHONPATH/PYTHONHOME and cds to the external workdir for every probe; `smoke_select_python` iterates candidates with the full spec; `QS_SMOKE_REGRESSION_CORE` env var allows a disposable modified core source for negative SA109 regression testing. **Implementation artifacts:** `scripts/smoke_install.sh` rewritten, `Makefile`/`publish.yml`/`roadmap.md`/`CHANGELOG.md` updated. No package API, CLI behavior, or version was changed. Advisory SA110-ADV-001 retained: actual 3.14/3.15 binaries unavailable — boundary tests use deterministic version-reporting shims; hosted Python 3.14 evidence deferred.

  - **SA108 — Frontend de-specialization migration-doc rewrite (2026-07-21; complete).** Corrected `docs/planning/beta-site-migration.md` frontend guidance to accurately reflect the project-agnostic, byte-identical `frontend/src` contract (SA105/SA106/SA107) while distinguishing legacy pre-SA105 in-place recipients from fresh current-theme generated recipients. Removed obsolete `useModules.ts` `projectName` identity-fix steps and `main.tsx` conditional-patch checklist items. Dropped overbroad per-module page-copy claims: blog/crm/listings pages are always-emitted dormant files only for fresh current-theme recipients (SA105); legacy pre-SA105 recipients have no retroactive dormant guarantee for any module surface. The shipped post-apply adoption step copies only missing forms/social surfaces; it does **not** backfill blog/crm/listings. Corrected validator name from `validateQuickScaleSeam()` to `validateQuickScaleConfig()`. Qualified main.tsx byte-identity claim (version-dependent). Clarified routing responsibility boundaries: SA107 `validateQuickScaleConfig()` validates seam fail-hard; App.tsx gates ordinary module routes; `renderQuickScaleRoot()` handles public-social dispatch; main.tsx is byte-identical only among projects generated from the same theme version. Updated file-classification table, checklists, and continuation guide consistently. Legacy compatibility finding documented: pre-SA105 in-place recipients have no retroactive dormant guarantee for any module surface; the post-apply adoption step copies only missing forms/social surfaces and preserves existing copies; blog/crm/listings absent after continuation require manual handling. Track 2 frontend de-specialization chain (arch Finding 10) is now fully closed. Roadmap SA108 marked `- [x]`; Track readiness date updated to 2026-07-21.

  - <a id="sa113"></a>**SA113 — Installed-context implication-resolver bundled-manifest fallback with fail-hard inventory validation (2026-07-21; complete).** `resolve_module_implications` (`quickscale_core/src/quickscale_core/manifest/implications.py`) previously called `get_modules_base_path()` with no fallback, crashing with `ImproperlyConfigured` in installed-wheel contexts where no source-tree `quickscale_modules/` workspace exists. Both CLI callers — `plan` (`plan_command.py:102`) and `apply` (`apply_command.py:973`) — reached the crash via `_materialize_implied_module_configs`. Phase 1 mirrors the SA109 pattern: when `get_modules_base_path()` raises `ImproperlyConfigured`, the resolver falls back to `get_bundled_manifests_path()` for bundled manifest snapshots (`quickscale_core/data/manifests/*/module.yml`). Phase 2 adds a fail-hard inventory boundary: in bundled-fallback mode, missing selected or implied module manifests raise `ImproperlyConfigured` instead of being silently skipped. The AF7 fail-hard contract is preserved — when both source-tree and bundled manifests are unavailable, `ImproperlyConfigured` is still raised. Source-required operations remain unchanged. **Validation:** 7 focused SA113 unit tests (fallback resolves billing→orgs chain, explicit path unaffected, explicit path still resolves when source tree unavailable, missing selected manifest raises, missing implied manifest raises, malformed manifest raises, both-unavailable fail-hard) passed; 30 existing implication tests unchanged; targeted implication suite 37 passed. Roadmap SA113 marked `- [x]`; all CR findings resolved.

  - **SA111a — Installed-context `plan` coverage complete (2026-07-22).** Extended `scripts/smoke_install.sh` with a 20th non-interactive `quickscale plan` probe that selects all 12 modules via scripted stdin and runs from the installed wheel outside the source tree. The probe exposed a second installed-context crash in `_load_notifications_manifest`; that loader now follows the SA113/AF7 pattern by falling back from `get_modules_base_path()` to bundled manifests on `ImproperlyConfigured`. Two regression tests cover both notifications option callers. The probe unsets inherited `PYTHONPATH`/`PYTHONHOME`; a deliberately source-contaminated `PYTHONPATH` run passed all 20 smoke probes. Ruff, MyPy, and 78 resolver tests passed; independent change-review resolved `SA111A-CR-001` and approved completion. The broad core suite retained 14 unrelated baseline failures (11 test-isolation contamination failures and 3 React-theme failures), with 2,562 passing and 90.48% coverage.

  - **SA111b — Fast in-monorepo fallback regression test (2026-07-23; complete).** Added `TestSa111bFallbackRegression` class with `test_fallback_resolves_billing_to_orgs` in `quickscale_core/tests/test_manifest_implications.py`. Monkeypatches `implications.get_modules_base_path` to raise `ImproperlyConfigured` and asserts `resolve_module_implications(["billing"])` still resolves billing → orgs via the bundled-manifest fallback (`get_bundled_manifests_path`). Runs inside `make check` as a fast early-signal companion to SA111a's authoritative installed-wheel probe. Focused test passes (1/1); existing SA113 bundled-fallback tests unchanged (7/7); targeted implication suite 38 passed. Roadmap SA111b marked `- [x]`.

  - **SA101 quality-remediation closeout (2026-07-19; complete).** Cleared all 19 SA96-GATE-BLK-002 baseline regressions (9 critical + 10 warning) against the **unchanged** `scripts/quality_baseline.json` per the Option A decision — no baseline, threshold, or quality-gate edits. Work ran in nine serial behavior-preserving phases on Track 3 (`wt-track3`, synced from `v87` at `d7ccf24`), with the Adaptive flow producing a discovery snapshot, a planner-authored nine-phase plan (`SA101-plan-r2`), and an independent plan-review that returned `STATUS: ok` after resolving `SA101-PR-001/002/003`. Per-phase quality-ledger progression: 19 → 16 → 14 → 11 → 9 → 8 → 6 → 4 → 2 → 0. Refactors were file-local extraction plus direct-identity aliases. Phase 9 compacted `quickscale_modules_crm/migrations/0001_initial.py` (605 → 413 lines) and `quickscale_modules_forms/migrations/0001_initial.py` (758 → 137 lines) with **exact parity** (SHA-256-matched). `make quality` exit 0. The v87 shrink-only quality-maxima policy was recorded in [`decisions.md`](docs/technical/decisions.md) §Migration-Squash Decision SA92 (§5): docs do not affect the gate, and these migrations were remediated, not allowlisted.
    - **`SA101-VAL-001` — RESOLVED via option (a) (2026-07-19 experiment):** the reported restricted-role failures were reproduced on **clean pre-SA101** (`d7ccf24b`) and confirmed **pre-existing, not caused by the compaction**. Same environment, restricted `quickscale_test_role` (`NOBYPASSRLS`), migration/RLS tests included: CRM **3 failed / 281 passed / 3 errors** on clean pre-SA101 vs **284 passed / 0 failed / 0 errors** at HEAD with SA101; Forms green in both states (212 passed). SA101's Phase 9 rewrite of `crm/tests/test_migrations.py` fixed the pre-existing fixture defect. No maintainer decision remains.
    - **Final closeout (2026-07-19):** `make check` exit 0 — 6,943 passed, 86 skipped, 12 deselected, zero failures; core 90.36%, CLI 90.49%, integration aggregate 94.41%. Independent full change-review returned `STATUS: ok`, confidence 94, no blocking or advisory findings. Worktree remained clean. Known BYPASSRLS-dependent skips are pre-existing. `make ci` and `make ci-e2e` were not run. SA101's feeder condition for SA96-GATE is met; the four-command join is the next pending step.
  - **TP3a implementation (completed).** The E2E harness allocates a per-run app port, exports lane-specific Compose/container scopes, and limits cleanup to that scope; CLI fixtures consume the allocated port and generated project prefix while pytest-docker PostgreSQL remains dynamically mapped. Independent review pass 2 returned **STATUS ok** and resolved CR-TP3A-001. The merged-v87 rerun passed stages 1–11 (Core E2E, unit/coverage, backups, integration, 91.90% mean coverage, every file ≥80%), but the final CLI E2E stage did not complete within the runner allowance, so integrated `make ci-e2e` green was not established — that evidence gap is carried forward as a TP3b precondition. Remaining low advisory follow-ups — CR-TP3A-002 (rare port allocation TOCTOU) and CR-TP3A-003 (unsafe custom scope leading character) — remain open and were not fixed.
  - **TP3b — concurrent Core/CLI E2E lanes (completed).** Core and CLI lanes run concurrently by default, with `QS_E2E_PARALLEL=0` for serial debugging. The harness uses isolated scopes/ports, deterministic log replay, signal-safe cleanup with active-PID handling, nonzero failure attribution, preserved `--headed`/`--no-cleanup`/`--full` flags, and no inter-suite sleep; the 47-test harness is maintained. Findings TP3B-CR-001/002/003 and QG-TP3B-001 are resolved; no open blocker remains. Final evidence: targeted registry 20 passed; harness 47 passed; coverage 91.90% with all 84 files ≥80%; integration 2,462 passed; Core E2E 35 in 317.02s; CLI E2E 29 in 816.51s; all 12 `ci-e2e` stages green. The concurrent critical path was 816.51s versus the precondition serial Core+CLI sum of 1,188.44s (~31.3% reduction); total runner wall-clock was not measured. One existing CLI deprecation warning and expected skips were non-blocking advisories.
  - **TP4 — Fast feedback loop documentation (2026-07-20; complete).** Added a "Fast feedback loop for AI-assisted / incremental development" subsection to `docs/technical/development.md` with the iterate → pre-merge → E2E-last ladder, section flags (`make lint -- --core`, `make typecheck -- --cli`, `make test-unit -- --core`), single-module reruns with concrete env var (`QS_ORGS_DB_USER=quickscale_test_role make MODULE=orgs test -- --modules`), bounded integration concurrency (`QS_INTEGRATION_JOBS=2 make test-integration`), and cross-references to the landed `QS_CI_PARALLEL` (TP1), `QS_E2E_PARALLEL` (TP3b), and `PYTEST_XDIST_WORKERS` (TP2) knobs. Five commands were verified on the clean `v87` baseline: `make lint -- --core` exit 0; `make typecheck -- --cli` exit 0; `make test-unit -- --core` exit 0 (2,489 passed); `QS_ORGS_DB_USER=quickscale_test_role make MODULE=orgs test -- --modules` exit 0 (874 passed, 11 skipped); `QS_INTEGRATION_JOBS=2 make test-integration` returned nonzero — the bounded worker pool ran all 12 modules (94.39% mean) but `orgs/test_debug.py::TestMiddlewareDebugOverride::test_debug_org_overrides_saas_session` failed with "server closed the connection unexpectedly". A serial rerun (`QS_INTEGRATION_JOBS=1 make test-integration`) subsequently passed (all 12 modules, 94.41% mean). The bounded-run failure is an unresolved non-blocking integration risk for this docs-only task. `QS_CI_PARALLEL=0` and `QS_E2E_PARALLEL=0` are documented as cross-references with their owning ticket context; their full verification is the responsibility of their respective TP1 and TP3b tickets, which are complete. Roadmap updated to mark TP4 `- [x]` and close the Track 1 TP suite. This is a docs-only change — no gate behavior, pass/fail set, baseline, or threshold was modified.

- v0.86.0 — Organizations Module (multi-tenancy with Solo/SaaS runtime modes, org-scoped RBAC and invitation flow, billing bridge with authoritative org ownership fields and `Plan.features` feature gates, `migrate_billing_to_orgs` and `promote_to_saas` management commands, React org management pages and org switcher, and billing wiring regression fix with planner/apply guard test)
- v0.85.0 — Billing Module (Stripe-backed one-time credit purchases and recurring subscriptions, credits-first Django-owned ledger, planner/apply readiness with auth-module dependency enforcement, module-owned `/billing/pricing/` and `/billing/dashboard/` pages, and starter-theme billing links)
- v0.84.0 — Backups Hardening Release (admin backup artifact download/create/integrity actions, upload-driven restore safety and compatibility checks, full-snapshot backup-contract hardening, and repo-owned runtime/tooling support alignment for the refreshed Python/frontend baseline)
- v0.83.0 — Hardening Release (repo-wide hardening across strict desired-config validation, notifications live-config placeholder rejection, generated production `SECRET_KEY` fail-hard behavior, CRM staff-only parity, forms CSV and backup artifact safety, blog upload/thumbnail/rate-limit hardening)
- v0.82.0 — Disaster Recovery & Environment Promotion Workflows (public `quickscale dr capture/plan/execute/report` workflows for local and Railway routes with `snapshot_id`-based stored snapshots, resumable capture and execute, rollback pins for production routes, conservative env-var sync, and source-side media sync)
- v0.81.0 — Beta-Site Migration Maintainer Tooling (maintainer-only Make/Python workflows for beta-site catch-up: fresh-first and in-place paths with checkpoint-first semantics)
- v0.80.0 — Analytics Module (PostHog-only website analytics with flat `QUICKSCALE_ANALYTICS_*` settings, service-style capture helpers, apply-time env-example sync, guarded forms integration, QuickScale-owned social click tracking)
- v0.79.0 — Social & Link Tree Module (curated social links and embeds, backend-owned YouTube/TikTok preview metadata, managed `/_quickscale/social/` integration endpoints, Django-owned public React pages)
- v0.78.0 — Notifications Module (transactional email foundation, Anymail-backed Resend delivery, recipient-granular tracking, signed delivery webhooks)
- v0.77.0 — internal main-branch baseline — Backups Module (private database backups, optional private remote offload, guarded CLI restore, scheduler-ready command hooks)
- v0.76.0 — Storage Module (cloud file hosting, media storage adapters, CDN-ready media infrastructure)
- v0.75.0 — Forms Module (generic customizable form builder with admin, DRF API, spam protection, GDPR anonymization, and React mount point)
- v0.74.0 — React Default Theme (showcase_react theme with Vite, TypeScript, TanStack Query, and Zustand)
- v0.73.0 — CRM Module (API-first Django CRM with 7 core models and CLI integration)
- v0.72.0 — Plan/Apply Cleanup (removed legacy init/embed commands, full transition to plan/apply)
- v0.71.0 — Module manifests & config mutability (Plan/Apply system complete)
- v0.70.0 — Existing project support (status, plan --add, plan --reconfigure)
- v0.69.0 — State management and incremental applies
- v0.68.0 — Plan/Apply System core commands (Terraform-style declarative workflow)
- v0.67.0 — Listings module with AbstractListing base model for verticals
- v0.66.0 — Blog module with Markdown, featured images, and RSS feeds
- v0.65.0 — Enhanced auth module and development tooling
- v0.64.0 — Theme rename to showcase_* (breaking change)
- v0.63.0 — Authentication Module with django-allauth and interactive embed
- v0.62.0 — Split Branch Infrastructure (module management CLI commands, GitHub Actions automation)
- v0.61.0 — Theme System Foundation (--theme CLI flag, theme abstraction layer, HTML theme)
- v0.60.0 — Railway Deployment Support (automated deployment via quickscale deploy railway)
- v0.59.0 — CLI Development Commands (Docker/Django operation wrappers)
- v0.58.0 — Comprehensive E2E testing infrastructure with Playwright and PostgreSQL
- v0.57.0 — Production-ready generator baseline
- v0.56.0 — Quality, Testing & CI/CD
- v0.55.0 — CLI implementation
- v0.54.0 — Project Generator
- v0.53.3 — Project Metadata & DevOps Templates
- v0.53.2 — Templates and Static Files
- v0.53.1 — Core Django Project Templates
- v0.52.0 — Project Foundation
- v0.51.0 — Documentation foundation
- v0.41.0 and earlier — legacy codebase (see Github repository for history)
