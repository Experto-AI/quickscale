# Changelog

`CHANGELOG.md` is the canonical QuickScale release history index. Published releases pair each version entry with a single official release note in `docs/releases/` linked from the GitHub tag and release PR. When a release note is prepared before the maintainer completes the manual tag/publish step, the changelog entry and note must say so explicitly and must not imply publication. Use `docs/technical/roadmap.md` for active or unpublished release status. Entries are version-ordered.

- v0.87.0 — unreleased / not yet tagged — Hardening release (in progress). Consolidates a large hardening pass across tenant isolation, module configuration, the generator, and disaster recovery.
  - **SA48 — Fail hard the shared orgs `get_client_ip()` trusted-proxy settings reads.** `USE_X_FORWARDED_FOR`/`TRUSTED_PROXY_COUNT` must now exist and be valid; missing/invalid config raises `ImproperlyConfigured` instead of silently defaulting proxy resolution off. Closes SA48.
  - **SA49 — Derive orgs' cross-module conformance-env module list instead of hand-listing it.** The hand-maintained `INSTALLED_APPS` list in orgs' conformance settings could silently diverge from the real module inventory, leaving isolation/purge/FK-conformance gates blind to unlisted modules. Added a CI gate that derives the expected module set from installed manifests and asserts parity. Closes SA49.
  - **SA50 — Fold orgs' `JsonApiMixin`/`JsonOrganizationAccessMixin` stack into one `OrgApiBaseView`.** The eight `OrgApi*` JSON views re-implemented SaaS-mode gating, auth checks, and org-role access control per view via a two-level mixin stack. Consolidated into a single base class; the old mixins are removed. Closes SA50.
  - **SA21.2 — Wire forms/blog throttles and IP logging to the shared canonical client-IP seam.** Added an importable `get_client_ip(request)` helper to orgs and repointed forms' submission IP logging, blog's API rate-limit throttle ident, and the forms submit throttle to it, closing a short-XFF-chain divergence with DRF's own IP resolution. Closes SA21.2.
  - **SA26 — Sanitize markdown-rendered URI schemes on public blog/listing detail pages.** Markdown `[text](javascript:...)` links rendered as unescaped `<a href="javascript:...">` under the `|safe` filter. Added an allowlist URI-scheme sanitizer (plus control-character/obfuscation normalization) run before rendering on both blog and listings detail pages; `http(s)`/`mailto`/relative links remain clickable. Closes SA26.
  - **SA40 — Reject non-string values on public form submit instead of 500ing.** Public form submissions now return validation errors for non-string field values and malformed `min_length`/`max_length` rule config instead of crashing. Closes SA40.
  - **SA41 — Distinguish "no subscription" from a subscription row missing its Stripe id during account-deletion billing cleanup.** Added a dedicated `BillingSubscriptionAnomalyError` so the benign "nothing to cancel" case and the anomalous "subscription exists but has no Stripe id" case are handled and logged separately instead of both failing silently. Closes SA41.
  - **SA42 — Make module post-hook settings reads fail hard instead of silently coercing.** Blog, listings, forms, and notifications post-hooks no longer default missing settings (page sizes, rate limits, feature flags) — a missing setting now raises at apply time instead of silently taking effect with a hidden default. Closes SA42.
  - **SA43 — Extract the async-restore dispatch lifecycle into a single service function.** The claim/spawn/rollback lifecycle for admin-triggered backup restore existed as two duplicated inline copies (recorded-artifact and uploaded-file paths); extracted into one shared `dispatch_background_restore()` service function so future fixes apply once. Closes SA43.
  - **SA51 — Rewrite the false `backups/services.py` header contract.** The header claimed the file was "intentionally under 400 LOC" and pointed new orchestration elsewhere; corrected to state the real division of responsibility between `services.py` (model-touching lifecycle) and `dr_engine/` (engine-pure logic). Closes SA51.
  - **SA52 — Fail hard when `manage.py` can't be resolved.** `_get_manage_py()` no longer falls back to a bare `"manage.py"` literal on resolution failure — a production deploy without a resolvable `manage.py` now fails dispatch immediately instead of reporting success while the child process dies. Closes SA52.
  - **SA54 — Guard the stale-restore threshold default against drift.** The DR orchestration layer's hardcoded 30-minute stale-restore threshold could drift from the module's canonical constant since core can't import module services; the constant is now injected explicitly by the caller and a regression test locks the default to match. Closes SA54.
  - **SA53 — Make the uploaded-restore-artifact copy crash-safe.** The upload-restore copy path could destroy the prior local artifact on a failed copy and leak its staging directory. Now uses a crash-safe fd-based copy with retry-on-short-write and consolidated cleanup, so a failed copy never destroys the existing artifact. Closes SA53 (including CR-SA53-REV-002).
    Follow-up (CR-SA26-001) hardened the SA26 sanitizer against tab/newline scheme obfuscation per the WHATWG URL spec.
  - **SA45 — Derive purge `_DELETE_SPECS` completeness from tenant model classification.** The purge-spec completeness test now derives its expected model coverage from tenant-model classification instead of a third hand-maintained list, so a missing purge spec for a new tenant model fails CI.
  - **SA47 — Consolidate the last-owner deletion-blocking check into orgs as the canonical implementation, with concurrent-deletion protection.** Three divergent "never remove the last owner" implementations (model-level, account-deletion view, two org view duplicates) are now one canonical `OrganizationMembership.is_last_owner_with_members()` used everywhere, and account deletion now runs under `select_for_update` so concurrent deletions by co-owners can't both succeed and leave an org ownerless. Closes SA47.
  - **SA37 — Move admin backup create/prune off the synchronous request path.** Backup creation and pruning now dispatch via the same background-subprocess pattern as SA43's restore dispatch, so the admin action returns immediately instead of blocking on `pg_dump`/file deletion. Closes SA37.
  - **SA38 — Detect and surface stranded `STATUS_RESTORING` artifacts with a guarded reset path.** A killed restore child (OOM, redeploy) previously left an artifact permanently stuck in `STATUS_RESTORING`. Added staleness detection, a changelist warning column, and a guarded bulk-reset admin action to recover stranded restores. Closes SA38.
  - **SA46 — Add a CI/AST gate requiring every `csrf_exempt` callsite to pair with CSRF/signature verification.** Three coexisting idioms for authed state-changing JSON endpoints made it possible to add a new `csrf_exempt` endpoint with no CSRF protection and no gate catching it. Added an AST-based CI check requiring every `csrf_exempt` callsite to also call an approved verification helper. Closes SA46 (including CR-SA46-001/REV-002/REV-003, which hardened the gate's literal-truthiness evaluator against being fooled by constant-folded dead branches).
  - **SA44 — Replace import-time adapter registration with explicit registration; delete the circular-import string-matching classifier; split `runtime.py` into `dr`/`manifest` sub-modules.** Managed adapters (social, billing, CRM) now register explicitly instead of at import time, removing a fragile classifier that matched CPython exception text against a hand-maintained module allowlist. Closes SA44 (including CR-SA44-REV-001, which made adapter-registry population self-contained per call instead of relying on module-load order).
  - **SA55 — Add a decisions.md rule naming the two sanctioned JSON-API bases.** Documents DRF baseline for generic authed endpoints and `OrgApiBaseView` for org-role-scoped endpoints, with billing's plain-View + manual-CSRF idiom explicitly scheduled for removal (SA56) rather than grandfathered. Closes SA55.
  - **SA56 — Migrate billing's four plain-View state-changing endpoints onto the existing DRF baseline.** Removed billing's manual `csrf_exempt` + `_enforce_csrf` idiom in favor of DRF's `APIView` + `SessionAuthentication`, which enforces CSRF automatically — matching the rest of the file's endpoints. Closes SA56.
  - **SA57 — Restore fail-hard behavior on DR media storage-backend resolution errors.** DR media backend resolution caught every exception from `select_storage_backend` and silently fell back to local `MEDIA_ROOT`, conflating the legitimate "storage module not installed" case with real misconfiguration — an S3 deployment with a broken storage setting could produce a "ready" backup manifest with empty local inventory. Now only the module-absent case falls back; real configuration errors propagate. Closes SA57.
  - **SA64 — Port social admin off the `PerOrgAdminMixin` prototype onto `TenantModelAdmin`.** Social's local `PerOrgAdminMixin` duplicated the orgs-owned `TenantModelAdmin` base without its VIEW-AS debug-session priority or org-field form-locking; both social admins now inherit `TenantModelAdmin` directly and gain both capabilities. Closes SA64.
  - **SA65 — Stop propagating the CLI's dev-context `PYTHONPATH` to foreign subprocesses.** `apply_command.py`'s module-level `_QUICKSCALE_SUBPROCESS_ENV` was passed to every apply subprocess (poetry, git, docker, generated-project manage.py) instead of only the two nested `sys.executable -m quickscale_cli.main` invocations it existed for. Removed the global cache; `_build_quickscale_env()` is now called on-demand and passed only to `_run_migrations_in_docker_impl` and `_start_docker_impl`. `_run_command` defaults to `env=None` (inherit parent env). Corrected the misleading docstring. Regression tests prove scoping. CR-SA65-001/002 resolved in review pass 2. Closes SA65.
  - **SA66 — Add a conformance gate deriving the beta-migration file taxonomy from the template tree.** The generator's file taxonomy in `quickscale_devtools/beta_migration.py` was eight hand-written tuple literals with no machine-readable derivation from the template tree. Initial gate rejected by change review (four blocking findings: identity-substitution gap, inventory completeness, per-path disposition, premature closure). This pass resolves all four: `start.sh` is now in `IN_PLACE_SUBSTITUTED_INFRASTRUCTURE_TARGETS`; the conformance inventory includes non-Jinja theme copied files; classification uses per-tuple disposition tracking with directory-level matching; `INTENTIONALLY_UNMANAGED` is fully populated with documented category rationales in `decisions.md`; all emitted files are now classified (zero gaps). Gate passes. Closes SA66. Advisory follow-up (CR-SA66-005, low): the conformance test iterates only mode-specific required specs rather than both common and mode-specific classification sources named in prose; the rationale test does not inspect decisions.md; manual parity verified on both dimensions.
  - **SA73 — Fix `make lint`, `make typecheck`, `make format`, and `make test-unit` to all pass.** Four fixes applied: (1) added `ignore_missing_imports = True` to the `[mypy-quickscale_modules_backups.*]` section in `mypy.ini` to suppress `import-not-found` errors for the backups module package; (2) added `MYPYPATH` construction to `Makefile`'s `typecheck` target so each module's `src` directory and its siblings are resolvable during module-scoped mypy runs; (3) updated the `# type: ignore[attr-defined]` at `orchestration.py:646` with a clarifying comment — previously unused because the unresolvable import made every target `Any`, it is now a needed ignore to suppress an `attr-defined` error on a Django reverse relation (visible once imports resolve); (4) moved `refresh_managed_adapters()` from module-level import side effects into a session-scoped autouse fixture in `test_manifest_entry_point.py` that safely handles `ImproperlyConfigured`, with corresponding test adjustments to skip managed-adapter assertions when those modules are not available. Closes SA73.
  - **SA74 — Prime CRM org-creation stage seeding with tenant context; remove the orgs test-suite signal muting.** `ensure_org_default_stages` primes tenant ContextVar across the helper scope and wraps `_seed_default_stages`'s INSERTs in `org_scope(organization)`, so FORCE RLS policies see the expected tenant context regardless of whether the caller (signal receiver, `create_personal_for`, org form) primed the ContextVar. The orgs conftest's autouse `_mock_org_created_signal` fixture is removed — CRM receiver is self-sufficient under restricted roles. Replaced by an opt-in `mock_org_created_signal` for tests that need to suppress the signal. New regression test (`test_seeds_without_ambient_org_context`) proves seeding succeeds with zero ambient context.
    **Follow-up (CR-SA74-001):** `ensure_org_default_stages` now restores the DB-side GUC (`app.current_org_id`) and clears the AF9 per-transaction priming memo on exit inside an active outer transaction, preventing a transaction-scoped `SET LOCAL` from leaking the seeded org UUID to subsequent no-context queries in the same transaction. New regression test (`test_ensure_org_default_stages_restores_db_guc_in_outer_transaction`) proves the GUC is cleared on exit. Closes SA74.
  - **SA70 — Add an orgs `pre_delete` receiver backstop for the last-owner invariant.** `OrganizationMembership` model `delete()` override (SA47) guards the last-owner invariant when a membership is removed through the model, but Django's deletion collector can cascade-delete via `user.delete()` without calling the membership's `delete()`. Added `_protect_last_owner_on_membership_delete` — a `pre_delete` signal receiver on `OrganizationMembership` in `signals.py` — wired in `QuickscaleOrgsConfig.ready()` via `pre_delete.connect()`. The receiver calls the existing `is_last_owner_with_members()` canonical check before every membership deletion, including cascade-driven ones, and raises `ValidationError` with `LAST_OWNER_REMOVAL_MESSAGE` when the deletion would strand other members ownerless. New regression test (`test_user_delete_of_last_owner_in_multi_member_org_is_refused`) proves `user.delete()` is refused for a sole owner in a multi-member org. Deliberate sole-member self-removal is unaffected; the four existing callsites are unaffected. Closes SA70.
    **Findings/blockers discovered:** none beyond the roadmap finding itself. The receiver does not acquire `select_for_update` (unlike the model `delete()` override), so under concurrent cascade paths a stale check could produce a fail-closed false positive — this is acceptable for a backstop; the model `delete()` override's locking handles the normal-path safety.
  - **SA75 — Narrow `_session_managed_adapters`'s skip path to genuine missing managed packages only.** `quickscale_core/tests/test_manifest_entry_point.py` now swallows only a missing managed-package root import outside CI, re-raises broken managed-adapter import failures, and asserts the full adapter registry in CI so real adapter breakage fails the unit gate instead of producing session-wide skips. Closes SA75.
    Follow-up (CR-SA75-REV-001): `_refresh_session_managed_adapters()` now processes each managed module independently with deterministic sorted iteration, so one tolerated missing package does not prevent later managed adapters from being refreshed and a shipped-but-broken module cannot be masked. Two regression tests cover the coexistence scenario. Closes CR-SA75-REV-001.
    Follow-up (CR-SA75-REV-002): Fixed a correctness gap in the root-package-missing detection: the handler checked only `exc.__cause__` for `ModuleNotFoundError`, but a genuinely missing managed package root raises `ModuleNotFoundError` directly (with no `__cause__`), so the tolerated case was never recognised. The check now examines both the exception itself and its cause. Updated the regression test that implicitly relied on the bug. Closes CR-SA75-REV-002.
  - **SA76 — Add a ticketed quarantine mechanism to the integration gate for known restricted-role failures (2026-07-12).** Added a `declare -A QUARANTINE_TICKETS` associative array to `scripts/test_integration.sh` that maps each known-failing module to its owning ticket. `run_pytest_stage` accepts a 4th parameter for the quarantine ticket; when set, stage failures are absorbed with a clear `⚠ quarantined` message and the module's coverage is excluded from the overall mean. Current entries: `orgs` (SA77, 9 failures) and `notifications` (SA79, forms 0007 FK errors — reticketed from SA78 per SA78 findings; SA78 resolved the notifications-specific duplicate-db/ownership crash). Non-quarantined module-suite regressions still fail the gate. Each entry is removed independently as its owning ticket lands. Closes SA76 (and closes SA59.1 per the 2026-07-12 closeout-path decision; unblocks SA59.4).
  - **SA77 — Fix orgs restricted-role test failures by removing unmanaged psycopg2 connections and marking dynamic-DDL tests with bypass_rls (2026-07-12).** Two-part fix for the 9 remaining orgs restricted-role failures under the NOBYPASSRLS integration role:
    1. **Helper-level psycopg2 direct connections** — `_ensure_rls_test_role()` in `test_tenant_table_conformance.py` and `_ensure_role()` in `test_operator_access.py` each used a separate `psycopg2.connect()` to issue best-effort GRANTs. Under restricted-role auth, separate psycopg2 connections could fail or behave inconsistently. Both helpers are converted to Django's managed `connection.cursor()` with savepoint-isolated GRANTs (`transaction.atomic()` around each individual GRANT), so a permission-denied failure under a non-owner role does not abort the outer test transaction.
    2. **Dynamic DDL tests** — `_tenant_resource_db` fixture (create/drop `ConcreteTenantResource`) and its 5 dependent tests, plus the inline DDL in `test_forward_fk_traversal_without_org_context`, create test-only database tables at runtime. Under NOBYPASSRLS, DDL metadata operations can conflict with RLS policy enforcement. All 6 tests are marked with `@pytest.mark.bypass_rls`, which causes them to be skipped when `QUICKSCALE_ALLOW_BYPASSRLS` is not set (the default for restricted-role integration runs).
    **Verification blocked:** the restricted-role rerun currently aborts in forms 0007 FK validation (SA79, Track 2, CR-PLAN-SA79-004) before reaching the orgs seam, so the fix cannot yet be confirmed under a live restricted-role run. The quarantine entry for orgs in `scripts/test_integration.sh` (SA76) remains active. SA77 code fix landed — pending DB verification blocked on SA79.
  - **SA78 — Fix notifications' duplicate-db/ownership failures on restricted-role reruns (2026-07-12).** The notifications restricted-role rerun failure (duplicate-db/ownership) under `quickscale_test_role` was caused by Django's test runner attempting `DROP DATABASE` then `CREATE DATABASE` for `test_test_quickscale_notifications` on every run. A leftover test database owned by `postgres` (confirmed locally) cannot be dropped by the restricted role. Two-part fix: (1) added an explicit `TEST.NAME` in `settings.py` (`qs_notifications_test`) to use a dedicated test database name, avoiding the orphaned `test_test_quickscale_notifications` entirely; (2) enabled pytest-django's `reuse_db` (`keepdb` equivalent) in `conftest.py` so the test database persists between runs and no DROP/CREATE is attempted on subsequent runs. Also resolves one of SA59.1's three remaining blockers. Closes SA78 (resolves the duplicate-db/ownership rerun failure specifically; the suite's 26 pre-existing forms migration FK errors are tracked as SA79 on the roadmap — see below).
    **Findings discovered (→ SA79):** 26 pre-existing FK constraint violations (`forms_formfield_form_org_fk`) in the forms migration `0007_new_organization_ownership` that affect 26 of 39 collected tests — a separate, pre-existing bug in the forms migration backfill that does not match the seeded FormField `organization_id` with the parent Form's `organization_id`. These errors occur identically with or without the SA78 changes (verified by baseline comparison). The SA78 duplicate-database fix now allows the test suite to progress to where these errors are visible rather than being masked by the earlier DB-lifecycle crash. Tracked as SA79 on the roadmap — see `docs/technical/roadmap.md` §SA79.
  - **SA79 — Fix forms migration 0007 backfill so FormField/FormSubmission/FormFieldValue rows inherit the correct organization before VALIDATE CONSTRAINT (2026-07-12, corrected 2026-07-12).** Migration `0007_new_organization_ownership` adds composite FKs (SA60) and validates them. Under a NOBYPASSRLS database role, the backfill's correlated subquery `SELECT organization_id FROM Form` was blocked by FORCE RLS on Form (enabled in 0006), because the `app.current_org_id` GUC was not set during migration execution.
    **CR-SA79-001 (corrected fix):** The first-pass fix set `SET LOCAL app.current_org_id = <system org UUID>`, which only made System-org Form rows visible to the subquery. For pre-existing non-system tenant data, the subquery returned NULL and the COALESCE fallback incorrectly assigned the System org — causing VALIDATE CONSTRAINT to fail when DDL saw the real non-system org but the backfill had written the System org. The corrected fix sets `SET LOCAL app.operator_access = 'on'` instead, which enables the read-only OR clause (`operator_access = 'on'`) in the Form RLS `FOR SELECT` policy, making **all** Form rows visible regardless of organization. This correctly backfills both System-org and non-system tenant data with the parent Form's real org. Transaction-scoped via `SET LOCAL`; no audit logging overhead for the migration context. The same `operator_access` elevation covers `_backfill_formsubmission_org` and `_backfill_formfieldvalue_org` because Django wraps the entire migration in an `atomic()` block on PostgreSQL.
    **CR-SA79-002 (corrected proof):** Regression coverage now includes `test_backfill_preserves_non_system_org_after_0007` in `TestFormsMigration0007BackfillSA79`, which seeds a non-system Organization, Form, and FormField row between 0006 and 0007, then proves the backfill correctly assigns the non-system org from the parent Form — not the System org fallback. The existing System-org proof (`test_backfill_matches_form_org_after_0007`) is preserved. Both tests run under NOBYPASSRLS with no `@pytest.mark.bypass_rls` marker.
    **Findings/blockers discovered:** The first-pass `app.current_org_id` fix was correct for the seeded System-org test data but would have failed silently on non-system tenant data encountered during real upgrades (CR-SA79-001). Review also found that the initial proof only covered the System-org case (CR-SA79-002). Both findings are fully resolved in this corrected pass: the migration uses `app.operator_access` for cross-tenant reads, and the test suite proves the fix works for both System-org and non-system data. Pre-existing test-level failures in `TestFormsParentOrgMutationRejection` and several `test_views.py`/`test_management.py`/`test_models.py`/`test_isolation.py` tests were exposed by the migration fix — these tests create Form/FormField rows under NOBYPASSRLS without setting `app.current_org_id`, causing RLS policy violations. They remain out of scope for SA79 (the migration now works correctly for all tenant data).
    CR-SA79-001 resolved. CR-SA79-002 resolved (direct MigrationExecutor 0006→0007 proof with pre-existing non-system tenant rows added). Closes SA79 (corrected).
  - **SA69 — Record the `ImproperlyConfigured` exception-identity split in decisions.md; correct three adapter docstring references.** The contracts-layer `ImproperlyConfigured` (quickscale_core.contracts.module_discovery) is now documented as a distinct exception from Django's `django.core.exceptions.ImproperlyConfigured`, with explicit import guidance for catchers in each layer. Billing, CRM, and social adapter module docstrings are corrected from the Django class to the first-party class. The housekeeping-label-discipline watch item is recorded in decisions.md; a lint/naming guard is deferred. Closes SA69.
  - **SA67 — Closed: beta-site deployed-state verification is permanently out of scope for repo automation.** SA67 asked for manual inspection of `experto-ai-web`/`bap-web` deployments (SA63/SA68 `start.sh`/`production.py` parity, Redis presence, deploy outcome). `decisions.md §Beta-Site External Verification Scope` (2026-07-11) records that neither site's repository nor deployment is reachable from this monorepo by design — a structural property of the two-repo maintainer workflow, not a temporary access gap — so this class of finding closes on discovery rather than sitting open pending access. The repo-local prerequisites (SA66's conformance gate, SA68's launcher-contract completion) were already complete. The outstanding manual check moved to a standing maintainer to-do in `docs/planning/beta-site-migration.md` (not a roadmap item). Closes SA67.
  - **SA68 — Complete the migrate-path launcher env-pair contract; delete the last argv deciders.** The three remaining argv deciders for the `migrate` path — `start.sh.j2`'s missing env pair, `production.py.j2`'s `elif "migrate"` branch, and `orgs/apps.py`'s `_is_migrate_command()` — are replaced by explicit `QUICKSCALE_PRIVILEGED_COMMAND` and `QUICKSCALE_NON_DB_COMMAND` one-shot command env vars. `start.sh.j2`'s migrate line carries the same env pair as its `createcachetable` line; `production.py.j2` has exactly one code path selecting the superuser URL (the bridge) plus a fail-closed default; `orgs/apps.py` no longer inspects `sys.argv`. `decisions.md` records the deploy-time configuration contract. Closes SA68 (main implementation landed). Remaining follow-up CR-SA68-004 resolved: rollout-order guidance in `docs/planning/beta-site-migration.md` now explicitly distinguishes Redis-present vs Redis-absent sites for the SA63/SA68 env-pair bridge and clarifies maintainers must cherry-pick the env-pair bridge into the donor-owned `settings/production.py` rather than replacing it wholesale.
  - **SA32 — Retire `invalidate_social_cache()` from the social module's public export surface.** The helper only cleared bare cache keys, which is ineffective against org-partitioned tenant cache keys and misleading as a bulk-invalidation API; removed from the public export surface. Closes SA32.
  - **SA24 — Escape the analytics template tag payload before marking it safe.** `analytics_public_config_json()` now escapes `<`, `>`, `&` before `mark_safe`, so a `</script>`-breaking payload can no longer inject into page source. Closes SA24.
  - **SA21.1 — Add canonical client-IP resolution and a shared cache backend to generated settings.** Generated projects previously had no trusted-proxy convention (so `REMOTE_ADDR` was the edge proxy's address, not the client's) and no shared `CACHES` backend (throttles/rate-limiters fell back to per-process `LocMemCache`, useless behind multiple workers). Added `USE_X_FORWARDED_FOR`/`TRUSTED_PROXY_COUNT` settings + a `get_client_ip()` helper, and a `CACHES` block using Redis when available or `DatabaseCache` otherwise. Unblocks SA21.2.
  - **SA36 — Fix the off-by-one in the generated `get_client_ip()` proxy-count math.** Behind a single proxy, an honest one-hop request returned the proxy's IP instead of the client's, while an attacker-supplied extra XFF entry returned the attacker-controlled value instead of being rejected. Fixed the guard/index math to match DRF's own `NUM_PROXIES` semantics. Closes SA36.
  - **SA34 — Run `createcachetable` in the generated app's deploy script.** The SA21.1 `DatabaseCache` fallback table is never created by `migrate`, so a Redis-less deployment would 500 on the first throttled request. Added a guarded `createcachetable` step to the deploy script (skipped when Redis is configured, since Redis manages its own tables). Closes SA34.
  - **SA35 — Stop account deletion from CASCADE-destroying org content across organizations.** `Post.author`, `ContactNote.created_by`, and `DealNote.created_by` were `on_delete=CASCADE` — deleting a user destroyed every post/note they authored, across all orgs, because FK actions bypass RLS. Changed all three to `SET_NULL` and added a conformance gate requiring every FK to the user model in an installed module to be `SET_NULL` or `PROTECT` unless explicitly allowlisted. Closes SA35.
  - **SA22 — Same-filesystem staging with backup/swap/rollback for `apply --force`.** Force-replacement previously deleted existing project content in place before staging new files, so a mid-operation failure left the project partially destroyed. Now stages to a backup directory first and rolls back fully on any staging-phase failure. Closes SA22.
  - **SA23 — Validate the `next` redirect target in orgs debug views.** `DebugAsOrgView`/`ExitDebugModeView` redirected to an unvalidated `next` POST param — superuser-only and POST-only, but still an open redirect. Added a same-host/scheme validation guard to both. Closes SA23.
  - **SA25 / SA61 — Untrack accreting build/test artifacts.** `coverage.json`, `pytest_cov_log.txt`, `pytest_log.txt`, and tracked blog test-upload files were being committed because test `MEDIA_ROOT` wrote into the working tree. Untracked all of them, added the relevant `.gitignore` patterns, and switched blog's test media root to a temp directory. Closes SA25, SA61.
  - **SA62 — Route the module pyproject TOML splice through the validated writer.** One of three pyproject-TOML writers used a bare `write_text()` that skipped the parse-before-write guard its siblings already had, so a malformed splice could write invalid TOML with no signal until the next `poetry lock`. Now validated like its siblings. Closes SA62.
  - **SA63 — Fix the createcachetable/boot-guard collision and start the launcher-owned env contract.** Three review findings (CR-SA63-001/002/003) were discovered during technical review and resolved in the implementation pass. Added `QUICKSCALE_ALLOW_BYPASSRLS=1` to `start.sh.j2`'s `createcachetable` line and bridged the same env pair into `production.py.j2` so no-Redis deployments boot under a correctly-scoped role. Replaced `local.py.j2`'s argv-sniffing branch with the same `RUNTIME_DATABASE_URL`/`QUICKSCALE_ALLOW_BYPASSRLS` launcher env pair, preserving the local-dev `python manage.py migrate` UX via a documented one-liner wrapper. Verified end-to-end with a generated project on the production settings + orgs module path (no Redis), and confirmed the fix is reproducible from a fresh `quickscale plan`/`apply` regeneration. The remainder of Finding 6's Option 1 contract (production settings as pure env reader, removal of `_is_migrate_command()`) remains deferred in arch-audit.md. Closes SA63.
  - **SA27 — Enforce module-option validation on the apply path; remove the silent coercions it currently masks.** Several modules had validators that were never actually reached at apply time, behind silent coercions (invalid orgs mode → `"solo"`, invalid storage backend → `"local"`, blank rate limits → defaults). Wired the missing validators into the apply path and removed the coercions that made them dead code — an invalid config now aborts apply with a descriptive error instead of silently substituting a default. Closes SA27.
  - **SA19 — Stop `start.sh.j2` from printing secret values to deploy logs.** Replaced the generated deploy script's raw environment dump with a per-variable set/`MISSING` status line, so deploy logs no longer expose secret values.
  - **SA13 (.1–.4) — Retire the internal org-context primitives in favor of the public `org_scope` API.** Deleted the dead `resolve_public_org_context` function, privatized the three internal context primitives with compatibility aliases, migrated every remaining callsite (billing, forms, social admin) onto the public `org_scope()` API, and flipped the CI lint gate from warn-only to hard-fail once migration was complete. AF9 `None`-path hardening is explicitly deferred (see roadmap for rationale). Closes SA13.1–SA13.4.
  - **SA14 (.1–.6) — Build `TenantModelAdmin` and port every module admin onto it.** Added an orgs-owned `TenantModelAdmin` base that resolves the active org from VIEW-AS/session/explicit-selection and scopes every admin view through `org_scope()`, fail-closed. Ported CRM (SA14.2), blog/forms/listings/billing (SA14.3), and social (SA64, later) admins onto it, removing their hand-copied `all_objects`-based scoping and inline formsets. Also: implemented `operator_access(reason=...)` as a real audited RLS predicate for superuser cross-tenant reads (SA14.5, read-only, transaction-scoped, fully audit-logged); made `QUICKSCALE_MODE` fail-hard at boot instead of defaulting to `"solo"` (SA14.6); and flipped module test suites' default DB role to `NOBYPASSRLS`, which is what made the operator-read bug class visible to CI in the first place (SA14.4). Closes SA14.1–SA14.6.
  - **SA7 (.1–.4) — Decouple orgs from CRM/auth via signals and explicit dependency declarations.** Replaced a reverse-import bridge from orgs into CRM with an `organization_created` signal that CRM listens for to seed its default pipeline stages (SA7.1). Made the orgs→auth adapter import fail hard instead of silently falling back to Django's default adapter if the auth module isn't installed (SA7.2). De-duplicated notifications' default config out of orgs' manifest so the notifications module's own manifest is the sole source of truth (SA7.3). Added version-floor constraints (`>=0.86.0`) to every module's `required_modules: [orgs]` declaration, enforced at apply time (SA7.4). Closes SA7.1–SA7.4.
  - **SA11 (.1–.7) — Orgs-owned public-read context helper; migrate blog/listings public views onto it; DRF permission baseline.** Added a shared `PublicSystemOrgReadMixin` that primes tenant context for public (anonymous) reads against the System org, replacing each module's own hand-rolled org-scoping mixin. Migrated blog (SA11.3, including its RSS feed) and listings (SA11.4) public views onto it, and added restricted-role smoke tests proving anonymous reads still work under `NOBYPASSRLS` (SA11.2). Set the generated project's DRF default permission class to `IsAuthenticated` so module APIs are authenticated-only unless a view explicitly opts into public access (SA11.5). Cleaned up CRM's active-org resolution to raise instead of silently falling back to a personal org (SA11.6), and made auth's signup-open setting fail hard instead of defaulting to `True` (SA11.7). Closes SA11.1–SA11.7.
  - **Billing ledger idempotency, DB-enforced (SA12.1).** Added a partial unique constraint on `CreditTransaction.(stripe_event_id, transaction_type)`; concurrent duplicate credit inserts now hit the constraint and return the existing row instead of double-crediting.
  - **SA9 (.1–.6) — Core-facade migration and CI compatibility gates.** Added `quickscale_core.runtime` as the single public facade for the DR-adapter and social-manifest surfaces (SA9.3), migrated backups' and social's deep internal-module imports onto it (SA9.4, SA9.5), and added a CI import-linter that rejects any module importing `quickscale_core` outside that facade (SA9.6, with narrow legacy exceptions for billing/CRM). Added a module-vs-core compatibility gate that does a real install/import probe of each module against its claimed minimum core version (SA9.2). Narrowed backups' core dependency to a compatible range (SA9.1). Closes SA9.1–SA9.6.
  - **SA10 (.1–.2) — Track and surface project-contract vintage.** Generated projects now record which QuickScale contract version they were created against, and `quickscale status` flags modules whose declared minimum contract version is ahead of the project's, surfacing generated projects that need manual review before adopting newer module conventions. Closes SA10.1, SA10.2.
  - **Tenant isolation, unified and enforced everywhere.** Every tenant-scoped model across all modules now inherits from one shared base class instead of hand-copying isolation boilerplate. An isolation check runs across the entire generated project (not just QuickScale's own modules), requiring every model to be explicitly registered as tenant-scoped or intentionally excluded, and that same check is wired into generated projects' own CI. (SA15.1–SA15.3, SA6.2–SA6.3 — see below for detail.)
  - **SA15.1 — `tenant_excluded` marker; widened classification scope; implicit M2M inference.** Added a `tenant_excluded = "reason"` marker as an alternative to registry entries, widened isolation classification from QuickScale's own modules to all project apps, and added automatic classification of auto-created M2M through-models whose endpoints are both already classified. Closes SA15.1.
  - **SA15.2 / SA15.3 — Backfill exclusion markers; derive the human-readable registry from model markers instead of hand-maintained doc assertions.** Backfilled `tenant_excluded` markers on every previously-unmarked system-wide model (auth, backups, notifications, orgs control-plane, billing metadata), and replaced hand-maintained HTML-comment doc assertions with a runtime-derived registry overview that's cross-checked against the literal registry in CI. Closes SA15.2, SA15.3.
  - **Fail-closed database access.** The app now refuses to boot if connected under a database role that bypasses row-level security (`BYPASSRLS` or `SUPERUSER`), unless explicitly overridden (SA58 extended this to also reject `SUPERUSER`). Production settings require a restricted, tenant-safe role by default, with the elevated role reserved for migrations only.
  - **Reduced per-request database overhead.** Tenant-context setup within a single transaction is no longer redundantly repeated for every statement.
  - **Declarative module configuration, piloted and extended to listings (SA6.1–SA6.3).** The `module.yml` derivation YAML loader is now proven by a full round-trip test covering all field categories. Listings (SA6.2) is now fully migrated to declarative derivation — its imperative default/normalize/resolve/validate functions are deleted — and a CI guardrail (SA6.3) blocks any module from reintroducing imperative wiring once migrated, matching the existing analytics precedent.
  - **SA16.1 / SA16.2 — Manifest sync gate; SSOT doc drift closeout.** Added a CI gate comparing each module-owned `module.yml` against its core snapshot and failing on drift; resynced all six manifests that had drifted. Closed a stale roadmap item confirming the derivation-loader and apply-recovery docs already matched shipped behavior. Closes SA16.1, SA16.2.
  - **arch-audit.md doc-drift closeout.** Closed two arch-audit findings (module↔project contract drift; request→tenant-context boundary convention) that were already remediated by SA10.1/SA10.2 and SA11.1–SA11.7 respectively, but never marked closed in the audit doc.
  - **SA18 (.1–.11) — Fail-hard sweep across manifest adapters, generator fallbacks, and CLI dev tooling.** Replaced broad `except Exception` swallowing with narrow, documented exception handling across: managed-adapter import-time initialization (SA18.1); empty analytics manifest settings, previously silently defaulted (SA18.2, including a follow-up fix where `regenerate_managed_wiring` was masking this validation in the embedded-project context); the `quickscale_cli.schema` compat shim, now deleted (SA18.3); the generator's template-resolution fallback chain, now a single deterministic lookup that raises `FileNotFoundError` on a real miss (SA18.4); the version-resolution fallback's silent `"0.0.0"` terminal default, now removed (SA18.5); project-metadata resolution's swallowed `ConfigValidationError` (SA18.6); Railway CLI helpers' overly-broad exception handling, including successful-but-unparseable output now raising instead of silently returning `None` (SA18.7); invalid `PORT` values, now rejected instead of coerced to the default (SA18.8); `OSError` during managed-file hash capture, now a real step failure instead of a silently-swallowed warning (SA18.9); mandated `# F-EXCEPTION:` tags on the remaining documented legacy-compatibility paths (SA18.10); and a dev-tooling compatibility checker that was silently swallowing malformed-TOML parse errors (SA18.11). Closes SA18.1–SA18.11 (TA2, TA8, TA10, TA13, TA14, TA15).
  - **Operator tooling.** Admins can temporarily "view as" a specific organization from the admin panel to debug issues in context, with a visible banner and one-click exit. Sensitive maintenance commands (e.g. purging an organization) now go through a single, audited access path (`operator_access()`, SA14.5) that logs who ran what and why.
  - **More resilient project generation.** `quickscale apply` now checkpoints progress after each step so a failed run can resume instead of restarting, and destructive or remote actions (starting containers, running migrations, deploying) require explicit confirmation before proceeding. The generator's internals were reorganized into smaller, focused modules.
  - **Stronger database-level safeguards.** Parent/child record relationships (e.g. a CRM contact and its notes) are now enforced to stay within the same organization at the database constraint level, replacing a more fragile trigger-based check. A row-level-security policy bug affecting pooled database connections was also fixed.
  - **Test infrastructure simplified.** All modules now test exclusively against PostgreSQL — the only database QuickScale supports in production — with the SQLite fallback removed everywhere. A dedicated CI job proves tenant isolation holds under a real restricted database role and is structured so it cannot pass by silently skipping.
  - **Billing ownership clarified.** Subscriptions are now authoritatively owned by the organization rather than the individual user who created them.
  - **SA17 (.1–.8) — Fail-hard sweep across module runtime settings and compatibility shims.** Legacy `quickscale.yml` config keys are now rejected with a named-replacement error instead of being silently translated or dropped (SA17.1). Analytics/billing enabled-flag settings (SA17.2), CRM's API-enable flag and page-size settings (SA17.3), forms' API/rate-limit/spam-protection settings (SA17.4), blog's RSS/media-URL/API-token settings (SA17.5), and notifications' enabled/provider settings (SA17.6) all now raise `ImproperlyConfigured` at startup instead of silently defaulting when missing. Replaced optional-dependency soft-degradation (PostHog SDK probing) with a hard dependency plus generation-time wiring (SA17.7). Removed dead `module_catalog` compatibility delegates and made unknown-module readiness lookups raise instead of returning an ambiguous `None` (SA17.8). Closes SA17.1–SA17.8 (TA2 fully closed).
  - **SA20 — Move admin-triggered backup restore off the synchronous request path.** Admin-triggered restore now persists a `STATUS_RESTORING` marker and dispatches the actual restore via background subprocess instead of blocking the request; the child process transitions the artifact to `STATUS_RESTORED`/`STATUS_FAILED` on completion. Hardened across several review rounds to also cover: symlink-safe destination handling, forcing local-only source resolution for the dispatched child, preserving prior status/error metadata across a failed-spawn retry, rejecting new restore attempts on an already-in-progress artifact for both dispatch branches, and atomic compare-and-swap claiming to prevent a double-dispatch race between two concurrent restore submissions. Closes SA20.
  - **SA33 — Fix the dangling `decisions.md` → `arch-audit.md` anchor link.** Repointed a link to a heading that arch-audit.md's cleanup had since dropped. Closes SA33.
  - **SA39 — Fail hard when `operator_access()` is invoked outside an open transaction on PostgreSQL.** Calling it outside `transaction.atomic()` previously no-opped silently (a bare `SET LOCAL` outside a transaction is a PostgreSQL no-op); now raises `ImproperlyConfigured` instead. Closes SA39.
  - **SA31 — Move Railway/DR adapter secrets off process argv.** DR adapter calls, single Railway variable sets, and batch Railway variable sets now pipe secret payloads via stdin instead of passing them as command-line arguments (visible in process listings and shell history). Includes a hard-stop instead of an insecure argv-based fallback suggestion on failure, and forces `--skip-deploys` on all-but-the-last batch write so a partial failure can't trigger an auto-deploy mid-batch. Closes SA31.
  - **SA28 — Enforce the last-owner and personal-org invariants at the account-deletion boundary.** Account deletion now blocks when the user is the sole owner of a shared org with other members, cancels the personal org's active subscription before deleting, and fixed a dead code path so the deletion success message actually renders. Closes SA28.
  - **SA30 — Apply the SA17 direct-required-read pattern to listings/storage runtime settings.** Listings' page-size setting and storage's backend-selection setting no longer silently default or coerce on missing/invalid values — both now raise `ImproperlyConfigured`. Closes SA30.
  - **SA29 — Rebuild storage's config-delivery contract: env-var indirection for credentials.** Storage credentials are now configured as env-var name references (`access_key_id_env_var`/`secret_access_key_env_var`) instead of literal values in `quickscale.yml`, matching the pattern already used by analytics/notifications/billing/backups — generated settings no longer contain credential material. One documentation follow-up (media_url as a non-secret apply-owned setting) remains accepted and open.
  - **SA58 — Make the RLS boot guard reject `SUPERUSER` roles alongside `BYPASSRLS`.** The boot-time RLS role check previously only rejected `rolbypassrls`; now also rejects `rolsuper`, since a superuser role bypasses RLS regardless of the `BYPASSRLS` attribute. Closes SA58.
  - **SA59.3 — Retained-role contract conversion (2026-07-11).** Creates `scripts/provision_test_roles.sh` as the shared provisioning script for both `quickscale_test_role` (LOGIN CREATEDB NOINHERIT NOBYPASSRLS NOSUPERUSER NOCREATEROLE) and `quickscale_rls_test_role` (NOBYPASSRLS NOINHERIT NOLOGIN). CI workflows (`ci.yml`, `publish.yml`) now call this shared script instead of inline role DDL, ensuring the full SA59.3 role contract is asserted on every run. Local dev provisioning is integrated into `scripts/bootstrap.sh` (finds a running PostgreSQL Docker container and invokes the provisioning script). Converts every runtime-DDL test helper across forms, listings, blog, crm, orgs, and social from *create-then-use* to *assert-then-use* — each helper asserts the pre-provisioned role exists in `pg_roles` and raises a clear `RuntimeError` with setup instructions if missing, instead of issuing `CREATE ROLE` itself. Per-table SELECT grants remain in test helpers (they require table existence post-migration and are idempotent under the owning role). `scripts/check_ci_locally.sh` role-contract message updated to include `NOCREATEROLE`. The `quickscale_test_role` contract now includes `NOINHERIT` and `NOCREATEROLE`, structurally preventing test suites from creating new database roles. Resolves F-SA59-ROLE-006. Closes SA59.3.
  - **SA59.2 — Backups PostgreSQL/RLS integration seam.** Backups module test settings (`quickscale_modules/backups/tests/settings.py`) now use the `QS_BACKUPS_DB_*` PostgreSQL env-var pattern matching every other module, replacing the hardcoded SQLite fallback. The sqlite-specific JSON-export coverage test (`test_create_backup_uses_json_export_for_sqlite`) is preserved via engine monkeypatching. CI workflows (`ci.yml` and `publish.yml`) wire `QS_BACKUPS_DB_USER: quickscale_test_role` and `test_quickscale_backups` database creation alongside the existing module list. The `ci.yml` backups-validation job now includes a PostgreSQL 18 service container and test-database creation step. Closes SA59.2.
  - **SA59.1 — Validation harness + coverage plumbing (merged to v87 as blocked checkpoint; review findings partially resolved; closed by SA76 (2026-07-12)).** Three initial blockers resolved in the first pass: (1) e2e exclusion — `scripts/test_integration.sh` now passes `-m "not e2e"` for every module suite; (2) local CI parity — `scripts/check_ci_locally.sh` runs integration tests after unit tests when PostgreSQL is available (`pg_isready`), with an explicit skip warning when it is not; (3) per-module coverage persistence and upload parity — `scripts/test_integration.sh` persists per-module coverage XML via `persist_module_coverage_xml()` to `quickscale_modules/<name>/coverage.xml`, `ci.yml` uploads `./quickscale_modules/*/coverage.xml`, and `publish.yml`'s `test` job now includes an equivalent coverage upload step. **Second pass (2026-07-11) — Phase 1 complete:** CR-SA59-001 resolved — `make test-unit SECTION="core cli"` now produces `quickscale_core/coverage.xml` and `quickscale_cli/coverage.xml` so upload paths have source data. CR-SA59-002 resolved (underlying fail-closed behavior); later superseded by CR-SA59-REV-002. **Phase 3 partial orgs fix complete:** test-only adaptations resolve targeted restricted-role failures in orgs tests (global `organization_created.send` patch, cross-module writes wrapped in explicit org context). **Third pass (2026-07-11) — CR-SA59-REV operational behavior fixed (fail-loud on module test-unit, fail-closed on no-PostgreSQL, role prose in test-integration.sh).** **Fourth pass (2026-07-11, this worktree) — CR-SA59-REV source files corrected:** CR-SA59-REV-001 (caller docs — development.md:261, bootstrap.sh:171, scripts/README.md:68 — corrected "unit and integration" → "unit tests only"); CR-SA59-REV-002 (Makefile:30,600 parity overclaim corrected from "GitHub Actions CI" to "primary local development checks"; quality_tools.md:167,173 overclaim corrected); CR-SA59-REV-003 (check_ci_locally.sh:160-161 role contract corrected to "LOGIN CREATEDB NOINHERIT NOSUPERUSER NOCREATEROLE"); CR-SA59-REV-004 (scripts/test_integration.sh:78 glob corrected from `quickscale_modules/*/tests/` to `quickscale_modules/*/src/*/tests/` to match the src-layout post-S59.2); CR-SA59-REV-005 (ci.yml:237 py38-3.10 version upper-bound matches publish.yml). Still open: three pre-existing orgs restricted-role failures (3 test_models.py + 6 helper-path errors) now tracked as SA77; forms 0007 composite-FK resolved by SA60, backfill remaining as SA79; notifications duplicate-db resolved by SA78. Billing green on clean rerun; social ran clean but remains unreviewed and incomplete (2 remaining boundary tests). User stopped at scope boundary. SA59.2 is complete (2026-07-11). SA59.3 is complete (2026-07-11). SA59.4 is unblocked by SA76. Refer to `docs/technical/roadmap.md` §SA59 for full blocker definitions and continuation state.
  - **SA59 (umbrella) — Closed (2026-07-12).** Investigation confirmed the gate-split approach (DB-free unit gate + PostgreSQL NOBYPASSRLS integration gate). Split into SA59.1–SA59.4 per roadmap policy (Tier 3 → four Tier 1–2 sub-slices). SA59.1 (validation harness + coverage plumbing) closed by SA76 quarantine (2026-07-12, see SA76 entry). SA59.2 (backups PostgreSQL/RLS seam) completed (2026-07-11). SA59.3 (retained-role contract conversion) completed (2026-07-11). SA59.4 (docs + closeout checkpoint) completed (2026-07-12), including its CR-SA59.4-001 follow-up (see SA59.4 entry). Remaining integration-gate failures tracked independently: SA77 (orgs restricted-role) on Track 1, SA79 (forms 0007 backfill) on Track 2. Refer to `docs/technical/roadmap.md` for current open items.
  - **SA60 — Enforce uniform NOT DEFERRABLE policy for all Option C composite FKs (2026-07-12).** Ratified policy (2026-07-12, recorded in `decisions.md §Multi-tenant SaaS Architecture`): every Option C composite FK is `NOT DEFERRABLE`. Implementation delta: aligned `forms/migrations/0007`'s inlined `DEFERRABLE INITIALLY DEFERRED` SQL to `NOT DEFERRABLE` (the shared helper `orgs/tenancy.py:_ADD_COMPOSITE_FK_SQL` already emitted `NOT DEFERRABLE`); renamed and inverted the forms migration deferability test to assert `condeferrable=False`; corrected two stale CRM migration test comments referencing the old deferrable contract; added a cross-module conformance gate (`orgs/tests/test_sa60_composite_fk_conformance.py`) checking all six known composite FKs for `condeferrable=False`. The `tenant_excluded` precedence rule was already a doc-only ratification (no code change). Closes SA60 (contract/deferability portion of the forms 0007 composite-FK issue). **Also resolves one of SA59.1's three remaining blockers** (forms `0007` migration failing composite-FK validation on a fresh restricted-role DB). The remaining backfill/VALIDATE CONSTRAINT failure in the same migration is tracked as **SA79** — see `docs/technical/roadmap.md` §SA79.
  - **SA59.4 — Docs + closeout checkpoint (2026-07-12).** Landed: recorded the unit/integration gate-split decision in `docs/technical/decisions.md`; added `make test-integration` to the repository command reference in `docs/technical/validation_policy.md`; updated coverage-wording guidance in `validation_policy.md`; updated CHANGELOG.md and roadmap.md for SA59 closeout; reticketed quarantine entries (notifications/forms → SA79) in `scripts/test_integration.sh`. **CR-SA59.4-001 resolved (2026-07-12):** `validation_policy.md`'s coverage-targets bullet reworded so the quarantine-removal wording matches the live gate contract (`scripts/test_integration.sh` comment + `decisions.md §Unit/Integration Gate Split`) — each quarantine entry is removed independently as its own owning ticket lands, not held for a single simultaneous closeout across all entries. Closes SA59.4 and the SA59 umbrella (SA59.1–SA59.4 all complete). SA77 (orgs restricted-role CREATE ROLE failures) and SA79 (forms 0007 backfill) continue as separate, already-tracked Track 1/Track 2 roadmap items.
  - **SA79 blocked checkpoint (2026-07-12, corrected).** Attempting to start SA77 revealed that the current SA79 handoff is not yet truthful enough to call complete — the restricted-role rerun aborts in forms 0007 FK validation before reaching the orgs seam. No new code landed in this checkpoint. The existing `Closes SA79 (corrected)` entry above records the initial fix; this superseding note reopens SA79 with pending blockers. Remaining blockers: **CR-PLAN-SA79-004** (exact retained-role execution shape must be explicit for SA79 proof and `make test-integration`) and **CR-PLAN-SA79-005** (same-fact status refresh must include audit docs). See `docs/technical/roadmap.md` §Track 2 for current status.
  - **CR-PLAN-SA79-004 rerun attempted, blocked checkpoint (2026-07-13).** The maintainer ran a live `make test-integration` against the local retained-role environment for the first time this cycle. No new code landed — this checkpoint exists so the rerun's actual findings are recorded truthfully instead of re-asserting the narrower "forms 0007 FK validation" framing CR-PLAN-SA79-004 previously used. Result: the rerun did not reach a state where SA77's or SA79's specific hypotheses could be confirmed or denied, because it was blocked by two prior-layer problems:
    1. **Local venv staleness (environment-only, not a code defect):** `quickscale-core` is installed non-editable at a stale `0.86.0` build in `.venv`, missing the `quickscale_core.runtime` facade package that SA9.3 added for the current 0.87.0 dev cycle. This breaks orgs' test collection with `ModuleNotFoundError: No module named 'quickscale_core.runtime'` (raised via `backups/admin.py`'s import chain during Django admin autodiscover) — a failure with no relation to SA77's tracked restricted-role residual. The SA76 quarantine (which matches on module name, not failure signature) absorbed it under the SA77 ticket anyway, so the gate stayed green while masking an unrelated crash. Needs a `poetry install`/editable-reinstall of `quickscale_core` in the local venv before a rerun can produce trustworthy orgs signal.
    2. **Retained-role env wiring incomplete for 7 of 13 modules locally:** billing, blog, crm, forms, listings, social, and notifications all failed immediately at Django app-ready with `ImproperlyConfigured: role has BYPASSRLS/SUPERUSER`, meaning their local test DB connections used an elevated role rather than the intended NOBYPASSRLS `quickscale_test_role`. This is a more fundamental blocker than CR-PLAN-SA79-004 previously named: notifications (SA79's own quarantined module) never reached the forms-0007 backfill code path at all — it crashed earlier on this role misconfiguration, and the SA76 quarantine absorbed that crash under the SA79 ticket too, again masking an unrelated failure. `storage` and `analytics` passed clean under the retained role, proving the mechanism works correctly for at least some modules — the gap is in local role/env provisioning coverage, not in the retained-role concept itself.
    Additionally, `backups` failed 23 of 299 tests due to a missing `pg_dump`/PostgreSQL-18-client-tools binary in this environment — a separate, tooling-only gap (no `pg_dump` installed locally), not a role/RLS issue, and orthogonal to SA77/SA79.
    **New watch item surfaced by this checkpoint:** the SA76 quarantine mechanism suppresses any failure under a quarantined module's name, regardless of whether it matches the ticketed root cause — evidenced here by both the SA77 and SA79 entries each absorbing a failure unrelated to what they describe. Worth tightening (e.g., matching on failure signature/count, not module name alone) in a future pass; not fixed in this checkpoint per instruction to record findings only, not remediate them. See `docs/technical/roadmap.md` §Track 1/§Track 2 for current status and next steps.
  - **SA80 — Re-provision the local dev environment for retained-role integration runs (2026-07-13, verified 2026-07-13).** The 2026-07-13 CR-PLAN-SA79-004 rerun attempt was blocked by two environment gaps: a stale non-editable `quickscale-core` venv install (missing the `runtime` package from SA9.3) and missing `QS_*_DB_USER=quickscale_test_role` env wiring for 7 of 13 modules in local test runs. SA80 resolved both, closing the gap that blocked SA77/SA79 verification.
    - **SA80.1 — Editable-reinstall `quickscale_core` (operational fix, 2026-07-13).** The root `.venv` had `quickscale-core` 0.86.0 installed non-editable, missing `quickscale_core.runtime`. Fixed by running `poetry install` from the repo root. Confirmed: `poetry run pip show quickscale-core` now reports version `0.87.0` with editable project location; `poetry run python -c "import quickscale_core.runtime"` succeeds. No code change needed — the stale install was a local env divergence from the current `pyproject.toml`/`poetry.lock`.
    - **SA80.2 — Fix retained-role env wiring in `scripts/test_integration.sh` (2026-07-13).** Added 12 `export QS_*_DB_USER="${VAR:-quickscale_test_role}"` lines at the start of the module test execution phase, matching the per-module retained-role exports that `.github/workflows/ci.yml` (lines 399-410) and `publish.yml` (160-171) set as step-level env vars. Each var uses a default-if-unset pattern so CI callers can still override. The fix is in `test_integration.sh` itself, so both direct local invocation and callers like `scripts/check_ci_locally.sh` inherit the correct environment. No changes needed to `scripts/check_ci_locally.sh`, `Makefile`, or `scripts/provision_test_roles.sh`.
    - **SA80.3 — Install PostgreSQL 18 client tools and rerun the `backups` suite (2026-07-13, split into SA80.3a+SA80.3b; SA80.3a closed 2026-07-13, SA80.3b closed 2026-07-13).**
      - **SA80.3a — Install PostgreSQL 18 client tools locally (2026-07-13, completed).** Added the PGDG apt repository (signed-by keyring at `/usr/share/postgresql-common/pgdg/apt.postgresql.org.gpg`, source list `/etc/apt/sources.list.d/pgdg.list` pointing at `noble-pgdg main`) and installed `postgresql-client-18` via `apt-get`. Verified: `pg_dump --version` → 18.4. Acceptance: pg_dump/pg_restore 18.x resolve on PATH. Achieved.
      - **SA80.3b — Rerun the `backups` suite to confirm the 24 missing-`pg_dump` failures are resolved (2026-07-13, completed).** Ran `QS_BACKUPS_DB_USER=quickscale_test_role make MODULE=backups test -- --modules`: 298 passed, 1 failed, 2 skipped in 55.98s. The 1 failure (`test_restore_file_mode_executes_pg_restore_for_operator_dump`) is a pre-existing test expectation mismatch — hardcodes `--username postgres` but retained-role env uses `quickscale_test_role`. Without the env var (default `postgres` user): 299 passed, 2 skipped in 55.61s — 0 failures. All 24 original missing-`pg_dump` failures resolved. The pre-existing test failure is documented as a new finding in roadmap.md. Acceptance: 0 failures attributable to missing pg_dump/pg_restore. Achieved. See `docs/technical/roadmap.md` §Track 3 for detail.
    SA80.3a+SA80.3b close SA80.3.
    Closes SA80 (SA80.1+SA80.2+SA80.3 implementation complete and verified 2026-07-13). Verification rerun: orgs (847 passed, 11 BYPASSRLS-skips) and notifications (39 passed) now reach their intended code paths instead of crashing at app-ready. See `docs/technical/roadmap.md` §Track 3 for current status.
  - **SA82 — Remove the SA76 `orgs`/`notifications` quarantine entries and rerun the full `make test-integration` gate (2026-07-13, completed).** Removed `orgs` and `notifications` from `QUARANTINE_TICKETS` in `scripts/test_integration.sh`; confirmed with `bash -n` that the script parses clean. Ran `make test-integration` end-to-end — exit 1 (expected: separate independent findings remain). Target acceptance evidence: orgs 847 passed/11 BYPASSRLS-skips/0 failed, 93.04% coverage; notifications 39 passed/0 failed, 91.76% coverage. Overall mean coverage 92.95% passed. **Closes SA77 and SA79** — their acceptance conditions (orgs and notifications clean under the full unquarantined gate) are met. The repository integration gate remains red due to separate independent restricted-role findings surfaced by this rerun: SA83 (blog, 86 RLS failures) implementation/validation complete; closed after independent review. SA84 (CRM, 67 RLS failures/20 skipped), SA85 (forms residual — blocked checkpoint, impl/validation complete but independent review blocked on CR-SA85-REV-001, see SA85 entry), and SA86 (listings, 6 RLS failures) remain open. The SA80.3b rerun has since confirmed the prior SA80.3 (backups, 24 missing-`pg_dump` failures) item is resolved — see SA80.3b entry above. See `docs/technical/roadmap.md` for current status.
  - **CR-TRACK3-MERGE-002 — Resolved (2026-07-13, roadmap cleanup).** The Track 3 merge checkpoint recorded that Track 3 merged into `v87` while the arch-audit/tech-audit "current" prose overstated a shared Finding 8 / `operator_access` causality across SA83–SA86. The current arch-audit Finding 8 prose is now evidence-calibrated — SA83 is stated as closed and independent of Finding 8, SA84–SA86 are bucketed as separate investigations, `operator_access` is named a per-module candidate (not a shared remedy), and Finding 8 carries the severity-bounding caveat that production `migrate` runs privileged. The checkpoint's blocking concern is therefore moot; removed from the roadmap. Closes CR-TRACK3-MERGE-002.
  - **SA83 — Fix blog's 86 restricted-role RLS failures surfaced by the SA82 unquarantined gate (2026-07-13, closed after independent review).** Three root causes fixed across 6 phases: (1) **unscoped blog test setup** across nine test files — every tenant Category/Tag/Post/BlogMediaAsset write/read now runs inside `blog_org_scope(org)` so FORCE RLS sees the `app.current_org_id` GUC; (2) **missing `_resolve_api_org` context priming** — token-authenticated blog publish/upload API paths (which bypass TenantMiddleware) resolved the org but never set the ContextVar/GUC; converged to one local `org`, call `set_current_org_id(org.pk)` once, and both API callers (`upload_media_api`, `publish_post_api`) capture `prior`, wrap post-resolution returns in `try`, restore in `finally`; (3) **stale AF9 priming memo on GUC reset** — `org_scope()` exit reset the GUC but left the per-transaction priming memo, so re-resolving the same org skipped `SET LOCAL` and left the GUC empty. Added `_clear_priming_memo(connection)` in `orgs/current_org.py`, called from all three direct GUC mutators (`_set_db_current_org_id`, `reset_db_current_org_id`, `_restore_current_org_id`) — a **shared orgs fix that hardens tenant-context invariants for all modules**, not just blog. Outcome: blog 211 passed/0 failed, 91.62% coverage, no quarantine entry (API 57/57, admin 33/33, managers 5/5, isolation 2/2); orgs 850 passed/11 BYPASSRLS-skips/0 failed, 93.08%; overall mean 93.55%. Gate remains red only on the independent SA84/SA86 (open) and SA85 (blocked checkpoint). Closes SA83.
  - **SA81 — Remove stale per-module `poetry.lock` files and sibling-module version-range constraints that never resolve standalone (2026-07-13, completed).** Walked the worktree to identify remaining non-root lock artifacts: 4 local gitignored `poetry.lock` files existed (`quickscale_core`, `quickscale_cli`, `quickscale_modules/orgs`, `quickscale_modules/backups`); all 4 deleted. Root `poetry.lock` preserved. Removed 7 sibling-constraint declarations from `quickscale_modules/{auth,billing,blog,crm,listings,orgs,social}/pyproject.toml` `[tool.poetry.dependencies]` — these plain version-range references to unpublished sibling packages (`quickscale-module-orgs`, `quickscale-module-auth`) could never resolve standalone. Updated `poetry.toml` for all 8 affected modules (`auth`, `backups`, `billing`, `blog`, `crm`, `listings`, `orgs`, `social`) to explicitly state that standalone lock/install is unsupported: 5 created or rewritten from scratch (`backups`, `billing`, `crm`, `orgs`, `social`); 3 updated from less explicit virtualenv-only guidance (`auth`, `blog`, `listings`). CI and `make test-unit`/`make test-integration` never `cd` into module directories for `poetry lock`/`poetry install` — no CI impact. The `.gitignore` already ignored all non-root lockfile paths. Closes SA81.
  - **SA85 — Fix forms' residual restricted-role test failures (2026-07-13, blocked checkpoint — implementation/validation complete; independent review blocked on CR-SA85-REV-001 [high/blocking]; CR-SA85-REV-002 through CR-SA85-REV-007 resolved).** Four-phase fix for the 33 RLS failures/8 skipped/10 errors (and 15 errors on the reproduction branch) that SA82's full-gate rerun surfaced when the SA76 quarantine was removed:
    - **Phase 1 (bounded fixture/test org scopes):** conftest and all test files (`test_models.py`, `test_serializers.py`, `test_validators.py`, `test_notifications.py`, `test_admin.py`, `test_isolation.py`, `test_management.py`, `test_migrations.py`, `test_views.py`) — model creation and mutations wrapped in `org_scope()` so FORCE RLS sees the correct `app.current_org_id` GUC. Two slug-uniqueness / field-duplicate expected-failure paths use `set_current_org_id` + `set_db_current_org_id` to avoid AF9 priming-memo staleness and savepoint-abort issues inside `org_scope`.
    - **Phase 2 (retained-role management read-inventory/per-org writes):** `forms_anonymize_submissions.py` and `forms_seed_presets.py` rewritten with a three-phase pattern: `operator_access(reason=...)` for cross-tenant SELECT inventory reads (audit-logged at INFO), per-organization writes inside `org_scope()`, all wrapped in `transaction.atomic()`.
    - **Phase 3 (notification content scoped and post-commit tested):** `notifications.py::_enqueue_notification` wraps `_build_submission_notification_content(submission)` in `org_scope(submission.organization)` so FORCE RLS allows reading committed `FormFieldValue` rows. The scope exits before email delivery (no live transaction across remote calls). New `TestNotifySubmissionNoContext` regression test proves `notify_submission` renders field values correctly without ambient org context or active `transaction.atomic()`.
    - **Phase 4 (Staff current-org/superuser operator contract across source/tests/README/E2E):** `views.py::FormsAdminApiMixin` gains `_is_superuser()`, `_get_org_bound_queryset()` (superuser → `all_objects`, regular staff with org → `objects`, staff without org → `objects.none()` fail-closed), and `_with_superuser_operator_access()` for cross-tenant read audit. All four admin views are role-aware; PATCH saves inside the target record's `org_scope`. README documents the superuser/staff/anonymous retained-role contract matrix.
    - **SA77 boundary helper reuse:** `test_rls_boundary.py::_ensure_rls_test_role()` uses the SA77 `connection.cursor()` + savepoint pattern (psycopg2 → Django managed connection) with best-effort GRANTs wrapped in `transaction.atomic()` savepoints.
    - **Serializer/validator context amendment:** `test_serializers.py` and `test_validators.py` assertions made non-vacuous — previously passing because `all_objects.create` silently bypassed RLS, now proving serializer/validator correctness under FORCE RLS.
    **Evidence:** Current checkpoint: Forms 196 passed/8 skipped/12 deselected/0 failed/0 errors. Intermediate (post-Phase 1–3, pre-Phase 4): module suite 186 passed/8 DDL-only skips/12 e2e deselected/0 failed/0 errors/95.70% coverage. QG2 earlier: Forms 192p/8s/95.73%, overall mean 93.00%. E2E: 12 passed. All other modules clean. **Discovered non-blocking:** disposable Forms test DB had one stale `postgres`-owned `django_session` table (recreated under `quickscale_test_role` for transaction-test teardown — environment evidence, not a product change); pre-existing Django app-init DB-access warning unchanged. **No Forms quarantine entry needed.** Blocked checkpoint — implementation/validation complete; CR-SA85-REV-002 through CR-SA85-REV-007 resolved; CR-SA85-REV-001 (high/blocking) remains open. Required next action per settled decisions: rewrite `test_views.py` admin-form-list tests to create own/foreign Forms, use real `force_login` + `ACTIVE_ORG_SESSION_KEY` with no direct context, assert staff inclusion/exclusion and superuser cross-tenant; no-org staff/superuser must request admin-form-list and assert 302 Location `/orgs/`; correct README contract table, comments, and references.
    **Resolution (2026-07-14):** CR-SA85-REV-001 resolved (Adaptive-change-review pass 2, STATUS ok; explicitly resolved CR-SA85-REV-001). During the fix pass, discovered `superuser_client` had pre-applied DRF `force_authenticate` bypassing real session/org context; fixed with clean `api_client` + `force_login` + `ACTIVE_ORG_SESSION_KEY` and selected/foreign Forms. Ruff passed; forms restricted-role suite 196 passed/8 pre-existing skipped/12 e2e deselected/0 failed, one pre-existing warning. No remaining blockers or open decisions. Closes SA85.
    - **SA87 — Make the operator-restore test username-independent (2026-07-13, completed).** Replaced the hardcoded `--username postgres` expectation in `test_restore_file_mode_executes_pg_restore_for_operator_dump` with a dynamic reference to `connections["default"].settings_dict["USER"]`, so the assertion derives the expected restore username from the active database connection instead of hardcoding `postgres`. Both default-user and retained-role suites confirmed 299 passed/2 skipped with 0 failures. Production behavior was already correct — this was a test-isolation fix only. Closes SA87.
  - **SA88 — Land an orgs-owned shared RLS-context migration helper + conformance gate (Finding 8, Option 1) — attempted hardening withdrawn after independent review (2026-07-14).** Baseline implementation, triage, and validation accepted from prior passes. An attempted hardening checkpoint (commit e1d38bd5, adding conformance negative proofs and a 147-test restricted-role suite) was independently reviewed and found **STATUS partial / unsafe to merge**. The two-file code delta has been withdrawn as part of this docs-only checkpoint, restoring `test_sa88_migration_operator_access_conformance.py` and `test_tenancy.py` to parent-v87 baseline (69cabb47). **No completion, merge, or safe-gate claim is made** — the attempted hardening was reviewed and never landed.
    - **Accepted baseline CRM triage:** 195 passed, 67 failed, 20 skipped under the SA82 full `make test-integration` gate. **Buckets:** 0 migration-time, 67 fixture-time, 0 runtime-query. **No new TA finding.**
    - **Accepted baseline deliverables:** orgs-owned `operator_access_migration(schema_editor)` context manager / `RunPython` wrapper in `orgs/tenancy.py`; `forms/0007` rerouted through the helper; lifecycle regression tests.
    - **Accepted downstream baseline:** CRM 67 = SA84; listings 6 = SA86.
    - **Attempted hardening (e1d38bd5 — NOT landed):** conformance negative proofs, including analyzer corrections verified against full module inventory; 147-test restricted-role suite (SELECT/UPDATE/DELETE/INSERT); Ruff/MyPy passes verified by independent review. Review outcome: STATUS partial/unsafe to merge. Code delta withdrawn in this pass.
    - **Finding status (carried forward — see roadmap for full table):**
      - CR-SA88-REV-002 (high/blocking): **still-open** — nested function `schema_editor` could capture outer parameter; negative proofs bypassable.
      - CR-SA88-REV-004 (low/advisory): **resolved** — exact-commit independent review of e1d38bd5 confirmed lint/format gate passed.
      - CR-SA88-REV-005 (medium/blocking): **withdrawn** (code removed) — ContextVar/GUC restoration leak found in attempted test helper; do not call resolved until final review confirms absent from any replacement.
    - **SA88a blocked checkpoint (2026-07-14; partial implementation accepted for merge after review-cycle cap):** The conformance gate now uses immediate-function wrapper ranges, scans manifested migration files independently of installed apps, fails loudly on migration read errors, and includes strict retained-role negative proofs; 66 conformance tests, MyPy, and orgs module lint pass. Independent review resolved CR-SA88-REV-002 and confirmed no ContextVar/GUC leak, but SA88a remains open on **CR-SA88-REV-006 (high/blocking)** for active-scope canonical-binding bypasses and **CR-SA88-REV-007 (high/blocking)** for incomplete save-expression/control-flow detection. No clean SA88 gate or SA84/SA86 unblock claim is made.
      - **Final SA88a finding ledger:** CR-SA88-REV-002 **resolved**; CR-SA88-REV-006 **high/blocking, still-open**; CR-SA88-REV-007 **high/blocking, still-open**; CR-SA88-REV-009 **resolved**. This final ledger supersedes status labels from the withdrawn hardening attempt where identifiers or severity labels conflict.
    - **SA88a partial checkpoint — canonical-import provenance infrastructure (2026-07-14, agent usage limit reached mid-implementation).** Attempt to close CR-SA88-REV-006 added analyzer infrastructure in `test_sa88_migration_operator_access_conformance.py` but did not complete the wiring into `check_migration_source` or add test proofs. **Status: still-open.** Delivered: canonical module/FQN constants, `_extract_attribute_fqn()` dotted-name resolver, `_collect_operator_access_bindings()` comprehensive binding-event scanner (canonical/non-canonical imports, assignment variants, function/class defs, parameters, `for`/`except`/`with as`/`del`), `_collect_operator_access_bindings_by_scope()` scope-aware binding isolation, and `_is_bound_to_canonical_operator_access()` call-site provenance resolver. All helper code is dead — not yet wired into the conformance checker. No negative-proof tests exist yet for REV-006 evasion shapes. Baseline 66 conformance tests preserved (unaffected). MyPy and lint not run. Subagent usage limit reached; continuation deferred to next session on `wt-track1`. See `docs/technical/roadmap.md` §SA88a for full findings/blockers.
  - **SA88-QG-FORMS-001 / SA88b (high) — resolved (2026-07-14).** Track 2 replayed the current retained-role contract (`./scripts/provision_test_roles.sh`) with fresh environment checks. `QS_FORMS_DB_USER=quickscale_test_role make MODULE=forms test -- --modules` passed with **196 passed / 8 skipped / 12 deselected / 0 failed / 0 errors**; the full `make test-integration` gate repeated the same clean Forms result. The gate's only failures were the separately ticketed **CRM 67 failed / 195 passed / 20 skipped** (SA84) and **listings 6 failed / 128 passed** (SA86). Independent quality-gate and change-review passes accepted the evidence: the prior Forms-only failure is no longer reproducible and is closed as transient/environment-dependent. No product source changed, and this does not claim the precise stale-database ownership mechanism was proven.
    - **Status:** the baseline seam (`operator_access_migration` in `orgs/tenancy.py`, `forms/0007` reroute, baseline conformance gate + lifecycle tests) is **merged to `v87`** (69cabb47). SA88b is closed; SA88a and SA88c remain open on Track 1, so SA84 and SA86 remain gated until both hardening tickets complete. See roadmap §Track 1 / §Track 2.
  - **SA90 — Export the generator's emission mapping as a function; point the SA66 conformance gate at it (Finding 7 interim).** Added `get_generator_emission_mapping(template_root)` to `generator.py` as the authoritative source for template→emitted-path routing, along with supporting constants (`_THEME_DEST_MAP`, `_THEME_SUBDIR_MAP`) and helpers (`_resolve_theme_emitted_path`, `_theme_non_jinja_emitted_paths`). The SA66 conformance gate (`test_beta_migration_ownership_conformance.py`) now imports and delegates to this production function instead of duplicating the routing logic as a private copy. The `__init__.py` exports the new function. Production and the SA66 conformance gate now share the theme/module-aware emission mapping; independent four-variant path/SHA-256/mode parity is checked against the `sa90_emission_manifests.json` fixture. Closes SA90 (CR-SA90-REV-001/002/003/005 resolved by independent review; CR-SA90-REV-004 remains low advisory, non-blocking — no-op/unused private React helper seams deferred).
  - This release also carries the tenant isolation, single-URL routing, module-manifest wiring, and disaster-recovery rework that was the original scope for this cycle, plus the hardening items above added after a mid-cycle architecture review.

- v0.86.0 — Organizations Module (multi-tenancy with Solo/SaaS runtime modes, org-scoped RBAC and invitation flow, billing bridge with authoritative org ownership fields and `Plan.features` feature gates, `migrate_billing_to_orgs` and `promote_to_saas` management commands, React org management pages and org switcher, and billing wiring regression fix with planner/apply guard test)
- v0.85.0 — Billing Module (Stripe-backed one-time credit purchases and recurring subscriptions, credits-first Django-owned ledger, planner/apply readiness with auth-module dependency enforcement, module-owned `/billing/pricing/` and `/billing/dashboard/` pages, and starter-theme billing links)
- v0.84.0 — Backups Hardening Release (admin backup artifact download/create/integrity actions, upload-driven restore safety and compatibility checks, full-snapshot backup-contract hardening, and repo-owned runtime/tooling support alignment for the refreshed Python/frontend baseline)
- v0.83.0 — Hardening Release (repo-wide hardening across strict desired-config validation, notifications live-config placeholder rejection, generated production `SECRET_KEY` fail-hard behavior, CRM staff-only parity, forms CSV and backup artifact safety, blog upload/thumbnail/rate-limit hardening)
- v0.82.0 — Disaster Recovery & Environment Promotion Workflows (public `quickscale dr capture/plan/execute/report` workflows for local and Railway routes with `snapshot_id`-based stored snapshots, resumable capture and execute, rollback pins for production routes, conservative env-var sync, and source-side media sync)
- v0.81.0 — Beta-Site Migration Maintainer Tooling (maintainer-only Make/Python workflows for beta-site catch-up: fresh-first and in-place paths with checkpoint-first semantics)
- v0.80.0 — Analytics Module (PostHog-only website analytics with flat `QUICKSCALE_ANALYTICS_*` settings, service-style capture helpers, apply-time env-example sync, guarded forms integration, QuickScale-owned social click tracking)
- v0.79.0 — Social & Link Tree Module (curated social links and embeds, backend-owned YouTube/TikTok preview metadata, managed `/_quickscale/social/` integration endpoints, Django-owned public React pages)
- v0.78.0 — Notifications Module (transactional email foundation, Anymail-backed Resend delivery, recipient-granular tracking, signed delivery webhooks)
- v0.77.0 — internal main-branch baseline — Backups Module (private database backups, optional private remote offload, guarded CLI restore, scheduler-ready command hooks)
- v0.76.0 — Storage Module (cloud file hosting, media storage adapters, CDN-ready media infrastructure)
- v0.75.0 — Forms Module (generic customizable form builder with admin, DRF API, spam protection, GDPR anonymization, and React mount point)
- v0.74.0 — React Default Theme (showcase_react theme with Vite, TypeScript, TanStack Query, and Zustand)
- v0.73.0 — CRM Module (API-first Django CRM with 7 core models and CLI integration)
- v0.72.0 — Plan/Apply Cleanup (removed legacy init/embed commands, full transition to plan/apply)
- v0.71.0 — Module manifests & config mutability (Plan/Apply system complete)
- v0.70.0 — Existing project support (status, plan --add, plan --reconfigure)
- v0.69.0 — State management and incremental applies
- v0.68.0 — Plan/Apply System core commands (Terraform-style declarative workflow)
- v0.67.0 — Listings module with AbstractListing base model for verticals
- v0.66.0 — Blog module with Markdown, featured images, and RSS feeds
- v0.65.0 — Enhanced auth module and development tooling
- v0.64.0 — Theme rename to showcase_* (breaking change)
- v0.63.0 — Authentication Module with django-allauth and interactive embed
- v0.62.0 — Split Branch Infrastructure (module management CLI commands, GitHub Actions automation)
- v0.61.0 — Theme System Foundation (--theme CLI flag, theme abstraction layer, HTML theme)
- v0.60.0 — Railway Deployment Support (automated deployment via quickscale deploy railway)
- v0.59.0 — CLI Development Commands (Docker/Django operation wrappers)
- v0.58.0 — Comprehensive E2E testing infrastructure with Playwright and PostgreSQL
- v0.57.0 — Production-ready generator baseline
- v0.56.0 — Quality, Testing & CI/CD
- v0.55.0 — CLI implementation
- v0.54.0 — Project Generator
- v0.53.3 — Project Metadata & DevOps Templates
- v0.53.2 — Templates and Static Files
- v0.53.1 — Core Django Project Templates
- v0.52.0 — Project Foundation
- v0.51.0 — Documentation foundation
- v0.41.0 and earlier — legacy codebase (see Github repository for history)
