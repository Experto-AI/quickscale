# Changelog

`CHANGELOG.md` is the canonical QuickScale release history index. Published releases pair each version entry with a single official release note in `docs/releases/` linked from the GitHub tag and release PR. When a release note is prepared before the maintainer completes the manual tag/publish step, the changelog entry and note must say so explicitly and must not imply publication. Use `docs/technical/roadmap.md` for active or unpublished release status. Entries are version-ordered; listed dates reflect the recorded artifact date when available.

- v0.87.0 — unreleased / not yet tagged — Hardening release. **Track 3 complete** — Railway recovery (M8), runtime-pin decoupling (M11), DR hard cutover (M12). **Track 2 complete** — DR engine extraction (M10) and manifest-backed module wiring rollout (M14 / T2.1–T2.4). **Track 1 complete** — all 4 phases: foundation / System org + NOT NULL + contextvar seam + middleware + RLS DB role (M13/M15), per-module contract adoption across CRM/Blog/Forms/Listings/Social/Billing (M16), RLS backstop across all 6 modules (M18), and teardown / `purge_organization` command (M19). (This entry records main-branch progress only and does not imply publication.)
  - 2026-06-25 — **T1.17: `purge_organization` management command (Track 1, Phase 4 — M19).** Delivered ordered, FK-safe org purge across all owned tables despite PROTECT FKs. Three-phase implementation:
    - **Phase 1 (contract lock):** `OrganizationTombstone` model/migration with minimal payload (deleted org UUID, purged_at, operator metadata) for tombstone-backed rerun semantics; `set_current_org_for_context()` shared helper combining ContextVar + `SET LOCAL app.current_org_id` for non-middleware callers; management command with UUID-only destructive targeting (`--organization-id`), slug-only non-destructive preflight (`--slug`), `--dry-run`, `--force` (bypasses System/personal org guard), reserved-org (System and personal) refusal by default, invitation inclusion in ownership counts, and contract tests.
    - **Phase 2 (transactional delete path):** Shared `_build_ownership_map()` single source of truth for counts across dry-run and destructive paths. `_delete_owned_rows()` in FK-safe order via `apps.get_model()` with graceful fallback: social → forms (FormSubmission before Form) → listings → blog (Post → Category → Tag → BlogMediaAsset) → crm (DealNote → ContactNote → Deal → Contact → Company → Stage → Tag) → billing (CreditTransaction → Subscription → CreditBalance) → org memberships + invitations. Postgres-backed test env support (`QUICKSCALE_TEST_DB=postgres` env flag, `QS_ORGS_DB_*` vars) with `current_setting('app.current_org_id', true)::uuid` RLS proof test. 3 additional tests: billing cross-module purge, rollback safety, slug-reuse safety.
    - **Phase 3 (bugs + docs closeout):** Fixed `_get_active_org_subscription()` in `permissions.py` — changed from `subscription_model.objects` (TenantManager, returns `.none()` when ambient org context absent) to `subscription_model.all_objects` (super-scope bypass). This was discovered during the Postgres orgs checkpoint where feature-requiring views resolved outside full middleware failed. Roadmap and changelog updated.
    - **Validation:** Stop-here rerun: PostgreSQL orgs suite (`make MODULE=orgs test -- --modules`) **278 passed, 3 skipped**. Supporting checks kept green on this branch: forms module **130 passed, 3 skipped, 11 deselected**; notifications module **33 passed**; core runtime suite **1552 passed, 28 deselected** (with a pre-existing unrelated coverage shortfall). Advisory follow-up: 3 legacy billing migration tests need cleanup; no interactive confirmation prompt (runs immediately with `--organization-id`).
    - **Stop-here follow-up (tracked in roadmap):** Generated `showcase_react` SaaS org-switch billing/session parity remains unresolved outside the locked T1.17 DB-rows-only scope. A separate decision is still needed before routing SPA org-dashboard billing flows through flat `/billing/...` and `/api/billing/...` surfaces.
  - 2026-06-25 — **T1.6: Blog adopt contract (Track 1).** Adopted the shared NOT NULL/PROTECT ownership contract (D3/D5) for the blog module. Replaced module-local `TenantScopedManager`/`OperatorManager` with `quickscale_modules_orgs.managers.TenantManager` (`super_scope=True` for operator bypass). Deleted `blog/managers.py`. Converted Category, Tag, BlogMediaAsset, and Post org FK to NOT NULL/PROTECT (was `null=True, CASCADE`). Removed route-sniffing helpers (`_is_org_scoped_route`, `_resolve_active_org`, `_resolve_active_org_optional`, `_resolve_org_for_token_auth`, etc.) and all `isnull` union logic from views. Simplified `OrgScopedViewMixin._scope_by_org` — anonymous/public readers see System-org content (D2), authenticated readers use `request.org`. Added `_resolve_org_for_read()` and `_resolve_api_org()` helpers for single-URL contract. Deleted all `/orgs/<slug:org_slug>/blog/...` URL patterns (D1/D5). Removed `LatestPostsFeedOrgScoped` from feeds; `LatestPostsFeed` scopes to System org. `get_absolute_url()` returns flat routes unconditionally. Squashed migrations to clean NOT NULL/PROTECT contract (single `0001_initial.py`, no backfill per D5). Removed 4 org-scoped test classes and rewrote views, feeds, API, and isolation tests for the flat-route/System-org contract. **Closeout completed** — documented the required manual-install `path('markdownx/', include('markdownx.urls'))` wiring to match the shipped contract, reran `make MODULE=blog test -- --modules` (179 passed), and closed T1.6. CR-T16-002 remains tracked in the roadmap as an advisory follow-up.
  - 2026-06-24 — **T1.5: CRM adopt contract (Track 1).** Removed CRM route-sniffing and org-scoped URL variants; replaced module-local `TenantScopedManager`/`OperatorManager` with shared `TenantManager` from orgs; removed all `isnull` fallback logic and `.for_org()` calls; deleted dead `backfill_crm_org_ownership` command; simplified views, serializers, services for flat `/crm/` route contract; updated admin form querysets and serializer `PrimaryKeyRelatedField` querysets to use `all_objects` to bypass TenantManager auto-scoping; added contextvar propagation in `_resolve_active_org`, `_read_org_id`, and `_request_org_id` for TenantManager compatibility; removed 11 obsolete org-scoped test classes; rewrote bootstrap, isolation, and org-bootstrap tests for flat routes; fixed all serializer/model/service tests for TenantManager contract. Final CRM suite: 241/241 passed. Roadmap updated.
  - 2026-06-24 — **T1.9 (wt-track3): Social adopt contract.** Replaced module-local `TenantScopedManager`/`OperatorManager` with `orgs.managers.TenantManager` (auto-scopes via ContextVar; `super_scope=True` for operator bypass). Replaced nullable `CASCADE` FK on `BaseSocialItem.organization` with `tenant_org_fk()` (NOT NULL/PROTECT per D3). Dropped `SocialItemQuerySet.for_org()` and the `organization_id` kwarg from all four service functions — tenant scoping is now ambient via the ContextVar. Added `_social_cache_key()` helper for org-aware cache keys. Squashed migrations to clean NOT NULL/PROTECT contract (single `0001_initial.py`, no backfill per D5). Updated tests to use `set_current_org_id()` for contextvar-based scoping. Roadmap updated: T1.9 closed.
  - 2026-06-25 — **T1.10 (wt-track3): Billing org-only subject.** Made org the sole billing subject — NOT NULL/PROTECT org FK on CreditBalance, CreditTransaction, Subscription. Collapsed CreditBalance to org-keyed (user retained as nullable provenance). Deleted `_sync_subscription_authority()` and all 6 callsites; replaced with inline field updates. All service functions now require `organization=` — user-subject duality removed. Simplified views: removed `_resolve_authenticated_billing_organization`, org-scoped route helpers, SaaS mode sniffing, and all user-subject fallback paths. Deleted all 16 `/orgs/<slug:org_slug>/...` URL pairs; flat routes only. Added `all_objects` admin overrides. Squashed migrations to single `0001_initial.py` with NOT NULL/PROTECT contract. Replaced `get_or_create_for_user` with `get_or_create_for_org`. Internal service lookups use `all_objects` for cross-org webhook access. 176 billing tests passing. Roadmap updated: T1.10 closed.
  - 2026-06-25 — **T1.15 (wt-track3): Social RLS policies.** Enabled RLS + FORCE ROW LEVEL SECURITY on social tables via `RunSQL` migration using `current_setting('app.current_org_id', true)::uuid` predicate. Generated social payload callers (`build_social_link_tree_payload`, `build_social_embeds_payload`, managed views renderer) establish DB and ContextVar org state. Social admin is per-org via explicit request selection → session persistence → fail-closed behavior under the runtime role (no `BYPASSRLS` or operator bypass policy). App role is `NOSUPERUSER` / `NOBYPASSRLS` per T1.4. PostgreSQL-backed social module test target: 81/81 passed; social admin contract tests: 40/40 passed; template-view tests: 11/11 passed. Roadmap updated: T1.15 closed, Track 3 next-task pointer advanced to T1.16, M17 added, organizations.md updated for partial RLS rollout.
  - 2026-06-25 — **T1.11 prep completed; remaining blocker recorded (Track 1).** Plan-review for CRM RLS policies completed (2 passes). Prerequisites confirmed resolved: T1.4 (RLS DB role), T1.5 (CRM contract adoption), D4 (RLS role policy). Worktree synced: clean-status check and fast-forward merge to v87. User-approved implementation choices established: parent-derived note-table RLS without schema change; explicit org-selection/fail-closed admin behavior; update organizations.md when implementation lands. Remaining blocker CR-PR-T111-002: the 0008 migration must include PostgreSQL guard, explicit reverse path, and forward+reverse migration proof. User opted to stop at plan-review cap; T1.11 remains pending. Roadmap updated with current status.
  - 2026-06-24 — **T2.4 (Track 2): Delete dead ladder/shims.** Deleted the CLI implication-defaults helper and the `quickscale_cli.module_catalog` re-export shim, then repointed the remaining CLI and test callers to `quickscale_core.manifest.implications.resolve_module_implications()` and `quickscale_core.contracts.module_catalog`. Roadmap updated: T2.4 closed, Track 2 marked complete, M14 added, and unrelated coverage debt noted. Canonical repo-root `make test` had zero test-case failures in touched suites; exit code remained non-zero solely because of the pre-existing unrelated per-file coverage gaps in `quickscale_core/src/quickscale_core/contracts/resolvers.py`, `quickscale_core/src/quickscale_core/manifest/social_manifest.py`, and `quickscale_modules/backups/src/quickscale_modules_backups/management/commands/dr_adapter_call.py`.
  - 2026-06-23 — **T3.3 (Track 3): DR hard cutover cleanup.** Refreshed `quickscale_modules_backups.services` to describe the single active DR path, removed a no-op `TYPE_CHECKING` block there, added a one-paragraph canonical-path docstring to `quickscale_core.dr_engine.adapter`, and updated `backups_sync_media` help text to describe the explicit adapter payload instead of the retired env-var wording. Roadmap updated: T3.3 closed, Track 3 marked complete, and M12 added. Canonical repo-root `make test` had zero test-case failures and passed all touched suites; exit code remained non-zero solely because of the pre-existing unrelated per-file coverage gap in `quickscale_modules/backups/src/quickscale_modules_backups/management/commands/dr_adapter_call.py` (0% coverage).
  - 2026-06-23 — **F11-deferred: Stage `terminal_semantic` per-org uniqueness (Track 1).** Replaced global `unique=True` on `Stage.terminal_semantic` with per-bucket partial `UniqueConstraints` (migration 0007); updated model, serializer, and API regression coverage. Final CRM suite: 332/332 passed. Canonical `make test` green repo-wide. Roadmap updated.
  - 2026-06-23 — **T2.1: Manifest implies support (Track 2).** Added `ImpliesEntry` dataclass and `ModuleManifest.implies` to the manifest schema; loader parses/validates `implies` blocks. Module manifests updated: billing→orgs, crm→orgs, social→orgs (empty default_config), orgs→notifications (with inlined default_config from notifications defaults). All manifests without `implies` continue to load. `ImpliesEntry` exported from `manifest/__init__.py`. Core schema/loader tests expanded. Roadmap updated.
  - 2026-06-23 — **T2.2: Generic implication resolver (Track 2).** Introduced `quickscale_core.manifest.implications.resolve_module_implications()` — reads `implies` blocks from each `module.yml` manifest and computes transitive closure (e.g. billing → orgs → notifications). The CLI shim `get_implied_module_default_configs()` delegates to the core resolver (deletion deferred to T2.4). Added 27 unit tests in `test_manifest_implications.py` covering edge cases, all implication chains, multi-implicator deduplication, transitive closure, and error paths. All 20 existing CLI shim tests continue to pass unchanged. The resolver accepts an optional `modules_base_path` parameter ready for T2.3 generated-project path migration. Core module `implications.py` at 100% coverage. Roadmap updated (T2.2 marked done). Remaining T2.3–T2.4 deferred.
  - 2026-06-23 — **T2.3: Migrate wiring into manifests; delete Python adapters (Track 2).** Deleted all 12 `quickscale_cli/src/quickscale_cli/*_manifest.py` adapter files; re-routed all callers through the generic resolver in `quickscale_core.manifest.resolver`. Replaced the purely static `MODULE_CATALOG` with manifest-backed discovery via `module_discovery.py` — dynamic scanning of `quickscale_modules/*/module.yml` is now the authoritative shipped-module inventory. Known placeholder directories (e.g. `teams`) remain fail-closed via explicit placeholder rejection. Shared resolver and helper ownership moved into `quickscale_core`. Surviving `quickscale_cli.module_catalog.py` is now a backward-compatible re-export shim (full deletion deferred to T2.4). Zero surviving `*_manifest.py` imports. Core and CLI test suites pass; `quickscale plan` and `quickscale apply` work end-to-end. Roadmap updated (T2.3 marked done). Remaining T2.4 deferred.
  - 2026-06-23 — **T1.1: System org + NOT NULL ownership contract (Track 1).** Added `Organization.is_system` field, unique singleton constraint, `OrganizationManager.get_system_org()`, `tenant_org_fk()` helper, and migration 0002_system_org. Org tests added. Cross-module closeout: billing `LATEST_ORGS_MIGRATION` updated to `0002_system_org` in `test_migrations.py`. Roadmap updated.
  - 2026-06-23 — **T1.1 review-driven fix: enforce reserved singleton invariants.** Made `is_system` non-nullable (migration 0003). Model `clean()` validates slug↔is_system coupling (`slug="__system__"` ↔ `is_system=True`), system row is non-personal, and non-null `is_system`. `get_system_org()` resolves by slug instead of trusting any `is_system=True` row, now fails closed on corrupt reserved-system rows, and regression tests cover reserved-slug collision, wrong-slug system rows, `is_system=None`, and personal-system corruption paths. Billing `LATEST_ORGS_MIGRATION` updated to `0003_alter_organization_is_system`; CRM migration tests now pin the latest orgs migration where historical org rows are created. Canonical `make test` passed for all changed suites; remaining backups coverage/typecheck noise is pre-existing and unrelated.
  - 2026-06-23 — **T1.2: Shared tenant-scoping seam (contextvar + base managers) (Track 1).** Added `current_org_id: ContextVar[uuid.UUID | None]` and `set_current_org_id()`/`get_current_org_id()`/`reset_current_org_id()` to `orgs/.../current_org.py`. Added `TenantManager.get_queryset()` (fail-closed — returns `.none()` when unset) and `TenantManager(super_scope=True)` as the unfiltered escape hatch to `orgs/.../managers.py`. Middleware propagation sets both the contextvar and `SET LOCAL app.current_org_id`. All orgs module tests pass (199 passed / 1 skipped). Repo-root `make test` passed for all changed suites; final exit code non-zero solely because of the pre-existing unrelated per-file coverage gap in `quickscale_modules/backups/src/quickscale_modules_backups/management/commands/dr_adapter_call.py` (0% coverage, already tracked in roadmap Deferred / Monitor). Roadmap updated.
  - 2026-06-24 — **T1.3: Middleware for the single-URL world (Track 1).** Converted org resolution to a session-based middleware contract. Solo mode resolves the personal org as before. Saas mode reads `session[ACTIVE_ORG_SESSION_KEY]` (new constant) instead of extracting `org_slug` from URL kwargs. Dropped `_resolve_org_slug()`, `_handle_saas_org_request()`, `_is_org_namespace()`, and `ORG_NAMESPACE_PREFIX`. Added narrowed `_is_org_management_path` — only orgs-module-owned paths (`/orgs/`, `/orgs/new/`, `/orgs/invitations/...`, `/orgs/<slug>/`, `/orgs/<slug>/members/...`, `/orgs/<slug>/settings/`, all `/api/orgs/...`) bypass middleware org resolution. Downstream module paths under `/orgs/<slug>/<module>/...` (crm, blog, forms, listings) go through normal session-based org resolution, preserving caller parity. Saas behaviors: no active org → redirect to `/orgs/`; valid member org → `request.org` + contextvar + `SET LOCAL` all populated; non-member org in session → forbidden + session cleared; stale/invalid org id → redirect + session cleared; superuser bypasses membership check. `OrgDashboardView.get()` writes the active org to the session after successful access (org-switcher). All middleware tests rewritten for the session-based contract. Fixes CR-T13-001 (security boundary — downstream module paths exposed cross-tenant data under blanket bypass) and CR-T13-002 (breaking caller parity for live `/orgs/<slug>/...` routes). **Integration regression fix (slug fallback):** Added `_resolve_org_from_path_slug()` and two backward-compatible fallbacks in SaaS mode — (A) slug-based org resolution for legacy downstream module paths, (B) personal org fallback for solo-format routes under `/crm/`, `/listings/`, `/forms/`, `/blog/`. Existing session org takes priority. CRM 332/332 passed, orgs 247 passed / 1 skipped. Canonical repo-root `make test` rerun — core 1452 passed, cli 1795 passed, analytics 39 passed, auth 38 passed, backups 229 passed, billing 242 passed, blog 219 passed, crm 332 passed, forms 138 passed, listings 111 passed, notifications 33 passed, orgs 247 passed / 1 skipped, social 52 passed, storage 23 passed. Exit code non-zero solely due to pre-existing unrelated per-file coverage gap in `quickscale_modules/backups/src/quickscale_modules_backups/management/commands/dr_adapter_call.py` (0%, already tracked in roadmap Deferred / Monitor). Roadmap updated.
  - 2026-06-24 — **CR-T13-003: Billing flat-route caller parity (T1.3 follow-up).** Added `/billing/` and `/api/billing/` to `_SOLO_ROUTE_PREFIXES` in `TenantMiddleware` so authenticated billing flat-route requests in SaaS mode resolve the personal org instead of redirecting to `/orgs/` when no session org is active. This preserves parity with existing CRM/listings/forms/blog fallback behavior. Added middleware tests for `/billing/pricing/` and `/api/billing/config/`. Roadmap updated.
  - 2026-06-24 — **T1.8: Listings adopt contract (Track 1).** Adopted the shared NOT NULL/PROTECT ownership contract (D3/D5) for the listings module. Replaced module-local `TenantScopedManager`/`OperatorManager` with `quickscale_modules_orgs.managers.TenantManager` (`super_scope=True` for operator bypass). Used `quickscale_modules_orgs.tenancy.tenant_org_fk()` for `AbstractListing.organization` (NOT NULL, PROTECT). Removed `OrgScopedViewMixin`, `_is_org_scoped_route`, `_resolve_active_org`, and `_resolve_active_org_optional` from views. Public listing pages now scope anonymous reads to the System org (D2) via `_scope_queryset()` helper. `get_absolute_url()` returns flat `/listings/<slug>/` unconditionally (D1). Deleted the partial `UniqueConstraint` for `(slug) WHERE organization IS NULL` (unreachable with NOT NULL). Removed all `/orgs/<slug:org_slug>/listings/...` URL patterns (D1/D5). Squashed migrations to clean NOT NULL/PROTECT contract (single `0001_initial.py`, no backfill per D5). Updated publish API to use `request.org` from middleware instead of route-based org detection. Updated test fixtures to NOT NULL contract (default org via `get_system_org()`). 110 listings module tests passing. Roadmap updated: T1.8 closed.
  - 2026-06-24 — **T1.8 follow-up (CR-T18-001/002/003):** Fixed `OperatorManager` alias in `managers.py` — replaced the simple `OperatorManager = TenantManager` with a proper subclass that passes `super_scope=True`, preserving operator-bypass semantics for callers who import the class directly. Added backward-compatibility alias regression tests (TenantScopedManager default-scope, OperatorManager super-scope). Added anonymous list/detail regression tests proving non-System-org published listings stay hidden under the D2 contract while System-org listings remain visible. Updated roadmap: T1.8 checkbox set to complete, track2 next-task pointer advanced from T1.7 to T1.13. All 110 listings module tests passing.
  - 2026-06-24 — **T1.7: Forms adopt contract (Track 1).** Adopted the shared NOT NULL/PROTECT ownership contract (D3/D5) for the forms module. Replaced module-local `TenantScopedManager`/`OperatorManager` with `quickscale_modules_orgs.managers.TenantManager` (`super_scope=True` for operator bypass). Used `quickscale_modules_orgs.tenancy.tenant_org_fk()` for `Form.organization` (NOT NULL, PROTECT). Deleted route-sniffing (`_is_org_scoped_route`, `_resolve_active_org_optional`, `_resolve_org_slug`) and all `/orgs/<slug:org_slug>/forms/...` URL patterns (D1/D5). Public schema/submit endpoints now resolve `Organization.objects.get_system_org()` for anonymous submissions (D2). Admin views use the operator path (`all_objects`). Added migration 0005 to convert the nullable FK to NOT NULL/PROTECT, assigning existing seed rows to the System org. Updated seed commands and all test fixtures to the NOT NULL contract. Updated isolation test for the single-URL contract. Roadmap updated: T1.7 closed.
  - 2026-06-24 — **T1.4: RLS DB role + generated-project settings (Track 1).** Added `db/init.sql.j2` (PostgreSQL init SQL that creates a restricted runtime role with NOSUPERUSER/NOBYPASSRLS), `OPERATIONS.md.j2` (generated operations guide documenting the operator role path), and wired both into `generator.py` via file mappings and template context variables (`runtime_db_role`/`runtime_db_password`). Role naming convention: `{package_name}_app`. Updated `docker-compose.yml.j2` to mount the init SQL as a `docker-entrypoint-initdb.d` volume and set `RUNTIME_DATABASE_URL` for the backend service. Updated `production.py.j2` to prefer `RUNTIME_DATABASE_URL` over `DATABASE_URL` when set (backward compatible — falls back when unset). Updated `start.sh.j2` to unset `RUNTIME_DATABASE_URL` during `migrate` so schema DDL uses the superuser connection. Updated `base.py.j2` with runtime role configuration comments. Updated `.env.example.j2` with documented runtime role env vars. Added template tests for `init.sql.j2` loading/rendering, `OPERATIONS.md.j2` loading/rendering, and production settings `RUNTIME_DATABASE_URL` override. Roadmap updated: T1.4 closed.
  - 2026-06-23 — **T3.2 (Track 3): Shrink services.py — move DR orchestration to dr_engine/orchestration.py.** Created `quickscale_core.dr_engine.orchestration` (~3,682 LOC) containing all remaining DR orchestration: backup capture/resume (consolidated via `_complete_capture_after_dump`), sidecar building/capture lifecycle, snapshot report building, prune lifecycle, media sync, private remote S3 operations, admin restore pipeline, verification/rollback-pin wrappers, and Django-backed restore wrappers. Moved `BackupPolicySnapshot` frozen dataclass to `quickscale_core.dr_engine.primitives` (Django-free; factory methods stay in services). Reduced `services.py` from 3,654 LOC to 205 LOC — thin re-exports, 4 protocols (referencing `BackupPolicySnapshot`), and 3 Django-facing policy wrappers. Updated test monkey-patches to target the orchestration module. Updated `dr_engine/__init__.py` docstring with orchestration layer description. Roadmap updated (T3.2 marked done). Remaining: T3.3 cleanup.
  - 2026-06-23 — **T3.1 (Track 3): Single DR adapter path — delete legacy env-var/stdout-JSON protocol.** Removed `_DR_TARGET_ENV_PREFIX` constant, `_load_target_runtime_settings()` function, and backward-compat env-var fallback from `sync_backup_snapshot_media()` in `services.py`. Made `target_runtime_settings` required. Added adapter functions `prune_backups`, `validate_artifact`, `clear_rollback_pin`, and `restore_backup` to `quickscale_core.dr_engine.adapter`. All 8 admin management commands (`backups_create`, `backups_restore`, `backups_prune`, `backups_report`, `backups_pin`, `backups_validate`, `backups_record_verification`, `backups_sync_media`) now dispatch through `ADAPTER_FUNCTIONS[...]` instead of importing services directly. `backups_sync_media` gained `--target-runtime-json` for explicit target settings. Updated tests for new dispatch pattern. Roadmap updated (T3.1 marked done).
  - 2026-06-22 — **F11.13b: Rollout closeout (M7, Track 1).** Completed all remaining closeout checklist items: non-view paths verified — admin `get_queryset` uses explicit operator managers; `forms_anonymize_submissions` uses `Form.all_objects.all()`. Structural isolation adoption path for existing projects documented in `organizations.md`. Module isolation tests: blog 219/219, forms 138/138, listings 111/111, social 52/52 — all passing. Pre-existing forms migration-ordering blocker (`0002_seed_forms.py` referencing `organization_id` before migration `0004`) fixed by rewriting `0002_seed_forms.py` to use historical models. Blog admin org-scoping/safeguards implemented via blog-local org-aware mixin plus same-org validation. Repo-root `make test` clean across all modules. F11 complete — all phases F11.2–F11.13b implemented, validated, and merged to `v87`. M7 closeout.
  - 2026-06-22 — **F5.2b (M10, Track 2): Restore and orchestration extraction.** Extracted restore/orchestration flow into `quickscale_core.dr_engine.recovery`; extracted verification recording and rollback-pin handling into `quickscale_core.dr_engine.verification`; preserved thin Django-facing wrappers in `quickscale_modules_backups.services`. Added core recovery/verification tests and targeted backups service tests. Final repo-root `make test` passed — quickscale_core 1392 passed / 28 deselected / 93.03% coverage; quickscale_cli 1778 passed / 28 deselected / 91.58% coverage; backups module 227 passed / 84.41% coverage; overall mean 93.33%; all modules green with only expected existing skips. Roadmap updated. Remaining: F5.3 (protocol replacement + module slimming) and F5.4 (migration docs).
  - 2026-06-22 — **F5.3 (M10, Track 2): DR adapter boundary + protocol slimming.** Replaced the hidden docker-exec/management-command/env-var/stdout-JSON protocol with an explicit typed adapter (`quickscale_core.dr_engine.adapter`) and a single `dr_adapter_call` management command bridge. Rewrote `dr_commands.py` to call the adapter — removed `_run_backend_container_command`, `_run_manage_json`, `_build_manage_overrides`/`_source_manage_overrides`/`_target_manage_overrides`, `_prefix_target_runtime_variables`, `_target_media_sync_variables`, `_is_manual_only_restore_gate`, and the `_TARGET_ENV_PREFIX` env-var protocol. Added explicit `target_runtime_settings` parameter to `sync_backup_snapshot_media()` in services.py (adapter path carries explicit parameters with a `ROUTE_KIND` marker; legacy env-var fallback preserved for admin/manual use through retained management commands). Remaining management commands preserved as thin Django/admin-facing surfaces with backward-compatible env-var fallback. CLI `quickscale dr` surface unchanged — capture/plan/execute/report workflow preserved. Roadmap updated. Remaining: F5.4 (migration docs).
  - 2026-06-22 — **F5.4 (M10 closeout, Track 2): Migration docs.** Authored `docs/technical/dr_engine_migration.md` — standalone migration guide covering the post-split DR architecture, migration path for existing generated projects (`quickscale apply` syncs the `quickscale-core` dependency entry automatically, but standalone projects may need to adjust the synced source for their layout), backward compatibility table, dependency changes, and verification checklist. Updated roadmap (F5 phases marked complete) and CHANGELOG. **M10 closeout — all Track 2 phases complete.**
  - 2026-06-20 — F12.1d: consumer-parity wiring for the single `apply-recovery.yml` channel across apply/update/remove, fail-hard top-level `managed_files` guard in `ledger.py`, and post-commit non-e2e `quickscale_core` / `quickscale_cli` closeout suites green before merge to `v87`.
  - 2026-06-20 — F12.1e: git-index checkpoint folded into the single `apply-recovery.yml` ledger; present ledgers fail hard when `git_index_checkpoint` is missing; remove-path rewrites preserve the checkpoint. Focused ledger suite (61 passed), CLI seam suites (318 passed), broader core suite (1223 passed + 28 deselected), broader CLI suite (1746 passed + 28 deselected), and all `quickscale_modules` suites passed — zero test-case failures repo-wide; `make test` exit nonzero solely from pre-existing per-file coverage misses in unrelated files. Change review returned STATUS ok.
  - 2026-06-20 — F12.2: adopt consistent fail-closed policy across the config/state mirroring path. `_populate_consolidated_tracking_from_legacy` no longer silently swallows all exceptions — `ConfigError`/`OSError` propagate (fail-closed). Helper-level fail-open in `_save_project_state` documented as deliberate (callers already handle `False` by persisting recovery state and aborting). Legacy read-through imports in `ProjectStateManager` documented as justified fail-open (compatibility path for pre-M2 migration). Targeted test coverage added for malformed/empty/missing config.yml behavior.
  - 2026-06-20 — F12.3a: pre-embed recovery coverage for generation failure and git-init failure. Added `_execute_apply_steps`-level regression tests verifying that generation failure (click.Abort) aborts apply before git init / lock, and git-init failure (non-fatal warning) does not block apply from continuing into the locked section. No production code change was needed. Roadmap and changelog updated.
  - 2026-06-20 — F11.10c: solo/personal-org stage bootstrap closeout. Live `/crm/` and `/crm/api/stages/` seeding replaced legacy NULL-owned stage rows with personal-org-backed `ensure_org_default_stages()`. `0001` no longer seeds default stages (resolved at migration level as prerequisite for `0006` clean installs).
  - 2026-06-20 — F11.10d: backfill + current-state regression split. Historical NULL/backfill proofs moved into `test_migrations.py` per Option A. Current-state `test_models.py`, `test_services.py`, `test_serializers.py`, and `test_views.py` assertions rewritten to post-`0006` contract. Root `make test` completed fully — CRM 320 passed; exit code 1 solely from pre-existing per-file coverage thresholds in 5 unrelated files outside CRM slice.
  - 2026-06-21 — **F11.10e: isolation + M3 merge closeout.** Same-org FK audit/fix across `serializers.py` — closed ContactDetailSerializer/DealDetailSerializer solo-route early-return gaps; repo-root CRM serializer/view/isolation slice passed **225/225**. Follow-up re-review found no remaining same-type security-boundary gaps. Pre-sync closeout slice (migration/history proofs + stage/bootstrap/runtime slices + `test_isolation.py`) passed **254/254**; post-sync closeout slice also passed **254/254**. Root `make test` all runtime tests passing; exit code 1 solely from pre-existing per-file coverage in 5 unrelated files outside CRM slice (`module_catalog.py`, `delta.py`, `backups_manifest.py`, `crm_manifest.py`, `crm_bootstrap.py`). M3 merged/closed. Roadmap updated: next open Track 1 work is M7 / F11.11.
  - 2026-06-21 — **F13/M9: Single billing customer SSOT (Track 1).** Declared organization as the authoritative billing subject; `_sync_subscription_authority()` enforces org-primary/user-provenance semantics. Enforced one active subscription per organization via `quickscale_billing_unique_current_subscription_per_organization` constraint (migration 0003). Backfilled dual-FK rows via migration `0003_org_authoritative_billing_contract.py` with mgmt command. Merged to `v87` as M9.
  - 2026-06-21 — **F11.11: blog isolation (M7, Track 1).** Org ownership for Category/Tag/Post/BlogMediaAsset with per-org uniqueness, migration 0003, orgs dependency wiring, and blog managers (Phase 1). Additive `/orgs/<slug>/blog/...` routes, org-scoped read filtering, org stamping on publish/upload APIs, same-org validation, and org-scoped feed behavior while preserving flat `/blog/...` compatibility (Phase 2). Org-aware blog test harness coverage, additional HTML view tests, and unskipped isolation test (Phase 3). **Review-driven fixes (Phase 4):** added token-auth org resolution/membership enforcement on org-scoped blog APIs; scoped flat routes/feed to tenant-agnostic rows while org-owned entities emit org-scoped URLs; corrected generated-project blog wiring to root `include()` to prevent route double-prefix. Final validation: `make test` passed across full suite — 4,481 passed / 0 failed / 4 skipped (+ deselected as reported); in-scope managers coverage fixed to 100%; no regression from review fixes. Final targeted re-review resolved CR-001 and CR-002. Root `make test` exit non-zero solely from pre-existing per-file coverage gaps in 4 unrelated files outside blog slice (`module_catalog.py`, `delta.py`, `backups_manifest.py`, `crm_manifest.py`). Merged to `v87`. Next: F11.12a (forms isolation).
  - 2026-06-22 — **F11.12a: forms isolation (M7, Track 1).** Added `organization` FK (nullable) to `Form` model with per-org slug uniqueness constraint (migration 0004). Dual-manager contract (`TenantScopedManager` + `OperatorManager`) with `Form.for_org()` queryset scoping. Additive `/orgs/<slug>/forms/...` org-scoped routes alongside flat `/forms/...` paths for public schema/submit APIs and admin submission management. Views route-aware via `_is_org_scoped_route()`/`_resolve_active_org_optional()`. Test settings updated with `quickscale_modules_orgs`, `TenantMiddleware`, and `QUICKSCALE_MODE=saas`. Org fixtures and org-form fixtures added to conftest. Isolation test unskipped and passing — Org A cannot read Org B submissions via org-scoped admin path. Pre-existing ordering-bug fix in `test_seed_presets_preserve_existing_form_data_retention_days`. Notifications test settings updated to include orgs dependency. All 123 forms module tests pass at 92.64% coverage. Next: F11.12b (listings isolation).
  - 2026-06-22 — **F11.12b: listings isolation (M7, Track 1).** Added `organization` FK (nullable) to `AbstractListing` with per-org `(slug, organization)` `UniqueConstraint` on `Listing` (migration `0002_org_ownership`). Dual-manager contract (`TenantScopedManager` + `OperatorManager`) with `Listing.for_org()` queryset scoping. Additive `orgs/<slug>/` org-scoped routes under the `listings/` URL prefix alongside flat paths. Route-aware views via `OrgScopedViewMixin._scope_by_org()` — flat routes scope to `organization__isnull=True`, org routes scope to active org. `create_published_listing_from_payload` accepts org context for stamping and per-org slug checks. `get_absolute_url` returns org-scoped `/listings/orgs/<slug>/<slug>/` for org-stamped listings. Test settings updated with `quickscale_modules_orgs`, `TenantMiddleware`, and `QUICKSCALE_MODE=saas`. Org fixtures added to conftest. Isolation test unskipped and passing — Org A cannot see Org B listings via org-scoped path. All 96 listings module tests pass at 91.70% coverage. Next: F11.13a (social isolation).
  - 2026-06-22 — **F11.13a: social isolation (M7, Track 1).** Added `organization` FK (nullable) to `BaseSocialItem` with dual-manager contract (`TenantScopedManager` + `OperatorManager`) on both `SocialLink` and `SocialEmbed` models (migration `0003_org_ownership`). Removed global `unique=True` constraint on `normalized_url` so multiple organizations may link to the same social URL. `list_published_social_links()` and `list_published_social_embeds()` accept an optional `organization_id` keyword parameter for tenant-scoped queries while preserving backward compatibility (no org_id returns all published items). Added `quickscale_modules_orgs` app, `TenantMiddleware`, and `QUICKSCALE_MODE=saas` to test settings. Added org fixtures (`org`, `org_a`, `org_b`, `org_a_admin`, `org_b_admin`) to conftest. Isolation tests unskipped and passing — Org A cannot see Org B's social links/embeds via org-scoped service queries, `for_org(None)` fails closed, and `all_objects` escape hatch returns all rows. Added model-level tests for org creation, `for_org()` manager scoping, operator escape hatch, and relaxed `normalized_url` uniqueness. All existing social module tests remain green. Next: F11.13b (rollout closeout). **Review-driven follow-up (CR-001/CR-002):** `build_social_link_tree_payload()` and `build_social_embeds_payload()` now accept an optional `organization_id` parameter and thread it through to the underlying queries; the managed views renderer (`social_manifest.py`) extracts `request.org` via `get_current_org()` and passes it to the build functions; `module.yml` declares `required_modules: [orgs]` and `pyproject.toml` adds `quickscale-module-orgs` as a Poetry dependency. Regression tests added for org-scoped payload isolation. All 47 social module tests pass. **F7.3 (M11 closeout, Track 3)**: runtime-pin drift-detection validation, `constraint_validation.py` with generator/module Python+Django parity checks, `ruff target-version` variableized in `pyproject.toml.j2`; M11 merged/closed. **F11.13b (M7 closeout, Track 1)**: non-view path audit (admin `get_queryset` uses operator managers; `forms_anonymize_submissions` uses `Form.all_objects.all()`), blog admin org safeguards, historical forms seed fix, adoption docs for existing projects (`organizations.md`), isolation test sweep — blog 219/219, forms 138/138, listings 111/111, social 52/52 all green; **M7 merged/closed — F11 structural multi-tenant isolation complete across all modules (CRM, blog, forms, listings, social)**.
  - 2026-06-22 — **SOCIAL-CR-002 (closeout):** Added `social → orgs` implication in `get_implied_module_default_configs()` so the planner/apply flow materializes the orgs module (and its `orgs → notifications` transitive chain) when social is selected. Existing billing/crm/orgs implication behavior preserved. Added 20 regression tests covering the implication seam including social→orgs, multi-implicator deduplication, and transitive chain behavior.
  - 2026-06-21 — F12.3b: Railway rollback/resume semantics for recoverable apply. Inserts Railway deploy step after DB migrations and before authoritative state persistence; triggers only for Railway-linked checkouts (`.railway/` present); immediate trigger failures save recovery state and abort for rerun; async downstream Railway failures remain manual/operator-owned. All assertions/tests passed (exit code impact only from 5 unrelated pre-existing per-file coverage gaps). Merged to `v87` in Track 3 closeout. Roadmap and changelog updated.
  - 2026-06-21 — F5.1 (M10, Track 2): DR engine boundary contract defined. Documents-only phase — authored the **Disaster Recovery Engine Boundary Contract (F5 / M10)** in `decisions.md` as the authoritative target that F5.2a–F5.4 implement against. Specifies the target ownership split (centrally owned CLI/core DR engine for snapshot/archive/restore/verification/rollback-pin/sidecar/remote-storage orchestration vs the embeddable `backups` module reduced to thin Django-facing surfaces: `BackupArtifact`/`BackupPolicy` models, admin UI, and a documented project-bound adapter), the typed-request/result internal boundary that replaces the hidden management-command + env-var + stdout-JSON protocol, and invariants preserved across the split (PG18 backups contract, unchanged `quickscale dr` CLI surface, guarded destructive restore). Discovery confirmed all orchestration lives in one file (`backups/.../services.py`, ~4200+ LOC) wrapped by 8 management commands with no cross-module deps — boundary is well-bounded for extraction. No code changed; `make test` runtime suites green (exit code impact only from pre-existing per-file coverage gaps in unrelated files).
  - 2026-06-21 — **F5.2a (M10, Track 2): Snapshot and archive primitives extracted to `quickscale_core.dr_engine`.** Created `quickscale_core/src/quickscale_core/dr_engine/__init__.py` and `primitives.py` containing all Django-free DR engine primitives: error classes (`BackupError`, `BackupConfigurationError`), `ShellCommandRunner` protocol, archive constants (`_REQUIRED_POSTGRESQL_MAJOR`, `_POSTGRESQL_CUSTOM_ARCHIVE_MAGIC`, `_SNAPSHOTS_DIRECTORY_NAME`, `_REQUIRED_SNAPSHOT_SIDECAR_FILENAMES`, and sidecar filename constants), version extraction helpers (`_extract_leading_major_version`, `_extract_any_major_version`, `_get_postgresql_tool_version`), database engine helpers (`_database_engine_family`, `_expected_backup_format_for_engine`), pg_dump/restore command builders and execution (`_build_pg_dump_command`, `_build_pg_restore_command`, `_dump_postgresql_database`, `_run_shell_command`, `_missing_executable_backup_error`), snapshot structure helpers (`_mint_snapshot_id`, `_relative_snapshot_child_path`, `_build_snapshot_child_descriptor`, `_write_json_file`), and archive integrity (`_compute_sha256`). `services.py` imports all extracted items from the new module (backward-compatible re-exports); added `quickscale-core` dependency to `quickscale_modules/backups/pyproject.toml`. Added 48 unit tests in `quickscale_core/tests/test_dr_engine_primitives.py` (100% primitives coverage, 96% module total). All 178 previously-passing backups tests remain green; 9 pre-existing failures in the backups test suite unchanged.
  - 2026-06-21 — **F7.1: Generator vs generated-project runtime-pin inventory** (Track 3, docs-only). Documented which Python, Django, PostgreSQL, Node.js, and frontend constraints belong to the generator runtime (repo `pyproject.toml` files), embedded modules (`quickscale_modules/*/pyproject.toml`), and generated-project templates (`.j2` files under `quickscale_core`). Inventory covers all three layers: generator (4 packages, Python only), embedded modules (12 modules, Python + Django), and generated-project templates (Python, Django, PostgreSQL, Node.js, pnpm). Added missing Node pin sites (`docker-compose.yml.j2` node:24-slim, `themes/showcase_react/package.json.j2` engines.node >=24). Confirmed generator has zero Django or PostgreSQL runtime dependency. Identified 7 pending items for F7.2/F7.3 (duplicated Python constraint, Docker base image hardcode, CI matrix duplication, PostgreSQL constraint duplication, embedded-module pin drift, frontend already decoupled, generator purity confirmed). Inventory recorded in `implementation_contract.md`. Roadmap and changelog updated.
  - 2026-06-21 — **F7.2: Generator vs generated-project runtime-pin ownership split** (Track 3, implementation phase). Added `quickscale_core/src/quickscale_core/generator/runtime_pins.py` as the generated-project-owned runtime-pin SSOT; `generator.py` now injects runtime-pin variables into template context; `pyproject.toml.j2`, `Dockerfile.j2`, `docker-compose.yml.j2`, and `github/workflows/ci.yml.j2` now read those values from template context rather than hardcoded literals. One unused-variable lint issue fixed in `test_templates.py`. `make test` passed repo-wide (quickscale_core 1234 passed, quickscale_cli 1766 passed, modules 498 passed/4 skipped). Pre-existing below-80% per-file coverage notes remain outside this phase. Remaining F7.3 items: validation for diverged pin sets, operator-messaging alignment, variableization of `ruff target-version` in `pyproject.toml.j2`. Roadmap and changelog updated.
  - 2026-06-22 — **F7.3: Generator vs generated-project runtime-pin validation and docs** (M11 closeout, Track 3). Added `quickscale_core/src/quickscale_core/generator/constraint_validation.py` with drift-detection functions for generator Python, module Python, and module Django constraint parity against `runtime_pins.py`. Twelve drift-detection tests in `TestRuntimePinDriftDetection` in `test_templates.py` (covering both `project.requires-python` and Poetry `tool.poetry.dependencies.python` parity surfaces). Variableized `ruff target-version` in `pyproject.toml.j2` to derive from `{{ python_version }}` template variable instead of hardcoded `py313`. Updated `implementation_contract.md` to document post-F7.3 state. Post-review CR-002 follow-up added Poetry `tool.poetry.dependencies.python` parity checks for generator/module pyprojects; final validation: `make lint -- --core` passed, `make typecheck -- --core` passed, targeted `test_templates.py` 156/156 passed, canonical `make test` passed: quickscale_core 1321/1321, quickscale_cli 1778/1778; module suites passed except `quickscale_modules/orgs` timing out at 600s (pre-existing unrelated behavior). Merged to `v87` as M11. Roadmap and changelog updated.
- v0.86.0 — 2026-05-22 — Organizations Module (multi-tenancy with Solo/SaaS runtime modes, org-scoped RBAC and invitation flow, billing bridge with authoritative org ownership fields and `Plan.features` feature gates, `migrate_billing_to_orgs` and `promote_to_saas` management commands, React org management pages and org switcher, and billing wiring regression fix with planner/apply guard test; archived in release note and changelog)
- v0.85.0 — 2026-05-18 — Billing Module (Stripe-backed one-time credit purchases and recurring subscriptions, credits-first Django-owned ledger, planner/apply readiness with auth-module dependency enforcement, module-owned `/billing/pricing/` and `/billing/dashboard/` pages, and starter-theme billing links; archived in release note and changelog)
- v0.84.0 — 2026-05-14 — Backups Hardening Release (admin backup artifact download/create/integrity actions, upload-driven restore safety and compatibility checks, full-snapshot backup-contract hardening, and repo-owned runtime/tooling support alignment for the refreshed Python/frontend baseline; this recorded docs closeout documents `make test` in-session and does not restate unverified Docker-backed gates)
- v0.83.0 — 2026-05-03 — Hardening Release (repo-wide hardening across strict desired-config validation, notifications live-config placeholder rejection, generated production `SECRET_KEY` fail-hard behavior, CRM staff-only parity, forms CSV and backup artifact safety, blog upload/thumbnail/rate-limit hardening, and release validation through `make version-check`, `make lint`, `make typecheck`, `make test`, `make test-e2e`, and `make ci-e2e`)
- v0.82.0 — 2026-04-06 — Disaster Recovery & Environment Promotion Workflows (public `quickscale dr capture/plan/execute/report` workflows for local and Railway routes with `snapshot_id`-based stored snapshots, resumable capture and execute, rollback pins for production routes, conservative env-var sync, and source-side media sync as a separate surface from database restore; Railway-target media sync requires storage-backed object storage rather than container disk)
- v0.81.0 — 2026-04-05 — Beta-Site Migration Maintainer Tooling (maintainer-only Make/Python workflows for beta-site catch-up: fresh-first executes deterministic recipient mutation plus local verification, while in-place stays checkpoint-first by default and continues through deterministic copy/apply/verification only with explicit opt-in; recipient-owned routing files, smoke checks, env vars, PR merge, deploy, and rollback remain manual)
- v0.80.0 — 2026-04-04 — Analytics Module (PostHog-only website analytics with flat `QUICKSCALE_ANALYTICS_*` settings, service-style capture helpers, apply-time env-example sync and operator guidance, guarded forms integration, QuickScale-owned social click tracking, dormant `showcase_react` starter support for fresh generations, and manual server-rendered template-tag support; existing projects adopt frontend snippets manually)
- v0.79.0 — 2026-04-03 — Social & Link Tree Module (curated social links and embeds, backend-owned YouTube/TikTok preview metadata, managed `/_quickscale/social/` integration endpoints, and Django-owned public React pages for fresh `showcase_react` generations; older projects adopt those pages manually)
- v0.78.0 — 2026-03-30 — Notifications Module (transactional email foundation, Anymail-backed Resend delivery, recipient-granular tracking, and signed delivery webhooks)
- v0.77.0 — internal main-branch baseline — Backups Module (private database backups, optional private remote offload, guarded CLI restore, and scheduler-ready command hooks; no tagged public release note was published for this internal baseline)
- v0.76.0 — 2026-03-21 — Storage Module (cloud file hosting, media storage adapters, CDN-ready media infrastructure)
- v0.75.0 — 2026-02-23 — Forms Module (generic customizable form builder with admin, DRF API, spam protection, GDPR anonymization, and React mount point)
- v0.74.0 — 2026-02-05 — React Default Theme (showcase_react theme with Vite, TypeScript, TanStack Query, and Zustand)
- v0.73.0 — 2026-02-04 — CRM Module (API-first Django CRM with 7 core models and CLI integration)
- v0.72.0 — 2025-12-07 — Plan/Apply Cleanup (removed legacy init/embed commands, full transition to plan/apply)
- v0.71.0 — 2025-06-25 — Module manifests & config mutability (Plan/Apply system complete)
- v0.70.0 — 2025-12-19 — Existing project support (status, plan --add, plan --reconfigure)
- v0.69.0 — 2025-12-03 — State management and incremental applies
- v0.68.0 — 2025-12-01 — Plan/Apply System core commands (Terraform-style declarative workflow)
- v0.67.0 — 2025-11-29 — Listings module with AbstractListing base model for verticals
- v0.66.0 — 2025-11-24 — Blog module with Markdown, featured images, and RSS feeds
- v0.65.0 — 2025-11-03 — Enhanced auth module and development tooling
- v0.64.0 — 2025-11-01 — Theme rename to showcase_* (breaking change)
- v0.63.0 — 2025-10-29 — Authentication Module with django-allauth and interactive embed
- v0.62.0 — 2025-10-25 — Split Branch Infrastructure (module management CLI commands, GitHub Actions automation)
- v0.61.0 — 2025-10-24 — Theme System Foundation (--theme CLI flag, theme abstraction layer, HTML theme)
- v0.60.0 — 2025-10-19 — Railway Deployment Support (automated deployment via quickscale deploy railway)
- v0.59.0 — 2025-10-18 — CLI Development Commands (Docker/Django operation wrappers)
- v0.58.0 — 2025-10-18 — Comprehensive E2E testing infrastructure with Playwright and PostgreSQL
- v0.57.0 — 2025-10-15 — Production-ready generator baseline
- v0.56.0 — 2025-10-13 — Quality, Testing & CI/CD
- v0.55.0 — 2025-10-13 — CLI implementation
- v0.54.0 — 2025-10-13 — Project Generator
- v0.53.3 — 2025-10-12 — Project Metadata & DevOps Templates
- v0.53.2 — 2025-01-11 — Templates and Static Files
- v0.53.1 — 2025-10-11 — Core Django Project Templates
- v0.52.0 — 2025-10-08 — Project Foundation
- v0.51.0 — 2025-10-08 — documentation foundation
- v0.41.0 and earlier — legacy codebase (see Github repository for history)
