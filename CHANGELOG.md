# Changelog

`CHANGELOG.md` is the canonical QuickScale release history index. Published releases pair each version entry with a single official release note in `docs/releases/` linked from the GitHub tag and release PR. When a release note is prepared before the maintainer completes the manual tag/publish step, the changelog entry and note must say so explicitly and must not imply publication. Use `docs/technical/roadmap.md` for active or unpublished release status. Entries are version-ordered.

- v0.87.0 — unreleased / not yet tagged — Hardening release (in progress). Original T-series scope complete: Track 1 (tenant isolation, single-URL contract), Track 2 (manifest wiring), Track 3 (DR hard cutover). Structural autopsy (2026-06-26) added D1 and AF1–AF7 as additional open scope. D1 completed and merged; AF1 plus AF1-CR follow-ups completed and merged; AF2+AF4 completed and merged; AF5 completed and merged; AF6 completed and merged; AF7 completed with fail-hard cleanup and merged; AF8 completed and merged; AF10 completed and merged; AF11 completed and merged; AF12 completed and merged (all blocking findings resolved — VALIDATE CONSTRAINT in Forms 0007, parent-org mutation rejection proofs enabled, docs updated); AF13 completed and merged; SA1.1, SA1.2, SA1.3, SA2.1, SA2.2, SA3.1, SA4.1, SA4.2 completed and merged (Finding 2 fully closed — both SA2.1 and SA2.2 shipped); VIEW-AS implemented and change-review findings resolved (admin caller path fixed end-to-end, docs corrected).
  - **SA1.1 — CRM tenant-model migration to TenantModel (wt-track2, 2026-06-30).** All 7 CRM models (Tag, Company, Contact, Stage, Deal, ContactNote, DealNote) now inherit `TenantModel` from `quickscale_modules_orgs` instead of hand-rolling `organization` FK, `TenantManager`, and `base_manager_name`. The inherited `organization` field uses `related_name="%(app_label)s_%(class)s_set"` — a Django-ORM-only change with no schema migration impact. No callers of the old `organization.crm_*` reverse accessors were found outside model definitions and historical migrations. Child→parent FKs (contact→company, deal→contact/stage, contactnote→contact, dealnote→deal) and `created_by` FKs preserved as-is. All model-specific constraints, save() overrides, properties, and Meta options retained. Migration `0011` records the state change with `SeparateDatabaseAndState` for the pre-existing unique constraints. Conformance gate and CRM isolation tests green on PostgreSQL. Closes SA1.1.
  - **SA5.1 — Analytics derivation pilot end-to-end: manifest-driven bridge replaces imperative builder (Track 3, wt-track3, 2026-07-01).** Replaced the analytics imperative options-resolution/builder path in `resolvers.py` (removed helpers `_load_analytics_manifest`, `_build_analytics_derivation_schema`, `_is_valid_posthog_host`; retained the backward-compatible `_normalize_posthog_host` helper) with a manifest-driven bridge that uses `build_schema_from_manifest` + `resolve_module_config` from the existing manifest runtime, applying the same post-resolution coercions and fallback defaults that the manifest adapter's `_analytics_post_hook` uses. All public API functions (`default_analytics_module_options`, `normalize_analytics_module_options`, `resolve_analytics_module_options`, `validate_analytics_module_options`, `validate_analytics_env_var_reference`, `analytics_production_targeted`) keep their signatures and flat-dict contract. Cleared the `ANALYTICS_MANIFEST` entries in `imperative_inventory.py` — all 7 formerly tracked symbols are now served by the manifest bridge. Removed unused `from urllib.parse import urlsplit` top-level import (moved to local import in `validate_analytics_module_options`). Added `build_schema_from_manifest` to the derivation import. CLI callers (`apply_command.py`, `module_config.py`) require no changes. Closes SA5.1.
  - **SA3.1 — Re-sync authoritative tenancy docs with shipped reality (Track 3, wt-track3, 2026-06-30).** Updated `docs/technical/decisions.md` and `docs/technical/organizations.md` so the authoritative SaaS contract now matches the live tenant surface: 21 ENROLLED FORCE-RLS models across CRM, Forms, Billing, Blog, Listings, and Social; ambient `TenantManager()` / `TenantManager(super_scope=True)` scoping via the current-org ContextVar + AF9 execute-wrapper; the fail-closed runtime-role posture after SA2.2; and the shipped VIEW-AS/admin surface. Removed stale social-only RLS wording and the old `TenantScopedManager` / `OperatorManager` / `.for_org()` framing. Closes SA3.1.
  - **SA2.2 — Invert runtime DB-role default to fail-closed (Track 1, wt-track1, 2026-06-30).** Restructured the generated `production.py.j2` settings template so runtime serving requires `RUNTIME_DATABASE_URL` (restricted NOSUPERUSER/NOBYPASSRLS role) and raises a clear `ValueError` when unset, instead of silently falling back to the privileged superuser `DATABASE_URL`. The migration path is preserved as the named exception: when `sys.argv` contains `"migrate"`, the template uses `DATABASE_URL` directly. `collectstatic`/`compilemessages` remain exempt with the dummy-URL fallback. Updated `base.py.j2` comments to document the fail-closed contract. Added `TestProductionSettingsRuntimeDatabaseUrlFailClosed` test class with execution-level proof: serving without `RUNTIME_DATABASE_URL` raises `ValueError`, and the migrate path correctly selects `DATABASE_URL`. All existing tests continue to pass. Completes SA2.2 — the last Track 1 Structural-Autopsy task before SA1.3.
  - **SA2.1 — Always-on BYPASSRLS boot guard (wt-track2, 2026-06-30).** Removed the `QUICKSCALE_MODE == "saas"` and `not DEBUG` bypass gates from `_check_rls_role()` — the guard now fires on any non-`migrate` boot under a BYPASSRLS role regardless of mode or debug setting. Added `QUICKSCALE_ALLOW_BYPASSRLS=1` env-var escape hatch for intentional single-tenant/development use. Guard order: migrate exemption → escape-hatch check → vendor check → pg_roles query. Track 2, Finding 2. Added 5 new escape-hatch tests (saas prod, solo, debug, exact-value strictness, empty-value rejection) and updated 3 earlier no-op tests (solo, debug, mode-unset) to expect `ImproperlyConfigured`. All 616 orgs tests pass. Closes SA2.1.
  - **SA4.1 — Instrument per-request statement/transaction counts (wt-track2, 2026-06-30, corrected 2026-07-01, final explicit-txn baseline 2026-07-01).** Added `test_sa41_statement_amplification.py` — a reproducible benchmark harness under the orgs test suite that captures and categorises SQL statements via `connection.queries`. Three scenarios measure the `OrgDashboardView` data-access pattern: no-org baseline (3 statements, 0 amplification), with-org autocommit (8 statements, 4× amplification — 2 data queries each wrapped in `BEGIN+SET LOCAL+query+COMMIT`; the middleware pre-sets `request.org` so the view skips the slug lookup), and with-org explicit transaction (4 statements, 2.0× amplification — `CaptureQueriesContext` is nested inside the outer `transaction.atomic()`, so the outer atomic's `BEGIN`/`COMMIT` are not captured; only 2 `SET LOCAL` + 2 data SELECTs appear in the measured total). **Correction (2026-07-01, CR-SA41-001):** the benchmark now routes through real `TenantMiddleware` session-org resolution (test-only URL `/sa41-bench/<slug>/`) instead of manual `set_current_org_id()`. The middleware pre-sets `request.org`, reducing data SELECTs from 3→2. Baselines updated: autocommit 12→8, explicit txn from initial 8→4 (final validated value — `CaptureQueriesContext` nesting excludes outer atomic's `BEGIN`/`COMMIT`). SA4.2 projection recalculated: 4→3 statements with per-transaction memo (eliminate 1 redundant `SET LOCAL`). Track 2, Finding 4. Closes SA4.1. **CR-SA41-002 resolved (2026-07-01):** changelog updated to match validated explicit-transaction baseline.
  - **SA4.2 — Per-transaction already-primed memo in the execute wrapper (wt-track2, 2026-07-01).** Added a per-transaction `_af9_primed_for_txn` connection attribute to `_make_priming_execute_wrapper()` in `quickscale_modules_orgs.current_org`. Inside an explicit transaction, the wrapper now issues `SET LOCAL app.current_org_id` only on the first statement; subsequent statements reuse the memo and skip the redundant `SET LOCAL`. Stale memo state is cleared when the wrapper next enters the autocommit path (between transactions). Autocommit path behavior is unchanged. Baseline updates in `test_sa41_statement_amplification.py`: explicit-transaction total 4→3, SET LOCAL count 2→1, amplification 2.0x→1.5x. AF9/AF11 regression guards (`test_af9_priming.py`, `test_af9_autocommit_proof.py`, `test_rls_boot_guard.py`) remain green. Track 2, Finding 4. Closes SA4.2.
  - **AF3 — Single audited operator-access seam (wt-track1):** Introduced `operator_access(reason=...)` context manager in `quickscale_modules_orgs.operator_access` with structured-logging audit trail (stable fields: command, reason, actor_identifier, target_scope, target_org_ids, touched_org_ids, status, error_class). Four management commands routed through the seam: `purge_organization`, `migrate_billing_to_orgs`, `forms_anonymize_submissions`, `forms_seed_presets`. Added `operator_queryset()` as the single centralized direct-`.all_objects.` exception — used by `migrate_billing_to_orgs` where an unfiltered queryset is required; the other three commands avoid `.all_objects.` via `operator_access()` plus scoped/default managers without `operator_queryset()`. Positive-proof conformance guard uses AST-level checking to structurally verify all four commands both import AND invoke `operator_access()`. Zero-direct-all_objects guard scans the full `quickscale_modules/**/management/commands/*.py` surface for any remaining direct `.all_objects.` references. Deferred-entrypoint manifest tracks 16 non-management `.all_objects.` sites across forms (admin, notifications, views), billing (admin, services, views), crm (admin, models, serializers, services, views), blog (admin, feeds, views), social (models), and orgs (permissions) for future phases. Non-skipped current-schema tests added for `migrate_billing_to_orgs`. **Completes AF3 — the last open work item from the structural autopsy.**
  - **SA1.2 — Migrate blog models to inherit `TenantModel` (Track 3, 2026-06-30, compatibility corrected 2026-07-01).** Category, Tag, BlogMediaAsset, and Post now inherit `quickscale_modules_orgs.models.TenantModel` instead of `models.Model`. Each model declares an explicit `organization` FK with its legacy `related_name` (`blog_categories`, `blog_tags`, `blog_media_assets`, `blog_posts`) — this overrides the abstract `TenantModel` field so downstream reverse-accessor consumers are not broken. Hand-copied `objects`/`all_objects` managers and `TenantManager` import removed — provided by `TenantModel`. Django migration `0004` carries only state-only `AlterModelOptions` and `AlterModelManagers` (no DB schema impact; the organization column and its `related_name` are unchanged). No `TENANT_TABLE_REGISTRY` update needed (already enrolled). Validation: blog `test_models.py` 41/41 PASS, `test_managers.py` 5/5 PASS, orgs `test_tenant_table_conformance.py` 183/183 PASS (2 skipped — the two `PENDING_REMEDIATION` parametrized tests are skipped by pytest because zero `PENDING_REMEDIATION` entries remain in the registry; all have been promoted to ENROLLED or EXCLUDED_REVIEWED). `AuthorProfile` (reviewed-excluded) left unchanged.
  - **SA1.3 — Generic, base-class-driven conformance check shipped as a management command (Track 1, wt-track1, 2026-06-30, regression tests completed 2026-07-01).** Added `manage.py check_tenant_isolation` (and a Django system check in `quickscale_modules_orgs.checks`) that discovers tenant models by marker (default manager is a `TenantManager` **or** model is a `TenantModel` subclass) across **all** app labels — not just the `quickscale_modules_*` prefix — and asserts each has `organization_id` + a live FORCE-RLS policy in `pg_policies`. Detection helpers extracted to `quickscale_modules_orgs.tenancy`: `is_tenant_model()`, `get_tenant_models()`, `has_organization_id_field()`, `table_has_force_rls()`, `check_tenant_model_isolation()`. The management command supports `--postgres-only` (skip in non-PostgreSQL environments) and `--format json` (structured CI output). The system check emits `WARNING` level messages (does not block startup) for models missing `organization_id` (W003) or FORCE-RLS (W004). System check registered via `apps.py ready()`. Thirteen focused tests validate: pass on current models, JSON output format, negative detection of non-tenant models, detection helper correctness, registry-vs-detection parity, no-org-id failure through full command surface, and direct JSON-only regression tests for the skip and no-models branches. Marker-based detection works whether CRM/blog are still using hand-rolled `objects = TenantManager()` or have already been migrated to `TenantModel` (SA1.1/SA1.2), so there is no cross-track ordering dependency. Validation: `MODULE=orgs make test-unit -- --modules` — all tests green. Unblocks SA1.4 (exclusion registry) and SA1.5 (generated-project CI wiring). See also Finding 3 dependency SA3.2 (CI doc-consistency gate, can now consume `get_tenant_models()` as the SSOT).
    - **CR-SA13-001 (resolved 2026-06-30; regression tests completed 2026-07-01):** `--format json` was not honored on the `--postgres-only` skip branch (always emitted human text) and the no-models branch (emitted both human warning AND JSON). Both branches now emit clean JSON-only output when `--format json` is specified. Two additional direct `call_command` regression tests added in the final pass: `test_check_tenant_isolation_json_postgres_only_skip` (proves `--postgres-only --format json` on non-PostgreSQL emits JSON-only with status `skip`) and `test_check_tenant_isolation_json_no_models` (proves `get_tenant_models()==[]` with `--format json` emits JSON-only with status `warning`). Both assert JSON parseability and absence of known human-only text markers.
    - **CR-SA13-002 (resolved 2026-06-30):** No test exercised the full command surface with a tenant-classified model missing `organization_id`. Added `test_check_tenant_isolation_model_without_org_id_through_command` — mocks `get_tenant_models` to return a model without `organization_id` and proves the command exits 1 with correct failure output in both human and JSON formats.
    - **CR-SA13-003 (resolved 2026-07-01):** Closeout docs (roadmap.md, CHANGELOG.md) refreshed to match the final shipped SA1.3 test surface of 13 tests and the completed regression-test coverage for both repaired JSON branches.
  - **AF9 — Connection-layer GUC priming execute wrapper (wt-track1, 2026-06-29 — completed 2026-06-30).** Installed a Django execute_wrapper on every `DatabaseWrapper` that derives `SET LOCAL app.current_org_id` from the ContextVar; the ContextVar is the sole source of truth — the two isolation layers can never desync; no per-view discipline required. Added `install_priming_wrapper()` to `quickscale_modules_orgs.current_org` with idempotent-install and recursion-guard ContextVars; wired via `apps.py ready()` (existing connections) and the `connection_created` signal (new connections). Two code paths: explicit transactions (SET LOCAL before every statement — idempotent and correct) and autocommit (short `transaction.atomic()` + SET LOCAL + execute — preserves AF4 no-request-long-transaction behavior). Phase-1 lifecycle tests (14 tests) cover install idempotence, recursion guard, explicit-transaction priming, autocommit priming, no-org pass-through, and transaction scoping. Phase 3 proofs: autocommit request-path proof (PR-AF9-001 — probe view through TenantMiddleware verifies GUC is primed inside the view's DB statement and resets after request), CRM restricted-role cursor proof (PR-AF9-003 — `set_current_org_id()` under SET ROLE with no manual SET LOCAL), and listings restricted-role cursor proof (PR-AF9-005 — relocated to orgs conformance surface via the plan-approved AF11-style SET ROLE + direct SELECT pattern). Validation: `make MODULE=orgs lint/typecheck` clean; `make MODULE=orgs test` 571 passed, 5 skipped. CRM targeted validation green. Non-AF9 findings discovered during validation: pre-existing CRM mypy import-not-found in adapter.py, pre-existing CRM T1.11 baseline-red tests, and pre-existing listings conftest DB-setup issue (all unrelated to AF9 scope). **CR-AF9-003 (blocking) resolved 2026-06-30:** Added `connection.vendor != "postgresql"` guard in the execute wrapper function (matching the existing pattern in `set_db_current_org_id`/`reset_db_current_org_id`). On non-PostgreSQL backends the wrapper passes through without priming, preventing SQL errors on SQLite connections. Added regression test `test_priming_wrapper_noop_on_non_postgresql` mocking `connection.vendor` to `"sqlite"` and verifying both normal query completion and unprimed GUC. Docstring updated. All change-review findings resolved — AF9 implementation complete, ready for merge. `connection.vendor == 'postgresql'` guard, which can break non-PostgreSQL backends. The wrapper needs a vendor guard plus a regression proof before this can merge. Resolved findings: CR-AF9-001 (listings proof uses transaction=True fresh-transaction harness), CR-AF9-002 (signal handler test uses `connections.create_connection()` fresh wrapper).**
  - **VIEW-AS — Operator org-impersonation debug mode (wt-track1, 2026-06-30, refreshed 2026-07-01).** Implemented Option A: session key `quickscale_modules_orgs.debug_as_org_id` (superuser-only), `TenantMiddleware._resolve_debug_org()` hook override overrides normal Solo/SaaS resolution. Initial admin affordance: thin redirectors that redirected to POST-only shared debug views, making the admin caller path unusable end-to-end. **Change-review follow-up (2026-07-01):** Admin entry/exit views rewritten to directly set/clear the debug session (no redirect to POST-only views). Admin `OrganizationAdmin` now carries: a per-row "VIEW-AS" button in the change list (`view_as_button` column, GET link) that activates the session and redirects to the org dashboard; an "Exit VIEW-AS" POST form in the change list template when a debug session is active. End-to-end test proves admin set → dashboard banner → exit cycle. Downstream banner smoke tests verify CRM, blog, and listings base templates include the `_debug_banner.html` partial. Docs updated to match the fixed shipped surface. Validation: `make MODULE=orgs lint -- --modules && make MODULE=orgs typecheck -- --modules && make MODULE=orgs test-unit -- --modules` — all green. Complements the Phase C operator tooling slice. See changelog finding CR-VIEWAS-001/002/003 for full resolution.
  - **M20 — Phase 5 + deferred items (Track 1+2+3):** RLS boot guard in orgs `AppConfig.ready()` (T1.18) — fails fast in saas/prod if connected role has BYPASSRLS. Unified `org_scope()` context manager (T1.19) — shared primitive for middleware, billing, and CRM; deleted `_billing_org_db_context` and redundant `set_current_org_id` calls. Slug-routing fallback deleted (T1.20) — `_resolve_org_from_path_slug`, frozensets, and Fallback A/B removed; React template emits `/crm/` unconditionally. `MODULE_CATALOG` static tuple retired as inventory (D2) — `get_discovered_module_entries()` is now the sole inventory path; static catalog frozen to description-only metadata. Backups DR adapter coverage + terminology cleanup (D4+D5). Structured logging baseline in generated-project templates (D9a) — JSON formatter, correlation-ID middleware, stdlib-only. Core coverage gaps closed (D6) — `resolvers.py` 40%→91%, `social_manifest.py` 47%→94%.
  - **D1 (Option B) — Generated showcase_react SPA org-switch billing parity:** Billing-surgery scope implemented on `wt-track1` (dashboard card, sidebar nav, org-dashboard billing cards/links, `modulePaths.billing` removed from React hook contract; module flags `modules.billing` remain present; `lint_frontend.sh` fixed to use proper Jinja rendering; docs refreshed). Template compile-safety gap D1-REV-005 resolved: `main.tsx.j2`, `App.test.tsx.j2`, and `PublicSocialPages.test.tsx.j2` hardened for empty/partial `selected_modules` variants with targeted compile-safety tests. Full validation gate passed: `make lint-frontend` PASS, targeted tests 274/274 PASS, broader `quickscale_core` non-E2E suite 1710 passed / 28 deselected with 92.73% coverage. Merged to `v87`.
    - 2026-06-26 — **T1.19: Unified `org_scope()` context manager (Track 1).** Promoted the shared org-scope entry point from `_billing_org_db_context` (billing/services.py) to `orgs.current_org.org_scope()`. Middleware `_call_with_org` now delegates to `org_scope()`. Billing services deleted `_billing_org_db_context`; all 4 webhook handler call sites import `org_scope` from `quickscale_modules_orgs.current_org` instead. CRM serializers removed redundant `set_current_org_id()` calls from `_request_org_id` and `_read_org_id` — the contextvar is already set by middleware. Cleaned up dead imports (`contextlib`, `Iterator` in billing; `transaction`, `_set_current_org_id`, `set_current_org_id`, `set_db_current_org_id` in middleware). **Scope:** Middleware+billing+CRM seam unified. Remaining callers (management commands, CRM views, social admin `_org_db_context`) still use `set_current_org_for_context()` / `set_current_org_id()` / `set_db_current_org_id()` directly. Roadmap updated: T1.19 closed.
    - 2026-06-27 — **AF6: Decompose generator god files into per-concern packages (Track 3).** Extracted all 16 apply step bodies from `apply_command.py` (3.1k→thin orchestrator) into 6 concern-focused `quickscale_core/apply/steps/*.py` modules (`embedding`, `wiring`, `env_sync`, `deps`, `infrastructure`, `finalize`) with thin CLI adapters and preserved import/patch seams. Decomposed `dr_engine/orchestration.py` (3.7k→~3.0k) into `_lock.py`, `_paths.py`, `_sidecar.py` sibling modules with stable facade re-exports; preserved all `quickscale_modules_backups.services` import paths and root-orchestration test patch targets. Behaviour-preserving — core tests (1,701), CLI tests (1,840), backups tests (234) all green. Phase 1: boundary types (`StepContext`, `StepOutcome`, `ApplyStepProtocol`). Phase 2: step body extraction. Phase 3: DR orchestration split. Enables AF5 (step executor) and AF7 (manifest adapter relocation).
    - 2026-06-28 — **AF1-CR follow-up (Track 1, forms-only).** Deferred AF1 review findings closed in a bounded serial slice.
    - **AF1-CR-002 (hardened):** `AdminSubmissionExportView` FK traversal replaced with explicit `all_objects` batch query — no longer depends on Prefetch cache or default RLS-scoped manager. Cross-org regression test added.
    - **AF1-CR-005 (proven):** Production code path already calls `notify_submission` inside `org_scope()`. Added regression tests proving notification content (field values, submitter-name suffix) is correctly rendered within the active org context.
    - **Validation:** `make MODULE=forms lint`, `typecheck`, `test` — all green. Unblocks AF3.
    - 2026-06-27 — **AF1: Tenant-table isolation conformance gate + declarative RLS (Track 1).** Phased implementation across 5 phases: Phase 1 (conformance gate registry + structural assertions in `quickscale_modules/orgs/tests/test_tenant_table_conformance.py`), Phase 2 (shared `apply_force_rls`/`revert_force_rls` helpers + child-parent equality infrastructure in `tenancy.py`), Phase 3 (CRM child-table schema migration — `organization_id` on ContactNote/DealNote with FORCE RLS and equality triggers), Phase 4 (Forms child-table promotion — `organization_id` on FormField/FormSubmission/FormFieldValue with FORCE RLS and equality triggers), Phase 5 (final enforcing gate: zero PENDING_REMEDIATION entries, all concrete models accounted for, FORCE RLS on every ENROLLED table, and live child-parent equality trigger verification in `pg_trigger` for child/detail tables). Registry promotes 5 formerly PENDING child/detail tables to ENROLLED with equality-footprint enforcement. Validated across orgs, crm, and forms modules. **Unblocks:** AF2, AF4. Two forms-only follow-up fixes (AF1-CR-002, AF1-CR-005) completed in the next wt-track1 cycle.
    - 2026-06-27 — **AF5: Apply step executor with per-step checkpoint/resume, destructive confirmation gate, and fault-injection harness (Track 3).** Each apply step now carries an `is_satisfied()`/`apply()` contract via `ApplyStepProtocol`. After each non-destructive step (1–10), `ApplyExecutor.checkpoint_step()` writes a `ResumeCheckpoint` to the `RecoveryLedger` (`.quickscale/apply-recovery.yml`). On rerun after a mid-pipeline failure, `ApplyExecutor.find_first_unsatisfied_step()` reads the checkpoint and resumes from the first incomplete step. Backward-compatible with AF6-era ledgers that lack the `resume_checkpoint` field (treated as `None`, triggering a full rerun). Steps 11–16 (mutable config, Docker startup, database migrations, Railway deploy, state finalization, next-steps display) are grouped into a separately-confirmable destructive/remote phase with an explicit operator prompt. A deterministic fault-injection test harness (AF5 Phase 3) kills after step N, verifies ledger checkpoint correctness, then reruns and asserts convergence across all 16 steps. Phase 1: `ResumeCheckpoint` + `RecoveryLedger` schema. Phase 2: executor-driven resume in `_execute_apply_steps_locked`. Phase 3: fault-injection test harness. Phase 4: destructive/remote confirmation gate.
    - 2026-06-28 — **AF2+AF4: Demote auto-scoping manager + connection-level org GUC (Track 2).** `TenantModel.base_manager_name` set to `"all_objects"` (unfiltered base), eliminating silent FK-traversal `DoesNotExist` bugs. Request-scoped callers now share `orgs.current_org.tenant_context()` while `org_scope()` remains the transactional/non-request helper. `TenantMiddleware` no longer holds request-long transactions; instead, forms public endpoints, generated social views, and billing webhook mutation phases open explicit short `transaction.atomic()` + `tenant_context()` windows only where DB-side org scope is required. Billing webhook Stripe retrieval/backfill runs outside local mutation transactions, and public-submit notification content now preserves submitted field/value data after post-commit dispatch. Connection pooling and runtime-role note added to generated `production.py.j2` template documenting `CONN_MAX_AGE`, `CONN_HEALTH_CHECKS`, and the `RUNTIME_DATABASE_URL` runtime role pattern. Regression tests cover no-context FK traversal, `tenant_context()` contract behavior, forms caller parity, billing transaction boundaries, managed social-view regeneration, and notification content after post-commit dispatch. **Completes Track 2.**
    - 2026-06-29 — **AF13: Delete SQLite fallback from all module test-settings files (Track 3).** Removed the `QUICKSCALE_TEST_DB` env-var branch and SQLite `:memory:` fallback from all 11 module `tests/settings.py` files. Each now targets PostgreSQL unconditionally via module-specific `QS_*_DB_*` env vars. Ancillary cleanup: replaced the SQLite smoke-test helper in `quickscale_core/tests/test_generated_project_runtime.py` with PostgreSQL-backed settings; removed obsolete SQLite-portability comments from two CRM migration files. Unblocks AF10.
    - 2026-06-29 — **AF10: Isolation-conformance CI job (Track 3).** Added `isolation-conformance` CI job to `.github/workflows/ci.yml` — Postgres 18 service with NOBYPASSRLS `quickscale_rls_test_role`, Poetry-managed Python 3.14. Created `scripts/test_isolation_conformance.sh` runner that executes the orgs conformance gate (PostgreSQL-only `test_tenant_table_conformance.py`), all five module `test_rls_boundary.py` suites (billing, blog, crm, forms, listings), and the CRM authenticated-request isolation test. Added `test_restricted_role_authenticated_list_view` to `test_isolation.py` — exercises the full Django request pipeline under `SET ROLE` without presetting `app.current_org_id`, serving as the red-green verification test for AF9. The test was RED on v87 until AF9's connection-layer `execute_wrapper` was implemented. JUnit XML post-processing fails the build if any isolation test is skipped, ensuring the gate cannot pass by skipping. **Completes Track 3 — AF10 is the last Track 3 task.** CR-AF10-001 fix (2026-06-29): removed the manual `SET app.current_org_id` GUC pre-set that was bypassing the AF9 execute_wrapper seam. CR-AF10-002 fix: narrowed runner script header to localhost-only.
    - 2026-06-29 — **AF11: NULLIF empty-string guard in RLS policy template (Track 2).** Fixed `_FORCE_RLS_FORWARD_SQL` in `orgs/tenancy.py` to use `NULLIF(current_setting(...), '')::uuid` instead of bare `current_setting(...)::uuid`, preventing `invalid input syntax for type uuid` from pooled connections whose GUC rests at `''`. One sweep migration per module (crm, blog, listings, forms, social, billing) recreates policies from the corrected template. Conformance gate extended with PostgreSQL-only restricted-role proof: seeds 21 enrolled tables and asserts both `RESET app.current_org_id` and `SET app.current_org_id = ''` yield zero rows without raising. Phases 1–3 (template fix, migrations, baseline gate consistency) completed in prior cycles; Phase 4 (restricted-role proof) landed in this phase.
    - 2026-06-29 — **AF12: Composite FK for child-parent org equality (wt-track2).** Replaced the child-only equality trigger with 6 composite `(child_fk, organization_id)` → `(parent.id, parent.organization_id)` foreign keys gated by `UNIQUE (id, organization_id)` constraints on all enrolled parent tables (Contact, Deal, Form, FormField, FormSubmission). Added `add_parent_unique_constraint`/`remove_parent_unique_constraint`/`add_composite_child_fk`/`remove_composite_child_fk` helpers to `orgs/tenancy.py`. Rewrote CRM migration `0009` and Forms migration `0007` to install parent unique constraints + composite FKs + RLS instead of equality triggers. FormFieldValue.field special case uses PG15+ partial-column `ON DELETE SET NULL (field_id)` — only `field_id` is nulled on parent FormField deletion; `organization_id` stays NOT NULL. Conformance gate switched from `pg_trigger` presence check to `pg_constraint` presence check for all 6 AF12 pairs. Schema+proof tests landed in Phase 1+2 (31 helper/conformance/delete-path tests in orgs suite, 5 MigrationExecutor tests in CRM, 6 in Forms). **CR-AF12-001 resolved:** Removed the blocking `@pytest.mark.skip` from `TestCrmParentOrgMutationRejection` and `TestFormsParentOrgMutationRejection`. **CR-AF12-004 resolved:** Forms migration `0007` composite FKs finish with `VALIDATE CONSTRAINT` after creation.
    - **AF12 follow-up (2026-06-29, same batch):** Change-review pass 1 hit its iteration cap with three remaining blocking findings. All three resolved in a scoped follow-up pass:
      - **CR-AF12-005 (resolved):** Forms 0007 composite FKs now carry `DEFERRABLE INITIALLY DEFERRED` alongside `NOT VALID` + `VALIDATE CONSTRAINT`. Added `pg_constraint` regression proof (condeferrable + condeferred assertion).
      - **CR-AF12-006 (resolved):** `test_admin.py` and `test_views.py` tests that violated the AF12 composite FK invariant fixed: `test_export_cross_org_field_values` creates separate forms per org; `test_authenticated_schema_returns_org_scoped_form` creates form under target org from the start.
      - **CR-AF12-003 (resolved):** Docs/changelog updated to accurately reflect the post-fix state.
    - **Current state:** Re-validation and change-review pass 2 complete. Gate closed — all CR-AF12 findings resolved, composite FKs live, parent-org mutation rejection proofs enabled.
    - 2026-06-28 — **AF8: Fix fail-hard violations in module-path discovery and Railway project-name inference (Track 3).** Removed the bundled-context fallback (`except Exception: pass`) and best-effort default return from `get_modules_base_path()` — now raises `ImproperlyConfigured` when the monorepo path is unavailable and no runtime override is set. Removed the `Path.cwd().name` fallback from `get_app_service_name()` — now raises `ValueError` when project_name is absent. Hardened `module_wiring_manager.py` save/restore pattern to catch `ImproperlyConfigured` and return a failure tuple. Updated all affected tests. Roadmap entry corrected: referenced function was `get_railway_service_name()` but actual name is `get_app_service_name()`. Closes Finding 8 violations 2–3.
    - **AF8-CR fixes (2026-06-28, same batch):** Deploy railway CLI now derives project name from CWD when `--project-name` is absent, fixing `ValueError` in caller. `regenerate_managed_wiring()` tolerates absent modules base path when embedded manifests are available, fixing ordering failure for embedded-project contexts outside the maintainer monorepo. Both findings resolved in one pass.
    - 2026-06-27 — **AF7: Push per-module manifest adapters out of core into the modules (Track 3).** Established the managed-adapter infrastructure (`MANAGED_ADAPTER_ORIGINS`, `refresh_managed_adapters()` with base-path-aware discovery) and moved the social, billing, and CRM manifest adapters onto the discovery seam. Each managed module now ships a module-owned `adapter.py` with the real adapter implementation (post-hooks, option resolution, settings assembly, managed-file rendering) in its package, used as the primary path in monorepo/embedded contexts. Provenance-sensitive tests added (6 tests in `TestManagedAdapterProvenance`). `build_generic_manifest_spec()` and `load_module_manifest()` were made public for module-owned adapter use. `module_wiring_manager.py` coordinates refresh around `set_modules_base_path()`. 280 tests pass.
    - 2026-06-28 — **AF7 Phase 2 — Fail-hard cleanup (AF7-CR-003 resolution, Track 3).** Deleted `_CORE_FALLBACK_ADAPTERS` dict and three core fallback adapters (`_billing_core_fallback`, `_crm_core_fallback`, `_social_core_fallback`). Rewrote `refresh_managed_adapters()` to raise `ImproperlyConfigured` when a managed module's adapter is not importable (bundled/installed-without-module-source is not supported). Updated module docstring and comments. Deleted three fallback-provenance tests. Updated roadmap and changelog to mark AF7 complete.
  - **M19 — Phase 4 (Track 1):** `purge_organization` management command — UUID-only destructive targeting, FK-safe delete order across all six modules, tombstone-backed rerun semantics, `--dry-run`/`--force`, System/personal-org guard, `set_current_org_for_context()` shared helper. Postgres-backed RLS proof. 278 orgs tests passed.
  - **M18 — Phase 3 complete (Track 1):** FORCE RLS enabled on all six modules (CRM T1.11, Blog T1.12, Forms T1.13, Listings T1.14, Billing T1.16) with `current_setting('app.current_org_id', true)::uuid` fail-closed predicate and full reverse SQL. PostgreSQL-backed boundary tests for each module.
  - **M17 — Phase 3 partial (Track 1):** Social RLS (T1.15) — FORCE ROW LEVEL SECURITY on social tables; per-org admin with fail-closed behavior under runtime role.
  - **M16 — Phase 2 (Track 1):** CRM (T1.5), Blog (T1.6), Forms (T1.7), Listings (T1.8), Social (T1.9), Billing (T1.10) all adopt the shared NOT NULL/PROTECT ownership contract — module-local managers replaced with `orgs.TenantManager`, nullable org FKs converted, all `/orgs/<slug>/` content routes deleted, migrations squashed to clean contract.
  - **M15 — Phase 1 Foundation (Track 1):** Session-based middleware single-URL contract (T1.3) — org resolved from session; slug extraction and route-sniffing helpers deleted. RLS DB role + generated-project template wiring (T1.4) — `db/init.sql.j2`, `OPERATIONS.md.j2`, `RUNTIME_DATABASE_URL` override, restricted-role docker-compose wiring.
  - **M14 — Track 2 complete:** Manifest-backed module wiring rollout (T2.1–T2.4) — `ImpliesEntry`/`implies` schema, generic `resolve_module_implications()`, all 12 CLI `*_manifest.py` adapters deleted, dead CLI shims removed.
  - **M13 — Phase 1 Foundation (Track 1):** System org + NOT NULL ownership contract (T1.1) — `Organization.is_system`, singleton constraint, `tenant_org_fk()`. Shared tenant-scoping seam (T1.2) — `current_org_id` ContextVar, `TenantManager` fail-closed queryset, `SET LOCAL` middleware propagation.
  - **M12 — Track 3 complete:** DR hard cutover (T3.1–T3.3) — typed `dr_engine.adapter`, single `dr_adapter_call` bridge command, all 8 admin management commands dispatch through adapter, legacy env-var/stdout-JSON protocol deleted, `services.py` slimmed to thin re-exports.
  - **M11 — Track 3:** Generator vs generated-project runtime-pin ownership split (F7.1–F7.3) — `runtime_pins.py` SSOT, template context injection, drift-detection validation, `ruff target-version` variableized.
  - **M10 — Track 2:** DR engine extracted to `quickscale_core.dr_engine` (F5.1–F5.4) — primitives, orchestration, recovery, verification layers; `services.py` 3,654 LOC → 205 LOC thin re-exports; typed-request/result boundary replaces hidden management-command protocol; `dr_engine_migration.md` authored.
  - **M9 — Track 1:** Org-authoritative billing contract (F13) — `_sync_subscription_authority()` enforces org-primary/user-provenance; unique active-subscription-per-org constraint; dual-FK backfill migration.
  - **M8 — Track 3:** Railway rollback/resume semantics for recoverable apply (F12.1–F12.3) — single `apply-recovery.yml` ledger, git-index checkpoint, fail-closed config/state mirroring, Railway deploy step inserted between migrations and state persistence.
  - **M7 — Track 1:** Structural multi-tenant isolation rollout across all modules (F11.2–F11.13b) — org FK + per-org uniqueness + dual-manager contract for CRM, blog, forms, listings, social; org-scoped read filtering, same-org validation, route-aware views, adoption docs.

- v0.86.0 — Organizations Module (multi-tenancy with Solo/SaaS runtime modes, org-scoped RBAC and invitation flow, billing bridge with authoritative org ownership fields and `Plan.features` feature gates, `migrate_billing_to_orgs` and `promote_to_saas` management commands, React org management pages and org switcher, and billing wiring regression fix with planner/apply guard test)
- v0.85.0 — Billing Module (Stripe-backed one-time credit purchases and recurring subscriptions, credits-first Django-owned ledger, planner/apply readiness with auth-module dependency enforcement, module-owned `/billing/pricing/` and `/billing/dashboard/` pages, and starter-theme billing links)
- v0.84.0 — Backups Hardening Release (admin backup artifact download/create/integrity actions, upload-driven restore safety and compatibility checks, full-snapshot backup-contract hardening, and repo-owned runtime/tooling support alignment for the refreshed Python/frontend baseline)
- v0.83.0 — Hardening Release (repo-wide hardening across strict desired-config validation, notifications live-config placeholder rejection, generated production `SECRET_KEY` fail-hard behavior, CRM staff-only parity, forms CSV and backup artifact safety, blog upload/thumbnail/rate-limit hardening)
- v0.82.0 — Disaster Recovery & Environment Promotion Workflows (public `quickscale dr capture/plan/execute/report` workflows for local and Railway routes with `snapshot_id`-based stored snapshots, resumable capture and execute, rollback pins for production routes, conservative env-var sync, and source-side media sync)
- v0.81.0 — Beta-Site Migration Maintainer Tooling (maintainer-only Make/Python workflows for beta-site catch-up: fresh-first and in-place paths with checkpoint-first semantics)
- v0.80.0 — Analytics Module (PostHog-only website analytics with flat `QUICKSCALE_ANALYTICS_*` settings, service-style capture helpers, apply-time env-example sync, guarded forms integration, QuickScale-owned social click tracking)
- v0.79.0 — Social & Link Tree Module (curated social links and embeds, backend-owned YouTube/TikTok preview metadata, managed `/_quickscale/social/` integration endpoints, Django-owned public React pages)
- v0.78.0 — Notifications Module (transactional email foundation, Anymail-backed Resend delivery, recipient-granular tracking, signed delivery webhooks)
- v0.77.0 — internal main-branch baseline — Backups Module (private database backups, optional private remote offload, guarded CLI restore, scheduler-ready command hooks)
- v0.76.0 — Storage Module (cloud file hosting, media storage adapters, CDN-ready media infrastructure)
- v0.75.0 — Forms Module (generic customizable form builder with admin, DRF API, spam protection, GDPR anonymization, and React mount point)
- v0.74.0 — React Default Theme (showcase_react theme with Vite, TypeScript, TanStack Query, and Zustand)
- v0.73.0 — CRM Module (API-first Django CRM with 7 core models and CLI integration)
- v0.72.0 — Plan/Apply Cleanup (removed legacy init/embed commands, full transition to plan/apply)
- v0.71.0 — Module manifests & config mutability (Plan/Apply system complete)
- v0.70.0 — Existing project support (status, plan --add, plan --reconfigure)
- v0.69.0 — State management and incremental applies
- v0.68.0 — Plan/Apply System core commands (Terraform-style declarative workflow)
- v0.67.0 — Listings module with AbstractListing base model for verticals
- v0.66.0 — Blog module with Markdown, featured images, and RSS feeds
- v0.65.0 — Enhanced auth module and development tooling
- v0.64.0 — Theme rename to showcase_* (breaking change)
- v0.63.0 — Authentication Module with django-allauth and interactive embed
- v0.62.0 — Split Branch Infrastructure (module management CLI commands, GitHub Actions automation)
- v0.61.0 — Theme System Foundation (--theme CLI flag, theme abstraction layer, HTML theme)
- v0.60.0 — Railway Deployment Support (automated deployment via quickscale deploy railway)
- v0.59.0 — CLI Development Commands (Docker/Django operation wrappers)
- v0.58.0 — Comprehensive E2E testing infrastructure with Playwright and PostgreSQL
- v0.57.0 — Production-ready generator baseline
- v0.56.0 — Quality, Testing & CI/CD
- v0.55.0 — CLI implementation
- v0.54.0 — Project Generator
- v0.53.3 — Project Metadata & DevOps Templates
- v0.53.2 — Templates and Static Files
- v0.53.1 — Core Django Project Templates
- v0.52.0 — Project Foundation
- v0.51.0 — Documentation foundation
- v0.41.0 and earlier — legacy codebase (see Github repository for history)
