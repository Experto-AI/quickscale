# Changelog

`CHANGELOG.md` is the canonical QuickScale release history index. Published releases pair each version entry with a single official release note in `docs/releases/` linked from the GitHub tag and release PR. When a release note is prepared before the maintainer completes the manual tag/publish step, the changelog entry and note must say so explicitly and must not imply publication. Use `docs/technical/roadmap.md` for active or unpublished release status. Entries are version-ordered.

- v0.87.0 — unreleased / not yet tagged — Hardening release. Consolidates a large hardening pass across tenant isolation, module configuration, the generator, and disaster recovery.
  - **SA136a COMPLETE — tag, tree, and local-tag helpers landed (2026-08-06; functional delta reviewed in DC-80).** Implemented canonical split-tag construction, exact peeled remote-tag resolution, remote-tag existence checks, tree/local-tag helpers, idempotent non-force annotated-tag creation, and explicit single-refspec no-force tag pushing, with hermetic tests and no real remote contact. Validation passed **198 focused tests**, `make lint`, `make typecheck`, and `make test-unit` with Core **90.46%** and CLI **90.78%** coverage. Final full-scope Adaptive-change-review **DC-80 returned `STATUS: ok`**; `F-001` (high/security), `F-002` (high/correctness), `F-003` (medium/correctness), and `F-004` (medium/test-gap) are resolved. `F-005` remains a **low advisory** for intermediate `.lock` component prevalidation parity; Git still fails closed and there is no injection/tag-sweep path. Track 3 advances to SA136b; the SA136 umbrella remains open.
  - **Loop/seal split publication ratified — SA136 opened, SA117e-4 restructured, SA119 closed by design (2026-08-06; planning only, no source or remote change).** The maintainer ratified two decisions that dissolve the circular dependency capping SA117e-4. **(1) Split-branch publication is an idempotent republish, not a one-shot irreversible mutation; the only irreversible gate is PyPI.** Branches are mutable working artifacts republished as often as verification requires, which is what makes it possible to verify published state at all — the prior plan could not, because verifying required publishing. **(2) Module splits are version-tagged in lockstep with core:** at core `X.Y.Z` each split carries the immutable tag `splits/<module>-module/X.Y.Z`, resolved by embed *by identity* from the running core version, so no version→ref mapping table exists. An unchanged tree keeps the same commit across versions and therefore carries both tags. Decision 2 **closes SA119** (immutable-ref pinning, previously deferred to v88) by dissolving its open "where does the mapping live?" design question; the v88 entry is removed. Ratified alongside: the `splits/…` tag namespacing (verified not to match `publish.yml`'s `[0-9]*`/`v[0-9]*` triggers, which would otherwise auto-publish to PyPI on a pushed tag), deletion of `.github/workflows/split-modules.yml` (fires on `v*`, force-pushes 3 of 12 modules with bare `--force`, and is the sole origin of the stale `splits/teams-module` branch), deletion of that stale remote branch, and restructuring SA117e-4 **in place** rather than superseding it. New umbrella **SA136** (six children, all Tier 1–2, Track 3, on the critical path) owns the machinery; **SA137** is opened in v88 for `quickscale_devtools/pyproject.toml`'s `0.86.0` drift outside `version_tool.sh`'s propagation set. The four SA117e-4 plan blockers are retired rather than resolved in place: `F-001` and `F-006` described hazards that exist only for a one-shot irreversible mutation, `F-005` becomes SA136b's deliverable, and `F-004` is re-derived from a smaller allowlist. `SA117E1-REV-001`'s explicit empty-expectation lease remains binding on the loop phase; `SA117E1-REV-002` is **discharged by design**, since digest-bound one-time-consumption protects against reusing approval for a *different* irreversible mutation and the loop is now reversible while the seal is protected by Git's refusal to move an existing remote tag plus the fresh twelve-row human confirmation. Track 3 becomes **truly green on the critical path** for the first time on this board, with head SA136a. No source, script, workflow, manifest, tag, split branch, package registry, Docker resource, or PostgreSQL service changed; only `docs/technical/roadmap.md` and this file were edited.
  - **SA117e-4 plan-review-cap blocked checkpoint — decisions ratified; no implementation or public mutation (2026-08-05; task remains unchecked).** **Done:** SA117e-3 is closed; Track 3 started clean, synced to `v87` at `ba34f594`, and reused Python 3.14.6 / Poetry 2.4.1 / Git 2.43.0. The maintainer ratified `SA117E1-REV-001` with the explicit atomic absence contract `--force-with-lease=refs/heads/<branch>:` after official Git 2.43.0 research and a disposable bare-remote matrix covering absent, present, fresh-tracking, stale-tracking, and concurrent-writer cases. This supersedes the proposal's inaccurate mechanism: a fresh tracking ref can silently authorize overwrite, a stale tracking ref can spuriously reject an absent branch, and a never-fetched present branch fails closed with `stale info`. The maintainer ratified `SA117E1-REV-002` as digest-bound bind-and-one-time-consume authorization, not SA124 deferment. Discovery mapped the exact push/evidence/caller/test seams. Two plan/review passes resolved plan `F-002` (one canonical byte serializer) and `F-003` (successful local-tag deletion plus no-tag recheck), but returned `STATUS: partial`; blocking count worsened **2 → 4** at unchanged highest severity **high**. **Pending-Blocking:** `F-001` (**high**, security boundary) requires deterministic interruption barriers, exact durable restart images, same-claim recovery, and replay/contender rejection; `F-004` (**medium**, scope) requires phase-local tracked/private-artifact modify/create allowlists and explicit read-only phases; `F-005` (**high**, completeness) requires one authoritative frozen twelve-module inventory consumed by authorization, journal, push, parity, and acceptance; `F-006` (**medium**, resilience) requires an armed concrete pre-write rollback transaction to `ba34f594` covering tracked files, refs, authorization artifacts, local-tag deletion, and clean/no-tag verification. `SA117E1-REV-001`, `SA117E1-REV-002`, and `SA117E3-PUBLIC-ANALYTICS-001` remain unimplemented/open. **Decisions needed:** none on repository design. At the two-pass cap, the maintainer selected `Stop here, record Done / Pending-Blocking / Decisions-needed, and merge the checkpoint`; a future plan/review round requires a new explicit continuation instruction. This ratification authorizes neither source implementation nor a push; the fresh twelve-row confirmation remains an execution-time gate and was not requested or granted. No source/test/config/schema file, local or remote tag, split branch, package registry, Docker resource, or PostgreSQL service changed.
  - **SA122b-1 COMPLETE — Make `check` aggregation now derives from the registry (2026-08-05; functional commit `2db57e48`).** `Makefile` and `scripts/test_gate_parity.py` now derive and test the five-gate Make aggregation; 188 parity tests passed, both `make check` modes exited 0, and the direct checker retained its expected exit 1 with exactly five publish-only omissions and zero Make diagnostics. Adaptive-change-review DC-68 returned `STATUS: ok` and resolved `F-001`; no new blockers or decisions were found, and pre-existing non-failing MyPy configuration notes remain advisory only. Track 1 advances to SA122b-2; the SA122b umbrella remains open and no later child is complete.
  - **SA122b-2 COMPLETE — local serial and parallel inventories derive from the registry (2026-08-05; functional commit `cf9c3dc2`).** QG DC-84 passed **24 local-CI tests**, **190 parity tests**, and **49 worker-pool tests**; syntax, Ruff, format, `make check QUIET=1`, and `git diff --check` were green. Parity retains only the five expected publish omissions. Final full-scope review DC-85 returned **`STATUS: ok`**, resolving `F-001`/`F-002` with no advisories; no open SA122b-2 blocker or decision was discovered. Track 1 advances to SA122b-3; the SA122b umbrella remains open, Arch Finding 11 remains open, and publish parity is not complete.
  - **SA117e-4 recommended disposition recorded — PROPOSAL, not a ratification (2026-08-05; documentation only; no source files touched; no ticket state changed; no decision closed).** `docs/technical/roadmap.md` now carries an assistant-authored recommendation for SA117e-4's two open maintainer decisions, so Track 3 can be handed off without re-deriving the analysis. **Recommended:** resolve `SA117E1-REV-001` by ratifying an explicit remote-absence lease contract (bare `--force-with-lease` reads its expected value from the local tracking ref, so a genuinely absent branch and a never-fetched branch are indistinguishable to it — the check passes in both cases and the failure is silent); resolve `SA117E1-REV-002` by binding and one-time-consuming digest-bound authorization evidence rather than deferring, and specifically **not** by relocating it into SA124, which is by definition the home for meta-tooling that gates nothing. Recorded compounding note: weak options on both would leave the release's one irreversible step protected solely by the twelve-row human confirmation, which must be an explicit choice rather than an inherited gap. **Handoff sequencing recorded:** (1) read-only lease research, startable with no further authorization and producing evidence only; (2) implement both corrections as one coherent review unit on the shared push path; (3) one full-scope review; (4) fresh twelve-row confirmation, then push, then post-push obligations. **Gate on the proposal:** a handoff may execute Step 1 only; Steps 2–4 require written maintainer ratification of both decisions in this file first, and a handoff finding none stops after Step 1 and reports. Track 3's readiness is unchanged — still cannot start implementation, still cannot finish, still can merge; the readiness table now distinguishes blocked implementation from the startable read-only step. **Decisions needed:** both remain open and unratified; this entry records advice, not authorization.
  - **Roadmap cleaning pass (2026-08-05, second pass of the day; documentation only; no ticket state changed, no ticket closed).** **Closeout transfer:** the checked **SA117e-3** child block was deleted from `docs/technical/roadmap.md`; its acceptance evidence (clean tip `c4b6e354`, one transient PyPI-timeout attempt then a bounded retry exiting 0 with Core 35 passed, CLI 29 passed, 93.01% combined coverage, empty `QUARANTINE_TICKETS`, frozen host PostgreSQL restored, `QG-INFRA-001` resolved) is preserved in the SA117e-3 entry below and is not restated in the roadmap. `SA117E3-PUBLIC-ANALYTICS-001`'s definition moved inline into SA117e-4, which owns it. **Audit transfer:** none — `tech-audit.md` has zero open findings and `arch-audit.md`'s Finding 11 is still open and still ticketed as SA122b, so no audit context became removable. **Rebalancing:** no move applied, the eighth consecutive pass to reach that conclusion. Critical path remains `SA117e-4 → SA112b → SA112c → SA112d → SA112e → SA112f → SA96-PUBLISH`. Track 1's SA122b-1…5 chain is serial and off-path, so relocating any of it onto Track 3 would put filler ahead of the blocked critical-path head; Track 2 is closed to new work by standing rule; every open v87 ticket already carries a track. **Readiness unchanged:** Track 3 can merge but cannot start or finish (maintainer decisions `SA117E1-REV-001`, `SA117E1-REV-002`); Track 1 is truly green at SA122b-1 and off-path; Track 2 is start-only, with `merge after SA112e` and acceptance-on-SA112d both hard upstream dependencies. **Also compressed:** stale narrative in the Track 3 status header, the readiness prose, the track-topology section, and the green-gate current-state paragraph, whose evidence pointer now names the `c4b6e354` `ci-e2e` run. **Decisions needed:** none new — the two standing `SA117E1-REV-*` decisions are unchanged.
  - **SA117e-3 COMPLETE — exclusive-capacity `make ci-e2e` acceptance passed; Track 3 advances to SA117e-4 (2026-08-05; no source delta, tag, push, or publication).** Track 3 started clean at `8a8811c3`, fast-forwarded to the then-current `v87` tip `c4b6e354`, and reused the ready Poetry 2.4.1 worktree `.venv`, Docker 29.1.3, and frozen PostgreSQL 18 container `ae8c2e18f8aa` with all required roles and 13 test databases. The first exact `make ci-e2e` attempt exited **1** only because Core E2E hit a transient `files.pythonhosted.org` read timeout after stages 1–11 and CLI E2E passed; cleanup and host restoration passed. After the index returned HTTP 200 and exclusive capacity was re-confirmed, the one bounded retry exited **0**: all 12 stages green, Core E2E **35 passed**, CLI E2E **29 passed**, combined coverage **93.01%** with every file at least 80%, integration mean **94.40%**, and `QUARANTINE_TICKETS` empty. Post-run probes confirmed the same PostgreSQL identity running/ready on port 5432, no task-owned E2E containers, and a clean tracked worktree. **Findings/blockers:** `QG-INFRA-001` (transient external package-index timeout) is resolved; pre-existing unrelated Docker residue was left untouched, and no task blocker remains. v87 advanced during the long run with SA122b-1's separately owned partial checkpoint; Track 3 fast-forwarded to `b262d2dc` before writing this shared-file closeout. SA117e-3 is checked, while SA117e-4 remains blocked on maintainer decisions `SA117E1-REV-001` and `SA117E1-REV-002`, its fresh twelve-row push confirmation, and post-push `SA117E3-PUBLIC-ANALYTICS-001` acceptance.
  - **SA122b-1 recorded-partial checkpoint — current Make inventory derives from the registry; oracle-aligned predicate remains open (2026-08-05; task remains unchecked).** **Done:** Track 1 started clean, fast-forwarded to `v87` `c4b6e354`, reused the ready Poetry 2.4.1 environment, and changed only `Makefile` plus `scripts/test_gate_parity.py`. Validation passed **187 focused tests**; the direct parity checker exited **1** with exactly the five known publish-only omissions and **zero Make diagnostics**; `make check` and `make check QUIET=1` both exited **0** (Core 2,620 passed / 1 skipped, CLI 2,043 passed, frontend green). **Pending-Blocking:** `F-001` (**medium**, correctness) — Make currently selects local-context bindings, while the authoritative checker defines `check` membership as ordered non-empty `make_target` values beginning with `check-`; continuation must align the predicate and add non-local `check-*` plus local non-`check` boundary fixtures. The first full-scope review returned **`STATUS: partial`**. **Decisions needed:** none — after two QG runs consumed the cycle budget, the maintainer selected `Stop here, record Done / Pending-Blocking / Decisions-needed, and merge the checkpoint`. This merge preserves the candidate; it is not executable approval, completion, or a waiver of `F-001`.
  - **`SA117E3-ORDER-001` RESOLVED — option (b): the public-split harness gate moves from SA117e-3 to SA117e-4 (2026-08-05; documentation only; no source files touched; no ticket closed; no push, tag, or publication authorized).** The maintainer selected **option (b)**: SA117e-3 closes on `make ci-e2e` alone, and the requirement that the approved installed-wheel harness succeed against the **public** splits — together with the open `SA117E3-PUBLIC-ANALYTICS-001` blocker (public `splits/analytics-module` reports `0.80.0` against core/source `0.87.0`) — transfers to **SA117e-4**, executed after the human-gated split push. **Rationale:** the acceptance order was circular — SA117e-3 required a green public-split harness, but only SA117e-4's push can make those manifests report `0.87.0`, and SA117e-4 depends on SA117e-3. Option (b) puts the assertion where its input exists and matches the split's own stated cut lines, under which `-3` is local and reversible while `-4` is the single outward-facing mutation; SA117e-4 already carried "installed all-module `apply` reaches managed-wiring regeneration with no `KeyError`" as a verify line, so the gate lands on a child that was already asserting the same property. **Declined: option (a)** (authorize a reviewed split refresh before SA117e-3 closes) — it still required `SA117E1-REV-001`, `SA117E1-REV-002`, and the twelve-row confirmation to close first, so it saved no step while pushing before the harness had proved what the push exists to enable. **Nothing is weakened:** the public-split assertion is relocated, not removed; SA117e-4 cannot close while `SA117E3-PUBLIC-ANALYTICS-001` stands, the harness is re-reviewed rather than trusted by provenance if its hash no longer matches the approved SHA-256, and the pre-push gates (both review blockers plus the execution-time twelve-row human confirmation) are untouched — option (b) adds a post-push obligation to `-4`. **Board effect:** Track 3 flips to **truly green** (can start, can finish, can merge) and is the only truly green track *on* the critical path; SA117e-3's sole remaining leg is Phase 6 `make ci-e2e` at `ffd97e66` in exclusive Docker/PG capacity, needing no further authorization. The critical path is unchanged: `SA117e-3 → -4 → SA112b → c → d → e → f → SA96-PUBLISH`. **Open maintainer decisions drop from three to two**, both owned by SA117e-4 (`SA117E1-REV-001`, `SA117E1-REV-002`) and neither blocking a track today, though both must be answered before `-4` executes. No worktree assignment, merge order, cross-track edge, or track placement changed.
  - **Roadmap cleaning pass (2026-08-05; documentation only; no ticket state changed, no ticket closed).** **Closeout transfer:** the closed **SA128** umbrella and its **SA128d** child were deleted from the roadmap — their acceptance evidence, four-row attempts ledger, and completion checkpoint are already recorded here and in version control, and the roadmap keeps only the one-line authority statement SA122b consumes. SA117e-3's consumed `SA117E3-EXEC-001` grant block, its completed Revised Phase 5 procedure (5a–5f, plan invariants, execution parameters), and the superseded "narrow correction is planned and approved" paragraph were removed for the same reason; the ticket now states its current blocker and decision only, and its stale pre-execution body (unexecuted ephemeral harness, `F-002` plan-review gate) was rewritten to the actual remaining work. **Ticketed, not lost:** SA117e-3's "recorded for a follow-up ticket" note became **SA135 — Give the test suites an owned PostgreSQL lifecycle** in the v88 backlog (`Tier 2 · deps: SA117e-4`), carrying its hard sequencing constraint that auto-provisioning makes the stage-11 `PostgreSQL Not Available` banner branch unreachable and so must redesign that negative control in the same change, never on a run gating an irreversible publish. **Rebalancing:** the seventh consecutive pass applied **no move**. Every open v87 ticket carries a track; Track 2 is closed to new work by standing rule; Track 1's SA122b-1…5 is a single serial chain whose children share `Makefile`/`check_ci_locally.sh`/three workflows in strict order, so it cannot be fanned out, and moving any of it to Track 3 would push off-path filler ahead of the critical path. The critical path is unchanged: `SA117e-3 → -4 → SA112b → c → d → e → f → SA96-PUBLISH`. **Track states:** Track 1 truly green (start/finish/merge all yes) but off the critical path; Track 3 can merge but cannot start or finish, blocked by maintainer decision `SA117E3-ORDER-001` and the `0.80.0` public analytics split; Track 2 can start but has hard dependencies on SA112d (finish) and SA112e (merge) that no decision can clear. **Open maintainer decisions remain three** — `SA117E3-ORDER-001` (blocks the critical path) plus SA117e-4's two, `SA117E1-REV-001` and `SA117E1-REV-002`; the discharged `SA112A-TRACK-003` standing-placement bullet was dropped as resolved. Diagram, readiness table, dependency tags, `deps:` references, and the topology section were made self-consistent with the deletions. Roadmap 435 → 374 lines; `START_HERE.md` line count refreshed.
  - **SA117e-3 fourth recorded-partial checkpoint — correction and negative control green; public analytics split exposes release-order blocker (2026-08-05; task remains unchecked; no public mutation).** **Done:** Track 3 synced cleanly to `v87`; the three-file correction was committed as `ffd97e66` after default/nondefault focused proofs, the full backups suite, Ruff, and full-scope review `STATUS: ok`, resolving `SA117E3-BLK-001` and `SA117E3-EVID-001`. Revised Phase 5 passed with stage 10 green and the literal stage-11 `PostgreSQL Not Available` banner/exit 1; evidence is `.quickscale/sa117e3/evidence/phase5-20260805T130413Z-1182566`, and the frozen host PostgreSQL identity/configuration/readiness plus scratch/FIFO/port cleanup all matched. The current-tip harness was rematerialized for `ffd97e66`, independently approved at SHA-256 `6757f901ba765af6cc7b398596fb9bb4356ed4120255b879b6e35717fb81cff3`, and executed under the approved wrapper: installed-wheel planning and immutable public split-SHA parity passed 12/12, while host restoration and all task-owned Docker cleanup passed. **Pending-Blocking:** `SA117E3-PUBLIC-ANALYTICS-001` (**high**, external contract) — installed `apply` exited 1 because public analytics reports module version `0.80.0` while core/source require `0.87.0`; the harness evidence is `.quickscale/sa117e3/runs/sa117e-harness.FNO2Eq/evidence/` and wrapper evidence is `.quickscale/sa117e3/evidence/harness-wrapper.20260805T132328Z.1206300/`. Internal-DB/all-module acceptance and Phase 6 `make ci-e2e` were not reached. **Decisions needed:** `SA117E3-ORDER-001` must reconcile the current harness-before-SA117e-4 requirement with SA117e-4's ownership of the human-gated split refresh: either explicitly authorize a reviewed pre-close split refresh without bypassing SA117e-4's two blockers and twelve-row confirmation, or move the public-harness success gate after SA117e-4 and revise the dependency contract. This checkpoint authorizes no push, tag, publication, completion, or release-readiness claim; `SA117E3-EXEC-001` is consumed.
  - **SA117e-3 correction candidate recorded (2026-08-05; pending independent review; task remains unchecked).** The blocked restore assertion now derives its expected port from `connections["default"].settings_dict["PORT"]`, matching the production restore command and preserving the stage-11 oracle. The sibling hardcoded `"localhost"` expectation remains unchanged and out of scope. **`SA117E3-EVID-001` hash provenance is reconciled without rewriting historical evidence:** `13bca454b297fb8f28e60e93dadabda1c11b4a58d9f13827ead816d79610ed2a` covers the derived summary projection over identity/readiness/default_connection/server_identity bytes from Phase 1 `manifest.json` and Phase 2 `pre-mutation.json`; `547b44b8384be36806fc779fe697e4e679fc35253ca7d40aa322c95b1a1b3c53` is the SHA-256 of the raw `00-config.projection.json` freeze-artifact bytes and is authoritative/byte-identical to the live projection. Stage 11 and Phase 6 remain unproved; no completion, tag, split push, publication, or release-readiness is claimed.
  - **SA128d and SA128 COMPLETE — structural parity checker accepted (2026-08-05; functional commit `6fcb8c35`).** Accepted evidence: `poetry run pytest scripts/test_gate_parity.py` — **186 passed, exit 0**; `poetry run ruff check scripts/check_gate_parity.py scripts/test_gate_parity.py` and `poetry run ruff format --check scripts/check_gate_parity.py scripts/test_gate_parity.py` — **exit 0**; `make typecheck` — **exit 0**; `make quality` — **exit 0**; `poetry run python scripts/check_gate_parity.py` — **exit 1**; `make check-gate-parity` — **exit 2**; and `git diff --check` — **exit 0**. The direct checker and Make wrapper emitted the identical five publish omissions, which remain open for SA122b-4; no publish parity is claimed. Adaptive-change-review DC-58 returned **`STATUS: ok`** (confidence 96), resolved `F-001` by full-scope review, and left no remaining findings or blockers. SA122b-1 is now the next/startable Track 1 child.
  - **SA128d `F-001` root cause diagnosed (2026-08-05; read-only analysis; documentation only; no source files touched; no ticket state changed).** The three prior attempts each added or refined bounds and still failed full-scope review, so `F-001` was re-examined at source rather than re-attempted. **One root cause explains all three reported symptoms: every bound is evaluated *after* the step that can exceed it, never during**, so the checker can only detect overflow already materialized — which is why "prove overflow while producers are live" is unsatisfiable in the current shape. The measurements are sound; the sampling cadence is not. `_bash_observation_file_bytes` (`scripts/check_gate_parity.py:1704-1723`) is correct and is kept. Instances: (1) `_communicate_bounded`'s selector loop (`:886-899`) calls `check_total_bound()` only inside `drain()`, which runs only on **pipe** readability, so an observer writing solely to file channels leaves `select()` blocked for the whole remaining deadline while recorder/launch/wait/worker files grow unsampled; (2) the combined launch+wait budget (`:2099-2105`) reads both logs to completion under *per-file* bounds before testing the sum, materializing up to **2×** the aggregate; (3) the combined worker-frame budget (`:2110-2120`) consumes an entire worker file before testing `_BASH_MAX_FRAMES`. The ordered corrective plan — time-based sampling cadence, one shared budget object threaded through the three readers, removal of harness pre-parsing, and producer-live overflow proof — plus new verify criteria are recorded on the SA128d ticket in `docs/technical/roadmap.md`; the two-file allowlist and every standing invariant are unchanged. Also recorded: the two allowlisted files import only stdlib and pytest, so **SA128d needs no PostgreSQL or Docker** and does not contend with Track 3 for serialized infra — only CPU during its repo-wide `make quality`/`make typecheck`. `SA128D-EXEC-001` remains open and is now an implementation-and-review round rather than a diagnostic one. The single-row "open child work" table was collapsed to a pointer, SA128d being the sole remaining child. No executable delta, no ticket closed, checker still non-authoritative, SA122b still barred from starting.
  - **`SA117E3-EXEC-001` GRANTED — Track 3 becomes truly green and the critical path is live again (2026-08-05; documentation only; no source files touched; no ticket closed).** The maintainer authorized **one fresh execution/review budget round** for SA117e-3, to be passed to the control plane as `user_authorization`. The already-approved narrow correction plan is reused **verbatim** — the prior stop was a spent `plan_review_cycles=2/2` allowance, not a design objection, and that plan holds two plan-review `STATUS: ok` passes resolving `F-001`, `F-002`, and `ORCH-GATE-001`. **The grant is per-attempt and waives no gate:** the three-file executable slice still needs full-scope `STATUS: ok`, the stage-11 negative control and Phase 6 `make ci-e2e` still gate the checkbox, and an attempt ending without one records its retrospective and needs fresh authorization. **Ordering inside the round:** land the correction and its review, reconcile `SA117E3-EVID-001` there or in a prior read-only evidence step, then resume Phase 5 from **5a** and Phase 6 in exclusive Docker/PostgreSQL capacity. **Execution and merge-back happen on the Track 3 worktree, which syncs `v87` first**; `v87` carries only this authorization record. **Sibling observation recorded, deliberately not fixed:** the same assertion block hardcodes `"localhost"` (`quickscale_modules/backups/tests/test_services.py:2881`) and `HOST` reaches it through the identical `QS_BACKUPS_DB_HOST` → `tests/settings.py:67` contract as the port, so it is the same defect class as `SA117E3-BLK-001`; approved Phase 5b redirects only `QS_BACKUPS_DB_PORT`, so it neither blocks the run nor justifies scope widening, and it is ticketed only if a later run redirects the host. **Board effect:** Track 3 (head SA117e-3) flips to **can start / can finish / can merge — truly green** and is the only track making critical-path progress; Track 1 (head SA128d) stays blocked on the still-open `SA128D-EXEC-001` and, being off the path, correctly yields PG/Docker priority; Track 2 (SA115) is unchanged with two hard dependencies. **Open maintainer decisions drop from four to three**, none on the critical path. No executable delta, no push, no tag, no ticket closed.
  - **Roadmap cleaning pass (2026-08-05; documentation only; no ticket state changed, no ticket closed).** **Closeout transfer:** no ticket closed this cycle, so nothing new was transferred; instead the roadmap's two duplicated checkpoint narratives were collapsed against the entries already recorded here. **SA117e-3**'s three stacked blocks (`SA117E3-HANDOFF-004`, `SA117E3-HANDOFF-005`, and the separate acceptance-boundary paragraph) became one current-checkpoint paragraph — the per-phase evidence paths, harness/wrapper SHA-256 recital, `F-003`–`F-010` history, `STOP_EFFECT_OBSERVED`/`RESTORE_FAILED` narrative, test counts, and the `plan_review_cycles=2/2` refusal mechanics are all recorded in the two SA117e-3 checkpoint entries below and were deleted from the roadmap; it now keeps only the open blockers (`SA117E3-BLK-001`, `SA117E3-EVID-001`), the approved three-file correction's binding constraints, the `SA117E3-EXEC-001` decision, and the forward-looking Phase 5 steps, plan invariants, execution parameters, and PostgreSQL-lifecycle follow-up note. **SA128d**'s checkpoint was reduced the same way to the preserved candidate (`d2b3ac24`, sync merge `21f35506`), the exact declared publish gap, and the open `F-001` correction obligation. **Contradiction fixed:** SA128d's checkpoint read `Decisions needed: none` while also requiring "fresh explicit continuation"; that is now the named decision **`SA128D-EXEC-001`** and appears in the open-decisions paragraph, raising the open count from three to four. **De-duplication:** track state had been stated three times (readiness table, a prose paragraph, and each track's `Status:` line); the table is now authoritative and the prose/status lines were trimmed to what the table cannot carry. The SA128 attempts table was left intact — all three rows are *rejected* designs, which is exactly what that table exists to preserve. **Audit reconciliation:** nothing transferred — `tech-audit.md` carries zero open findings (`TA1`–`TA62` reconciled), arch Finding 12 stays closed by SA121, and arch Findings 11 (ticketed as SA122/SA128), 7 (unscheduled), and 2/4 (deferred with `teams`) remain open with their context still load-bearing. **Track assignment and rebalancing:** every open v87 ticket already carries a track and **no move was applied** — the sixth consecutive pass, for the unchanged structural reason: Track 2 is closed to new work by standing rule, Track 3's SA117e/SA112 children are one coherent serial umbrella on the critical path, and Track 1's queue (SA128d → SA122b-1 … -5) is one dependency-chained series with no independent member to relocate. **Track states — no track is truly green:** Track 3 (head SA117e-3, on the critical path) and Track 1 (head SA128d, off it) can each merge their recorded partial but can neither start nor finish, both blocked by **maintainer decisions** rather than upstream work; Track 2 (SA115) can start but can neither finish (**hard dep** SA112d) nor merge (**hard dep** SA112e), and no decision reaches either. **Open maintainer decisions: four, two live today** — `SA117E3-EXEC-001` (critical path) and `SA128D-EXEC-001` (off path), each an execution/review budget round for an already-approved correction, plus SA117e-4's `SA117E1-REV-001` lease-syntax ratification and `SA117E1-REV-002` disposition (not needed until that child implements). **Conflict surface** remains the shared closeout files (`CHANGELOG.md`, `roadmap.md`, `decisions.md`), covered by the mandatory `git merge v87`-before-merge-back step. No source file, script, workflow, tag, split ref, package registry, Docker resource, or PostgreSQL service changed.
  - **SA117e-3 third recorded-partial checkpoint — narrow correction fully planned and approved, execution budget declined (2026-08-05; task remains unchecked; no tracked correction).** After the stage-10 hardcoded-port blocker was merged as checkpoint `e9a8744a`, the maintainer authorized a separate three-file correction. The final plan changes only `quickscale_modules/backups/tests/test_services.py`, `docs/technical/roadmap.md`, and `CHANGELOG.md`; derives the expected restore port from `connections["default"].settings_dict["PORT"]`; proves default and Docker-assigned nondefault ports; reconciles legacy metadata digest `13bca454…` against canonical full-projection digest `547b44b8…` without changing historical evidence; requires independent full-scope review before commit; preserves reviewed patch bytes through merge; and retains the approved stage-11 negative control plus final `make ci-e2e`. Plan-review returned **`STATUS: ok`** twice in the user-authorized round, resolving `F-001`, `F-002`, and `ORCH-GATE-001`. **Pending-Blocking:** those two passes consumed `plan_review_cycles=2/2`; the first implementation preflight was refused because no review allowance remained for a produced delta. The maintainer selected stop-and-record instead of a fresh execution/review round. `SA117E3-EXEC-001` is the sole clean-continuation decision: authorize that round and execute the approved plan verbatim. No test, production, historical evidence, Docker, host PostgreSQL, tag, split, package, or public remote changed in this checkpoint.
  - **SA128d recorded-partial checkpoint — cross-cutting parity candidate preserved; aggregate event transport remains unapproved (2026-08-05; task remains unchecked; checker non-authoritative).** **Done:** the two-file candidate preserved at functional commit `d2b3ac24` implements canonical no-bypass containment for consumed paths, arbitrary dependency-cycle handling, uniform controlled failures, bounded observer lifecycle, and the complete adversarial/caller-parity matrix; Track 1 synchronized with concurrent `v87` work as merge `21f35506`. Validation passed with **183 focused tests**, `make typecheck`, `make quality`, formatter, and `git diff --check`; the declared publish gap remains exact — direct checker exit **1** and Make wrapper exit **2**, both reporting the same five publish-missing records. Inherited **SA128a/F-003** is resolved by the deadline-aware communication lifecycle and did not survive the terminal review ledger. **Pending-Blocking:** `F-001` (**high**, completeness; `scripts/check_gate_parity.py` and `scripts/test_gate_parity.py`) remains open because file-backed recorder/launch/wait/worker channels and harness pre-parsing are not one live aggregate-bounded event stream; overflow proof can therefore pass only after channels have already grown without the checker observing them. A clean correction must bound every channel under one aggregate budget, remove unbounded harness pre-parsing, and prove producer-live overflow including combined worker frames. Two executable full reviews ended **medium / 1 → high / 1**, so unresolved count did not decrease and highest severity increased; no `STATUS: ok` was obtained. **Decisions needed:** none — the maintainer selected recorded partial delivery and merge-back at the cap. SA128d and SA128 remain open, SA122b cannot start, and this preservation merge is not executable approval, finding waiver, checker authority, completion, or release readiness.
  - **SA117e-3 second recorded-partial checkpoint — revised negative control exposed a tracked test-contract blocker (2026-08-05; task remains unchecked; no public mutation).** Track 3 started clean, fast-forwarded to `v87` `b151073c513663f08adcd88d472564e98a18b852`, and obtained scoped plan-review **`STATUS: ok`** for the task-owned PostgreSQL endpoint design. Immutable preflight passed at the approved harness/lifecycle hashes; PostgreSQL 18 scratch endpoint `127.0.0.1:32773` was ready; and the approved wrapper emitted exactly one `STOP_EFFECT_OBSERVED`. The one bounded `bash scripts/check_ci_locally.sh --e2e` attempt exited the wrapper 0 with no `RESTORE_FAILED` and restored the same frozen host container to running/ready; subsequent parent/recovery cleanup confirmed the scratch container, port binding, and FIFO absent. **Pending-Blocking:** stage 10 stopped after 4,717 core/CLI passes and backups 322 passed / 2 skipped / 1 failed because `TestBackupLifecycle::test_restore_file_mode_executes_pg_restore_for_operator_dump` hardcodes expected port `5432` while the real environment-backed restore command correctly used `QS_BACKUPS_DB_PORT=32773`; stage 11 and `make ci-e2e` were not reached. `SA117E3-BLK-001` requires a separately approved narrow tracked test-fix handoff before rerun; `SA117E3-EVID-001` requires reconciliation of the differently reported Phase 2/3 projection hashes into one canonical oracle. Evidence: `.quickscale/sa117e3/evidence/phase1-2026-08-05T074404Z-26ac5068/manifest.json` and `.quickscale/sa117e3/evidence/phase3-2026-08-05T081932Z-dc39b/`. No production code, test, tag, split, package, or public remote was changed; this checkpoint preserves the blocker and does not complete SA117e-3.
  - **Roadmap cleaning pass (2026-08-04, third pass of the day; documentation only; no ticket state changed, no ticket closed).** **Closeout transfer:** the checked **SA128c** child block was deleted from `docs/technical/roadmap.md` — its verify list (16 publish `run` values, 10 bound hosted `run` values, `needs`/stage topology, uniform E2E path extraction, the five preserved publish omissions, the two retired SA122a MyPy errors) is already recorded in the SA128c COMPLETE entry below. The SA128 attempts table's row 3 was removed for the standing reason that the table preserves **rejected** designs and SA128c was accepted. **Correction to the SA128c COMPLETE entry below:** that entry recorded acceptance for integration with the closeout merge not yet performed; the merge has since occurred — functional commit `82202674` reached `v87` as merge `2eae365a`, so SA128a, SA128b, and SA128c are now closed **and merged**, and the roadmap's pre-integration wording was replaced accordingly. The only surviving finding from the three closed children is **SA128a/F-003** (medium/advisory, `_communicate_bounded` resilience), owned by **SA128d**. **Audit reconciliation:** nothing transferred — `tech-audit.md` carries zero open findings (`TA1`–`TA62` reconciled), arch Finding 12 stays closed by SA121, and arch Findings 11 (ticketed as SA122/SA128), 7 (unscheduled), and 2/4 (deferred with `teams`) remain open with their context still load-bearing. **Track assignment and rebalancing:** every open v87 ticket already carries a track and **no move was applied**, for the unchanged structural reason — Track 2 is closed to new work by standing rule, Track 3's SA117e/SA112 children are one coherent serial umbrella on the critical path, and Track 1's remaining queue (SA128d → SA122b-1 … -5) is one dependency-chained series with no independent member to relocate; moving any of it to Track 3 would put off-path filler ahead of the critical path. **Track states:** Track 3 (head SA117e-3) can merge its recorded partial but can neither start nor finish — blocked on the **maintainer acceptance decision** for the unreachable stage-11 negative control; it is on the critical path. Track 1 (head SA128d) is truly green on all three states, off the critical path. Track 2 (SA115) can start but can neither finish (**hard dep** SA112d) nor merge (**hard dep** SA112e). **Open maintainer decisions: three** — the SA117e-3 stage-10/stage-11 acceptance boundary (blocks the critical path today), plus SA117e-4's `SA117E1-REV-001` lease-syntax ratification and `SA117E1-REV-002` disposition (not needed until that child implements). **Conflict surface** remains the shared closeout files (`CHANGELOG.md`, `roadmap.md`, `decisions.md`), covered by the mandatory `git merge v87`-before-merge-back step. No source file, tag, split ref, package registry, Docker resource, or PostgreSQL service changed.
  - **SA117e-3 recorded-partial checkpoint — approved pre-use artifacts and interruption proof preserved; local-CI negative control is unreachable (2026-08-04; task remains unchecked; no public mutation).** **Done:** the ignored current-tip installed-wheel harness and host-PG lifecycle wrapper received fresh full-file approval at exact SHA-256 `68787fd487795b4c9dfae9e87448dc495eade65c0efdcb89b38364582ea9109e` and `41741154a84e26f3944464e40c4eb297c0f9985b94fe0ff560c7351922213dfd`, resolving pre-use findings `F-003`–`F-010`. The Phase 3 stop-effect-before-bookkeeping proof observed `STOP_EFFECT_OBSERVED`, delivered TERM before normal bookkeeping, returned 143, and restored the same immutable PostgreSQL container/configuration projection to running and ready with no `RESTORE_FAILED`; evidence is `.quickscale/sa117e3/evidence/phase3-2026-08-04T164407Z-784108/`. Phase 4 verified all twelve source SHAs but observed public analytics at version `0.80.0` against core `0.87.0`; this was diagnostic only and transfers to SA117e-4's human-gated public mutation. **Pending-Blocking:** Phase 5 evidence `.quickscale/sa117e3/evidence/phase5-20260804T185416Z/` proves `bash scripts/check_ci_locally.sh --e2e` exits during stage 10 when host PostgreSQL is unavailable because `make test-cov REQUIRE_BACKUPS_COVERAGE=1` requires PostgreSQL; the intended stage-11 PostgreSQL-not-available branch/banner is never reached. The wrapper restored the same host service successfully, but `make ci-e2e` has not run at current tip `2eae365a490c8dbfe074aa8b3a3ab040b3eaee68`. Evidence was gathered at `9f5092f719e59a38ae6bed5f0bc6f27934cab7cf` and does not complete the advanced tip. **Decisions needed:** a later continuation must obtain a revised scoped plan choosing either an alternate task-owned PostgreSQL endpoint that keeps stage 10 green while the frozen host endpoint remains unavailable for stage 11, or revision/removal of the unreachable stage-11 negative control. The maintainer selected recorded partial delivery. This checkpoint is preservation only: SA117e-3, SA117e, and SA117 remain open; no tag, split push, installed public apply, PyPI publication, completion, or release-readiness is claimed.
  - **SA128c COMPLETE — hosted, publish, and E2E structural parity accepted (2026-08-04; review-approved pre-integration documentation closeout).** The checker now covers hosted commands, `needs`, stage topology, all **16** publish `run` values through bounded Bash observation, and uniform E2E path extraction. **Accepted validation:** **155 tests**, five focused regressions, strict MyPy, Ruff, `make typecheck`, `make quality`, and `git diff --check` all pass. The checker intentionally still reports exactly the five publish omissions: direct execution exits **1**, while the unchanged Make wrapper exits **2** with the same records; SA122b-4 remains responsible for closing them. **Review pass 1** recorded **F-001** (medium — complete ordered literal oracle for all 16 publish run values) and **F-002** (high — fresh bounded recorder session per publish step and no-state-leak regression). **Review pass 2 returned `STATUS: ok`** and confirmed both findings resolved. Track 1 advances to **SA128d**, which remains pending and is where checker authority begins; the SA128 umbrella stays open. This records acceptance for integration only; the closeout commit or merge has not yet occurred.
  - **SA128a COMPLETE — Make-context parity checker rebuilt on GNU Make semantics (2026-08-04; functional commit `ba26cb1b`).** `_extract_makefile_targets` in `scripts/check_gate_parity.py` now derives the Make inventory from Make's own `-qp`/`--dry-run` semantics — `$(VAR)`-composed targets, `include`, delegation, and `check` aggregation are observed through Make rather than a hand-reimplemented grammar; the Make context reproduces today's inventory with no false differences. **Validation (QG DC-67 `STATUS: ok`):** **139 focused tests** plus **24 focused aggregation/include tests**, strict MyPy, Ruff, `make quality` and `make typecheck` exit 0, `git diff --check` green; the checker still reports exactly the five publish omissions — direct execution exits **1** and the unchanged `make check-gate-parity` wrapper exits **2** (the declared SA122b-4 gap; no tolerance added). **Full-scope review (DC-68) `STATUS: ok`:** **F-001** (high, blocking — check reachability) and **F-002** (medium, blocking — aggregation/include test gap) are **resolved**; **SA128a/F-003** (medium, advisory — `_communicate_bounded` resilience) remains open and is owned by **SA128d**, separate from SA128b's F-003. **No unresolved prerequisite or open decision blocked SA128a.** The roadmap SA128a item is checked and the attempt recorded in the SA128 attempts table; Track 1's head advances to **SA128b**. The SA128 umbrella stays open and the checker remains non-authoritative until SA128d.
  - **SA128b COMPLETE — shell inventories observed by actual Bash (2026-08-04; functional commit `7e35556c`).** The checker now observes both local shell inventories with actual bounded Bash, a controlled recorder `PATH`/environment, inherited-errexit fidelity, exact argv framing, explicit parallel launch/wait/order/failure proof, residual process-group cleanup, and a deterministic completion-order handshake. **Accepted evidence:** **151 focused tests**, static and output evidence, strict MyPy, Ruff format/lint, and diff checks all pass; Linux `/proc` is a declared platform constraint, not a blocker. The checker intentionally still reports exactly the five publish omissions: direct execution exits **1**, while the unchanged Make wrapper exits **2** with the same records. **Full-scope review `STATUS: ok`:** **SA128b/F-001** (errexit), **SA128b/F-002** (parallel lifecycle/order), and **SA128b/F-003** (argv transport) are resolved; no active SA128b blocker or advisory remains. **SA128a/F-003** is a separate medium/advisory `_communicate_bounded` resilience item, remains open, and is owned by **SA128d**. One user-authorized continuation was consumed. The roadmap SA128b item is checked and Track 1's head advances to **SA128c**; SA128 remains open and the checker remains non-authoritative until SA128d.
  - **Roadmap cleaning pass (2026-08-04, second pass of the day; documentation only; no ticket state changed, no ticket closed).** **Closeout transfer:** the checked **SA128b** child block was deleted from `docs/technical/roadmap.md` — its functional commit `7e35556c`, accepted evidence, full-scope review outcome, and the `SA128b/F-001`–`F-003` resolutions are already recorded in the SA128b COMPLETE entry above; the roadmap now carries a single combined closed-note for SA128a and SA128b, preserving only the forward-looking fact that **SA128a/F-003** (`_communicate_bounded` resilience, medium/advisory) stays open under **SA128d**. The SA128 attempts table's rows 3–4 and their duplicate summary line were removed for the same reason — the table exists to preserve *rejected* designs, and both closed children succeeded — replaced by one sentence pointing here. The duplicate "SA128d carries the remaining cross-cutting proof work" paragraph was dropped as a restatement of the open-child-work table directly beneath it. **Audit reconciliation:** no arch-audit or tech-audit finding became newly closed this pass — `tech-audit.md` carries zero open findings (`TA1`–`TA62` reconciled), arch Finding 12 remains closed by SA121, and arch Findings 11 (ticketed as SA122/SA128), 7 (unscheduled), and 2/4 (deferred with `teams`) remain open with their context still load-bearing, so nothing was transferred from either document. **Track assignment and rebalancing:** every open v87 ticket already carries a track and **no move was applied**, for the unchanged structural reason — Track 2 is closed to new work by standing rule (homing anything there drags merge-gated SA115 onto `v87`), Track 3's SA117e/SA112 children are one coherent serial umbrella on the critical path, and Track 1's remaining queue (SA128c → SA128d → SA122b-1 … -5) is one dependency-chained series with no independent member to relocate; moving any of it to Track 3 would put off-path filler ahead of the critical path. **Track states:** Track 3 (head SA117e-3, Phase 2) can start and can merge but cannot finish until `F-003`–`F-010` are resolved and a fresh pre-use review returns `STATUS: ok`; Track 1 (head SA128c) is truly green on all three states, off the critical path; Track 2 (SA115) can start but can neither finish (**hard dep** SA112d) nor merge (**hard dep** SA112e). **Open maintainer decisions: two**, both owned by SA117e-4 and neither blocking today — the explicit-absence Git lease contract (`SA117E1-REV-001`) and the bind-and-consume versus approved de-advertise disposition (`SA117E1-REV-002`). **Conflict surface** remains the shared closeout files (`CHANGELOG.md`, `roadmap.md`, `decisions.md`), covered by the mandatory `git merge v87`-before-merge-back step. Track 3 was running during this pass; only `v87`-owned documentation was edited.
  - **Roadmap cleaning pass (2026-08-04; documentation only; no ticket state changed, no ticket closed).** **Closeout transfer:** the checked **SA112a** ticket block was deleted from `docs/technical/roadmap.md` — its four-file allowlist, approved seam signature `quickscale_provision_installed_venv REPO_ROOT OUTPUT_DIR`, the six ordered build/install stderr markers, the four allocation classes and signal/exit contract, the plan-gate and continuation-plan recitals, and the completion State block are all superseded by the SA112a COMPLETE entry above; the roadmap now carries a two-sentence closed-note pointing here. The checked **SA117e-2** child block was likewise deleted and replaced by a one-line closed-note; its evidence lives in the SA117e-2 COMPLETE entry above. **Consistency edits following those deletions:** the dependency diagram's `SA112a ✓ (Track 1)` annotation and a duplicated `required from b on` line were removed; the cross-track edge count dropped from three to **two** (SA122b-5 after SA112e, SA115 after SA112) with the SA112a→SA112b edge recorded as **discharged**; SA112b's tag is now `deps: SA117e-4` alone; SA117e-3's tag reads `deps: SA117e-2 ✓ closed`; the Track 1 readiness row, Track 1 status line, Track 3 status paragraph, shared-executable-surfaces paragraph, and Track topology bullets were rewritten to state SA112a's placement as discharged rather than in progress; and `SA112A-RESET-001` was dropped from the open-decision paragraph (it remains listed among earlier resolved authorizations). `arch-audit.md`'s orientation summary was corrected from "SA112a now parallelized onto Track 1" to SA112a closed with SA112b–f remaining. **Audit reconciliation:** no arch-audit or tech-audit finding became newly closed this pass — tech-audit has zero open findings with `TA1`–`TA62` already reconciled to this file, arch Finding 12 was closed by SA121, and Findings 11/7/2/4 remain open with their context still load-bearing, so nothing was transferred from either audit. **Track rebalancing:** reviewed, **no move applied**. Track 2 is closed to new work by standing rule; Track 3's SA117e/SA112 children are one serial umbrella on the critical path; Track 1's remaining queue (SA128a→d, SA122b-1→5) is a single dependency-chained series with no independent member to relocate, and moving any of it to Track 3 would put off-path filler ahead of the critical path. **Open decisions are unchanged at three, all the maintainer's:** `SA117E3-RESET-001` (blocks Track 3 today) plus SA117e-4's two implementation-time decisions.
  - **SA117e-3 recorded-partial checkpoint — approved continuation plan preserved; pre-use executable review remains blocked (2026-08-03; task remains unchecked; no tracked source or host-service mutation).** **Done:** Track 3 started clean, fast-forwarded to `v87` `fbde0a82c7819f05e572eddaec501ee2b28e4959`, verified the original ephemeral harness SHA `53762f9dd837679808754094d0b4f199665f801ca91f2d69710b2115651c552b`, and obtained independent plan-review **`STATUS: ok`** for the durable nine-phase continuation now recorded verbatim on the roadmap, resolving plan finding `F-002`. Phase 1 froze the source/v87 tip and original running/ready `quickscale-ci-postgres` identity/configuration with no stop/start. Worktree-local ignored harness/wrapper candidates passed Bash syntax/static checks, but the first full-file pre-use review returned **`STATUS: partial`**; the rejected hashes are evidence only and authorize no execution. **Pending-Blocking:** `F-003`/`F-004` require worktree-compatible, parent-config-isolated Git handling in the harness; `F-005`/`F-006` require immutable full-ID Docker targeting and exact frozen running/ready preflight; `F-007`/`F-008` require signal-safe reconciliation and truly bounded stop/readiness deadlines; `F-009`/`F-010` require a prevalidated private FIFO and canonical contained evidence paths. No interruption proof, harness run, host-PG negative control, `make ci-e2e`, tag, split push, installed apply, or publication ran. **Decisions needed:** `SA117E3-RESET-001` — restore the write-capable provider and perform a supported reset of the persisted changed-file scope after two empty correction handbacks spent `infra_retries=1/1` and the user-authorized retry allowance; then resume the roadmap's approved plan at Phase 2 with all eight findings carried, without re-planning unless scope/topology changed. The maintainer selected recorded partial delivery and merge-back; this is preservation, not a finding waiver, executable approval, completion claim, or release-readiness evidence.
  - **SA112a COMPLETE — installed-wheel provisioner approved at frozen HEAD `44938f75760bee0f51aa0c289754ff4c865d966d`; completion merged to `v87` as `1cee5823` (2026-08-03).** The exact four-file slice (`scripts/smoke_install.sh`, `scripts/_installed_wheel_venv.sh`, `scripts/provision_installed_venv.sh`, `scripts/test_installed_wheel_venv.py`) is approved with SHA256 smoke=`46af240c…`, helper=`ba36084e…`, wrapper=`72b968c3…`, tests=`0c5b3ebf…`. **QG (DC-52) `STATUS: ok`:** Bash syntax; **67 focused tests**; two positive `make smoke-install` runs with the exact six markers and all 20 probes; the missing-manifests negative control produced gate script exit **1** / GNU Make wrapper exit **2** with `ImproperlyConfigured` and no `ImportError` — the expected red control, not a failure; the expected parity exit **2** with the five publish omissions; lint, typecheck, diff, and NUL-safe exact-scope checks. **Full-scope review (DC-53) `STATUS: ok`:** **F-005** (medium, correctness — probe-state restoration after `..` cancellation) **resolved**, caller parity passed, no breaking changes or test gaps. **F-006** (low/advisory — unreadable-directory hardening) remains **open and out of SA112a's correction scope**. The roadmap SA112a item is checked and Track 1's head advances to **SA128a**; the completion merge to `v87` satisfies SA112b's precondition. The `SA112A-HANDOFF-001`…`-004` attempt history and `SA112A-RESET-001` reset record are below; this entry supersedes their open-state language.
  - **SA117e-2 COMPLETE — rebaselined ordered source validation approved (2026-08-03; documentation-only closeout; no source files touched).** The existing five-command ordered validation against `SA117_BASELINE_REF=1333eb4a3558941f45ac70b2b76eb296a1c0faeb` remains the completion evidence and was not rerun: every command exited 0, `make check` and `make quality` were green, and no source delta existed. The fresh full-scope independent closeout review authorized by `SA117E2-RESET-001` covered exactly `docs/technical/roadmap.md` and `CHANGELOG.md`, returned **`STATUS: ok`**, and closed `SA117E-VAL-002`. **Findings/blockers at that closeout:** none remained for SA117e-2; downstream plan-review finding `F-002` was then owned by SA117e-3 and was later closed at plan-detail level by DC-25, with implementation blockers `F-003`–`F-010` now superseding it as recorded above. The two publication blockers remain owned by SA117e-4. Track 3 advanced to SA117e-3; no host PostgreSQL mutation, tag, split push, installed apply, or publication was claimed by this closeout.
  - **Roadmap cleaning pass (2026-08-03, fourth pass of the day; documentation only; no ticket state changed).** **Closeout transfer:** the **SA112a** ticket's two per-attempt retrospective blocks (`SA112A-HANDOFF-003` and `-004`) and the `HANDOFF-003` grant paragraph were collapsed in `docs/technical/roadmap.md` into one authorization-history line plus one current-checkpoint paragraph — every delivery narrative, validation-evidence list, and per-dispatch counter trace they restated is already recorded here in the entries below. The roadmap now keeps only what a continuation needs: that the merged twelve-case candidate supersedes the six-case one, that `F-005` is open with implementation-agent evidence only, that `F-006` stays out of scope, and the `SA112A-RESET-001` blocker. The same duplication was trimmed from the dependency-overview and Track 1 status paragraphs. **Rebalancing review applied no move**, for the fifth consecutive pass and for the unchanged structural reason: every open v87 ticket carries a track; Track 2 is closed to new work by standing rule (homing anything there drags merge-gated SA115 onto `v87`); Track 1's remaining off-path work (SA128a → b → c → d → SA122b-1 … -5) is one serial dependency chain that cannot be spread; and Track 3's SA117e children are one coherent serial umbrella. **Track states:** Track 3 (head SA117e-2) is truly green — can start, finish, and merge — and is on the critical path. Track 1 (head SA112a) is on the critical path but start- and finish-blocked by the `SA112A-RESET-001` **maintainer decision**, not by upstream work; its merge state is yes. Track 2 (SA115) can start but can neither finish (**hard dep** SA112d) nor merge (**hard dep** SA112e); no maintainer decision reaches either. **Open maintainer decisions: three** — `SA112A-RESET-001` (blocks Track 1 today), plus SA117e-4's `SA117E1-REV-001` lease-syntax ratification and `SA117E1-REV-002` disposition (not needed until that child implements). **Conflict surface** remains the shared closeout files (`CHANGELOG.md`, `roadmap.md`, `decisions.md`), covered by the mandatory `git merge v87`-before-merge-back step. **Audit documents verified current:** `tech-audit.md` carries zero open findings; `arch-audit.md` carries Findings 11 (ticketed as SA122/SA128), 7 (unscheduled), and 2/4 (deferred with `teams`).
  - **SA112a recorded partial checkpoint — `SA112A-HANDOFF-004` granted and consumed; narrower `F-005` candidate remains unapproved (2026-08-03; task remains unchecked; not SA112b-ready; no completion claim).** **Done:** Track 1 began clean, merged `v87` with shared-file reconciliation as `c678a42c`, and reused the ready Poetry 2.4.1 / Python 3.14.6 environment. The maintainer granted `SA112A-HANDOFF-004`. DC-48 changed only `scripts/_installed_wheel_venv.sh` and `scripts/test_installed_wheel_venv.py`: the candidate guards the empty component stack/root spellings and adds twelve cases spanning repeated/reverse/root cancellation, accepted symlink-ancestor identity, status/stdout/native-trap behavior, and target preservation. DC-48 self-reported pre-fix reproduction, 67 focused tests, Bash syntax, smoke and missing-manifests controls, baseline-identical parity, lint, typecheck, diff, and exact-scope evidence — implementation-agent evidence only. **Pending-Blocking:** `F-005` (**medium**, correctness) remains open and unapproved because no independent QG result and no approving full-scope `STATUS: ok` review exist. The required QG dispatch was refused before execution even with the exact user authorization: the persisted scope reports `qg_runs=3/2`, `review_cycles=2/2`, and `authorized_retries=1/1`, so the grant restored no executable allowance. `F-006` (**low/advisory**) remains outside scope. SA112a stays unchecked and does not satisfy SA112b. **Decisions needed:** `SA112A-RESET-001` — a future continuation must use a supported control-plane reset that clears all three persisted counters, then rerun the complete ordered QG and obtain full-scope `STATUS: ok`; another authorization-only retry cannot buy either dispatch. At the cap, the maintainer selected `Stop here, record Done / Pending-Blocking / Decisions-needed, and merge the checkpoint`. This merge preserves the candidate only; it is not validation, review approval, a waiver, completion, or release-readiness evidence.
  - **SA112a recorded partial checkpoint — `SA112A-HANDOFF-003` attempt consumed, `F-005` candidate unapproved (2026-08-03; task remains unchecked; not SA112b-ready; no completion claim).** **Done:** Track 1 synced cleanly to `v87` `952806c13f03c7899dbdc40aa09e1f0f4fceda42`; DC-41 implemented the `F-005` correction candidate in `scripts/_installed_wheel_venv.sh` and `scripts/test_installed_wheel_venv.py`, self-reporting reproduction, six added cases, focused tests, positive and negative smoke controls, parity baseline, lint, typecheck, diff, and scope checks — implementation-agent evidence only. **Pending-Blocking:** no independent QG result exists and no approving `STATUS: ok` full-scope review exists — the independent full-scope review (DC-44) over the two scripts plus roadmap and changelog returned `STATUS: partial`. The authorized QG dispatch (DC-42) incremented `qg_runs` to 3/2 but returned empty with no ResultEnvelope or evidence; `record_failed_handback` marked an infrastructure failure, and a fresh shortened retry was refused because `qg_runs` remained 3/2. `F-005` (**medium**, correctness; `scripts/_installed_wheel_venv.sh` / `scripts/test_installed_wheel_venv.py`) remains open and unapproved — the required repeated-cancellation, root-cancellation, accepted `link/../sibling` symlink-ancestor identity, and native caller-trap parity for `missing/../link` rejection tests remain missing; SA112a remains unchecked and does not satisfy SA112b. `F-006` (**low/advisory**) remains outside this correction. **Decisions needed:** fresh maintainer authorization (`SA112A-HANDOFF-004`) restoring **both** one QG dispatch **and** one full-scope review dispatch — the consumed shared override ("one extra cycle on blocking findings only") cannot supply both; the approved continuation plan on the roadmap is reused verbatim. The grant was per-attempt and waived no gate; this checkpoint is not an executable approval, a completion claim, or satisfaction of SA112b's dependency.
  - **`SA117E2-RESET-001` GRANTED — control plane reset for SA117e-2; Track 3 truly green (2026-08-03; documentation only; no source files touched; no ticket closed).** The maintainer reset the SA117e-2 control plane, clearing the consumed `review_cycles=3/2` / `authorized_retries=1/1` ledger and granting **one fresh full-scope independent closeout review** over the child's unchanged two-document scope (`docs/technical/roadmap.md`, `CHANGELOG.md`), to be passed to the control plane as `user_authorization`. **The already-green validation is not re-run:** the five approved commands exited 0 in exact order against `SA117_BASELINE_REF=1333eb4a…` with no source delta, and that evidence stands. **The grant waives no gate** — `SA117E-VAL-002` (**high**, validation) closes only if the review returns `STATUS: ok` and confirms no completion language exceeds the green evidence; a review ending without a checked box records its retrospective and needs fresh authorization; the changed-file scope may not be re-shaped to avoid the cap. **Board effect:** Track 3 (head SA117e-2) moves to **can start yes / can finish yes / can merge yes — truly green**, joining Track 1 (SA112a under `SA112A-HANDOFF-003`) so **both critical-path legs are live**. Track 2 (SA115) is unchanged: can start, cannot finish (**hard dep** SA112d), cannot merge (**hard dep** SA112e). **Open maintainer decisions drop to two**, both owned by SA117e-4 and not needed until it implements — the `SA117E1-REV-001` explicit-absence lease-syntax ratification and the `SA117E1-REV-002` bind-and-consume versus approved de-advertise/defer disposition. Downstream ordering is untouched: SA117e-3 still needs its own scoped plan review (`F-002`) before any host-service mutation, SA117e-4 still carries the execution-time twelve-row human push confirmation, and SA112b still waits on SA117e-4's pushed splits plus SA112a's completion merge.
  - **Roadmap cleaning pass (2026-08-03, third pass of the day; documentation only; no ticket state changed).** **Closeout transfer:** SA117e-2's checkpoint narrative in `docs/technical/roadmap.md` was reduced to its blocking condition — the per-command evidence paths, the 106-path evidence digest, and the 2,620-core/2,043-CLI test counts are already recorded here, and Track 3's status paragraph was trimmed of the same duplication. In `arch-audit.md`, the resolved former **Finding 12** (`quality-baseline-monotonicity-unenforced`, SA121) reconciliation line was reduced to a dated pointer, matching the tech-audit convention that closed findings survive only as dated log lines. **Rebalancing review applied no move**, for the fourth consecutive pass and for the unchanged structural reason: every open v87 ticket carries a track; Track 2 is closed to new work by standing rule (homing anything there drags merge-gated SA115 onto `v87`); Track 1's remaining off-path work (SA128a → b → c → d → SA122b-1 … -5) is one serial dependency chain that cannot be spread; and Track 3's SA117e children are one coherent serial umbrella. **Track states:** Track 1 (head SA112a) is truly green — can start, finish, and merge — and is on the critical path via the `F-005` correction under the granted `SA112A-HANDOFF-003`. Track 3 (head SA117e-2) can merge its recorded partial but can neither start nor finish the next child: `SA117E-VAL-002`'s mandatory full-scope closeout review cannot dispatch at `review_cycles=3/2` with `authorized_retries=1/1` spent — a **maintainer decision** (grant a fresh review budget or reset the control plane), not upstream work. Track 2 (SA115) can start but can neither finish (**hard dep** SA112d) nor merge (**hard dep** SA112e); no maintainer decision reaches either. **Open maintainer decisions: three** — the SA117e-2 review-budget grant (blocks the critical path today), plus SA117e-4's `SA117E1-REV-001` lease-syntax ratification and `SA117E1-REV-002` disposition (not needed until that child implements). **Conflict surface** remains the shared closeout files (`CHANGELOG.md`, `roadmap.md`, `decisions.md`), covered by the mandatory `git merge v87`-before-merge-back step. **Audit documents verified current:** `tech-audit.md` carries zero open findings; `arch-audit.md` carries Findings 11 (ticketed as SA122/SA128), 7 (unscheduled), and 2/4 (deferred with `teams`).
  - **SA117e-2 second recorded-partial checkpoint — complete ordered validation is green; mandatory closeout review is cap-blocked; child remains unchecked (2026-08-03; documentation only; no source files touched).** **Done:** Track 3 started clean at `f364298e` after fast-forwarding `v87`, then ran the approved five-command chain in exact order, each exiting **0** with no source delta and the repository left clean: (1) `make sa117-lock-diff SA117_BASELINE_REF=1333eb4a3558941f45ac70b2b76eb296a1c0faeb` — clean normalized structures, evidence in `/tmp/sa117-lock-diff-evidence.json`; (2) `make sa117-capture VERSION=0.87.0 PHASE=final` — evidence captured at `/tmp/opencode/sa117-evidence/sa117_evidence_20260803T122400Z.json`, **106 paths**, digest `d41e698c05affcbd…`; (3) `make sa117-verify EVIDENCE=/tmp/opencode/sa117-evidence/sa117_evidence_20260803T122400Z.json` — **`VERIFY PASSED`**, matching digest; (4) `make check` — all checks passed, **2,620 core tests passed + 1 skipped**, **2,043 CLI tests passed**, lint/type/compatibility/frontend green; (5) `make quality` — baseline gate passed, **0 warning regressions and 0 critical regressions**, reports in `.quickscale`. Both evidence files exist on disk and checker semantics stayed unchanged. **Pending-Blocking:** `SA117E-VAL-002` (**high**, validation) has direct green evidence but remains open in the task ledger because its completion prose could not obtain the mandatory full-scope independent closeout review: the control plane reports `review_cycles=3/2` and `authorized_retries=1/1`, and refused the user's additional continuation authorization. SA117e-2 therefore remains unchecked, Track 3's head remains SA117e-2, and SA117e-3 host-service work remains forbidden. **Decisions needed:** none on repository design — the maintainer selected recorded partial delivery; clean continuation requires a supported fresh review budget or control-plane reset, never a ledger bypass. This checkpoint preserves evidence only and is not a waiver, completion claim, permission to start SA117e-3, or release-ready approval.
  - **SA117e-2 recorded-partial checkpoint — ordered command 1 exposed lock drift; replacement baseline decision recorded; child remains unchecked (2026-08-03; documentation only; no source delta; no ticket closed).** **Done:** Track 3 began clean, merged `v87` (already up to date), reused Poetry 2.4.1 and its worktree-local `.venv`, then started the approved five-command chain in exact order. Command 1, `make sa117-lock-diff SA117_BASELINE_REF=43d9b8fc0abd1c3193a3358f41dee0e09a3c106c`, exited **2** at the make level (checker exit **1**) with `SA117 lock-diff: unauthorized lock structure drift detected.` Evidence `/tmp/sa117-lock-diff-evidence.json` records `status: drift`, `normalized_equal: false`, and package counts **91 → 99**. Commands 2–5 (`sa117-capture`, `sa117-verify`, `make check`, `make quality`) were not run; no repair or checker-semantic change was attempted; no source, test, script, Makefile, manifest, configuration, tag, push, Docker, or PostgreSQL mutation occurred. **Pending-Blocking:** `SA117E-VAL-002` (**high**, validation) remains open until a complete ordered rerun passes. The only post-baseline `poetry.lock` commit is `1333eb4a3558941f45ac70b2b76eb296a1c0faeb`, carrying the intended Python/dependency upgrade. **Decisions needed:** none — at the review-cap stop, the maintainer chose recorded partial delivery and approved `1333eb4a` as the replacement immutable baseline candidate. That decision authorizes candidate verification and a full rerun without source changes; it is not a command-1 pass, waiver, completion claim, or authorization to start SA117e-3. Track 3 head remains SA117e-2; SA117e-3/-4/-5 and the umbrella stay open.
  - **`SA112A-HANDOFF-003` GRANTED — cap counters reset, Track 1 truly green, both critical-path legs live (2026-08-03; documentation only; no source files touched; no ticket closed).** The maintainer granted **one fresh implementation attempt, one fresh quality-gate run, and one fresh full-scope independent review** over SA112a's unchanged four-file scope (`scripts/smoke_install.sh`, `scripts/_installed_wheel_venv.sh`, `scripts/provision_installed_venv.sh`, `scripts/test_installed_wheel_venv.py`), to be passed to the control plane as `user_authorization`, and reset the per-attempt cap counters (`qg_runs`, `review_cycles`). The prior stop was a consumed ledger, not a design objection; the approved continuation plan on the roadmap carries plan review `STATUS: ok` and is reused verbatim. **The grant is per-attempt and waives no gate.** **Scope:** the single remaining blocker `F-005`. **Board effect:** Track 1 flips to truly green (can start, can finish, can merge) and rejoins Track 3 as the second parallel leg of the critical path; SA112a's *completion* merge remains SA112b's precondition and the existing recorded-partial merge does not satisfy it. **Open maintainer decisions drop from three to two**, both owned by SA117e-4 and neither blocking a track today. **Pre-dispatch investigation (no delta retained; the Track 1 worktree was restored clean at `bfcf2a50`):** two findings were established and written onto the SA112a ticket so the attempt does not re-derive them — (1) `F-005` is **confirmed exploitable** with a ready before/after negative control (`/tmp/X/missing/../link` exits 0 at the merged tip, begins staging, and writes into the unowned symlink target, while `/tmp/X/missing/../link/.` exits 2 on the wrong diagnostic); and (2) **the obvious fix is wrong** — restoring a saved pre-component walk state on `..` repairs `missing/../link` but breaks `link/../sibling`, because `mkdir -p` resolves `..` through the symlink *target*, so the correction must keep the canonical `%/*` trim and recompute only the probe flag after the pop, with `link/../sibling` added to the test matrix as the regression guard. No executable delta, no push, no tag, no ticket closed.
  - **Roadmap cleaning pass (2026-08-03, second pass of the day; documentation only; no ticket state changed).** **Closeout transfer:** the closed **SA117e-1** child block was deleted from `docs/technical/roadmap.md` — its verify/rollback text, `State (2026-08-03)` narrative, and four-row `SA117E1-REV-*` table are already recorded here in full. The roadmap now retains only the residue that open tickets need: `SA117E1-REV-001` (high) and `-002` (medium) carried by **SA117e-4**, `-004` carried by **SA124**, with `-003`/`CR-SA117E-005`/`-006` noted resolved. Track 3's status paragraph and the **SA112a** checkpoint block were reduced the same way — delivered work, validation evidence, `F-003`'s resolution, and the post-review Ruff reviewed-tip identity gap live here; the roadmap keeps only `F-005`, `F-006`, and the `SA112A-HANDOFF-003` decision. **Rebalancing review applied no move**, for the third consecutive pass and for the same structural reason: every open v87 ticket carries a track; Track 2 is closed to new work by standing rule (homing anything there drags merge-gated SA115 onto `v87`); Track 1's remaining off-path work (SA128a → b → c → d → SA122b-1 … -5) is one serial dependency chain that cannot be spread; and Track 3's SA117e children are one coherent serial umbrella. **Track states:** Track 3 (head SA117e-2) is the only truly green track and is on the critical path. Track 1 (head SA112a) is on the critical path but start- and finish-blocked by the `SA112A-HANDOFF-003` **maintainer decision**, not by upstream work; its merge state is yes. Track 2 (SA115) can start but can neither finish (hard dep SA112d) nor merge (hard dep SA112e) — no maintainer decision reaches either. **Open maintainer decisions: three** — `SA112A-HANDOFF-003` (blocks Track 1 today), plus SA117e-4's `SA117E1-REV-001` lease-syntax ratification and `SA117E1-REV-002` disposition (not needed until that child implements). **Audit documents unchanged and verified current** — `arch-audit.md` carries one open ticketed finding (11, ticketed as SA122/SA128) plus three deferred (7, 2, 4) and Finding 12 resolved by SA121; `tech-audit.md` has zero open findings; both already delegate closed-finding narrative here. No source file, tag, split ref, package registry, Docker resource, or PostgreSQL service changed; no ticket was checked.
  - **SA112a second recorded partial delivery — F-003 resolved, F-005 lexical-alias correction still blocking (2026-08-03; task remains unchecked and is not SA112b-ready).** **Done:** Track 1 started clean, fast-forwarded to `v87` `0caf490a`, reused the ready Poetry/Python 3.14.6 environment, and kept all executable work inside the approved four-file allowlist. The helper/test delta rejects direct regular-file, broken-symlink, directory/file-symlink, FIFO, socket, trailing-slash, and first dot-component `OUTPUT_DIR` aliases before trap arming while preserving caller data, empty failure stdout, signal/trap behavior, and smoke parity. Two complete service-free QG chains passed: syntax; 38 then 49 focused tests; two positive `make smoke-install` runs per chain with the exact six build/install markers and all 20 probes; the missing-manifests negative control red with `ImproperlyConfigured` and no `ImportError`; the exact documented five-omission parity baseline; lint; typecheck; diff; and NUL-safe scope checks. Independent review resolved executable `F-003` (early output ownership, cleanup, repeated invocation, and caller traps). **Pending-Blocking:** `F-005` (**medium**, correctness; `scripts/_installed_wheel_venv.sh` / `scripts/test_installed_wheel_venv.py`) remains open because component-probe state is not restored after `..` cancels a nonexistent tail: `missing/../link` and `missing/../link/.` can treat a later existing directory symlink as nonexistent and provision through it. A clean continuation must recompute existence/probe state after cancellation and cover reverse-order/repeated cancellation, root cancellation, root spellings, accepted `link/../sibling` symlink-ancestor identity, status 2, empty stdout, native caller-trap parity for `missing/../link` rejection, and target preservation. `F-006` is **low/advisory** unreadable-directory hardening and remains outside this correction. **Decisions needed:** `SA112A-HANDOFF-002` is consumed; a later continuation needs fresh `SA112A-HANDOFF-003` authorization for one focused correction/validation/review cycle. Review pass 1 and pass 2 both ended at highest blocking severity **medium**, count **1**; the maintainer selected **Stop here, record Done / Pending-Blocking / Decisions-needed, and merge the checkpoint**. The full checkpoint review returned `STATUS: ok` for preservation only; mandatory Ruff hooks then made docstring-format-only changes, and the maintainer explicitly selected merge without a fresh post-cap tip review after quality-gate evidence proved AST identity, word-identical docstrings, and all eight hooks green. That reviewed-tip identity gap is recorded and is not executable approval. This merge preserves progress only — it is not a waiver, completion claim, executable approval, or satisfaction of SA112b's dependency.
  - **SA117e-1 COMPLETE — full-scope documentation closeout review returns `STATUS: ok`; Track 3 head advances to SA117e-2 (2026-08-03; documentation only; no source files touched).** The fresh full-scope documentation closeout review (DC-14) over the corrected SA117e-2 handoff returned **`STATUS: ok`**, resolving **`CR-SA117E-006`** (handoff command exactness) and **`SA117E1-REV-003`** (command consistency for default and overridden evidence directories) by approving the full-value `EVIDENCE=` substitution: the path after `EVIDENCE=` in ordered command 3 is the literal default shape only, and the entire value after `EVIDENCE=` is replaced with the exact complete path printed after `Evidence captured:` by command 2. Command 3's corrected wording stays verbatim on the roadmap ticket. `SA117E1-REV-001`/`-002` remain ticketed to SA117e-4 and plan-review `F-002` to SA117e-3, ownership unchanged. SA117e-1 produced evidence only — the roadmap checkbox is now `- [x]` and no source delta was made or authorized. **SA117e-2 is the next pending Track 3 child and remains unexecuted — its ordered commands have not run**, and it will start from a fresh `v87` sync after this closeout merges. No completion language is claimed for SA117e, SA117, or SA117e-2.
  - **`SA112A-HANDOFF-002` GRANTED — Track 1 becomes truly green and the critical path regains its second parallel leg (2026-08-03; documentation only; no source files touched; no ticket closed).** After the cleaning pass reclassified SA112a's blocker as a maintainer decision rather than upstream work, the maintainer granted **one fresh implementation attempt, one fresh quality-gate run, and one fresh full-scope independent review** over SA112a's unchanged four-file scope (`scripts/smoke_install.sh`, `scripts/_installed_wheel_venv.sh`, `scripts/provision_installed_venv.sh`, `scripts/test_installed_wheel_venv.py`), to be passed to the control plane as `user_authorization`. The prior stop was a consumed per-attempt ledger (`qg_runs=2/2`, `review_cycles=2/2`), not a design objection; the scoped plan already carries plan review `STATUS: ok` and is reused verbatim. **The grant is per-attempt and waives no gate** — the executable slice still needs `STATUS: ok`, closeout still needs its own final full-scope review, and an attempt ending without a checked box records its retrospective and needs fresh authorization. **Scope of the attempt:** resume from the recorded-partial checkpoint, fix `F-005` (non-directory / disallowed-symlink `OUTPUT_DIR` must be rejected before trap arming, failing with argument status 2 rather than status 1, with caller-data-preservation and empty-stdout coverage), rerun the literal service-free validation chain, and obtain one review closing both `F-003` and `F-005`. **Board effect:** Track 1 flips to truly green (can start, can finish, can merge) and rejoins Track 3 as the second parallel leg of the critical path; SA112a's *completion* merge remains SA112b's precondition alongside SA117e-4, and the existing recorded-partial merge does not satisfy it. **Open maintainer decisions drop from three to two**, both owned by SA117e-4 and neither blocking a track today. No executable delta, no push, no tag, no ticket closed.
  - **Roadmap cleaning pass — resolved history transferred, SA112a's blocker reclassified as a maintainer decision (2026-08-03; documentation only; no source files touched; no ticket closed).** `docs/technical/roadmap.md` was condensed to open work: the SA112a recorded-partial delivery narrative, SA117a/b/c closure detail, SA122a's registry delivery, SA132's three remediation decisions, and the resolved authorization ledger (`SA117E-VAL-001`, `SA112A-AUTH-006`, `SA112A-HANDOFF-001`, `SA112A-TRACK-003`, the Option A reordering) were removed from the roadmap and now live only in this changelog; the roadmap retains one-line pointers. **Substantive correction:** SA112a's checkpoint previously recorded *"decisions needed: none"* on the ground that both corrections are fully specified. That is true of the engineering but not of executability — the control plane refused a third quality-gate and third review dispatch at `qg_runs=2/2`, `review_cycles=2/2`, so clearing `F-003`/`F-005` requires a **fresh maintainer dispatch authorization** of the same shape as the granted `SA112A-HANDOFF-001`. Track 1's *can start* and *can finish* are therefore blocked by a maintainer decision, not by upstream work, and the readiness table now says so. **Rebalancing review applied no move:** the only off-path work (SA128a–d → SA122b-1…5) is one serial dependency chain that cannot be spread, Track 2 stays closed to new work by standing rule, and moving filler onto Track 3 would delay the critical path. Track 3 (head SA117e-1) remains the only truly green track and is on the critical path. **Audit documents unchanged** — `arch-audit.md` carries one open ticketed finding (11) plus three deferred, `tech-audit.md` has zero open findings, and both already delegate closed-finding narrative here.
  - **SA112a recorded partial delivery — installed-wheel provisioner candidate preserved; task remains unchecked (2026-08-01; not SA112b-ready).** **Done:** Track 1 synced cleanly to `v87` `b43a57ce` and produced the exact four-file candidate: a sourceable `quickscale_provision_installed_venv REPO_ROOT OUTPUT_DIR` helper, thin wrapper, refactored smoke caller, and focused regression suite. Two full service-free QG passes covered syntax, focused tests, exact six build/install markers, all 20 smoke probes, terminal stdout, missing-manifests negative control plus green rerun, lint, typecheck, diff, and exact scope; the parity checker's exit 2 was baseline-identical and remains the declared publish-workflow gap. Review closed `F-001` (repeat-invocation state) and `F-002` (caller traps on failure/signals). The user-authorized post-cap correction for early output ownership passed 9 targeted and 27 focused tests, with 8/9 targeted tests failing when the fix was reverted; the later Ruff hook delta was mechanical and the 27 tests reran green. **Pending-Blocking:** executable `F-003` (**medium**, correctness; `scripts/_installed_wheel_venv.sh` / `scripts/test_installed_wheel_venv.py`) remains open because its test-backed correction lacks fresh independent executable approval. Final checkpoint review also found `F-005` (**medium**, correctness): existing non-directory or disallowed-symlink `OUTPUT_DIR` shapes can pass validation and fail later with status 1 rather than argument status 2; clean continuation must reject them before trap arming and add caller-data-preservation coverage. **Decisions needed:** none; fix `F-005`, rerun the literal validation chain, and obtain fresh full-scope independent `STATUS: ok` closing both blockers before SA112a is checked, completion prose is written, or SA112b starts. After seeing the final `STATUS: partial`, the maintainer explicitly instructed this checkpoint to be committed and merged. This is preservation, not a waiver, executable approval, or completion claim.
  - **`SA112A-HANDOFF-001` AUTHORIZED and the Track 1 reordering question resolved — SA112a becomes executable on the critical path (2026-08-01; documentation only; no source files touched; no ticket closed).** The maintainer explicitly granted **one fresh `Adaptive-implement` attempt** for SA112a's exact four-file scope (`scripts/smoke_install.sh`, `scripts/_installed_wheel_venv.sh`, `scripts/provision_installed_venv.sh`, `scripts/test_installed_wheel_venv.py`), to be passed to the control plane as `user_authorization`. The prior refusal was a persisted `blocked_retries=1/1` ledger rather than a design objection, and SA112a's scoped plan already carried plan review `STATUS: ok`. **The grant is per-attempt, not standing, and waives no other gate** — the executable slice still requires a full-scope review returning `STATUS: ok`, and closeout still requires its own final full-scope review; an attempt ending without a checked box records its retrospective and needs fresh authorization. The approved four-phase continuation plan in `docs/technical/roadmap.md` is reused verbatim. **Reordering question resolved as Option A:** Track 1's queue is unchanged (**SA112a → SA128a → … → SA122b-5**) and off-path SA128a was **not** promoted ahead of SA112a — SA128a's consumer SA122b-1 is independently gated behind SA128d, so queueing it delays no release property, whereas starting it would push a critical-path node behind filler under the one-child-at-a-time serial contract. **Board effect:** Track 1 flips to **truly green** (can start, can finish, can merge) and joins Track 3 as the second parallel leg of the critical path; SA112a's merge-back remains SA112b's precondition alongside SA117e-4. **Open decisions reduce from four to two**, both owned by SA117e-4 and neither blocking a track today: the `SA117E1-REV-001` explicit-absence Git lease contract, and the `SA117E1-REV-002` bind-and-consume versus approved de-advertise/defer disposition. No executable delta exists; no push, tag, registry, Docker, or PostgreSQL action was taken.
  - **Roadmap cleaning pass (2026-08-01, second pass of the day; documentation only; no ticket state changed).** **Closeout transfer:** the **SA117e-1 recorded-partial checkpoint** narrative moves here in full (below), and the roadmap's duplicated **SA112a implementation-handoff**, **SA117e plan-review-cap**, `SA117E-VAL-001`, and `SA112A-AUTH-006` checkpoint prose is reduced to its actionable residue, since each is already recorded in this file. **Consistency fix (closes `F-003`, medium, from the SA112a checkpoint):** `scripts/test_installed_wheel_venv.py` is added to SA112a's ticket `Files` line and to the Track-topology independence allowlist, so both sites now match the plan-approved four-file scope. **Track states after the pass:** Track 3 is the only **truly green** track (SA117e-1 can start, finish, and merge on its own work); Track 1's SA112a is start-blocked on `SA112A-HANDOFF-001` (maintainer authorization); Track 2's SA115 can start but can neither finish (SA112d) nor merge (SA112e) — both hard upstream dependencies. **No rebalancing move was applied:** every open v87 ticket already carries a track, Track 2 is closed to new work by standing rule, and SA112a is already the parallelized critical-path node. **Decisions surfaced to the maintainer:** `SA112A-HANDOFF-001` authorization; `SA117E1-REV-002` disposition (bind-and-consume digest-bound authorization versus approved de-advertise/deferment under SA124); `SA117E1-REV-001` ratification of the exact remote-absence lease syntax; and whether Track 1 should start off-path SA128a while SA112a stays blocked. No source file, tag, split ref, package registry, Docker resource, or PostgreSQL service changed; no ticket was checked.
  - **SA117e-1 recorded-partial checkpoint — full-scope historical review executed, returned `STATUS: partial` (2026-08-01; documentation only; task remains unchecked; no source delta).** **Done:** Track 3 started clean at `b22bfca3`, fast-forwarded to `v87` `fc82ba64`, reused the existing Poetry environment, and discovered the authoritative 106-path SA117 review set. Adaptive-change-review DC-9 reviewed the complete historical delta including `43d9b8fc` and `b11c24c5` at tip `fc82ba6421353fd960c21e6ab8ab264b8c049250` and returned **`STATUS: partial`**. Stamp/assert and manifest caller parity were substantially sound; advisory `CR-SA117E-005` (evidence wording) is **resolved** because the evidence distinguishes reviewed from executed work. The SA117e-2 ordered-command handoff was improved with full copyable command lines, but `CR-SA117E-006` (handoff command exactness) remains open pending a fresh documentation closeout review. **Pending-Blocking:** DC-9's scope-local findings are remapped to a separate `SA117E1-REV-*` ledger to avoid collision with the older plan-review `F-001`/`F-002` IDs — `SA117E1-REV-001` (**high**, security boundary; `quickscale_core/src/quickscale_core/utils/git_utils.py:636-680`), an `ABSENT` expectation emits bare `--force-with-lease` rather than proving explicit remote absence, owner **SA117e-4**; `SA117E1-REV-002` (**medium**, completeness; `scripts/verify_sa117_publication.py:199-439,498-530`, `Makefile:919-949`, `scripts/README.md:115-124`), publication authorization is not bound to exact verified evidence nor consumed by the publisher, owner **SA117e-4**; `SA117E1-REV-003` (**low/advisory**, command consistency), candidate correction is the literal SA117e-2 handoff in the roadmap; `SA117E1-REV-004` (**low/advisory**, deferred scope inventory), owner **SA124**, with no whole-file certification claimed for the absent `quickscale/src/quickscale/_version.py`. **Decisions needed:** before SA117e-4 implementation, choose whether `SA117E1-REV-002` is resolved by binding and one-time consumption of digest-bound authorization evidence or by de-advertising/deferring that gate under SA124; and verify and ratify the exact supported Git syntax for a branch-scoped lease requiring remote absence, including bare-remote race and stale-tracking-state cases. No decision authorizes a push — SA117e-4 still requires its fresh execution-time twelve-row human confirmation. No source/test file, tag, split ref, package registry, Docker resource, or PostgreSQL service changed; no SA117e-2 command, harness, `make ci-e2e`, public push, parity proof, or installed `apply` ran. SA117e-1 stays unchecked pending a fresh full-scope documentation closeout review; SA117e-2 must not start first.
  - **SA112a implementation-handoff checkpoint — scoped plan approved, executable work not started (2026-08-01; documentation only; task remains unchecked).** **Done:** Track 1 started clean, fast-forwarded to `v87` `fc82ba6421353fd960c21e6ab8ab264b8c049250`, and confirmed Bash, Make, Poetry 2.4.1, and the worktree `.venv` ready. Discovery produced validated `sa112a-topology-v1`. The fresh plan fixed the exact `quickscale_provision_installed_venv REPO_ROOT OUTPUT_DIR` seam, absolute-`OUTPUT_DIR` one-line stdout, core → CLI → umbrella order, six full `[installed-wheel]` stderr markers, four allocation classes, deterministic signal/status/trap behavior, exact 20-probe parity, and the two terminal success lines with one significant blank line. Plan-review pass 1 resolved `SA112A-PLAN-003` and found only `F-001`; planner-owned correction plus pass 2 returned **`STATUS: ok`**, resolving both with no open design question. The complete approved four-phase continuation is now durable in `docs/technical/roadmap.md`, so it should be reused rather than re-planned unless `VERSION`, `scripts/smoke_install.sh`, or mapped caller topology changes. **Pending-Blocking:** `SA112A-HANDOFF-001` (**medium**, orchestration) — the Adaptive control plane refused the exact four-file implementation dispatch because a persisted prior block already spent `blocked_retries=1/1`; the maintainer chose to stop and record rather than spend the one human-authorized override in this run. `F-003` (**medium**, consistency) — the legacy ticket `Files` line and Track-topology allowlist still omit the approved `scripts/test_installed_wheel_venv.py` path; add it to both sites and obtain a fresh full-scope documentation review. The checkpoint review cap ended `STATUS: partial` at **medium / 1 → medium / 1**. **Decisions needed:** a later continuation must explicitly authorize one fresh `Adaptive-implement` attempt for the outstanding exact scope and pass the authorization to the control plane. No executable/test file changed; no syntax, focused, smoke, regression-core, parity, lint, type, executable-review, or completion-review evidence is claimed, and SA112a/SA112 remain open.
  - **SA117e plan-review-cap checkpoint — monolithic execution plan superseded by the five-child split (2026-08-01; documentation only; no ticket closed).** **Done:** Track 3 was clean at `074061ca`, fast-forwarded to `v87` `6d0da500`, and discovery/re-planning rebound the then-monolithic SA117e continuation to the approved immutable baseline `43d9b8fc0abd1c3193a3358f41dee0e09a3c106c`. Two bounded plan-review passes reduced the blocking ledger from **high+medium / 2** to **medium / 1**. `F-001` is resolved at plan-detail level and is carried by **SA117e-4**: each split operation must use the exact SHA/`ABSENT` pre-state frozen in the human-authorized twelve-row Decision Brief, reject any immediate reread mismatch before mutation, and rebrief/re-authorize the complete matrix rather than substitute a new lease value. **Pending-Blocking:** `F-002` (**medium**, resilience) is carried by **SA117e-3**: restoration obligation must be armed before host PostgreSQL is stopped, the handler must reconcile in-flight/uncertain state to the captured original state, and an injected interruption after stop effect but before bookkeeping must prove restoration and readiness. **Decisions needed:** none. The concurrent v87 split below supersedes the monolithic plan and permits each child to proceed only through its own scoped gates; SA117e-1 remains the next task. `CR-SA117E-005` and `CR-SA117E-006` remain low/advisory and are assigned to SA117e-1. No source/test file, local or remote tag, split branch, package registry, Docker resource, or PostgreSQL service changed; no validation command, historical executable review, harness execution, public push, parity proof, or installed apply is claimed.
  - **SA117e split from Tier 3 into five Tier 1–2 children (2026-08-01; documentation only; no source files touched; no ticket closed).** SA117e sized **Tier 3 / high risk**, above the [Purpose](docs/technical/roadmap.md) ceiling that requires splitting before implementing. The monolithic form fused five distinct risk classes into one handoff — historical code review, local source validation, exclusive-capacity infrastructure validation, an irreversible public remote mutation, and closeout — so no single gate could be proportionate to all of them. SA117e becomes an **acceptance-only umbrella** with children cut at each change in what can go wrong: **SA117e-1** (Tier 2, deps none) full-scope historical review of the complete executable delta including `43d9b8fc` and `b11c24c5`, evidence-only with no source delta authorized, also closing advisories `CR-SA117E-005`/`-006`; **SA117e-2** (Tier 2) the rebaselined ordered source validation resuming at command 4 with `SA117_BASELINE_REF=43d9b8fc…`, local and service-free — confirmed to need no source delta because `SA117_BASELINE_REF` is an invocation variable (`Makefile:895`, defaulted empty); **SA117e-3** (Tier 2) harness rematerialization plus `make ci-e2e` in exclusive Docker/PG capacity with a host-PG negative control, where a harness whose SHA no longer matches `53762f9d…` is re-reviewed rather than trusted by provenance; **SA117e-4** (Tier 2, human-gated) the local tag, fresh execution-time human confirmation, the twelve protected `splits/*` pushes, and post-push parity plus installed all-module `apply`, kept as **one unit** because a partially pushed split set is the hazard the ticket exists to prevent; **SA117e-5** (Tier 1) closeout review and umbrella close. **Board effect — a shortening, not a topology change:** no track is added or moved and no new shared writer is created, but SA112b's precondition resolves to the *pushed splits* delivered by **SA117e-4** rather than the whole umbrella, so closeout child `-5` leaves the critical path and SA112b starts once `-4` merges. All dependent references were updated for self-consistency: the dependency diagram, critical-path prose, infra-serialization list, Track 3 status, the SA117 umbrella state paragraph, the SA112 umbrella `deps` tag and prerequisite prose, SA112b's `deps` tag, SA112a's track-independence note, and the track-topology section.
  - **Two authorization decisions resolved — both critical-path tracks unblocked (2026-08-01; documentation only; no source files touched).** **`SA117E-VAL-001` — RESOLVED.** The maintainer approved `43d9b8fc0abd1c3193a3358f41dee0e09a3c106c` as the SA117e lock-diff baseline, replacing `97404f3e…`, which was internally inconsistent (VERSION `0.87.0` carrying 60 pre-lockstep module values) and made ordered command 4 exit 2. `43d9b8fc` is the first internally consistent 0.87.0 lockstep checkpoint and is immutable; only `SA117_BASELINE_REF` changes, and **checker semantics are not weakened** — tolerance or exception logic to make the lock diff pass remains rejected. SA117e's continuation resumes at ordered command 4, with everything downstream of it (full historical executable-delta review including `43d9b8fc`, remaining ordered validation, harness rematerialization, exclusive Docker/PG with host-PG negative control, human split-push confirmation, local-only tag, the 12 protected split pushes, parity, installed all-module `apply`, closeout review) unchanged. **`SA112A-AUTH-006` — GRANTED.** A fresh plan/review authorization was issued for SA112a, whose bounded retry and authorized post-cap cycle (`medium/4 → medium/2 → medium/1`) had been exhausted with plan review at `STATUS: partial` on `SA112A-PLAN-003`. The grant restores the review budget only — implementation remains forbidden until plan review returns `STATUS: ok`. **Effect:** Track 3 (SA117e) and Track 1 (SA112a) both become *can start* and run in parallel; no track-sequencing, worktree-assignment, or other decision is open on the board. The remaining human gates (SA117e's public split push, SA96-PUBLISH) are execution-time confirmations, not standing decisions. Track 2 (SA115) is unaffected and stays hard-dependent on SA112f/SA112 for *can finish* and *can merge*.
  - **Roadmap cleaning pass (2026-08-01; documentation only; no ticket state changed).** **Closeout transfer:** the SA117e and SA112a recorded-partial-delivery recitals were condensed in `docs/technical/roadmap.md` to their blocker plus what remains, with the completed-work evidence left in this changelog (SA117e's own entry below; SA112a's transferred by this entry). **Track state:** no track-sequencing or worktree-assignment question is open, and no rebalancing move was available — Track 2 is closed to new work by the three-worktree rule, so Track 1's serial queue cannot be relieved. The roadmap's "no decision of any kind is open" claim was corrected: two authorization decisions are open, `SA117E-VAL-001` (Track 3 baseline reapproval) and the newly tagged `SA112A-AUTH-006` (fresh SA112a plan/review authorization); both gate *can start* on the critical path, and neither is a hard upstream dependency. No track is truly green.
  - **SA112a recorded partial delivery — scoped plan produced, plan review still `partial` (2026-07-31; no functional commit; task remains unchecked).** Track assignment remained resolved and `wt-track1` was clean and synced to `v87`; the user-authorized plan/review re-attempt produced the scoped implementation, validation, rollback, reviewed-tip, and merge-back plan. **SA112A-PLAN-002** (fail-fast Git NUL-path evidence), **SA112A-PLAN-004** (retain and check the existing roadmap item), and **SA112A-PLAN-005** (post-commit and merge-back recovery) are resolved. No executable or test file was changed. **SA112A-PLAN-003** (medium, completeness) remains: validation must parse exact complete build/install marker lines rather than substring matches, assert the exact six-marker sequence, and allocate plus verify all four helper-owned directory classes (stage, build-venv, wheel, output) across cleanup and signal paths while preserving successful caller-owned output. Plan review stayed `STATUS: partial` after the bounded retry and the explicitly authorized post-cap cycle (`medium/4 → medium/2 → medium/1`); the remaining correction is mechanical but the authorized continuations are exhausted, so a further attempt requires explicit new plan/review authorization (`SA112A-AUTH-006`). Implementation remains forbidden until plan review returns `STATUS: ok`. This checkpoint records planning progress and its blocker, not implementation completion.
  - **SA117e recorded partial delivery — correction reviewed, lock-diff baseline blocking (2026-07-31; functional correction `b11c24c5`; SA117e/SA117 remain unchecked; no public mutation).** Track 3 started clean and synced to current `v87`; the SA117e plan and its independent plan review returned `STATUS: ok`. The two-file verifier fallback portability/correctness correction (`b11c24c5`, reviewed `STATUS: ok`, no findings) is a reviewed slice only — it does not substitute for the still-missing full-scope historical SA117 executable review over the complete delta including `43d9b8fc`. The disposable installed-wheel harness `/tmp/sa117e-harness.HnXjQA/harness.reviewed.sh` (exact SHA `53762f9dd837679808754094d0b4f199665f801ca91f2d69710b2115651c552b`) was reviewed `STATUS: ok` after `CR-SA117E-001`–`004` resolved, but was **not executed** and is **ephemeral/untracked**; `CR-SA117E-005` (evidence wording) and `CR-SA117E-006` (handoff command exactness) remain advisory. Ordered source validation passed: **32 focused correction tests** and **929 broader tests**, `make version-check`, and `make check-manifest-sync` 12/12; ordered command 4 exited 2 because `SA117_BASELINE_REF=97404f3e…` is internally inconsistent (VERSION `0.87.0` with 60 module values pre-lockstep). **SA117E-VAL-001** (high, blocking): continuation must revise/reapprove the lock-diff baseline — recommended immutable baseline `43d9b8fc`, the first internally consistent 0.87.0 lockstep checkpoint — without weakening checker semantics; `sa117-capture`, `sa117-verify`, `make check`, `make quality`, and `make ci-e2e` were not run (stop-on-first-failure). Remaining after the baseline: full historical executable-delta review including `43d9b8fc`, remaining ordered validation, rematerialized/revalidated harness if the temp artifact/hash is unavailable or changed, exclusive Docker/PG state with a host-PG negative control, fresh human public-push confirmation, local-only tag, protected 12 split pushes, public/source/bundled parity, installed all-module apply, and final closeout review. This stop does not approve a future baseline change: continuation requires explicit revision/reapproval of ordered command 4 to use an internally consistent immutable baseline, with `43d9b8fc0abd1c3193a3358f41dee0e09a3c106c` recommended; alternatively the task remains blocked, and checker weakening is not an accepted option. Fresh human split-push confirmation remains a separate execution-time gate to be obtained at the push, not granted by this stop. No local tag, split push, package/release action, or public state mutation occurred. This checkpoint is recorded partial delivery, not completion or release readiness.
  - **Roadmap cleaning pass (2026-07-31, second pass of the day; documentation only; no ticket state changed).** **Closeout transfer:** the completed SA135 and SA133 sections were deleted from `docs/technical/roadmap.md`; their full closeout recitals already live in the entries above and are unchanged. Every dependent reference was reconciled — the track diagram (Track 3 now heads at SA117e), the critical-path line, infra-serialization and shared-executable-surface prose, SA117e's ticket tag (`deps: SA133 ✓ closed` → `deps: none`), its Open bullet, the green-gate milestone paragraph, and the Track-topology independence bullet. **Audit hygiene:** the resolved `TA62` tooling-gap bullet was retired from `tech-audit.md` (closed by SA120; context preserved here), and `arch-audit.md`'s orientation growth-path sentence was corrected to name SA117e as the head and SA112a's Track 1 parallelization. **Track rebalance: no moves.** Track 2 is the only idle capacity and is closed to new work — homing a ticket there would drag merge-gated SA115 onto `v87` at merge-back — so Track 1's serial queue (SA112a → SA128a → … → SA122b-5) and Track 3's release chain are unchanged. Conflict surface remains the shared closeout files, covered by the `git merge v87`-before-merge-back preserve-both-sides step.
  - **SA133 COMPLETE — the three Core E2E blockers fixed; Track 3 head advances to SA117e (2026-07-31; functional commit `167ddd23`; synced to `v87` `026246a5`).** SA135 satisfied the prerequisite (functional commit `6a779dbb`; **QG-SA90-BASELINE cleared**), then the acceptance rerun ran **entirely in the Track 3 worktree** (`/home/victor/code/quickscale-wt-track3` at `026246a5`) as the ordered chain `make ci-e2e` → `make check QUIET=1` → `make quality` — the workspace-correct terminal recovery, not any sibling-worktree run, is the completion evidence. **`make ci-e2e` exit 0:** Core E2E **35 passed / 0 failed** (2,676 deselected, 321.16s), CLI E2E **29 passed / 0 failed** (2,043 deselected, 1,003.80s), all 12 stages green, unit 4,717 passed / 2 skipped, backups 323 passed / 2 skipped, all 12 module suites passed (mean 94.40%), combined coverage **93.01%** with all 90 files ≥80%, **`QUARANTINE_TICKETS` empty**; only expected migration skips and Django runtime warnings. **`make check QUIET=1` exit 0** and **`make quality` exit 0** with **zero warning/critical baseline regressions**; worktree clean. **SA133-BLK-01/02/03 and QG-SA90-BASELINE resolved**; final full-scope review returned **`STATUS: ok`**. **Remaining low advisories, carried:** CR-SA133-001 (exotic legal checkout paths with quotes/control characters could require TOML-safe serialization; normal paths validated) and CR-SA133-002 (one pre-existing `PytestRemovedIn10Warning` in CLI E2E). **Green-gate milestone remains unclaimed:** this chain did not rerun `make ci`, so the four-command join remains unproved even though three of the four commands are green. **SA117e is next** (full-scope review of the complete delta including `43d9b8fc`, then the human-confirmed split push); it is not started or complete.
  - **SA135 COMPLETE — the three SA90 Dockerfile emission hashes refreshed; SA133 resumes (2026-07-31; functional commit `6a779dbb`; synced to `v87` `a0ed6d39`).** The fixture delta in `quickscale_core/tests/fixtures/sa90_emission_manifests.json` is exactly the three `Dockerfile` `sha256` records under `react_default`/`react_empty`/`react_selected` (`676edaa0…` → `beda7d0…`; 0 removed, 0 added, `_provenance` preserved). The deterministic `ProjectGenerator` procedure printed three identical `beda7d0…` digests, exit 0. Focused: `TestSa90ExactManifestParity` 6 passed and `TestDockerfileContent` 11 passed on isolated `-o addopts=` runs (the raw literal selections exit 1 on the deselected coverage remainder only). Broad: `make check QUIET=1` exit 0; `make quality` exit 0 with zero warning/critical regressions. Full-scope independent review returned `STATUS: ok` with no findings. **`QG-SA90-BASELINE` cleared.** SA133 resumes as Track 3 head: the ordered broad rerun `make ci-e2e` → `make check QUIET=1` → `make quality` and the final full-scope review remain to be run; SA133 stays unchecked until that evidence lands.
  - **SA133 recorded partial delivery — functional delta reviewed `STATUS: ok`; acceptance blocked by new SA135 (2026-07-31; functional commit `167ddd23`; task remains unchecked).** SA133's three Core E2E blocker fixes are committed and full-scope reviewed `STATUS: ok`; focused E2E evidence is exit 0, 4 passed / 27 deselected (the exact command is preserved on the roadmap ticket, resolving `CR-SA133-META-001`). Broad rerun: `make ci-e2e` exit 2 (before E2E), `make check QUIET=1` exit 2, `make quality` exit 0 — both broad failures are the same pre-existing three-react SA90 Dockerfile fixture drift from SA130's commit `196e6770`, reproduced with the SA133 delta removed. **New blocker ticket SA135 (`Tier 2 · deps: none · Track 3 head`)** owns the one-file / three-hash refresh of `quickscale_core/tests/fixtures/sa90_emission_manifests.json` (the `Dockerfile` `sha256` records for `react_default`/`react_empty`/`react_selected`, `676edaa0…` → regenerated `beda7d0…`), with the literal deterministic `ProjectGenerator` command on the ticket; provenance stays minimal (three hash lines only; rationale here), per the SA114 precedent. **Acceptance remains blocked by SA135:** SA133 is not checked; after SA135 merges, SA133 reruns `make ci-e2e` → `make check QUIET=1` → `make quality` and obtains the final full-scope review. `QG-SA90-BASELINE` (medium, blocking) stays open on SA135; `CR-SA133-001` (low, advisory) — unusual legal checkout paths containing quotes/control characters could require TOML-safe serialization; normal paths are validated.
  - **SA132 COMPLETE and four decisions resolved — `make ci-e2e` clause retired, blockers ticketed as SA133, SA112a moved to Track 1 (2026-07-31; documentation only; no source files touched).** **`SA132-SCOPE-DEC` — option (b).** SA132's authorized scope was exactly `SA117E-QG-001`–`003`, and all three are fixed and merged (`eac4d82e`): `make check QUIET=1` passes, Core E2E imports `scripts.publish_module`, and the CLI update E2E passes with exit 0. Its verify list additionally required `make ci-e2e` exit 0 — a clause written on the assumption those three findings were the only thing red. They were not, and the remaining failures live in `quickscale_core/tests/test_e2e_full_workflow.py`, outside SA132's allowlist, which the standing **no scope widening** rule bars it from touching. **That clause is retired and SA132 is checked complete**; keeping it open would have left a fully-delivered ticket permanently unclosable behind work it was forbidden to do. Options (a) (new ticket, SA132 stays open) and (c) (widen SA132's allowlist) were declined — (a) because the board would misreport a delivered ticket as open, (c) because absorbing newly discovered defects into a validation slice is the exact failure mode `SA117E-QG-DEC-001` was written to prevent. **New ticket SA133 (`Tier 2 · Track 3 head · deps: none`)** owns the three discovered blockers, renamed `SA133-BLK-01`/`-02`/`-03`. **`SA133-BLK-02` — lock strategy, option (a).** `poetry lock` in the generated project cannot resolve `quickscale-core >=0.87.0,<0.88.0`, unpublished by design. Ratified fix: **rewrite the copied module's core dependency to a path/`develop` dependency on the monorepo core for the duration of the E2E**, matching the pattern the same test already proves for module path-dependencies. Prime suspect for the mechanism is `quickscale_modules/backups/pyproject.toml:19` — the only module declaring core, as `{path = "../../quickscale_core", develop = true}`, valid in the monorepo and broken once copied to `./modules/backups` — but SA133 must confirm before changing. Declined: publishing to TestPyPI first, which inverts SA117's mandatory tag → split-push → PyPI ordering and makes a gate depend on the mutation it gates; and quarantining the leg, which the green-gate milestone's empty-`QUARANTINE_TICKETS` requirement forbids. Resolution against a *published* core stays deliberately unproved here — SA112d's installed-artifact lifecycle E2E owns that axis, so the gap is owned rather than created. **`SA133-BLK-01`/`-03` — assertion alignment.** Ratified policy: **Python 3.14 is the runtime pin and stays, and DRF tracks latest.** Both failures are stale *test* assertions from the 2026-07-29 dependency upgrade, not product defects — DRF is asserted at `^3.16.1` against a `^3.17.1` module pin, and the CI-template test asserts both `"...== '3.14'..." in ci_content` and `"3.14" not in ci_content`, which is unsatisfiable because a blanket version bump rewrote a negative control that had been guarding the *previous* pin. The assertions move to match the authoritative sources (`runtime_pins.PYTHON_VERSION`, the forms module pin) and the negative control is restored by naming the retired version; **no product pin is downgraded to satisfy a test.** Deriving these assertions from the pins instead of restating them — the fix that would end the class — is deliberately **not** in SA133 and is recorded as v88 **SA134**, because widening a remediation ticket into a test-architecture change is the pattern that capped two full-scope reviews on this board. **`SA112A-TRACK-003` — ACCEPTED: SA112a moves to Track 1, ahead of SA128a.** Independence re-verified in all three directions (`deps: none`, SA117e bound begins at SA112b, three-file allowlist disjoint from SA128a's, SA133's, and every other open ticket, service-free so it never contends for Track 3's exclusive PostgreSQL/Docker, and it consumes nothing from Track 3's working tree). **Board effect:** the critical path becomes `SA133 → SA117e → SA112b → SA112c → SA112d → SA112e → SA112f → SA96-PUBLISH` with **SA112a running in parallel on Track 1** and joining as a merge-order precondition of SA112b; cross-track edges go from two to three, all still merge-order only; Track 1's head flips from off-path filler to on-path work, delaying SA128a in a way that reaches no release property since SA122b-1 is independently gated behind SA128d. **Conflict surface:** shared closeout files only, covered by the Merge procedure's `git merge v87`-before-merge-back step — SA112e only *names* SA112a's scripts in the e2e trigger list and never edits them, so no executable surface gains a second writer. **The board now carries zero open decisions.** Roadmap 417 → 411 lines; `START_HERE.md` line count refreshed.
  - **Roadmap cleaning pass (2026-07-31, second pass of the day; documentation only; no ticket state changed).** **Closeout transfer:** the completed **SA130** block (checked `[x]`, its diagnosis, adjacent-lines-are-clean note, file allowlist, verify/rollback, and full completion recital — 14 lines) was removed from `docs/technical/roadmap.md`; its closeout entry already exists in this file. SA132's **Done** recital and its three struck-through original findings were compressed to one clause plus a pointer here; the **Pending-Blocking** ledger (`SA132-BLK-01`/`-02`/`-03`), **Decisions-needed** lines, and deferred advisory were kept verbatim — they are live. Roadmap 440 → 417 lines; `START_HERE.md` line count refreshed. **Audits:** no finding became newly closeable — `arch-audit.md` carries Findings 11 (ticketed as SA128/SA122b), 7 (unscheduled), 2 and 4 (deferred with `teams`); `tech-audit.md` stands at zero open findings with four tooling gaps parked as v88 SA123. Two stale roadmap line-anchors in arch Finding 11's evidence and Probe B were repointed to the document rather than to moved line ranges. **Track assignment: complete, no gaps** — every open entry carries a track; v88 items carry the `v88` scope tag. **Rebalancing — one move surfaced as a decision, none applied.** `SA112A-TRACK-003` reopens moving **SA112a to Track 1 ahead of SA128a**: independence re-verified in all three directions (`deps: none`, SA117e bound begins at SA112b, three-file allowlist disjoint from SA128a's two-file allowlist and every other open ticket, service-free so it never contends for Track 3's exclusive PostgreSQL/Docker, and it does not split one logical change), Track 3's head SA132 is decision-blocked rather than work-blocked, and Track 1's head SA128a is off the critical path. Because the move would delay an otherwise-mergeable Track 1 ticket it is **surfaced, not applied**, with the recommendation to **take it** unless SA132's three questions are answered immediately. **Conflict surface named:** shared closeout files only (`CHANGELOG.md`, `docs/technical/roadmap.md`, plus `decisions.md` on policy change), covered by the Merge procedure's `git merge v87`-before-merge-back preserve-both-sides step; no executable surface is shared by the move. **Branch state re-measured:** `wt-track1` and `wt-track3` carry zero commits unmerged into `v87`; `wt-track2` still carries exactly `5193f198` + `5b5de830` and remains closed to new work. **Truly green tickets:** SA128a only (Track 1 — start/finish/merge all YES), and it is **off the critical path (filler work)**. SA132, SA117e, SA112a, SA122b-5, and SA115 are not truly green; **no ticket is truly green on the critical path.** **Open decisions:** SA132's three remediation questions, plus `SA112A-TRACK-003`. No source files touched.
  - **SA132 recorded partial delivery — functional checkpoint committed; three Core E2E blockers remain (2026-07-31; functional commit `eac4d82e`; task remains unchecked).** **Done:** `make check QUIET=1` now passes with unit marker/timeout parity; Core E2E imports `scripts.publish_module` and collects 35 tests; focused CLI update E2E passes using canonical `_core_version` fixture; `make test-cov-policy` 86 passed; `make test-unit` 4,660 passed; CLI E2E 29 passed; all 11 pre-E2E `ci-e2e` subgates pass; `make quality` passes; `make test-cov` passes 93.01%, all files ≥80%; two full functional reviews returned `STATUS: ok` for partial merge; no public mutation. **Pending-blocking:** `make ci-e2e` exits 1 because Core E2E is 31 passed / 4 failed in `quickscale_core/tests/test_e2e_full_workflow.py`: **SA132-BLK-01** (medium, external) stale DRF version assertion at line 312; **SA132-BLK-02** (medium, external) generated-project Poetry lock cannot resolve unpublished `quickscale-core >=0.87.0,<0.88.0` at line 420; **SA132-BLK-03** (medium, external) two stale Python 3.14 rejection assertions at line 1009. These three blockers are causally independent of `eac4d82e` but block SA132 completion and the release claim. **Decisions-needed:** define separate ticket scope/allowlist before touching out-of-scope Core test/harness behavior; select a valid unpublished-core lock-resolution strategy; align DRF and Python 3.14 assertions with authoritative current policy. **Deferred advisory:** `test-cov` QUIET path remains unfiltered and must be separately ticketed if pursued. SA132 remains unchecked; this checkpoint is not a waiver, completion claim, or release-ready evidence.
  - **SA130 complete — Dockerfile Poetry timeout variable corrected (2026-07-31; functional commit `196e6770`).** `Dockerfile.j2` now exports `POETRY_REQUESTS_TIMEOUT=120` — Poetry's actual timeout variable — instead of the no-op `POETRY_HTTP_TIMEOUT`. The value (120), `timeout 600` wrapper, 3-retry loop, and all surrounding output are byte-unchanged; this changes one identifier. Tests: positive/negative direct rendering plus actual service-free `ProjectGenerator` emitted-Dockerfile assertions; `TestDockerfileContent` 11 passed, 216 deselected; `make quality` exit 0 with zero baseline regressions. Full-scope Adaptive-change-review returned `STATUS: ok`. **Finding lifecycle:** `SA130-CR-001` (medium, test-gap) resolved by actual ProjectGenerator emission test; `SA130-CR-002` (low, advisory, consistency) resolved by exact pinned Poetry 2.4.1 wording. No unresolved blocker or finding remains under the revised acceptance criterion. (2026-07-31: the maintainer waived the original Docker image/network build criterion; literal proof requires shared Docker capacity and Track 3 currently owns it.) The service-free actual `ProjectGenerator` emitted-Dockerfile proof — correct identifier present, wrong identifier absent — is the accepted criterion. Track 1 advances to SA128a.
  - **SA129 complete — superuser sentinel behavior closes SA129-TEST-001, SA129-DOC-002; SA129-E2E-002 waived (2026-07-31; functional commit `c7e53f9d`; docs-only closeout).** `_superuser_exists_in_backend` now emits and reverse-scans `QUICKSCALE_SUPERUSER=0|1` on an explicit sentinel line, avoiding Django 5.2+ shell auto-import banner coupling while preserving fail-closed nonzero/absent/malformed handling and the existing caller/messages. `SA129-TEST-001` resolved by literal independent complete-probe expectation and byte-exact stdout/stderr plus create-command presence/absence assertions across every handler branch; 54 tests passed, `make quality` zero baseline regressions, full-scope review confirmed production/test correctness. `SA129-DOC-002` resolved: closeout re-authors SA129 topology without duplicating the `SA128a → b → c → d` node. **Advisory:** `SA129-E2E-002` remains **low/advisory, `waived-not-passed`** because Track 3 held the shared Docker/PostgreSQL infrastructure; no E2E run is claimed. No unresolved blocker or decision remains; Track 1 advances to SA130.
  - **SA129 second recorded partial delivery — test-completeness gap closed; documentation closeout remains open (2026-07-31; functional commit `c7e53f9d`).** **Done:** `SA129-TEST-001` is resolved by a literal independent complete-probe expectation and byte-exact stdout/stderr plus create-command presence/absence assertions across every handler branch. Fresh evidence is green: all 54 tests in `test_development_commands_extended.py` passed and `make quality` reported zero baseline regressions; full-scope review reconfirmed production/test correctness. **Pending / blocking:** `SA129-DOC-002` (**medium**, consistency) — pre-commit inspection found that the completion-document candidate duplicated the `SA128a → b → c → d` node in the open-work diagram after the two-cycle review cap. That candidate was discarded back to the synced `v87` documentation baseline; SA129 remains unchecked and Track 1 does not advance. **Advisory:** `SA129-E2E-002` remains **low/advisory, `waived-not-passed`** because Track 3 held the shared Docker/PostgreSQL infrastructure. **Decisions needed:** none; a fresh continuation must re-author the closure/topology update and obtain full-scope `STATUS: ok`.
  - **`SA117E-QG-DEC-001` RESOLVED — AUTHORIZED; the SA117e exact-gate remediation slice is ticketed as SA132 (2026-07-31; documentation only; no source files touched).** The maintainer **authorized** the separately discovered, planned, and reviewed remediation slice that SA117e's validation-blocked checkpoint required, and it enters the board as **SA132 — Fix the three exact quiet/E2E gate blockers so SA117e can resume** (`Tier 2 · Track 3 · deps: none · Track 3 head`). **Scope is exactly three findings:** `SA117E-QG-001` (**high**, validation infrastructure — exact `make check QUIET=1` hangs in the redirected pytest/xdist recipe although the same component gates pass individually); `SA117E-QG-002` (**high**, completeness — Core E2E collection cannot import `scripts.publish_module` under the runner's package-root `PYTHONPATH`); `SA117E-QG-003` (**high**, correctness — CLI E2E `test_update_auto_commits_each_module_e2e` receives update exit 1). **Rationale:** declining would leave the entire critical path — SA117e, SA112a–f, and SA96-PUBLISH — parked behind three already-diagnosed validation defects, and the green-gate milestone's four-command join cannot be claimed while the exact gates are red regardless of who runs them. The 2026-07-30 run's choice to checkpoint rather than widen scope stands as correct; SA132 is new authorized work, not a continuation of that capped run. **Forward-binding rules:** SA132 is a **separate reviewed handoff** under the Track 3 serial contract — its own discovery, scoped plan, file allowlist, and independent change review, merged back to `v87` before SA117e restarts from a fresh sync; **no scope widening** — anything found outside the three findings is recorded and ticketed, never absorbed; SA132 carries **no tag, split push, or other public mutation**, which remain SA117e's; `SA117E-QG-003` must be triaged as test-defect versus real `update` regression **before** a fix is chosen, since silently absorbing a production defect into a validation slice is the failure mode this authorization exists to avoid; the `SA117-DEC-005` proportionality rule and the standing `make quality`-no-worse rule apply unchanged; and Track 3 keeps infra priority over SA115 for the exclusive Docker/PostgreSQL legs. **Verify (SA132):** exact `make check QUIET=1` exits 0 with no hang; Core E2E imports `scripts.publish_module` cleanly; `test_update_auto_commits_each_module_e2e` passes with update exit 0; `make ci-e2e` exits 0 with `QUARANTINE_TICKETS` empty; `make quality` exits 0 with zero baseline regressions; independent change review returns `STATUS: ok`. **Board effect:** Track 3's *can-finish* flips **NO → YES** at the head and the track is no longer blocked; the critical path gains one node and reads `SA132 → SA117e → SA112a → SA112b → SA112c → SA112d → SA112e → SA112f → SA96-PUBLISH`; the open-decision count drops from two to one, leaving **`SA112A-TRACK-002`** as the sole open decision on the board. No cross-track edge, merge order, Tier census, or other track's states changed. Roadmap 646 → 667 lines; `START_HERE.md` line count refreshed.
  - **Roadmap cleaning pass (2026-07-30, fourth pass of the day; documentation only; no ticket state changed).** **Closeout transfer:** the three open tickets' *completed-work* recitals were removed from `docs/technical/roadmap.md`, each replaced by a one-clause summary plus a pointer here, since all three closeouts already exist in this file — SA117e's validation-blocked **Done** bullet (clean `2466f738` sync, 50-commit/155-path/twelve-split candidate binding, two bounded green QG runs, no source/tag/split/artifact/workflow mutation), SA128's second-checkpoint **Done** bullet (clean sync, plan-reviewed two-phase candidate, green mechanical gates, two `STATUS: partial` full-scope reviews, byte-identical revert to `10ff5c89`), and SA129's partial-delivery **Done** bullet (retained two-file sentinel delta, 20 targeted + 54 module tests, `make quality` with zero baseline regressions). The **pending/blocking** ledgers and **decisions-needed** lines of all three were kept verbatim — they are live, not closed. SA128's *Why replacement rather than remediation* paragraph was compressed to its forward-binding conclusion plus a pointer, its full rationale being already recorded above. Roadmap 649 → 646 lines; `START_HERE.md` line count refreshed. **Audits:** no finding became newly closeable — `arch-audit.md` carries Findings 11 (ticketed as SA128/SA122b), 7 (unscheduled), 2 and 4 (deferred with `teams`); `tech-audit.md` stands at zero open findings with its four tooling gaps parked as v88 SA123. One closed-context compression applied there: the `TA1`…`TA59` per-ID recital in the reconciliation log became a `TA1`–`TA59` range line pointing here, since all 59 pre-date the audit baseline and none regressed. **Track assignment: complete, no gaps** — all five open entries carry a track; v88 items carry the `v88` scope tag. **Rebalancing: no move available, and none taken.** The critical path (`SA117E-QG-DEC-001` + `SA117E-QG-001`–`003` remediation → SA117e → SA112a…f → SA96-PUBLISH) is entirely on Track 3 and causally serial on evidence validity, so it cannot fan out. Track 2 remains a closed worktree (`5193f198` + `5b5de830` unmerged, SA115 merge-gated), and Track 1 is one serial worktree whose head SA129 carries a **retained** two-file delta, so nothing may be inserted ahead of it. **Conflict surface named:** shared closeout files only (`CHANGELOG.md`, `docs/technical/roadmap.md`, plus `decisions.md` when policy changes), covered by the Merge procedure's `git merge v87`-before-merge-back preserve-both-sides step; no executable surface was touched. **Open decisions carried, both unchanged:** `SA117E-QG-DEC-001` (authorize the remediation slice for the three exact-gate blockers) and `SA112A-TRACK-002` (move SA112a to Track 1, recommendation **decline** while executable SA129/SA130 head the queue). **Truly green tickets:** SA129, SA130, and SA128a on Track 1 — all start/finish/merge YES, and all **off the critical path (filler work)**. SA129's open `SA129-TEST-001` is an intra-track correction, not a finish blocker. SA117e, SA122b-5, and SA115 are not truly green. **No ticket is truly green on the critical path.** No source files touched.
  - **SA117e validation-blocked checkpoint — split publication remains open (2026-07-30; documentation only; no public mutation).** The resumed Track 3 branch was clean and synchronized at `2466f738`; dependencies and human push authorization were confirmed, and the approved full-scope plan bound 50 commits, 155 review paths, and twelve pending splits. Two QG runs left version/manifest/lock checks, 660 focused tests, 9 mismatch/publication nodes, lint, format, MyPy, unit/integration coverage, smoke-install, quality, and diff checks green, while scoped cleanup removed stale test Docker resources. **Pending / blocking:** `SA117E-QG-001` (quiet-check pytest/xdist redirection hang), `SA117E-QG-002` (Core E2E `scripts.publish_module` import failure under the runner path), and `SA117E-QG-003` (CLI update lifecycle E2E exit 1) remain high/blocking at the two-run QG cap. No source change, local or remote release tag, split push, package workflow, TestPyPI/PyPI publication, full-scope source approval, remote parity proof, or installed all-module apply is claimed. SA117e/SA117 remain unchecked; a separately authorized remediation slice must make the exact quiet/E2E gates green before publication resumes.
  - **SA129 recorded partial delivery — explicit superuser sentinel retained; task remains open (2026-07-30).** **Done:** `_superuser_exists_in_backend` now emits and reverse-scans `QUICKSCALE_SUPERUSER=0|1`, avoiding Django 5.2+ shell auto-import banner coupling while preserving fail-closed nonzero/absent/malformed handling and the existing caller/messages. The exact two-file delta includes producer/parser/caller regressions; post-delta validation passed 20 focused tests, all 54 tests in the touched test module, and `make quality` with zero baseline regressions. **Pending / blocking:** final full-scope review remained `STATUS: partial` at the two-cycle cap on `SA129-TEST-001` (**medium**, test-gap): complete argv/script equality must use an independent literal expectation, and four handler branches still need byte-exact stdout/stderr and consistent create-command presence/absence proof. `SA129-E2E-002` (**medium**, completeness) is **user-waived, not passed** because Track 3 held the shared Docker/PostgreSQL infrastructure. **Decisions needed:** none; SA129 remains unchecked pending the narrow test correction, fresh validation, and full-scope `STATUS: ok`.
  - **`SA128-SIZE-001` RESOLVED — SA128 split by observation domain into SA128a–SA128d (2026-07-30, option (a); documentation only; no source files touched).** The maintainer selected **option (a): four serial children, one per observation domain.** SA128 becomes an **acceptance-only umbrella** — Tier 3, never to be executed as one handoff — over four Tier 2 children, each owning a disjoint slice of the stable nine-finding ledger: **SA128a** GNU Make expansion/dry-run/delegation/`check` aggregation (`SA128-CR-001`); **SA128b** Bash execution with a recorder `make`, covering serial/parallel order, join, failure, and completeness (`SA128-CR-002`); **SA128c** hosted+publish+E2E YAML structure, commands, `needs`, stage topology, and the literal 16-publish/10-hosted `run` oracle (`SA128-CR-003`, `SA128-CR-004`); **SA128d** cross-cutting contracts — arbitrary DAGs, no-bypass path containment, uniform controlled exit 2, bounded output/process-group lifecycle — plus the complete adversarial/caller-parity proof matrix and umbrella closeout (`SA128-CR-005`, `SA128-SEC-006`, `SA128-CR-007`, `SA128-CR-008`, `SA128-TEST-009`). No finding is orphaned or duplicated. **Rationale:** a single full-scope review had to find all five domains proved at once, so a candidate strong in four was correctly rejected — the mechanism behind the frozen 9 → 9 ledger across two capped attempts. Splitting gives each child a review scoped to what it actually built, the same remedy that converged SA117 (`SA117-DEC-003`) and SA122 after both stalled as monoliths. **Declined:** option (b) (two children by trust boundary) because its extractor child still bundled three tooling domains and three high findings and was plausibly still Tier 3, deferring the discovery to attempt 4; option (c) (narrow the acceptance bar, declaring Bash/publish unobserved) because it re-creates the partial-authority state `SA122A-AUTH-001` closed and contradicts `SA128-DEC-001`'s no-fallback rule. **Forward-binding rules:** the checker remains **non-authoritative until SA128d returns `STATUS: ok`** — no partial authority after a/b/c; `SA128-DEC-001`/`-002`/`-003` carry to every child unchanged; the registry, its schema, the Make entrypoint, and `SA122-DEC-001`'s publish-as-full-coverage rule stay **inputs, not scope**; the five publish omissions remain declared-open and owned by SA122b, with direct exit **1** / Make wrapper exit **2** preserved; and every child appends its row to the umbrella *Attempts and why they failed* table before any checkpoint. **Merge safety:** all four children share `scripts/check_gate_parity.py` and `scripts/test_gate_parity.py`, but Track 1 is a single serial worktree — one child at a time, each merged back to `v87` before the next starts, exactly as the SA117 chain runs — so the conflict surface stays the shared closeout files (`CHANGELOG.md`, `docs/technical/roadmap.md`), covered by the Merge procedure's `git merge v87`-before-merge-back step. This is the same reasoning that made `SA126-DEC-001` safe. **Board effect:** SA128's *can-start* is restored (the split was its only blocker) and its *can-finish* becomes reachable per child; Track 1 is now **truly green** across its whole queue and reports start/finish/merge all YES, while remaining entirely off the critical path. SA122b's `deps` re-point to SA128d and v88 SA123's follow the umbrella; the Tier census returns to no open *executable* Tier 3. **`SA112A-TRACK-002` is once again the sole open decision anywhere on the board.** Roadmap 562 → 593 lines; `START_HERE.md` line count refreshed.
  - **SA128 retrospective block added and tier re-cost Tier 2 → Tier 3 (2026-07-30; documentation only; no ticket checked, no source touched).** **Why:** a review of the two SA128 blocked checkpoints found they record *state* (clean revert, ratified constraints, restart point) but no *diagnosis*. The nine findings are the same nine IDs at the same severities across both attempts, differing only in tense, so the ledger moved **9 → 9 across two full independent attempts** — and neither candidate survives: both were reverted before any commit, with `wt-track1`'s reflog and stash list confirming nothing dangling or stashed. Attempt 3 would have started with no memory of two rejected designs. **Added:** an *Attempts and why they failed* table on the SA128 ticket with one row per attempt (design, findings targeted, gates, review outcome, cause of rejection). Attempt 1's design is unrecorded beyond the negative constraints it produced (`SA128-DEC-001`/`-002`/`-003`); attempt 2's cause of rejection is marked **UNRECORDED — needs maintainer recall**, and is the highest-value gap on the ticket. A **do-not-repeat** list is recorded alongside it, whose load-bearing item is that green mechanical gates are not evidence of acceptance — attempt 2 passed 132 focused tests, strict MyPy, Ruff, exact five-record exit parity, `make typecheck`, `make quality`, and `git diff --check`, and was still rejected at full scope. **`SA128-SIZE-001` — tier re-cost applied, split shape open.** SA128's Tier 2 sizing derived from its two-file allowlist rather than its acceptance surface. Measured breakdown: nine high findings across **five independent observation domains** (Make expansion/delegation/aggregation; Bash execution with a recorder `make`, covering order/join/failure; hosted+publish+E2E YAML structure/commands/`needs`/topology; cross-cutting arbitrary-DAG, path-containment, fail-hard-exit-2 and lifecycle contracts; and the complete adversarial/caller-parity proof matrix). A single full-scope review must find all five proved, so a candidate strong in four is correctly rejected — exactly the observed 9 → 9. Re-cost to **Tier 3**, which triggers this document's own rule that a Tier 3 item is split before implementation; SA128 is therefore **not executable as one handoff** until the split shape is chosen. Three options are recorded with trade-offs — (a) four children by observation domain, (b) two children by trust boundary, (c) narrow the acceptance bar instead of splitting — with **(a) recommended** as the remedy that already converged SA117 (`SA117-DEC-003`) and SA122 after both stalled as monoliths. Children share `check_gate_parity.py`/`test_gate_parity.py` but Track 1 is serial, so they never run concurrently and the conflict surface stays the shared closeout files, covered by the Merge procedure; this is the same reasoning that made `SA126-DEC-001` safe. **A mandatory retrospective row is now standing policy** for every future SA128 attempt, to be appended before its checkpoint is written. **Board effect:** SA128 becomes start-NO pending a **maintainer decision** (not a hard dependency — no upstream work is required); Track 1 still starts today because `SA131-ORDER-001` already moved SA129/SA130 ahead of it. No merge order, cross-track edge, critical path, or other ticket's states changed. The Tier census flips from "no open ticket is Tier 3" to one, and the board now carries **two** open decisions (`SA128-SIZE-001`, `SA112A-TRACK-002`). Roadmap 540 → 562 lines; `START_HERE.md` line count refreshed.
  - **Roadmap cleaning pass (2026-07-30, third pass of the day; documentation only; no ticket state changed).** **Closeout transfer:** the superseded **first SA128 blocked checkpoint** block (2026-07-29 — Done / Pending-blocking / Decisions-needed recital, 4 lines) was removed from `docs/technical/roadmap.md`; its record already exists in this file, and the second checkpoint carries the stable finding ledger forward. Roadmap 546 → 540 lines; `START_HERE.md` line count refreshed. **Audits:** no finding became newly closeable — `arch-audit.md` carries Findings 11 (ticketed as SA128/SA122b), 7 (unscheduled), 2 and 4 (deferred with `teams`); `tech-audit.md` stands at zero open findings, and its four tooling gaps remain parked as v88 SA123. **Track assignment: complete, no gaps** — all five open entries carry a track; v88 items carry the `v88` scope tag. **Rebalancing — one pure speedup applied: `SA131-ORDER-001`, Track 1 reordered to SA129 → SA130 → SA128 → SA122b.** Independence verified in all three directions: all `deps: none`; allowlists mutually disjoint (`quickscale_cli/.../development_commands.py` + its test; `quickscale_core/.../Dockerfile.j2` + its test; `scripts/check_gate_parity.py` + `scripts/test_gate_parity.py`) and disjoint from SA122b's registry consumers and every Track 3 surface; none needs PostgreSQL/Docker; none splits one logical change. The move delays nothing real — SA128 retained no executable delta across two review caps and needs a fresh continuation whenever scheduled, and its sole consumer SA122b is independently merge-gated behind SA112e, five Track 3 children away. Effect: Track 1's head flips *can-finish* from NO to YES; no merge order, cross-track edge, or critical path changes. Every reference re-pointed — open-work summary and entry 4, Track 1 header and ordering paragraph, the SA128/SA129/SA130 ticket tags, SA128's decisions-needed line, the green-gate status paragraph, the dependency diagram and its checkpoint, the parallelism bullets, the `SA112A-TRACK-002` cost bullets, the Track 1 three-state bullets, the Net, both open-decision paragraphs, and Standing decisions. **Conflict surface named:** shared closeout files only (`CHANGELOG.md`, `docs/technical/roadmap.md`, plus `decisions.md` when policy changes), covered by the Merge procedure's `git merge v87`-before-merge-back preserve-both-sides step; no executable surface is added or shared by the reorder. **`SA112A-TRACK-002` re-costed and carried as the sole open decision:** its cost rose again — the move would now displace two immediately-executable Tier 2 defects rather than a stalled continuation — recommendation **decline while SA129/SA130 are the head**. **Branch state re-measured:** `wt-track1` and `wt-track3` carry zero commits unmerged into `v87`; `wt-track2` still carries exactly `5193f198` + `5b5de830` and remains closed to new work. **Truly green tickets:** SA117e (Track 3, **in process**, on the critical path — real progress), SA129 and SA130 (Track 1, off-path filler). SA128, SA122b, and SA115 are not truly green. No source files touched.
  - **SA128 second review-cap blocked checkpoint — semantic checker candidate reverted (2026-07-30; status only; task remains unchecked).** **Done:** Track 1 was clean, merged current `v87`, rebuilt its Poetry/Python 3.14.6 environment, refreshed discovery, and obtained `STATUS: ok` for a new two-phase plan. Two implementation/QG rounds produced a rejected candidate that passed 132 focused tests, strict MyPy, Ruff, exact direct-exit-1 / Make-exit-2 five-record parity, `make typecheck`, `make quality`, and `git diff --check`. Two full-scope reviews still returned `STATUS: partial`; non-convergence was explicit at high severity and 9 → 9 unresolved findings. Both executable/test files were then restored byte-identically to synced start commit `10ff5c89`, so **no SA128 executable delta is retained or merged** and the SA122a checker remains non-authoritative. **Pending / blocking:** `SA128-CR-001`, `SA128-CR-002`, `SA128-CR-003`, `SA128-CR-004`, `SA128-CR-005`, `SA128-SEC-006`, `SA128-CR-007`, `SA128-CR-008`, and `SA128-TEST-009` remain **high/blocking**, covering effective Make delegation/aggregation, local serial/parallel execution, publish/hosted/E2E semantics, arbitrary DAGs, complete path containment, fail-hard ambiguity, bounded lifecycle cleanup, and the adversarial proof matrix. **Decisions needed:** none; the next continuation must start from the unchanged baseline and ratified `SA128-DEC-001` through `SA128-DEC-003` constraints, resolve the full ledger, and obtain full-scope `STATUS: ok` before SA128 closes or SA122b begins.
  - **SA117c complete — exhaustive Python-binding lock-drift validation closes SA117C-CR-012 (2026-07-30; functional commit `46a2f186`).** Delivered the exhaustive Python-binding inventory over `ast.Import`, `ast.ImportFrom`, `TypeVar`, `ParamSpec`, and `TypeVarTuple` — all recognized as possible `__version__` rebindings and rejected with controlled exit 2 and no evidence output. Valid non-binding references (local-shadow, re-`import`-as, `getattr` read, `__all__` export) are tolerated without false rejection. The lock-structure comparison and fail-closed inventory from the partial-delivery checkpoint are preserved and re-validated. **Validation:** 50 focused tests, 93 combined tests, Ruff check/format, `make version-check` (all 12 modules at `0.87.0`), `make quality` exit 0, `git diff --check` — all green. **Finding lifecycle:** `SA117C-CR-012` resolved; `SA117-CR-006`, `SA117C-CR-007`–`SA117C-CR-011`, `SA117C-CR-013` previously resolved. Full-scope Adaptive-change-review returned `STATUS: ok` at confidence 97 with **no new source findings or blockers**. SA117c roadmap checkbox marked `- [x]`. Track 3 continues with SA117e. **Documentation review (2026-07-30):** Full-scope docs review found `SA117C-DOC-CR-001` (stale current-state prose still described SA117c as open/forward-binding) and `SA117C-DOC-CR-002` (wording conflated SA117c's local comparison gate with SA117e's published-split proof) — the follow-up correction in this edit singularizes SA117e as the sole remaining child and reserves published-parity proof for SA117e; no source change was required. SA117c source closeout is complete.
  - **Roadmap cleaning pass (2026-07-30; documentation only; no ticket state changed).** **Closeout transfer:** the completed **SA117c** block (checked `[x]`, its verify/why lines, the COMPLETE evidence paragraph, validation recital, finding lifecycle, and documentation-review record — 9 lines) was removed from `docs/technical/roadmap.md`; its closeout entry already exists in this file. It is folded into the single closed-children pointer with only its forward-binding residue retained (SA117c's local lock-comparison gate is the input SA117e's post-push verification consumes). Also removed or compressed as closed context: the SA117 split narrative's per-child closure recital, the resolved `SA117-CR-006`/`SA117C-CR-012` recital, the closed SA117c node in the dependency diagram, the SA117c entry in the conflict-surface paragraph, and the SA117a re-split rationale in the Tier census. Roadmap 546 → 535 lines; `START_HERE.md` line count refreshed. **Audits:** no finding became newly closeable — `arch-audit.md` carries Findings 11 (ticketed as SA128/SA122b), 7, 2, 4; `tech-audit.md` stands at zero open findings. **Track assignment: complete, no gaps** — all five open entries carry a track; v88 items carry the `v88` scope tag. **Rebalancing: no move available, and none taken.** The critical path shortens by one closed node to `SA117e → SA112a…f → SA96-PUBLISH`, entirely on Track 3 and causally serial on evidence validity, so it cannot fan out. Track 2 remains a closed worktree (`5193f198` + `5b5de830` unmerged, SA115 merge-gated). Track 1 now carries **in-process** SA128 work on `wt-track1` (`c4e6560d`, decision-recording only, unmerged), so nothing may be inserted ahead of it; SA129/SA130 are already correctly homed and file-disjoint. **`SA112A-TRACK-002` re-costed and carried as the sole open decision:** its cost rose — moving SA112a to Track 1 would now preempt live work rather than displace a stalled queue — and the recommendation is recorded as **decline while SA128 is live**. **Truly green:** Track 3 only (on the critical path); Track 1 is start-yes/finish-no (SA128's own nine high findings) and Track 2 remains finish-blocked by SA112f and merge-blocked by SA112e. No source files touched.
  - **SA128 review-cap blocked checkpoint — semantic gate-parity rebuild remains open (2026-07-29; status only; no executable delta landed).** **Done:** Track 1 was clean and synchronized; task prerequisites were `deps: none` and the task block had no open decision. The maintainer ratified the implementation boundary: remove Bubblewrap entirely, never fall back to text/regex extraction, delegate semantics to GNU Make/Bash, keep all six inert adversarial forms, apply one repository-contained path contract to every input, preserve direct exit **1** / Make wrapper exit **2** while the five publish omissions remain, and leave the checker standalone until SA122b. Two bounded fix/re-review rounds reached the cap; the maintainer authorized one post-cap re-plan, whose independent plan review also returned `STATUS: partial`. Every checker/test candidate was restored byte-identically and the Track 1 worktree was re-synced before this entry, so the existing SA122a checker is unchanged and explicitly non-authoritative. **Pending / blocking:** `SA128-CR-001`, `SA128-CR-002`, `SA128-CR-003`, `SA128-CR-004`, reopened `SA128-CR-005` (its candidate-only DFS fix was reverted), `SA128-SEC-006`, `SA128-CR-007`, `SA128-CR-008`, and `SA128-TEST-009` are all **high/blocking** across effective Make delegation, local serial/parallel execution, publish/hosted/E2E semantics, metadata/topology, path containment, fail-hard ambiguity, bounded process lifecycle, and acceptance proof. No independent quality gate or executable approval is claimed. **Decisions needed:** none. A fresh continuation must supply a literal current-source oracle—including all **16** publish `run` values and **10** bound hosted `run` values—resolve the complete stable ledger, run focused plus repository validation, and obtain full-scope `STATUS: ok` before SA128 is checked or SA122b begins. The roadmap acceptance is corrected from the impossible "report five differences and exit 0" combination to direct exit 1 / unchanged Make exit 2; SA122b makes the target green by fixing inventories rather than adding exception logic.
  - **Roadmap cleaning pass (2026-07-29, third pass of the day; documentation only; no ticket state changed).** **Closeout transfer:** the completed **SA127** block (checked `[x]`, its full diagnosis, "why every gate missed it", AF7 note, file allowlist, verify/rollback, ordering, dependency check, and closure evidence — 61 lines) was removed from `docs/technical/roadmap.md`; its closeout entry already exists in this file. Also removed as closed context: open-work entry 6 (SA127 CLOSED), the Track 1 SA127 bullet, the resolved-and-delivered `SA127-ORDER-001` parallelism bullet, and the verbose `SA122A-AUTH-001` rationale bullet (compressed to its forward-binding rule plus a pointer here). SA122a's closure bullet was compressed to the registry's *input, not scope* status. `arch-audit.md`'s Finding 12 reconciliation was compressed to one resolved line and its superseded 2026-07-28 narrowing record dropped, since SA121's closure detail already lives here. Roadmap 608 → 540 lines; `START_HERE.md` line count refreshed (it was stale at 503). **Audits:** no finding became newly closeable — `arch-audit.md` carries Findings 11 (ticketed as SA128/SA122b), 7, 2, 4; `tech-audit.md` stands at zero open findings. **Track assignment: complete, no gaps** — all five open entries carry a track; v88 items carry the `v88` scope tag. **Rebalancing: no move available, and none taken.** The critical path is unchanged and entirely on Track 3 (`SA117c → SA117e → SA112a…f → SA96-PUBLISH`), causally serial on evidence validity. Track 2 remains a closed worktree (`5193f198` + `5b5de830` unmerged, SA115 merge-gated). Track 1 is one serial worktree with SA128 **in flight**, so nothing may be inserted ahead of it without preempting in-process work; SA129/SA130 are already correctly homed and file-disjoint. `SA112A-TRACK-002` is carried unchanged as the sole open decision, and remains a decision rather than an applied speedup precisely because taking it would delay in-flight SA128. **Truly green:** Track 1 (off-path) and Track 3 (on the critical path); Track 2 remains not truly green (finish blocked by SA112f, merge blocked by SA112e). No source files touched.
  - **Dependency upgrade to latest stable across both surfaces — generator repo and generated projects (2026-07-29).** Moved every pin to the current stable release, verified against PyPI/npm/endoflife.date on the day. **Runtime floor:** `runtime_pins.PYTHON_VERSION` 3.13 → **3.14** (Docker tag `python:3.14-slim-bookworm`, CI matrix, mypy/ruff targets) and `PYTHON_CONSTRAINT` → `>=3.14,<3.15`. Because `check_generator_python_constraints`/`check_module_python_constraints` require exact equality, this raised `requires-python` on all four generator packages, `quickscale_devtools`, and all 12 modules — **Python 3.13 is no longer supported**. `DJANGO_CONSTRAINT` `>=6.0.3` → `>=6.0.7` with the 12 module pins moved in lockstep (`_EXPECTED_MODULE_DJANGO_CONSTRAINT`), retiring the previously-documented `>=6.0.5`/`>=6.0.3` intentional drift. PostgreSQL 18 and Node 24 unchanged — 18 is the current major, and Node 24 is the Active LTS (26 is Current until Oct 2026). **Generated-project deps:** gunicorn 25→26, mypy 1.19→2.3, ruff 0.15→0.16, whitenoise 6.8→6.12, pytest 9.0→9.1.1, pytest-django 4.8→4.12, django-stubs 6.0.2→6.0.7, pre-commit 4.1→4.6.1, psycopg2-binary/dj-database-url patch bumps; Poetry in `Dockerfile.j2` 2.4.0→2.4.1. `virtualenv` deliberately held at `>=20.29,<21.0` (the recorded Docker import breakage). **Repo dev deps:** ruff 0.16, mypy 2.3, pytest 9.1.1, coverage 7.15.2, pylint 4.0.6, vulture 2.16, pytest-playwright 0.8, click 8.4.2, plus type stubs. **Module manifests:** `dependencies:` floors in all 12 `module.yml` files were badly stale versus their own package pins (auth declared `django-allauth>=0.63.0` against a `>=65.14.1` pin; analytics `posthog>=3.7.4` against `>=7.22.1`) and are now realigned; source and `data/manifests` snapshots re-synced via `scripts/sync_module_manifests.py`. **Frontend (`showcase_react`):** pnpm 11.0.9→11.18.0, eslint 9→10, `@eslint/js` 9→10, eslint-plugin-react-hooks 5→7, react-refresh 0.4→0.5, jsdom 25→30, globals 15→17, jest-dom 6→7, prettier-plugin-tailwindcss 0.6→0.8, typescript-eslint 8.65, plus Vite 8.1.5, Vitest 4.1.10, React 19.2.8, Playwright 1.62 and all Radix packages. **Deliberately frozen:** TypeScript held at `^6.0.3` — typescript-eslint 8.65 (its newest) peer-requires `typescript <6.1.0`, so TS 7 is not installable; Tailwind 4, zod 4 and lucide 1 deferred as breaking rewrites; `@types/node` held at `^24` to track the Node 24 runtime rather than npm's newest. **Defects fixed, surfaced by react-hooks 7:** `OrgSettingsPage` and `OrgMembersPage` synced state via `useEffect` (now adjust-during-render and derived-at-render respectively) and `FormRenderer` assigned `window.location.href` (now `location.assign`). **Ruff config-root shadowing (latent, pre-existing):** every package/module `pyproject.toml` carrying a `[tool.ruff.*]` table shadows the root `ruff.toml`, so those trees had always been linted with Ruff's built-in defaults; 0.16 widened those defaults and surfaced ~900 findings. Each now restates `target-version` and pins `select = ["E4","E7","E9","F"]` explicitly, documented in the implementation contract as its own follow-up cleanup. `test_git_utils.py` invoked a bare `"python"` off PATH for its hermetic wrapper runs and now uses `sys.executable`. **Undeclared dev dependency fixed:** the installed `.git/hooks/pre-commit` invokes `python -m pre_commit` from the project venv, but `pre-commit` was declared in no `pyproject.toml` — it existed only ad hoc in whatever venv a maintainer had built, so any `poetry env use` / venv rebuild silently broke committing. Now a declared root dev dependency (`^4.6.1`); `poetry install` alone is sufficient. All hooks pass `--all-files`. **Docs swept for the new floor:** `implementation_contract.md` runtime-pin inventory (Python/Django rows, the retired Django drift note, pnpm literal) plus three newly-recorded properties — that generator/module constraints are *enforced*-equal rather than merely conventional, that `quickscale-devtools` carries the constraint but sits outside `_GENERATOR_PACKAGES` so drift there is silent, and that repo sources now require a 3.14 interpreter to **parse** (so nothing may execute them via a bare `python3`). `development.md`, `user_manual.md` and `smoke_install.sh` comments moved to 3.14 (`bootstrap.sh` needed no change — it derives the spec from `pyproject.toml`). `module-extension.md`'s new-module template was corrected: it advertised `requires-python >=3.13` and `Django >=6.0.3,<7.0.0`, the latter an upper bound no module actually uses, so a module built from it would have failed `check_module_django_constraints` on its first test run; it now also carries the required `[tool.ruff]` block. Generated-project README now states the pin exactly rather than `3.14+`, which wrongly implied 3.15 was acceptable. **Validation:** `poetry lock` + `poetry install` clean on 3.14.6; **4697 passed, 2 skipped** (core + CLI); per-module suites green except pre-existing PostgreSQL-role (BYPASSRLS) env requirements and one pre-existing billing cwd artifact; `make lint` all green; mypy 2.3 clean over 92 files; rendered theme `pnpm install`/`type-check`/`lint --max-warnings 0`/`build`/**75 tests** all pass. SA90 golden emission manifests re-hashed for the 9 changed files per variant (no files added or removed).
  - **SA117c recorded partial delivery — lock-structure comparison checkpoint preserved, exhaustive Python-binding proof still open (2026-07-29; task remains unchecked and release-blocking).** **Done:** added the preferred `make sa117-lock-diff` route and a lock-structure comparison plus broad fail-closed inventory validation over an immutable baseline SHA and root-identified candidate: 55 required files, 66 canonical version values, exactly twelve allowed module-lock version leaves normalized, every other lock change treated as drift, and malformed/partial input treated as a controlled error for the covered forms. Evidence is schema-versioned, deterministic, atomic, digest-bound, stale-safe, and protected against input/output collisions; direct/Make parity, the legacy weak route, nested mirrors, structural YAML/TOML parsing, broad Python-binding validation, temporal TOML values, and cleanup failures have focused regressions. Exhaustive Python binding validation is explicitly not claimed. **Validation:** Ruff check/format, `make version-check`, direct/Make clean evidence parity at 55/66, exact four-file allowlist, `git diff --check`, **33 focused tests**, and **76 combined tests** passed. Review resolved `SA117-CR-006`, `SA117C-CR-007` through `SA117C-CR-011`, and `SA117C-CR-013`. **Pending / blocking:** terminal delta review returned `STATUS: partial` on `SA117C-CR-012` (**high**, correctness): dotted imports, unprovable star imports, and Python 3.13 type-parameter bindings are not yet exhaustively recognized as possible `__version__` rebindings, so the 66-value oracle can still false-green. `ACR-META-001` is **low/advisory** handoff-command precision. **Decisions needed:** none — the maintainer explicitly selected recorded partial delivery. This merge is preservation only, not a waiver, completion claim, split-push authorization, or release-ready evidence; SA117c remains unchecked and SA117e remains blocked pending the narrow parser/test correction, fresh QG, and full-scope `STATUS: ok` review.
  - **SA127 complete — zero-module installed `apply` fixed (2026-07-29; functional commit `a2eb7a6c`).** Short-circuits managed wiring on an empty module selection so a module-less project can `plan`/`apply` from an installed wheel without requiring embedded manifests. **Behavior:** in `regenerate_managed_wiring`, when `selected_modules` is empty, base-path preparation and adapter refresh are skipped and empty managed wiring is written directly; `_prepare_modules_base_path` and its error message are byte-unchanged for the non-empty case. **Exact file scope:** `quickscale_cli/src/quickscale_cli/utils/module_wiring_manager.py` and `quickscale_cli/tests/test_module_wiring_manager_manifest.py`. **Focused test result:** 36/36 pytest passed (empty-selection success + written-but-empty managed files; non-empty selection under unconfigured context still fails with the existing message). **Quality gates:** `make quality`, `make format`, `make lint`, `make typecheck` all green. **Isolated local-wheel external zero-module apply:** local wheel build, pip install into throwaway external venv, `quickscale plan` with modules skipped, `quickscale apply` completed through `poetry install` — zero modules, zero embedded manifests, zero source-tree references. **Independent full-scope review:** `STATUS: ok` with no blocking findings. **Discovered findings/blockers:** `SA127-REV-001` is low/advisory, handoff-metadata-only — concerns how `TEST COMMAND` prose was recorded in handoff metadata; corrected in orchestration, not a product/docs blocker. No unresolved SA127 blocker exists. SA127 roadmap checkbox marked `- [x]`; Track 1 continues with SA128.
  - **`SA122A-AUTH-001` RESOLVED — SA122a closed as superseded, parity checker rebuild ticketed as SA128 (2026-07-29; documentation only; no source files touched).** The maintainer selected **option (b): replace the checker, keep the registry.** **Kept from `73c153b2`:** `scripts/gate_registry.json` (seven entries, publish a required context for all five conformance gates per `SA122-DEC-001`), the `make check-gate-parity` entrypoint, and the maintainer documentation — all remain merged and usable, so SA112e and `SA115-CI-001` can record their e2e paths in the registry today, which was the sequencing benefit SA122a existed to protect. **Rejected:** the inventory extractors. They decide gate membership by regex over lines — `_extract_makefile_targets` re-implements Make's grammar by hand, and the shell/`run:` scrapers match `\bmake\s+(...)` with prefix-based `echo`/`#` filters — which false-greens in both directions: a gate named in a heredoc, a comment on a continuation line, or an uncalled shell function reads as covered, while `make $(GATES)`, a variable-composed target, an `include`d definition, or a gate reached through `check`'s delegation is invisible. **Option (a) declined** because each remediation cycle closes one deceptive form and leaves the next — the compounding shape arch Finding 11 exists to remove, and that lane already terminated at `STATUS: partial`. **Option (c) declined** because reverting discards a sound registry. **New ticket SA128 (`Tier 2 · Track 1 · deps: none`)** derives each context's inventory from its source's semantics: Make asked for its own expansion (`make -qp` / `--dry-run`), shell parsed to a syntax tree or dry-run with a stubbed `make` recording argv, workflow YAML keeping the existing `yaml.BaseLoader` structural path and extending it to E2E extraction, with **unparseable input a hard error rather than an empty inventory** ([decisions.md §fail-hard-principle](./docs/technical/decisions.md#fail-hard-principle)). Its acceptance requires an adversarial fixture set that false-greens none of the known forms, presence for delegated/variable-composed targets, the five expected publish omissions still reported exactly, and the two focused MyPy errors gone rather than baselined. Allowlist: `scripts/check_gate_parity.py`, `scripts/test_gate_parity.py`, plus `pyproject.toml` only if a shell-AST dependency is chosen; the registry and Makefile target are **inputs, not scope**. **Finding disposition:** SA128 owns `SA122A-CR-003` (high/security-boundary), `SA122A-CR-002` (high/correctness), and `SA122A-QG-001` (medium/completeness); `SA122A-CR-004` is carried low/advisory. **Board effect:** Track 1's queue is **SA127 → SA128 → SA122b** (SA122b's `deps` re-pointed to SA128, merge bound behind SA112e unchanged; v88's SA123 `deps` also re-pointed); no track's can-start/can-finish/can-merge changed, and Track 1 stays truly green via SA127. **One open decision remains anywhere on the board: `SA112A-TRACK-002`.** Roadmap 489 → 503 lines; `START_HERE.md` line count refreshed.
  - **Roadmap cleaning pass (2026-07-29, second pass of the day; documentation only; no ticket state changed).** **Closeout transfer:** the completed **SA117b** block (checked `[x]`, ~11 lines of completion evidence, absorbed-finding note, and pending/out-of-scope recital) and the SA117a closure recital were removed from `docs/technical/roadmap.md` — both closeouts already exist in this file — leaving one two-line pointer that carries only the forward-binding residue (`SA117-CR-006` → SA117c). Also removed as closed context: the `SA117B-REVB-003` diagram-checkpoint narrative, the SA117b track-readiness detail bullet, the "current state after SA117b closeout" paragraph, and the resolved-findings recital in the SA117 umbrella. SA122a's verbose partial-delivery recital was compressed to its still-live blockers plus a CHANGELOG pointer. Roadmap 507 → 489 lines; `START_HERE.md` line count refreshed. **Audits:** no finding became newly closeable — `arch-audit.md` carries Findings 11 (ticketed as SA122a/SA122b), 7, 2, 4, and `tech-audit.md` stands at zero open findings; arch Finding 12's closure was already transferred in the entry below. **Track assignment: complete, no gaps.** **Rebalancing — one pure speedup applied, one decision reframed.** `SA127-ORDER-001` is **RESOLVED and applied**: Track 1 now runs **SA127 → SA122a → SA122b**. The prior pass surfaced this as a maintainer decision because reordering would preempt in-process SA122a; that cost no longer exists, because SA122a's merged partial delivery is itself blocked pending maintainer authorization, so putting the release blocker first parks nothing. Independence re-verified (both `deps: none`; SA127's two-file allowlist disjoint from the registry/checker files; neither needs PostgreSQL/Docker; neither splits one logical change). Conflict surface: shared closeout files only, covered by the Merge procedure's `git merge v87`-before-merge-back step. Every reference was re-pointed — open-work summary, entry 4, the Track 1 header and ordering list, SA127's ticket tag and Ordering paragraph, the dependency diagram, the diagram checkpoint, the parallelism bullets, the `SA112A-TRACK-002` cost bullet, the Track 1 three-state bullets, the Net, and both open-decision paragraphs. **New open decision `SA122A-AUTH-001`** replaces the resolved `SA127-ORDER-001` on the board: authorize one more scoped remediation cycle, replace the checker with a structural parser, or revert the groundwork — recommendation (b), which is what the security-boundary finding actually requires and preserves every ratified rule including `SA122-DEC-001`. **Branch state re-measured:** `wt-track1` and `wt-track3` carry zero commits unmerged into `v87`; `wt-track2` still carries `5193f198` + `5b5de830` and remains closed to new work. **Truly green:** Track 1 (via SA127, on the critical path) and Track 3 (SA117c, on the critical path); Track 2 remains not truly green. No source files touched.
  - **SA122a recorded partial delivery — gate-registry groundwork preserved (2026-07-29; functional commit `73c153b2`; task remains open; not published).** **Done:** added the seven-entry JSON registry, standalone parity checker/Make target, maintainer documentation, and focused tests; current diagnostics report exactly the five expected publish omissions, 112 focused tests pass, and Ruff/format plus `make typecheck`, `make quality`, and `make check` are green. **Pending / blocking:** `SA122A-CR-003` (**high**, security-boundary) for false-green-capable workflow/Bash parsing; `SA122A-CR-002` (**high**, correctness) for incomplete binding/stage/dependency/Make-delegation/E2E-ownership enforcement; `SA122A-QG-001` (**medium**, completeness) for missing adversarial proof and two focused-MyPy errors; `SA122A-CR-004` is **low/advisory** documentation consistency. **Decisions needed:** a fresh continuation requires explicit maintainer authorization for another narrow remediation/review cycle or replacement/revert; terminal review stayed `STATUS: partial`, so the merge is preservation only and does not authorize SA122b or release use.
  - **SA117b complete — split-publication safety and resilience (2026-07-29; not published).** Functional commit `efd86d21` remains green and independently approved: blank origins fail closed before mutation; verifier-owned PostgreSQL validation resources clean up across signals/timeouts; trusted Git bootstrap and filename-safe staged-index checks protect publication; and the absorbed dependency-sync non-continuation regression is covered. The accepted executable evidence remains 656 tests plus Ruff, format, typecheck, quality, diff, and live Docker cleanup checks. A fresh full-file roadmap review returned `STATUS: ok` and resolved the sole remaining documentation finding, `SA117B-REVB-003`, by confirming one consistent SA117b → SA117c → SA117e → SA112a chain. No SA117b blocker or decision remains; SA117c is next. Splits, tag, PyPI publication, and SA112 remain open.
  - **arch-audit Finding 12 CLOSED — `quality-baseline-monotonicity-unenforced` (2026-07-29).** SA121's delivered gate was re-verified in code this pass and the finding is retired from `arch-audit.md`'s live findings: `scripts/check_quality_baseline_monotonicity.py` diffs `scripts/quality_baseline.json` keys and ceilings against the **merge base**, rejects any positive delta or new exemption, and reports entry key, old value, new value, and the required waiver reference; `scripts/check_quality.sh:117-138` invokes it as a **blocking** gate that aborts `make quality` on a policy violation; the only escape is a structured waiver (owner, reason, expiry, ceiling) in the separate `scripts/quality_waivers.json` ledger, which is present and empty. This is arch-audit Option 1 as recommended, and it separates measurement from permission, so the snapshot can no longer authorize its own growth. The `max_lines` half of the original finding had already dissolved with SA125 (`SA125-DEC-001`). Enforcement-census row 14 flips from *convention / deteriorated* to *gated*; the summary table drops from five ranked findings to four (11, 7, 2, 4); `tech-audit.md`'s matching structural smell and quality-baseline watch item are updated. **Arch Finding 11 is now the only open, ticketed audit finding** (SA122a/SA122b); Findings 7/2/4 remain deliberately unscheduled.
  - **Roadmap cleaning pass (2026-07-29; documentation only; no ticket state changed).** **Closeout transfer:** the completed **SA121** block (checked `[x]`, ~25 lines including `SA121-DEC-002`'s full rationale) was removed from `docs/technical/roadmap.md` — its closeout entry already exists in this file — along with the superseded SA117b 2026-07-28 historical checkpoint, the now-delivered `SA117A-CR-002` pre-written literal diff transcription block, SA117b's verbose validation-metadata recital (the exact reproducible command is preserved below for `SA117-META-001`), and the SA117a closure recital. `SA121-DEC-001` and `SA121-DEC-002` were dropped from Standing decisions as closed-ticket execution context. Roadmap 562 → 507 lines. Every reference was re-pointed: the open-work summary, the Track 1 header and ordering list, the green-gate status paragraph, the audit-standing paragraph, the cross-track-edges paragraph, the dependency diagram, the parallelism bullets, the conflict-surface paragraph, and the track-readiness three-state bullets. **`SA117B-REVB-003` substantively corrected:** the dependency diagram no longer duplicates SA117e and no longer carries the closed SA121 node; it states one `SA117b closeout → SA117c → SA117e → SA112a` chain. The finding's *review* remains outstanding — SA117b stays unchecked until a fresh full-scope documentation review returns `STATUS: ok`; this pass supplies the correction, not the review. **Track assignment: complete, no gaps** — all six open entries carry a track; v88 items correctly carry the `v88` scope tag. **Rebalancing: no pure speedup available; one new decision surfaced.** The critical path is unchanged and entirely on Track 3 (`SA117b closeout → SA117c → SA117e → SA112a…f → SA96-PUBLISH`), causally serial on evidence validity and unable to fan out. Track 2 remains a closed worktree (`wt-track2` still carries `5193f198` + `5b5de830`, verified this pass; `wt-track1`/`wt-track3` are fully merged into `v87`). New **`SA127-ORDER-001`**: Track 1 currently queues release blocker **SA127** *behind* in-process governance-filler **SA122a**. The two are `deps: none`, file-disjoint, and service-free, so reordering is technically free — but SA122a is in process and is meant to land before SA112e/`SA115-CI-001` so those write into the registry rather than hand-syncing. Because reordering would delay an in-process ticket, it is **surfaced as a maintainer decision rather than applied**, with the recommendation to reorder unless SA122a is near merge. **`SA112A-TRACK-002` carried unchanged.** Audits: **arch Finding 12 closed** (entry above); Finding 11 stays live; `tech-audit.md` remains at zero open findings. No source files touched.

  - **SA117b recorded partial delivery — executable safety complete, documentation closeout open (2026-07-29; functional commit `efd86d21`; not published).** **Done:** blank-origin fail-close, uniquely scoped verifier-owned Compose cleanup across SIGINT/SIGTERM/timeout, publication-only trusted Git/origin/NUL controls, five-consumer parity, and the dependency-sync non-continuation spy are implemented; 656 tests and all Ruff/format/type/quality/diff gates passed, live Docker owned-resource cleanup passed, and final executable Review A returned `STATUS: ok`. **Pending / blocking:** `SA117B-REVB-003` (**medium**, consistency) — the roadmap dependency diagram duplicates SA117e and needs a fresh full-scope documentation review after correction. **Decisions needed:** none. SA117b remains unchecked; SA117c/e, SA112, split push, tag, and PyPI publication remain unimplemented/open. **Exact reproducible validation command (preserved for `SA117-META-001`, moved here from the roadmap):**

    ```bash
    PYTHONPATH=. poetry run pytest scripts/test_verify_public_module_apply.py quickscale_core/tests/test_git_utils.py quickscale_cli/tests/commands/test_module_commands.py quickscale_cli/tests/commands/test_development_commands_extended.py quickscale_cli/tests/test_apply_command_extended.py quickscale_core/tests/test_apply_embedding_steps.py -x --tb=short && poetry run ruff check scripts/verify_public_module_apply.py scripts/test_verify_public_module_apply.py scripts/publish_module.py quickscale_core/src/quickscale_core/utils/git_utils.py quickscale_core/tests/test_git_utils.py quickscale_cli/src/quickscale_cli/commands/development_commands.py quickscale_cli/tests/commands/test_development_commands_extended.py quickscale_cli/tests/test_apply_command_extended.py quickscale_cli/tests/commands/test_module_commands.py quickscale_core/tests/test_apply_embedding_steps.py && poetry run ruff format --check scripts/verify_public_module_apply.py scripts/test_verify_public_module_apply.py scripts/publish_module.py quickscale_core/src/quickscale_core/utils/git_utils.py quickscale_core/tests/test_git_utils.py quickscale_cli/src/quickscale_cli/commands/development_commands.py quickscale_cli/tests/commands/test_development_commands_extended.py quickscale_cli/tests/test_apply_command_extended.py quickscale_cli/tests/commands/test_module_commands.py quickscale_core/tests/test_apply_embedding_steps.py && make typecheck && make quality && git diff --check
    ```

    Live Docker evidence: SIGINT returned 130; SIGTERM returned 143 with forced-cleanup failure; the timeout failure was captured; the exact owned container, volume, and network were removed; unrelated controls survived; no residuals remained.
  - **SA121 closeout — quality-baseline monotonicity gate complete (2026-07-28).** `SA121-DOC-CR-002` resolved by full-scope documentation review returning **STATUS: ok** over the corrected topology. The duplicated SA122a node and the former "no cross-track edge" claim are replaced by the explicit two-edge statement (SA122b→SA112e, SA115→SA112), verified consistent across all dependency, ordering, and diagram surfaces. Functional delivery (`9448a0f7`) was already independently reviewed and green: 153 focused tests in 6.68s, 0 violations / 0 waivers, `make quality` exit 0 with 0 baseline regressions; `SA121-CR-003` and `SA121-CR-006` resolved. `SA121-CR-007` remains **low/advisory, non-blocking**: historical CHANGELOG prose says 156/156 tests while final accepted evidence says 153; no cleanup required for closure and historical entries are preserved as-is. **No blocker remains.** SA121 roadmap checkbox marked `[x]`. The green-gate milestone and release remain unclaimed under this ticket — SA122a follows.
  - **Roadmap cleaning pass (2026-07-28; documentation only; no ticket state changed).** Sixth-pass sweep of `docs/technical/roadmap.md`. Closed narrative removed from the roadmap into this file: the SA117a closure account (`SA117-DEC-005`) and the retired `SA117-QG-REMED` quality-remediation block, both already recorded here in full — only their forward-binding rules were retained (no CC criterion in any SA117 child's verify list; no line-reduction refactor on `module_commands.py` / `manifest/loader.py` / `git_utils.py`; leave `make quality` no worse than found). The dependency-and-parallelization overview was redrawn: the duplicated SA122a node is gone and the former "no cross-track edge" claim is replaced by an explicit two-edge statement (SA122b merges after SA112e; SA115 merges after SA112), which is the substance of `SA121-DOC-CR-002`. That finding still awaits full-scope documentation review before it may be marked resolved, so SA121 stays unchecked. Branch state re-measured: `wt-track1` and `wt-track3` zero commits unmerged into `v87`; `wt-track2` still carries `5193f198` + `5b5de830`. No audit finding became closeable; both audits stand where the 2026-07-26 reruns left them.
  - **SA117b plan-review-cap checkpoint (2026-07-28; documentation only; task remains open).** **Done:** Track 3 was clean and synced to `v87` at `76462b13`; the Poetry environment, Docker daemon, and GitHub origin were ready; focused discovery mapped the publication, verifier, Compose-owner, shared-Git, and caller-parity seams. Two bounded review-driven re-plan cycles resolved `SA117B-PLAN-001` through `SA117B-PLAN-004` at plan-detail level: exclusive verifier-owned Compose identity with signal/timeout cleanup, publication-only trusted Git/NUL controls, five-consumer caller parity, and exact reviewed base/tip merge identity. **Pending / blocking:** `SA117B-PLAN-005` (**medium**, completeness) requires Ruff check, Ruff format-check, and repository type-check gates before executable review, after resynchronization, and post-merge. The task findings `SA117-CR-003`, `SA117-CONT-PR-003`, `SA117-CONT-PR-006`, and absorbed `SA117A-CR-002` remain unimplemented. **Decisions needed:** none. The plan-review cap triggered recorded partial delivery; no source/test file changed, no task validation or executable review ran, SA117b remains unchecked, SA117c remains blocked, and no split push or publication is authorized.
  - **SA121 review-cap checkpoint — quality-baseline monotonicity gate (2026-07-28; recorded partial delivery; task remains open).** **Done:** functional commit `9448a0f7` landed the minimal strict-key parser correction and completed the reviewed executable SA121 scope. QG run 2 passed **153 focused tests in 6.68s**; the helper against `v87` reported **0 violations / 0 waivers**; `bash -n` passed; `make quality` exited 0 with **0 baseline regressions**; `scripts/quality_baseline.json` remained unchanged and the waiver ledger stayed empty. Formatter-normalized full-scope functional review returned **STATUS: ok**; `SA121-CR-003` and `SA121-CR-006` are resolved. **Pending / blocking:** after the shared-file reconciliation consumed two fix/re-review cycles, `SA121-DOC-CR-002` (**medium**, consistency) remains: the roadmap topology duplicates SA122a and denies surviving SA122b→SA112e / SA115→SA112 merge-order bounds. `SA121-CR-007` remains **low/advisory, non-blocking** for stale historical numeric test prose. **Decisions needed:** none. The review cap triggered recorded partial delivery; SA121 remains unchecked, its executable delta is preserved, and a fresh continuation must correct the topology and obtain full-scope documentation review before SA121 may close. The green-gate milestone and release remain unclaimed.
  - **SA117a third plan-cap checkpoint (2026-07-28; documentation only; task remains open).** **Done:** Track 3 was clean and synced to `v87` at `fc8a0474`; Poetry 2.4.1, Python 3.13.14, GNU Make 4.3, and the worktree `.venv` were ready; focused discovery confirmed the sole source target; and two bounded planner/reviewer cycles made the normal implementation, validation, staging, commit, rollback-ref, abort, reset, and disarm paths literal. `SA117A-PLAN-014` is resolved at plan-detail level, and the proposed `SA117A-CR-002` spy target is correct. **Pending / blocking:** `SA117A-PLAN-010` (high) requires the immutable accepted post-sync Track 3 tip to be bound to the current full-scope Review B and consumed unchanged by merge-back; `SA117A-PLAN-013` (medium) requires a copyable shared-document conflict sequence that verifies the recorded unmerged set before staging, emptiness after staging, and reruns affected roadmap/changelog invariants; `SA117A-CR-002` (medium) remains unimplemented. **Decisions needed:** none. The plan-review cap triggered recorded partial delivery; no source or test file changed, no task validation ran, SA117a remains unchecked, and SA117b remains blocked.
  - **`SA117-DEC-005` — SA117a closed, residue folded into SA117b, gates matched to change size (2026-07-28; documentation only).** **Why.** SA117a's load-bearing content — the exact canonical lockstep comparison closing `SA117-CR-001` — was verified present in code: the private lockstep-only canonical parser in `quickscale_core/src/quickscale_core/manifest/loader.py`, and all twelve source plus all twelve bundled `module.yml` versions equal to `VERSION` `0.87.0`. That is the entirety of what SA112b's evidence validity depends on. After `SA126-DEC-001` removed its complexity share, SA117a's remaining scope was **one `assert_not_called()` spy in one existing test** (`SA117A-CR-002`) — which nonetheless consumed **one change-review cap, three plan-review caps, and a full-scope documentation review certifying a two-document checkpoint that implemented nothing: five review artifacts, zero lines.** **Actions.** (1) **SA117a closed**; the SA117 chain is now SA117b → SA117c → SA117e, and **SA117b is Track 3's next action** with `deps: none`. (2) **`SA117A-CR-002` absorbed into SA117b with its literal diff pre-written** — target `quickscale_cli/tests/commands/test_module_commands.py`, test `test_update_rolls_back_noncanonical_manifest_version` (`:1560`), inserting a third `patch(...) as spy_sync_deps` after the existing `spy_commit` patch (`:1632-1634`) and `spy_sync_deps.assert_not_called()` after `spy_sync_state.assert_not_called()` (`:1651`), in the identical shape as the two spies that block already uses. The assertion holds because `_read_embedded_module_version` (`module_commands.py:1302`) raises before `_sync_module_dependencies` (`:1318`); a non-tautology check is specified. This makes the remaining work transcription rather than planning. (3) **`SA117A-PLAN-010` and `SA117A-PLAN-013` retired as ticket blockers** — they described reviewed-tip identity and shared-document conflict reconciliation, generic merge ceremony applying to every child rather than to SA117a's content, and are restated once as standing merge-procedure guidance in the handoff contract. (4) **Proportionality rule added**, binding SA117b–SA117e: a child whose delta is confined to test assertions or documentation, with no production-behavior change, **requires no plan-review cycle of its own** — implementation plus one change review; scoped plan review is reserved for production behavior, security boundaries, and external/public surfaces. **Splitting SA117a further was explicitly rejected** as the wrong remedy — the ticket was already two lines, and additional handoffs were the cost rather than the cure; this is the inverse of `SA126-DEC-001`, where splitting was correct because it separated two genuinely different concerns. **Net effect on the critical path:** the chain loses a node and its head becomes real production work (`SA117-CR-003` security, `SA117-CONT-PR-003` resilience, `SA117-CONT-PR-006` completeness) instead of a stalled test assertion. Every reference re-pointed: open-work list and entry 6, the SA117 umbrella and its split note, the handoff contract, SA117b's tag/deps/body, SA117e's deps, the dependency diagram, the critical-path chains, the parallelism and cross-track paragraphs, the Track 3 readiness bullets and detail bullet, the Net, and Standing decisions. No source files touched; SA117b remains unchecked.
  - **Roadmap cleaning pass (2026-07-28, fifth pass of the day; documentation only).** **Closeout transfer:** the completed **SA126** block was removed from `docs/technical/roadmap.md` — it was checked `[x]` with full completion evidence and its closeout entry already exists below, so per the roadmap's own rule it no longer belongs in the open-work file. Every reference was re-pointed: the `SA117-QG-REMED` retirement note, the Track 1 header and ordering list (now **SA121 → SA122a**, with the now-obsolete single-owner exception note deleted), the green-gate status block, the dependency diagram, the parallelism bullets, the cross-track-edge paragraph, the track-readiness bullets, and the Net. **No audit finding became closeable:** arch Findings 11, 12, 7, 2, 4 remain open (12 stays open precisely because SA121 has not landed), and `tech-audit.md` still carries zero open findings. **Track assignment complete; no gaps.** **Two verified state changes.** (1) `make quality` was **rerun directly and exits 0** with 0 warning and 0 critical baseline regressions; `radon cc` confirms SA126's closeout at `_perform_module_embed` **18** (ceiling 20) and `_update_single_module` **13** (ceiling 17); `QUARANTINE_TICKETS` verified **empty**. This cleared `SA121-QG-001`, making **Track 1 truly green for the first time in this release train** (start/finish/merge all yes). (2) **SA117a took a fourth review cap with still zero source delta** — one change-review plus three plan-review caps — for a ticket now scoped to a single `assert_not_called()` spy in one test file, with both surviving findings (`SA117A-PLAN-010`, `SA117A-PLAN-013`) concerning merge/staging procedure rather than the test. A process warning was added to the Track 3 readiness bullet: the three states are all YES, so nothing blocks it, but a fifth cap should be read as the plan-gate process rather than engineering being the critical-path constraint. **Green-gate milestone remains unclaimed, and the roadmap now says why precisely:** the remaining gap is *evidence, not defects* — only `make quality` was rerun; `make ci` and `make ci-e2e` need exclusive PostgreSQL/Docker, are Track 3's to schedule, and **no open ticket owns the four-command rerun** (it is SA112f's closeout duty and SA96-PUBLISH's precondition). **Rebalancing: one open decision, `SA112A-TRACK-002`** — move SA112a onto Track 1, which needs **no fourth worktree** and so differs materially from the permanently-declined `SA112A-TRACK-001`. It is newly worth asking because SA126 closed and SA117a stalled again: Track 1's entire remaining queue is off the critical path while Track 3 is stuck at the critical-path head. Independence checks pass (deps satisfied, no SA117e bound before SA112b, three-file allowlist disjoint from SA121/SA122a and the whole SA117 chain, service-free validation, self-contained seam extraction). **Not applied, because it delays truly-green SA121** — which closes arch Finding 12 — so it is the maintainer's call, with the honest counter-argument recorded that SA117a's four caps were all plan-review failures, so a second plan-gated ticket may yield two stalled tickets rather than one. Conflict surface if taken is the shared closeout files only, covered by the Merge procedure. **Tier census: no open ticket is Tier 3.** No source files touched.
  - **SA126 complete — module-command complexity remediation (2026-07-28).** Functional commit `60c5d4f3` reduced `_perform_module_embed` to CC **18** (from 22; ceiling 20) and `_update_single_module` to CC **13** (from 18; ceiling 17). **102 focused tests passed**; `make quality`, `make lint`, `make typecheck`, and `git diff --check` all exited 0, with no tests or ceilings changed. Independent functional review returned **STATUS: ok** with exact behavior/message/caller parity and **no new findings/blockers**. Existing repository-wide quality warnings and MyPy informational notes were non-regressing and non-blocking. The roadmap checkbox is checked; **SA121 is now the next pending Track 1 task**. The final docs-inclusive review runs after this edit and is not claimed here.
  - **SA117a second plan-cap checkpoint (2026-07-28; documentation only; task remains open).** **Done:** Track 3 was clean, fast-forwarded to `v87` at `24919792`, and its Poetry environment was ready; two bounded planner/reviewer cycles resolved the original synchronization-transaction design gap (`SA117A-CONT-PR-008` / `SA117A-PLAN-011`) and specified the eight-case cleanup parity matrix (`SA117A-PLAN-012`). **Pending / blocking:** final review staging remains incomplete (`SA117A-PLAN-010`, high), validation/final-stage commands are not uniformly fail-fast and literal (`SA117A-PLAN-013`, medium), and merge-back lacks an armed rollback transaction (`SA117A-PLAN-014`, medium); source blockers `SA117A-CR-002` and `_perform_module_embed` CC 22 > 20 therefore remain untouched. **Decisions needed:** none — the maintainer selected recorded partial delivery at the cap. No source or test file changed, no implementation or completion evidence landed, SA117a remains unchecked, and SA117b remains blocked.
  - **Ticket split for parallelism — SA126 created, `SA112A-TRACK-001` resolved (2026-07-28, fifth pass of the day; documentation only).** **Tier census: no open ticket is Tier 3.** SA126, SA112e, and SA96-PUBLISH are Tier 1; every other open ticket is Tier 2. Both historical Tier 3s were already split (SA117 → SA117a/b/c/e by `SA117-DEC-003`; SA122 → SA122a/SA122b). **`SA112A-TRACK-001` resolved: the repository holds at three worktrees.** No Track 4 is created and SA112a stays on Track 3 — a second concurrent review lane is not worth one critical-path node while SA117a is still converging. Parallelism was obtained by splitting a ticket instead. **`SA126-DEC-001` — new Tier 1 ticket `SA126` on Track 1**, holding the two `make quality` blockers extracted from SA117a and SA117b: `_perform_module_embed` (CC 22 > 20) and `_update_single_module` (CC 18 > 17). The split was made on empirical rather than declared-Tier grounds — SA117a was declared Tier 2 but had produced three review caps (one change-review, two plan-review) with zero source delta while bundling a test-only assertion with a production refactor in a different file. **Both blocking functions live in the same file**, `quickscale_cli/src/quickscale_cli/commands/module_commands.py`, so assigning one to SA117a and the other to SA117b was itself the sole cause of that file being shared between two serial critical-path children; one owner **removes** a conflict surface rather than adding one. **Effects:** Track 1 moves from *can-finish: NO* to *can-finish: YES*, because `SA121-QG-001` (SA121's only external blocker) is now cleared by SA126 immediately upstream on the same track instead of waiting on the SA117 chain's plan gates; SA117a shrinks to a single test-file assertion with a correspondingly smaller staging allowlist; and the green-gate milestone stops sitting behind Track 3. Track 1 order becomes **SA126 → SA121 → SA122a**, with SA122b still merge-gated behind SA112e. SA126 is behaviour-preserving only, is forbidden from raising any `scripts/quality_baseline.json` ceiling (SA121 shrink-only rule), and its refactor falls inside SA117e's mandatory full-scope review as part of `v87`'s delta — a review-scope note, not a merge hazard, since SA126 merges well before SA117e runs. **`SA117-QG-REMED` retired**; neither SA117a nor SA117b carries a complexity criterion or touches `module_commands.py`. Every reference was re-pointed in the same pass: open-work summary and list numbering, the Track 1 section header and its new single-owner exception note, `SA117-QG-REMED`, SA117a's and SA117b's verify lists, SA117a's checkpoint and open-findings block, the green-gate current-status block, SA121's blocker list, the dependency diagram, the Track 1 ordering list, four parallelism bullets, the conflict-surface paragraph, the track-readiness three-state bullets, the Net paragraph, and Standing decisions. **Two tracks are now truly green and can run in parallel today** — Track 3 (SA117a) on the critical path and Track 1 (SA126) feeding it, with disjoint files, no ordering constraint between them, and no shared infra. **No open decision remains anywhere on the board.** No source files touched.
  - **Roadmap rebalancing pass (2026-07-28, fourth pass of the day; documentation only).** **No closeout transfer owed:** all seven tickets remain genuinely open, and no `arch-audit.md` or `tech-audit.md` finding became closeable (arch 11/12/7/2/4 open; tech-audit at zero open findings). **Track assignment complete**, no gaps. **One rebalancing move found, surfaced as open decision `SA112A-TRACK-001`** — SA112a is the only open ticket that is simultaneously on the critical path, dependency-satisfied today (it does not carry the SA117e bound, which begins at SA112b), file-disjoint from the entire SA117a–SA117c allowlist, and service-free to validate via `make smoke-install`. Running it concurrently removes one node from the critical path, but no existing track can host it (Track 3 is one-child-at-a-time, Track 1 holds runnable SA121, Track 2 is a closed worktree), so it costs a fourth worktree and a second review lane. Because that is a real cost rather than a pure speedup, the move was **not applied** — it is recorded for the maintainer, with SA112a staying on Track 3 by default. This **corrects a self-inconsistency**: the prior pass asserted "no rebalancing move is available" while simultaneously recording that SA112a is technically unblocked. The Parallelism bullet on SA112's serial chain was rewritten to scope its no-move claim to SA112b–SA112f, and SA112a's ticket tag now names the open decision. **Track states restated as three independent verdicts** (can start / can finish / can merge) per track, replacing single-verdict prose: **Track 3 is the only truly green track and is on the critical path** — SA117a can start, finish, and merge entirely on its own work. Track 1 can start and merge but **cannot finish** — SA121's acceptance needs `make quality` green, hard-blocked by SA117a and SA117b's complexity findings, unclearable by any maintainer decision. Track 2 can start but **cannot finish** (blocked by SA112f) **and cannot merge** (order-gated behind SA112e); both hard dependencies. **Verified against code, not prose:** `radon cc` re-measured `_perform_module_embed` 22 > 20 and `_update_single_module` 18 > 17; `scripts/quality_baseline.json` top-level keys confirmed as `schema_version`/`dead_code`/`complexity`/`duplication` with zero `large_files`; `scripts/quality_waivers.json` empty; `SA117-DEC-001` step 1 (stamp) confirmed landed with all twelve source **and** all twelve bundled `module.yml` versions at `0.87.0` and the canonical lockstep parser present in `manifest/loader.py`; `wt-track1`/`wt-track3` confirmed fully merged into `v87` and `wt-track2` still carrying `5193f198` + `5b5de830`. Conflict surface unchanged and covered by the Merge procedure. No source files touched, no ticket state or dependency changed.
  - **Roadmap verification pass (2026-07-28, third pass of the day; documentation only).** **No closeout transfer was owed:** every ticket in `docs/technical/roadmap.md` is genuinely open (SA117a/b/c/e, SA112a–f, SA96-PUBLISH, SA115, SA121, SA122a/b), and no `arch-audit.md` or `tech-audit.md` finding became closeable — arch Findings 11, 12, 7, 2, and 4 all remain open, and `tech-audit.md` already carries zero open findings with `TA62` reduced to a reconciliation line. **No track assignment was missing and no rebalancing move was available:** the critical path `SA117a → SA117b → SA117c → SA117e → SA112a → … → SA112f → SA96-PUBLISH` is entirely Track 3 and causally serial (evidence validity, not file overlap), so no off-path ticket can be moved onto it to shorten it; Track 2 remains closed to new work while merge-gated SA115 commits sit on its branch, and Track 1's two file-disjoint tickets stay on one worktree because neither feeds the critical path. **Verification against code rather than prose:** `radon cc` re-measured both green-gate blockers live (`_perform_module_embed` 22 > 20, `_update_single_module` 18 > 17); `SA125-DEC-001` was confirmed implemented (zero `large_files` entries in `scripts/quality_baseline.json`, that data retained as report-only in `scripts/check_quality.sh:587`); and SA121's merged partial delivery was confirmed present and inert (`scripts/quality_waivers.json` empty). A dated re-verification note recording those measurements was added above the Track readiness bullets; no other roadmap surface changed, because none was found stale or self-inconsistent.
  - **Roadmap closeout/rebalancing pass (2026-07-28, second pass of the day; documentation only).** **Closeout transfer:** the completed **SA125** block was removed from `docs/technical/roadmap.md` (455 → 425 lines); its decision rationale, implementation steps, completion evidence, sequencing, and SA121 file-overlap record are already held verbatim in the two SA125 entries below, so nothing was lost. Every surface that referenced it was re-pointed in the same pass — the open-work summary, the `SA117-QG-REMED` block, SA117a's and SA121's ticket tags (`deps: SA125 ✓` → `deps: none (SA125 ✓ closed)`), SA117a's now-obsolete deps note, the green-gate current-status block (rewritten to state **zero line-count blockers** and exactly two complexity blockers), the Track 1 ordering list, the dependency diagram, the "completed temporary upstream node" paragraph (deleted — the critical path no longer runs through Track 1), the parallelism and conflict-surface bullets, the track-readiness bullets, the Net paragraph, and the standing-decisions entry for `SA125-DEC-001` (kept as policy, restated as implemented and closed). Also corrected a stale code anchor: SA121's problem statement cited `scripts/check_quality.sh:792-814,824-846`, whose second range was the deleted `max_lines` block; it now reads `818-831`. `START_HERE.md`'s roadmap length refreshed to 425 lines. **Audits:** `tech-audit.md` stands at **zero open findings** and needed no transfer. `arch-audit.md` Finding 12 was **narrowed, not closed** — its evidence cited the `max_lines` half of the gate that SA125 deleted, so the anchor was corrected to the surviving `max_complexity` comparison and a dated Reconciliation-log line records that two of the three census increases (`module_commands.py` 1596→1608, `config_schema.py` 605→611) no longer have a gate to be self-authorized against, while `_validate_modules_section` 11→12 remains the live instance and SA121 still owns the fix. Finding 11 stays fully live for SA122a/SA122b; Findings 7/2/4 stay deliberately unscheduled. **Track assignment: complete, no gaps.** All six open entries (SA117a–e, SA112a–f, SA96-PUBLISH, SA115, SA121, SA122a/SA122b) carry a track; v88 backlog items correctly carry the `v88` scope tag under the assign-at-kickoff rule. **Rebalancing verdict: no move available, and none would help.** The critical path is unchanged at `SA117a → SA117b → SA117c → SA117e → SA112a…f → SA96-PUBLISH`, entirely on Track 3 and causally serial (evidence validity, not file overlap), so it cannot fan out. SA125's closure removed the last cross-track dependency, so Track 1 no longer feeds the critical path at all. The one theoretically splittable pair — SA121's continuation and SA122a, which are file-disjoint with no ordering constraint — was **left serial on Track 1**: Track 2 remains closed to new tickets because `wt-track2` still carries the merge-gated SA115 commits (`5193f198`, `5b5de830`, verified still unmerged this pass) and a track merges as a branch rather than as a ticket, and a fourth worktree would buy no release time because neither ticket is on or feeds the critical path. The parallelism bullet was rewritten to say this explicitly — the pair is serial by choice, not by dependency. No ticket changed track, so no ticket tag, per-module gate list, dependency diagram, ordering prose, or track-readiness bullet needed re-pointing on that account. **Conflict surface unchanged:** `CHANGELOG.md`, `docs/technical/roadmap.md`, and conditionally `docs/technical/decisions.md`, all covered by the Merge procedure's `git merge v87`-before-merge-back step; `.github/workflows/e2e.yml`'s path list stays serialized by SA115's `merge after SA112` bound; `scripts/quality_baseline.json` is a declared non-conflict now that its `large_files` section is gone — SA117a/SA117b do not touch it and SA121 is contractually forbidden from doing so (`SA121-DEC-001`). **Track review: all three tracks clean to continue, with no open maintainer decision anywhere.** Track 1 — SA121's parser continuation under `SA121-DEC-002`, then SA122a. Track 2 — SA115 heavy validation authorized under `SA115-DEC-001` step 3, yielding infra to Track 3, merge still queued behind SA112. Track 3 — a fresh scoped SA117a continuation closing `SA117A-CR-002` plus its single-item quality share (`_perform_module_embed` CC 22 > 20). `make quality` remains RED at `v87` HEAD, owned by Track 3, so the green-gate milestone stays unclaimed. No ticket state, dependency, or decision changed; no source files touched.
  - **SA125 — file line-ceiling quality gate retired (2026-07-28; complete).** Functional commit `05d0f2b8` removed blocking `large_files` enforcement, narrowed the monotonicity helper/tests to the surviving sections, and retained advisory large-file diagnostics as non-blocking output; they no longer affect the baseline, monotonicity checks, waivers, or exit status. Validation from the functional closeout: focused pytest **149 passed**; the SA125 subset **4 passed / 145 deselected**; Bash syntax, `make lint`, `make typecheck`, and `git diff --check` passed. `make quality` exited 2 with exactly the two pre-existing Track 3 complexity findings — `_perform_module_embed` CC **22 > 20** and `_update_single_module` CC **18 > 17** — and zero line-count blockers; these are not SA125 failures. `SA125-CR-ADV-001` remains a low/advisory consistency finding for stale comments/docstrings in the monotonicity helper/tests; behavioral semantics are correct, so it is non-blocking and does not reopen SA125. Functional independent review returned **STATUS: ok** and safe to commit. SA121 is now Track 1's next pending task, and SA117a's SA125 dependency is satisfied and unblocked.
  - **`SA125-DEC-001` — file line ceilings retired, complexity ceilings kept (2026-07-28; documentation only, SA125 complete).** The maintainer retired per-file `max_lines` enforcement from the quality gate and kept per-function `max_complexity`. Rationale: the ceilings are **measurement fossils** — `manifest/loader.py`'s 562 is exactly its size at `43f49350`, the commit that happened to be HEAD when `76c5cc55` captured the baseline, so an accident of timing had been promoted into a binding target (the mirror image of arch Finding 12, which names the same artifact acting as its own waiver authority). The gate also **taxes documentation**: `loader.py` is 686 lines — 348 code, 171 docstring, 134 blank, 33 comment — so the cheapest legal route to compliance was deleting ~124 of 204 documentation lines, including the docstring specifying SA117a's fail-hard rejection contract. It is **aimed against the project's own direction**, since `loader.py` grew 187 → 686 absorbing the declarative-manifest migration that `decisions.md` §module-derivation-schema mandates. And it **cannot distinguish mandated growth from decay** — the 124-line overage is almost exactly the 123-line region SA117a added for the reviewed exact-lockstep correction. Complexity ceilings are kept because they are local, actionable, ignore docstrings, and name a specific function. **SA125 implemented it** (`Tier 1 · Track 1 · deps: none`): drop `large_files` (41 entries) from `scripts/quality_baseline.json` and its blocking enforcement in `scripts/check_quality.sh`, narrow the SA121 monotonicity helper and its tests to the surviving sections, update `quality_tools.md`, and record the rule in [decisions.md §file-size-metric-policy](docs/technical/decisions.md#file-size-metric-policy); `complexity` (151 entries), `dead_code`, and `duplication` are untouched. **Consequences recorded across the roadmap:** `SA117-QG-REMED` collapses from five findings to two — `_perform_module_embed` CC 22 > 20 (SA117a) and `_update_single_module` CC 18 > 17 (SA117b) — while `module_commands.py` 1642 > 1608, `manifest/loader.py` 686 > 562, and unbaselined `git_utils.py` at 509 **dissolve with the gate rather than being remediated**, and SA117a/SA117b no longer touch `quality_baseline.json` at all. **Sequencing (maintainer decision):** SA125 runs first on Track 1 and Track 3's SA117a continuation waits for it, trading a short pause at the head of the critical path for the deletion of three of SA117a's five blockers and a 124-line `loader.py` refactor that would have been discarded; the rejected alternative was SA117a proceeding CC-only in parallel. SA125 also precedes SA121's continuation — they share `check_quality.sh`, both monotonicity scripts, and `quality_tools.md`, and are one coherent unit of gate-policy work — and it narrows `SA121-CR-003`'s remaining matrix, since the large-file separator-key cases stop existing. `SA121-DEC-001`'s prohibition on SA121 touching `quality_baseline.json` is unaffected; SA125 owns that file. Track 1's order is now **SA125 → SA121 → SA122a**, with SA122b still merge-gated behind SA112e. No source file was touched and no ticket completed.
  - **Roadmap closeout/rebalancing pass (2026-07-27, second pass of the day; documentation only).** No ticket completed since the previous pass, so nothing moved from `docs/technical/roadmap.md` to this file — the same seven tickets (SA117a–e, SA112a–f, SA96-PUBLISH, SA115, SA121, SA122a, SA122b) remain open, three at recorded partial checkpoints. Both audits were re-checked and need no closeout transfer: `tech-audit.md` already stands at **zero open findings** (its last closure, `TA62`, exists only as a Reconciliation-log line), and `arch-audit.md`'s open Findings 11 and 12 are still live context for SA122a/SA122b and SA121 respectively, while Findings 7/2/4 stay deliberately unscheduled. **Track assignment:** one gap fixed — `SA96-PUBLISH`'s ticket tag read `Tier 1 · v87` while the open-work list homed it on Track 3; the tag now reads `Tier 1 · Track 3 · v87`. Every other open ticket already carried a track, and v88 backlog items correctly carry the `v88` scope tag under the assign-at-kickoff rule. **Rebalancing verdict: no move available, and none needed.** Track 3 owns the entire critical path (`SA117a → SA117b → SA117c → SA117e → SA112a…f → SA96-PUBLISH`) as a causally serial chain that cannot fan out; Track 2 stays **closed to new tickets** while `wt-track2` carries merge-gated SA115 commits (`5193f198`, `5b5de830`); Track 1's SA121 and SA122a are file-disjoint but share one worktree, and splitting them onto a fourth worktree would buy no release time because neither is on the critical path. No ticket changed track, so no ticket tag, per-module gate list, dependency diagram, ordering prose, or track-readiness bullet needed re-pointing beyond the SA96-PUBLISH tag. Conflict surface unchanged: `CHANGELOG.md`, `docs/technical/roadmap.md`, and conditionally `docs/technical/decisions.md`, all covered by the Merge procedure's `git merge v87`-before-merge-back step; `scripts/quality_baseline.json` remains a declared non-conflict; after SA125's closeout, SA117a/SA117b do not modify it because their file-line findings are retired, while SA121's continuation remains contractually forbidden from touching it (`SA121-DEC-001`). **Roadmap purge:** superseded narrative was removed from `docs/technical/roadmap.md` (457 → 417 lines) — the SA117 Tier-3 sizing rationale and measured candidate breakdown, the SA117/SA117a/SA121 checkpoint "Done" recitals, SA117's byte-level manifest measurements and its duplicate "two independent gaps" restatement, SA112's pre-SA113 gap-analysis prose, SA115's pre-implementation design narrative and `SA115-DEC-002` load arithmetic, and duplicated Track 1/Track 2 parallelism bullets, two overlapping conflict-surface paragraphs, and a Net paragraph that restated the track-readiness bullets. All of it remains in this file; every open blocker, decision, verify criterion, allowlist, and ordering bound was preserved verbatim. Also refreshed `START_HERE.md`'s roadmap length. **Track review: all three tracks clean to continue, with no open maintainer decision anywhere for the first time in this release train.** Track 1 — SA121's parser continuation under `SA121-DEC-002`, then SA122a. Track 2 — SA115 heavy validation authorized under `SA115-DEC-001` step 3, yielding infra to Track 3, merge still queued behind SA112. Track 3 — a fresh scoped SA117a continuation closing `SA117A-CR-002` plus its `SA117-QG-REMED` quality share. `make quality` remains RED at `v87` HEAD, owned by Track 3, so the green-gate milestone stays unclaimed. No ticket state, dependency, or decision changed; no source files touched.
  - **SA117a exact-lockstep recorded partial delivery (2026-07-27; task remains open).** **Done:** the Track 3 correction now validates manifest and running-core versions as exact canonical three-component ASCII-decimal strings before tuple comparison, preserving the dedicated mismatch error while rejecting padded, zero-padded, signed, Unicode-digit, empty-component, malformed, and unequal values. Four-file regression coverage spans managed-wiring regeneration, standalone/apply embed fail-stop behavior, update rollback, and the real all-twelve apply-wrapper → step → manager path. Accepted task-scoped evidence is green: 42 targeted tests, a separate passing all-twelve JUnit proof, 4,606 core/CLI unit tests with 1 skip, 12/12 version and manifest parity, lint, typecheck, and diff checks. Independent review explicitly resolved `SA117-CR-001` and found production correctness, caller parity, breaking-change safety, and scope sound. **Pending / blocking:** `SA117A-CR-002` (medium, test gap) remains because the update rollback regression lacks an explicit `_sync_module_dependencies` spy and `assert_not_called()`; production sequencing is correct, but this non-continuation claim is not regression-proven. The synchronized `v87` state initially assigned SA117a its `SA117-QG-REMED` share across `module_commands.py` 1642 > 1608 lines, `manifest/loader.py` 686 > 562 lines (the incoming pre-SA117a checkpoint measured 624), and `_perform_module_embed` CC 22 > 20. `SA125-DEC-001` subsequently retired the line-ceiling gate, so the current SA117a quality-remediation share is only `_perform_module_embed` CC 22 > 20; together with SA117b's `_update_single_module` CC 18 > 17, these are the only remaining `make quality` blockers. No baseline ceiling may be raised as the remedy. The second review-fix cycle reduced the medium review ledger from two findings to one and hit the review cap, so SA117a remains unchecked and SA117b cannot start. **Decisions needed:** none; the next continuation must add the missing spy, clear SA117a's quality-remediation share, rerun the full validation/quality chain, and obtain independent review. This entry supersedes the earlier checkpoint's statement that `SA117-CR-001` itself remains open; it does not approve split publication or close SA117a/SA117.
  - **SA121 focused continuation recorded partial delivery (2026-07-27; task remains open).** Track 1 corrected the `quality_tools.md` account of policy-artifact `waiver_evaluations`, expanded strict-key/canonical-diagnostic tests, and isolated shell success tests from live-repository line-count drift; no runtime helper, baseline, waiver ledger, threshold, or Make target changed. Evidence: 156/156 focused tests, the helper against `v87`, Bash syntax, and `make check` passed. Full review remained `STATUS: partial`: `SA121-CR-003` is high/blocking because the triple-colon key is accepted as an orphan and the strict malformed-envelope matrix is incomplete; `SA121-CR-006` is medium/blocking because broad/subset assertions remain; `SA121-QG-001` is high/blocking because `make quality` exits 2 on the two pre-existing Track 3 complexity findings. SA125-DEC-001 retired the three file line-ceiling blockers; SA121 therefore remains unchecked pending its review findings and the two Track 3 complexity fixes. By explicit maintainer instruction this checkpoint is merged despite those findings; SA121 stays unchecked, and the merge is not a waiver, review approval, completion claim, or release-ready evidence.
  - **SA117 recorded partial delivery merged to `v87` (2026-07-27; task remains open and unapproved).** The Track 3 lockstep candidate — previously preserved as checkpoint `43d9b8fc` (69 files changed, 8 new `scripts/` helpers) — is merged to `v87` by explicit maintainer instruction as blocked preservation only. It stamps all twelve module metadata sets and bundled manifests to `0.87.0`, adds embed/managed-wiring lockstep enforcement, and adds lease-safe split-publication tooling; none of that executable content received independent approval in this continuation. The continuation made no source edit and ran no test because its mandatory plan gate remained `STATUS: partial`. Four executable blockers remain: `SA117-CR-001` (high, correctness — padded versions pass literal lockstep), `SA117-CR-003` (high, security — blank direct-CLI origins bypass the pre-mutation public-source gate), `SA117-CR-005` (medium, breaking — scope CLI/help/Make caller contracts disagree), and `SA117-CR-006` (high, completeness — lock-drift omits complete baseline semantics). Plan review resolved `SA117-CONT-PR-001`, `-002`, `-004`, and `-005` at plan-detail level but remains blocked by `SA117-CONT-PR-003` (high, resilience — validation-container cleanup is not armed throughout creation) and `SA117-CONT-PR-006` (medium, completeness — external Git controls lack a secure literal bootstrap and a filename-safe final staged-index check). Before clean continuation, a maintainer must authorize another focused plan/review cycle for those two findings or supply an accepted literal plan; plan-review `STATUS: ok` is required before source edits, and full-scope executable review `STATUS: ok` is still required before checking SA117, pushing splits, starting SA112, or treating this merge as release-ready. SA115 heavy validation remains **frozen** under `SA115-DEC-001` step 3 **Keep unauthorized**.
  - **Three maintainer decisions recorded (2026-07-27; documentation only).** **`SA115-DEC-001` step 3 — AUTHORIZED**, reversing the 2026-07-26 "Keep unauthorized" hold: SA115's heavy Docker/PostgreSQL validation may proceed, and the ratified `SA115-DEC-002` guard clamp (a fired memory guard must also clamp `E2E_XDIST_WORKERS=1`, visibly when it overrides an explicit value) implements as part of that phase. Two constraints attach — exclusive PG/Docker capacity is contended with SA117e and every SA112 heavy leg, so **Track 3 holds priority** and an SA115 run is abandoned/restarted rather than made to block a critical-path leg; and validation authorization is **not** merge authorization, since the `merge after SA112` bound exists for `.github/workflows/e2e.yml` path-list coordination with SA112e/`SA115-CI-001`, not for validation risk. Track 2 moves from validation-paused to runnable while staying closed to new tickets. **`SA117-DEC-004` — DEFER (option b)**: the SA117 scope meta-tooling and its `SA117-CR-005` caller-contract inconsistency are dropped from the SA117 acceptance set and tracked as new v88 backlog item **SA124**; the merged code at `43d9b8fc` stays in place but unadvertised, reviewed by SA117e as inert merged code rather than as a contract to prove. SA117 therefore has four children (SA117a, SA117b, SA117c, SA117e), not five, and Track 3 carries no open decision. **`SA122-DEC-001` — publish is a full-coverage gate context**: every repository conformance gate required locally and in hosted CI is also required in `.github/workflows/publish.yml`. Rationale is asymmetry of cost — publish is the last step and the only irreversible one, so a gap there costs a release while a gap in any editable-state context costs a fix; the last step re-verifies rather than trusts. The five conformance gates absent from `publish.yml` are consequently a **declared-open gap, not an accepted narrowing**: SA122a's parity checker is expected to fail on it and must not silence it with an exclusion, and SA122b closes it by adding the gates. Rule text added to `decisions.md` as §publish-path-gate-coverage; the disposition that previously sat inside SA122a step 3 is now pre-resolved, so Track 1 has no mid-ticket decision either. Release ordering (tag → push splits → PyPI) was reconfirmed unchanged. No ticket completed and no source file touched.
  - **SA121 focused continuation authorized (2026-07-27; `SA121-DEC-001`).** The maintainer granted the tests/documentation-only cycle that SA121's recorded partial checkpoint required, scoped to `SA121-CR-003` (strict-parser coverage), `SA121-CR-005` (`quality_tools.md` contradicting code and `decisions.md` on raw `waiver_evaluations`), and `SA121-CR-006` (broad assertions that must become exact ordered records/counts/exits). The cycle must preserve the reviewed runtime/source delta unchanged, leave `scripts/quality_baseline.json` untouched, run the full focused and quality gates, and obtain full-scope independent `STATUS: ok` before SA121 is checked or a completion entry is added here; if closing a finding turns out to require a runtime change, it stops and re-opens the decision rather than widening scope. Track 1 now has **two runnable tickets and no open pre-start decision** — SA121 and SA122a are file-disjoint with no ordering constraint between them, so the track takes them serially in either order, with SA121 first as the nearer to closure. SA121 remains unchecked and open.
  - **SA117 resized from Tier 3 into five serial children (2026-07-27; `SA117-DEC-003`; documentation only).** The roadmap requires every phase to be Adaptive Tier 1–2, and SA117 failed that gate on three counts: its objective could not be stated in one sentence (a product lockstep contract, a release-operations publication toolchain, and a scope-control checker built to police this ticket's own review), its candidate `43d9b8fc` is 77 files / 7,424 changed lines against an EDIT budget of ≲2,000 across ≲20 sites, and its coupling is ≥6 interdependent elements against a limit of 3. The failure was empirical: two plan-review cycles hit the non-convergence cap without reaching `STATUS: ok`, because each pass had to hold all three objectives at once. Measured split of the candidate: 625 lines product lockstep · 4,156 publication tooling · 2,586 scope meta-tooling · 200 docs/Make/lock. SA117 is now umbrella-acceptance only, with the six open findings distributed across **SA117a** (`CR-001`, exact/canonical lockstep comparison — critical path), **SA117b** (`CR-003` security, `CONT-PR-003` resilience, `CONT-PR-006` completeness — publish-path safety, critical path), **SA117c** (`CR-006`, lock-drift completeness), **SA117d** (`CR-005`, scope-tooling caller contract — **off the critical path**), and **SA117e** (full-scope review over the complete delta including `43d9b8fc`, then the human-confirmed refreshed split push). Because the executable content is already merged as unapproved recorded partial delivery, the children are correction-and-review handoffs over existing code, not greenfield builds; each carries its own scoped plan review, and SA117e still requires one full-scope review before SA117 closes. Critical path becomes `SA117a → SA117b → SA117c → SA117e → SA112a…f → SA96-PUBLISH` — longer in node count but not in work, and SA117d leaves the path entirely since the meta-tooling gates no release property. **Track 3's next action is SA117a, which no longer needs a maintainer decision to start.** New open decision `SA117-DEC-004`: defer the SA117d scope meta-tooling to v88, reconcile it in v87, or revert it. `decisions.md` §module-version-lockstep gains **Rule 2a** — version equality is decided on a canonical parse, and an unparseable version is a hard error rather than a fallback to literal string comparison. Track 1 sizing was reviewed in the same pass and needs no split: SA121's remaining scope is tests/documentation-only against three named findings, and SA122a/SA122b are each a single coherent objective already sized Tier 2. No ticket was completed, no source file touched, and no other track assignment or dependency changed.
  - **Roadmap/audit rebalancing pass (2026-07-27; documentation only).** No ticket completed this pass, so nothing moved from `docs/technical/roadmap.md` to this file — the seven open items (SA117, SA112a–f, SA96-PUBLISH, SA115, SA121, SA122a, SA122b) all remain open, three of them at recorded partial checkpoints. `tech-audit.md`'s closed `TA62` finding body and summary-table row were removed, adopting arch-audit's convention that closed findings exist only as dated Reconciliation-log lines; the closure record stays in the SA120 entries below and the structural cause stays open as arch Finding 11. **Rebalancing verdict: no safe move exists.** The one candidate — re-homing SA122a (`deps: none`, purely additive) from Track 1 onto the idle Track 2 — was rejected because `wt-track2` carries two committed but unmerged commits (`5193f198` SA115 phases 1–2, `5b5de830` the `v87` reconciliation merge) and a track merges as a branch, not as a ticket; landing SA122a from there would drag SA115 onto `v87` in violation of its `merge after SA112` bound. Track 2 is now documented as **closed to new tickets** until SA115 merges. Track 1 needs no relief regardless: SA121's remaining tests/documentation-only cycle and SA122a's new registry/checker are file-disjoint with no ordering constraint, so SA122a runs now while SA121 waits on authorization. Also fixed: eleven stale `../others/arch-audit.md` / `../others/tech-audit.md` links in `docs/technical/decisions.md` and one in `arch-audit.md`'s reconciliation log (both audits moved to the repository root on 2026-07-26); `START_HERE.md` roadmap length refreshed to 375 lines; v88 backlog items given an explicit track-assignment rule (assigned at v88 kickoff, deliberately not homed on a v87 release-train worktree). Conflict surface unchanged and covered by the Merge procedure: `CHANGELOG.md`, `docs/technical/roadmap.md`, and `docs/technical/decisions.md` (SA122a writes its publish-context disposition there). Track review: **Track 1 clean to continue on SA122a**, SA121 authorization-blocked; **Track 2 blocked** — validation-paused under `SA115-DEC-001` step 3 **Keep unauthorized**, merge-gated behind SA112, closed to new work; **Track 3 blocked** at SA117 with four executable and two plan findings awaiting maintainer authorization. No ticket state, track assignment, dependency, or decision changed; no source files touched.
  - **Roadmap/audit closeout pass (2026-07-27; documentation only).** `docs/technical/roadmap.md` no longer carries the completed SA120 block — its implementation detail, resolved review IDs, and validation evidence live in the SA120 entries below. Track 1 intake prose, the Finding-7/tooling-gap paragraph, the dependency-diagram note, the parallelism bullets, and the track-readiness/Net lines were reconciled to the three remaining governance tickets (SA121, SA122a, SA122b); `TA62` survives only as named drift evidence for arch Finding 11. `tech-audit.md`'s `TA62` finding body was compacted to a closed-status summary pointing at this changelog, since no open work depends on its pre-fix narrative (the structural cause stays open as arch Finding 11). `START_HERE.md`'s roadmap length was refreshed to 365 lines. No ticket state, track assignment, dependency, or decision changed; no source files touched. Track review: Track 1 clean to continue (next: SA121); Track 2 implementation-ready but validation-paused under `SA115-DEC-001` step 3 **Keep unauthorized** and merge-gated behind SA112; Track 3 blocked at SA117 independent review with four carried findings and no maintainer decision pending.
  - **SA120 complete — quiet-check frontend-lint parity (2026-07-26).** Full-scope independent review returned `STATUS: ok` with no new findings; SA120-REV-002, SA120-REV-003, and SA120-REV-006 resolved. Normal and quiet `make check` now share the same guarded `lint-frontend` target; quiet mode uses collision-safe temporary capture, is silent on success, and replays diagnostics exactly once on failure with nonzero exit status. Validation: 79/79 policy tests passed; 4,542 unit tests passed; `make check QUIET=1` completed green on first run; normal dry-run exit 0 contained `lint-frontend`. No unresolved blockers or decisions. SA120 roadmap item marked `- [x]`. SA121 is now Track 1's next pending task. The prior partial checkpoint entry below documents implementation history.
  - **SA120 recorded partial checkpoint (2026-07-26; task remains open).** Track 1 added normal/quiet `make check` frontend-lint parity after the shared repository gates, explicit failure propagation, and recursive-target policy coverage. Validation passed 29 focused and 79 full policy tests; the final quality-gate run also proved dry-run order and reversible rendered-TypeScript failures in both modes. The two-cycle independent review cap ended `STATUS: partial`: **SA120-REV-002** (medium, blocking) still requires opposite-tool-present fixtures for independent Node/pnpm absence; **SA120-REV-003** (medium, blocking) still requires exact quiet silence/single replay and collision-safe capture for overlapping runs; **SA120-REV-006** is low/advisory unreachable test-helper cleanup. No product decision is open. SA120 remains unchecked; this checkpoint is not a waiver or completion claim.
  - **SA112 plan-review non-convergence recorded; umbrella split into six serial handoffs (2026-07-26; no implementation landed).** Two focused plan-review cycles over the former monolithic SA112 A–E artifact confirmed the exact five-path GitHub Actions trigger tuple and the deterministic `yaml.BaseLoader` contract (resolving `SA112-CR-005` at plan-detail level) and corrected the installed `apply` stdin to the current three confirmations (`"n\ny\ny\n"`). Highest unresolved severity stayed high across both cycles (`SA112-PR-002`, completeness), so the plan loop hit its non-convergence cap. An unexpected uncommitted SA112 prototype was preserved as evidence in `45e37a86` and fully neutralized by `56b27045` — its 13 paths have zero net delta from the synced `v87` baseline, so the mergeable tree carries no SA112 executable delta. The maintainer chose recorded partial delivery and replaced the all-at-once handoff with the serial children **SA112a–SA112f** (provisioner extraction, traceback capture, root fix, permanent lifecycle E2E, workflow-trigger contract, ordered acceptance/closeout), each with its own narrow literal plan, scoped plan review, independent change review, and merge-back before the next begins. The superseded monolithic artifact must not be reconstructed or executed. `SA112-PR-002` remains open and is discharged by the children's per-task literal plans.
  - **arch-audit Finding 10 bookkeeping completed (2026-07-26; documentation only).** The closed-status stub left in `docs/others/arch-audit.md`'s live findings section for `frontend-source-generation-specialized` was removed, restoring that document's convention that closed findings exist only as dated Reconciliation-log lines. Closure detail remains in this file (SA104/SA105/SA106/SA107/SA108) and in the arch-audit log entries dated 2026-07-21 and 2026-07-23. No finding status changed and no audit was re-run.
  - **SA114/SA116 complete — Track 1 re-verification closed (2026-07-26).** Patch-fed full-scope review returned `STATUS: ok` with confidence 98 and zero findings. **SA114:** ordered chain `make check && make quality && make ci && make ci-e2e` exited 0 on 2026-07-25; QUARANTINE_TICKETS empty; coverage 92.88%/94.41%; Core 35/CLI 29 E2E; no source fix required. **SA116:** 1761→1749 comment/docstring-only compaction, `scripts/quality_baseline.json` unchanged at `max_lines` 1749, `make quality` exit 0, independent source review `STATUS: ok`. **SA114-CR-001** (consistency) resolved by SA115-DEC-001 **Keep unauthorized** maintainer decision — all roadmap authority surfaces now consistently state SA115 step 3 is not authorized. **SA116-CR-002** (completeness) resolved by exact patch-fed review: `git show a58f07e0 -- quickscale_core/src/quickscale_core/contracts/resolvers.py` confirmed 2 insertions / 14 deletions, comments/docstring/blank only, no executable or API-surface change, baseline 1749 unchanged. **SA115-DEC-001** step 3 **Keep unauthorized** remains in effect — SA115 heavy validation remains unauthorized pending explicit future approval; human-authorization-gated, not SA112-gated. SA115 remains open, implementation-ready but validation-paused, merge after SA112. Track 1 removed from open-work roadmap. Tasks completed 2026-07-26.
  - **SA114/SA116 partial re-review — SA115 authority resolved (2026-07-26; SA115-DEC-001 step 3 decision recorded; SA114-CR-001 resolved; SA116-CR-002 awaits patch-fed review).** The full-scope re-review authorized by SA114-DEC-002 completed and returned `STATUS: partial` as previously recorded. SA115-DEC-001 step 3 was resolved on 2026-07-26: the maintainer chose **Keep unauthorized** — SA115 heavy Docker/PostgreSQL validation remains unauthorized pending explicit future approval; it is human-authorization-gated, not SA112-gated. **SA114-CR-001** (medium, consistency) is **resolved** by this decision; all roadmap authority surfaces now consistently state step 3 is not authorized. **SA116-CR-002** (medium, blocking, completeness) remains open; the maintainer authorized one fresh Track 1 patch-fed full-scope review that supplies the plaintext SA116 patch (or a git-show-capable reviewer). SA114 and SA116 remain unchecked/open pending that review. No source changes were made or authorized.
  - **SA115 committed and reconciled with `v87` (2026-07-25; `SA115-DEC-001` steps 1–2; still open, merge still queued behind SA112).** The four previously parked SA115 files were committed on `wt-track2` (`5193f198`) and `v87` was merged in (`5b5de830`), ending the uncommitted-work hazard and clearing the accumulated `scripts/test_e2e.sh` drift. Three conflicts — Environment header, `--help` list, and the worker-count resolution block against the landed memory preflight — were all additive on both sides and resolved by keeping both, adopting `v87`'s column alignment. SA115's banner and `pytest_cmd` hunks merged without conflict. Post-merge (combined pytest exit 0): `bash -n` clean, 31 focused tests pass (16 fixture + 15 harness), Ruff check/format clean. The merge was verified non-destructive — `git diff v87...HEAD` is 274 insertions / 0 deletions across exactly the four SA115 files, so nothing landed on `v87` was dropped. Two items recorded for the validation phase: (a) the memory guard demoted to serial *lanes* under low RAM while `E2E_XDIST_WORKERS` resolved independently, so a demoted run still fanned out N workers per lane and could exceed the load the guard existed to prevent — **resolved by decision `SA115-DEC-002` (2026-07-25): the guard wins and clamps workers to serial when it fires**, visibly, with the existing `QS_E2E_NO_MEMORY_GUARD=1` as the sole opt-out; implementation lands in SA115's validation phase; (b) `SA115-CI-001` — neither `test_e2e_xdist_fixtures.py` nor `quickscale_core/tests/conftest.py` appears in `.github/workflows/e2e.yml`'s trigger paths, the same defect class as `SA112-CR-005`. Heavy validation (container lifecycle, teardown, `make ci-e2e`) is not yet run.
  - **SA114-CR-001 documentation reconciliation (2026-07-25; roadmap cleanup pass).** The two same-fact scheduling sites the SA114 final review named were reconciled in `docs/technical/roadmap.md`: the "only open work is shown" dependency diagram no longer carries completed Track 1/Track 2 rows, and the SA115 concurrency bound, parallelism bullets, infra-serialization prose, and track-readiness bullets no longer treat SA114's heavy `ci`/`ci-e2e` legs as future contention. Completed Track 2 (SA104→SA108) and Track 3 (prior) narrative was compacted to CHANGELOG pointers, retaining only the standing AF7 discovery reference. A follow-up deep-review pass the same day found three further stale-fact sites the first pass missed — SA114's own intro paragraph still asserting the gates had not been re-run, SA116's present-tense "`make quality` exits 2" problem statement, and SA115's "parked" label after the work was committed — plus a stale `START_HERE.md` roadmap length (550 → 280 lines). All corrected. Source unchanged; SA114-CR-001 stays open on its remaining half — a full-scope independent re-review returning `STATUS: ok` — and SA114/SA116 remain unchecked.
  - **SA114/SA116 recorded partial checkpoint (2026-07-25; tasks remain open).** **Done:** the exact ordered chain `make check && make quality && make ci && make ci-e2e` exited 0 from a clean wt-track1 synced to latest v87; QUARANTINE_TICKETS empty; core+CLI equal-weight coverage 92.88%; integration module mean 94.41%; Core E2E 35 passed, CLI E2E 29 passed; no source fix required. SA114-ORDER-001 is satisfied. Non-gating Django/pytest warnings were observed. **Pending / blocking:** `SA114-CR-001` (medium, blocking, consistency) remains open after the second/final review continuation found stale roadmap diagram and SA115 scheduling/infra references that treated completed SA114 work as active; final review returned `STATUS: partial`. SA114 and SA116 remain unchecked, and this checkpoint is not a waiver. **Decisions needed:** none; a post-cap fix/re-review requires explicit user approval or a fresh scoped continuation.
  - **SA114/SA116 blocked checkpoint (2026-07-23; implementation and compaction complete — task remains open on SA114-ORDER-001).** Prior SA114 gate-drift checkpoint (gate-drift fixes, fail-closed donor preflight, normal/quiet `make check` dispatch, runtime-branding/leakage proof) independent review returned `STATUS: ok` with checkpoint merge-approved. SA116 resolved `SA114-QUALITY-001` via policy-compliant `contracts/resolvers.py` comment/docstring-only compaction (1761→1749 lines; `scripts/quality_baseline.json` unchanged at `max_lines` 1749; `make quality` exit 0 with 0 warning/critical regressions). Independent source review `STATUS: ok` with no findings. **SA114-ORDER-001** (medium, blocking for completion, completeness) — the exact ordered gate chain (`make check && make quality && make ci && make ci-e2e`) lacks a final `make ci-e2e` exit status on the literal ordered run. Second/final QG ran exact prefix `make check` → `make quality` → `make ci` successfully; `make ci-e2e` exceeded the 30-minute agent runtime window and returned no final exit code. Prior identical-tree E2E 30/30 is supporting evidence but does not resolve ORDER-001. Continuation: from clean Track 1 worktree synced to latest v87, rerun exact `make check && make quality && make ci && make ci-e2e` with sufficient runtime; all exit 0 and quarantine empty; obtain independent full-scope change review `STATUS: ok`; only then mark SA114/SA116 `[x]`. User authorized checkpoint merge despite incomplete completion criterion — this is not waiver of ORDER-001. SA114 and SA116 both remain unchecked/open.
  - **E2E/unit test-infra hardening (2026-07-23; landed on `v87`).** `scripts/test_e2e.sh` gained a low-memory preflight guard — it falls back to serial Core/CLI lanes when resting RAM/swap headroom is low, guarding against `systemd-oomd` reaping a long run (thresholds `QS_E2E_MIN_AVAIL_MB` default 4096 / `QS_E2E_MIN_SWAP_MB` default 3072; bypass `QS_E2E_NO_MEMORY_GUARD=1`) — plus a `QS_E2E_HEARTBEAT_INTERVAL` (default 15s) "still running" progress heartbeat during the concurrent-lane phase. `scripts/test_unit.sh` gained parallel-execution and coverage-handling enhancements. Both documented in `docs/technical/development.md`. This directly mitigates the **SA114-ORDER-001** failure mode — a long `make ci-e2e` hanging/returning no final exit code within the runtime window — and is available for that ticket's clean-continuation run. No product code, CLI behavior, or gate threshold changed.
  - **Generated-Dockerfile `pnpm install` network resilience (2026-07-24; landed on `v87`) — targets SA114-ORDER-001.** The generated project's `Dockerfile.j2` gave the Python/pip install step retry + timeout resilience but left the frontend `pnpm install` with none, so slow `registry.npmjs.org` throughput failed the Docker build on a single timed-out download — the exact SA114 CLI E2E failure mode. The frontend install now mirrors the pip house pattern: a 3-attempt retry loop plus raised pnpm fetch timeouts (`--config.fetch-retries=5`, `--config.fetch-retry-maxtimeout=120000`, `--config.fetch-timeout=300000`). `test_react_theme_integration` updated to assert on `pnpm install` (the literal `RUN pnpm install` line is now inside the retry loop); the SA90 emission-parity fixture rebaselined for the 3 identical react-variant Dockerfile hashes. Fast validation: SA90 parity green; 257 generator/template/theme tests pass. Hardens every generated project's build; full `ci-e2e` proof still pending Track 1 continuation.
  - **Installed-context managed-wiring traceback surface (2026-07-24; landed on `v87`) — unblocks SA112 Phase-1 diagnosis.** The `apply` managed-wiring regeneration wrapper (`apply_command.py`, `_regenerate_managed_wiring_for_apply._wiring_fn`) caught every exception and reduced it to `str(exc)`, discarding the traceback — the reason SA112's installed-context `NameError: QUICKSCALE_BILLING_ENABLED` has stayed pre-diagnostic across cycles (static passes confirmed the billing adapter uses a dict access, not a bare-name site, so the raising frame genuinely needs a runtime traceback). The wrapper now prints the full traceback to stderr when `QUICKSCALE_DEBUG` is set; default behavior and the user-facing failure banner are unchanged. The SA112 Phase-1 capture procedure is now `QUICKSCALE_DEBUG=1` on the installed-apply run. Validation: ruff clean; apply-wiring regeneration suites 65 passed.
  - **E2E memory-guard swap-veto correction (2026-07-25; landed on `v87`).** The low-memory preflight in `scripts/test_e2e.sh` treated low `SwapFree` as an independent veto on concurrent lanes, so a RAM-rich host demoted every `make ci-e2e` run to serial lanes purely because idle desktop processes had accumulated swapped-out pages — a condition that is normal under `vm.swappiness` defaults, does not clear on its own, and is not evidence of memory pressure (observed: 14.7 GB `MemAvailable` alongside 1.7 GB `SwapFree`, forcing serial and roughly doubling E2E wall time). The swap check is now gated on `MemAvailable` also sitting below a new comfort threshold `QS_E2E_COMFORT_AVAIL_MB` (default 8192); `MemAvailable` under `QS_E2E_MIN_AVAIL_MB` still forces serial on its own, and comfort is clamped up to the hard floor if configured inverted. Related fail-hard fix: a non-numeric threshold override previously made each `[ x -lt $threshold ]` error out and evaluate false, silently disabling the guard the operator was tuning; overrides are now validated and abort with a clear message, per [decisions.md §fail-hard-principle](docs/technical/decisions.md#fail-hard-principle). Validation: 9 threshold scenarios exercised against the extracted guard (current host state, mediocre-RAM with low and healthy swap, below-floor RAM, swapless host, garbage override, empty override, inverted comfort, bypass) — all correct; `bash -n` clean. Documented in `docs/technical/development.md`. No product code, CLI behavior, or gate threshold changed.
  - **E2E runner provenance banner and serial-cause attribution.** The runner prints a start timestamp and provenance banner (checkout path, HEAD, local changes, and an out-of-date warning against `QS_E2E_INTEGRATION_REF`) before any lane starts, so a long run's revision is identifiable; serial runs now report their real cause instead of always blaming `QS_E2E_PARALLEL=0`.
  - **E2E heartbeat stuck-lane detection.** The concurrent-lane heartbeat ticks every 60s and reports per-lane silence (`running` vs `running, quiet Nm`) derived from lane-log growth, so a wedged lane is distinguishable from one doing quiet work.
  - **SA114 Dockerfile QG1 hardening and fixture parity (2026-07-25; landed on `v87`).** The generated `Dockerfile.j2` now carries the QG1-corrected pnpm/poetry install resilience: `timeout 600` wrapper kills stalled (dead-socket) installs, `--reporter=append-only` emits timestamped progress under buildkit (no TTY spinner), `rc=$?` capture reports failure codes, `rc=124` identifies `timeout`-killed attempts explicitly. Two Node 24 incompatible pnpm config flags (`--config.fetch-retry-maxtimeout`, `--config.fetch-timeout`) removed per QG1 finding — Node 24's `AbortSignal.timeout` rejects string-typed timeout configuration at startup. Three SA90 emission-manifest Dockerfile SHA-256 hashes refreshed (identical across all three react variants). QG1 outcomes — all on the 2026-07-25 run from the Track 1 worktree synced to `v87` `76f36c83`: `make check` exit 0 (core 2527 passed / 1 skipped; CLI 1998 passed); `make quality` exit 0 with zero baseline regressions; `make ci` exit 0 (92.88% core/CLI equal-weight coverage mean; 94.41% module integration mean); `make ci-e2e` exit 1 (Core 35 passed; CLI 28 passed + 1 Docker build timeout at the unchanged 300-second caller timeout). Quarantine remained empty. File-scope distinction: `Dockerfile.j2` is the sole executable delta (35 lines changed); SA90 fixture is the sole data delta (3 sha256-only lines); `docs/technical/roadmap.md` and this changelog are checkpoint bookkeeping only; E2E test has no net diff — the QG2 timeout expansion was discarded. QG2 was canceled and returned no valid result; its partial unvalidated mutations (e2e test timeout expansion, non-minimal fixture rewrite) have been discarded. SA114 remains unchecked/open on **SA114-ORDER-001**. No completion claim or ORDER-001 waiver.
  - **SA112 pre-diagnostic plan cycles (2026-07-24; no implementation landed).** Four Track 3 continuations produced no product change. A disposable prototype proved installed-wheel `plan` for all 12 modules and reached installed `apply` through all 12 module embeds before failing with `NameError: name 'QUICKSCALE_BILLING_ENABLED' is not defined` during managed-wiring regeneration; `up`/`ps`/`manage migrate`/`down` were never reached. Independent change review rejected the prototype for merge and every executable, test, workflow, and changelog change from it was reverted to `v87` — no SA112 implementation exists on any branch. Two static discovery passes confirmed no bare-name evaluation of that symbol in the traced managed-wiring path (the billing adapter at `adapter.py:35` uses `settings["QUICKSCALE_BILLING_ENABLED"]`, a dict access that would raise `KeyError`) and no billing source/bundled/package version skew, establishing that the raising frame requires a runtime traceback — the obstacle since removed by the `QUICKSCALE_DEBUG` traceback surface above. Discovery produced validated topology snapshots `sa112-topology-v1` and `sa112-topology-v2`. Successive plan/plan-review cycles resolved `SA112-PR-001`, `SA112-PR-003`, `SA112-PR-004`, `SA112-PR-005`, `SA112-CR-001`..`SA112-CR-004`, `SA112-CR-006`, `SA112-CR-007`, `SA112-CR-008`, and `SA112-CONT-001` at plan level; the maintainer-authorized extra blocking-only cycle still returned `STATUS: partial`, leaving `SA112-PR-002` (high) and `SA112-CR-005` (medium). The maintainer then authored the literal continuation artifact (wheel-provision helper, lifecycle-lane argv/stdin registry, Phase-1 traceback procedure, fully-expanded ordered CI trigger tuple, validation order), which addresses both at plan-detail level; it is retained in `docs/technical/roadmap.md` as the specification for the next continuation. No diagnostic instrumentation, product code, runner, or workflow change was made, and no heavy Docker/PostgreSQL lane ran.
  - **SA112 plan-cycle status supersession (2026-07-26; task remains open).** The preceding SA112 historical entry's statement that the monolithic artifact is retained as the next executable specification is superseded. Two later focused plan-review cycles both returned `STATUS: partial`: `SA112-PR-002` remains high/blocking, while `SA112-CR-005` is resolved at plan-detail level. The unexpected prototype checkpoint `45e37a86` was fully neutralized by `56b27045`, leaving its 13 paths with zero net delta from synced `v87`; no SA112 implementation is present in the mergeable tree. Active work is now the serial SA112a–SA112f handoff chain in `docs/technical/roadmap.md`, beginning with SA112a's scoped plan-review gate. This is status clarification, not completion language.
  - **SA115 parked implementation checkpoint (2026-07-23; merge-order-blocked, uncommitted).** On `wt-track2` synced to `v87` at `c09e2093`: **Phase 1** added a session-scoped override of pytest-docker's `docker_compose_project_name` in `quickscale_core/tests/conftest.py`, deriving a worker-unique Compose project name from the lane's `QS_E2E_COMPOSE_PROJECT_NAME` plus `PYTEST_XDIST_WORKER` (single name when not under xdist), keeping the lane prefix intact so `cleanup_scoped_containers` still matches; `postgres_service` is reused unchanged. **Phase 2** added `QS_E2E_XDIST_WORKERS` to `scripts/test_e2e.sh` — RAM-derived default cap mirroring the Makefile `PYTEST_XDIST_WORKERS` heuristic, `0`/`1` degrading to the serial no-`-n` path, `>=2` appending `-n <N> --dist loadscope`, documented in `--help` and the `# Environment:` header, with the chosen worker count printed in the lane banner. Harness tests live in `scripts/test_e2e_parallel.py`. Evidence: 16 fixture tests, 13 runner-harness tests, `bash -n`, focused `ruff check` and `ruff format --check` — all pass. The fixture tests are deterministic naming/isolation tests and exercise no container lifecycle. The four changed files remain **uncommitted on `wt-track2`**; SA115 stays open and cannot merge until SA112 lands, since both edit `scripts/test_e2e.sh` and the shared CI surface.
  - **Frontend-source de-specialization stage 2a (SA105) — complete (2026-07-20).** Checkpoint content: removed `selected_modules`-driven frontend source omission (`REACT_THEME_OPTIONAL_FILES`, Pass 6 mapping filter, `_should_skip_optional_react_file`); converted `App.tsx`, `main.tsx`, `App.test.tsx`, and `PublicSocialPages.test.tsx` to static verbatim-copy files; `useModules.ts`, `Sidebar.tsx`, `Dashboard.tsx`, and `templates/index.html` always emit every module entry regardless of generation-time selection. Module and public-social routes use React.lazy/Suspense with fail-closed runtime flag checks via `window.__QUICKSCALE__`. Checkpoint SA90 exact-manifest parity 6/6 green. **Completion closure:** CR-002 resolved via side-effect-free `renderQuickScaleRoot` dispatcher seam with direct Vitest cases for `modules.social=true` (both social surfaces), `modules.social=false` (ordinary app root), and absent `modules.social` (fallback to app root) — all green. SYNC-001 resolved with static explanatory banners on all 10 dormant module surfaces: BlogPage, CrmPage, FormsPage, ListingsPage, FormRenderer, FormFieldRenderer, FormSuccess, useFormSchema, SocialLinkTreePublicPage, SocialEmbedsPublicPage; all banners hardcoded (no generation-time interpolation), preserving byte identity across projects. SA90 final delta: 11 approved hashes changed + 2 entries added, 0 removed/unexpected, provenance/.venv preserved. Validation: targeted pytest 21 passed; generated Vitest 20 passed; typecheck/build passed; SA90 6 passed; lint-frontend/frontend-proof passed; core unit 2,482 passed at 90.47%; core E2E 35 passed; policy checks passed. CLI E2E 8 passes with two pre-existing apply-confirmation failures and one infrastructure timeout (judged non-causal by review). **Independent change-review STATUS ok (confidence 94).** Advisory SA105-ADV-001 (non-blocking): `.venv` fixture records symmetrically excluded from parity. SA106 (identity de-spec) depends only on SA104 ✓ and remains independently assistant-executable.
  - **Frontend-source de-specialization stage 2b (SA106) — identity de-spec complete (2026-07-20).** Converted three identity-bearing `showcase_react` frontend source templates from Jinja-specialized `.j2` files to static verbatim-copy files: `useModules.ts` (was `useModules.ts.j2`), `Dashboard.tsx` (was `Dashboard.tsx.j2`), `Sidebar.tsx` (was `Sidebar.tsx.j2`). The `useModules.ts` `defaultConfig.projectName` is set to empty string — runtime value is injected via `window.__QUICKSCALE__.projectName` (already present in `templates/index.html.j2`). Dashboard and Sidebar read project identity at runtime via `useProjectConfig().projectName` instead of Jinja-rendered `{{ project_name }}`. All three files are now project-agnostic and byte-identical across generated projects on the same theme version. The generator's existing non-Jinja copy path (`_theme_non_jinja_emitted_paths`) handles emission automatically — no generator.py logic changes needed. SA90 emission fixture: 3 hashes updated (useModules.ts, Dashboard.tsx, Sidebar.tsx) across all 3 variants. **Quality-gate validation:** contract test stale references to `useModules.ts.j2` fixed (two occurrences in `test_module_manifest_contract.py`); beta-migration taxonomy updated — `renderQuickScaleRoot.tsx` and `RootDispatch.test.tsx` classified as `INTENTIONALLY_UNMANAGED` (user-owned frontend source, following `main.tsx`/`App.test.tsx` precedent). Both fix groups verified by targeted pytest. **Two-project identity proof:** `frontend/src` diff between two independently generated projects confirmed zero identity-bearing differences — `useModules.ts`, `Dashboard.tsx`, and `Sidebar.tsx` are byte-identical across projects; project name renders correctly from `window.__QUICKSCALE__.projectName` at runtime. **Evidence:** targeted pytest (contract-fix tests, taxonomy conformance checked); SA90 emission-parity fixture green across all 3 variants (3 hashes updated, 0 removed/unexpected, provenance/.venv preserved); frontend proof (`make frontend-proof`) passed; `make check` gate green. **Resolved findings:** QG-SA106-001 (consistency — `useModules.ts` correctly attributed to SA106, not SA105), QG-SA106-002 (consistency — taxonomy rationale for `renderQuickScaleRoot.tsx` and `RootDispatch.test.tsx` recorded in decisions.md U7). **Advisory (non-blocking):** CR-SA106-002 (test-gap — no durable focused test pins all three non-Jinja mappings and runtime rendering in a single test; deferred, not implemented).
   - **Fail-hard runtime-seam validation (SA107) — complete (2026-07-20).** Added `src/lib/validateQuickScaleSeam.ts` — a shared fail-hard validation gateway that every load-bearing `window.__QUICKSCALE__` read must pass through. The `validateQuickScaleConfig()` function requires the seam to exist, `projectName` to be a non-empty string, and `modules` to be an object whose present values are strict booleans; optional `publicPage` is validated when present. Replaced the silent fallback/optional-chaining in three consumers: `resolveProjectConfig()` (useModules.ts now calls `validateQuickScaleConfig()` instead of merging with a default config), `renderQuickScaleRoot()` (reads config through validation instead of `config?.modules?.social ?? false`), and `main.tsx` (reads `publicPage?.surface` from the validated config instead of `window.__QUICKSCALE__?.publicPage?.surface`). Removed the `defaultConfig` constant from `useModules.ts` — runtime validation is authoritative; no silent defaults remain. **Public-social contract resolved:** social embeds/link_tree templates inject `owner` and complete `modulePaths`; validation enforces `modulePaths` shape (`crm`, `social`, `analytics` as non-empty strings), `owner` (mode `solo`|`saas`, `currentOrgSlug` string|null), and `publicPage.module`/`publicPage.surface` enum checks. **Strict own-boolean module validation:** every module flag in `modules` is validated as a strict boolean; no `?? false` fallback or optional-chaining remains. **Production-shaped/inherited-boolean tests:** 16 seam validation tests plus 3 production-shaped public-social boot tests cover missing/malformed/well-formed seams, inherited-boolean module flags, and public-social boot smoke. **Fixture parity:** SA90 emission fixture updated with 2 new entries (validateQuickScaleSeam.ts, validateQuickScaleSeam.test.ts) + 3 updated hashes (useModules.ts, main.tsx, renderQuickScaleRoot.tsx) across all 3 variants, 0 removed/unexpected. **Validation (pre-sync independent QG):** `make lint-frontend` ✓, `make frontend-proof` ✓, `make test-unit -- --core` 2,482/0 at 90.47% ✓, Vitest 35/35 ✓. **Post-sync phase:** `make lint-frontend` ✓; direct isolated `ProjectGenerator.generate` render + generated `pnpm install`/`type-check`/`build` ✓; `make test-unit -- --core` 2,512/0 at 90.46% ✓. Post-sync `make frontend-proof` timed out twice at the render step (identical isolated generator succeeded; remaining commands passed) — the wrapper timeout is noted transparently and does not indicate a product regression. **Independent change-review STATUS ok.** SA107-CR-001, SA107-CR-002, SA107-TEST-001, SA107-DOC-001 all resolved; no remaining blocker or advisory.
  - **Tenant isolation unified.** Every tenant-scoped model inherits one shared isolation base instead of hand-copied boilerplate; a project-wide check requires each model to be explicitly tenant-scoped or excluded and is wired into generated projects' own CI.
  - **Fail-closed database access.** The app refuses to boot under a database role that bypasses row-level security (`BYPASSRLS` or `SUPERUSER`) unless explicitly overridden; production requires a restricted tenant-safe role, with the elevated role reserved for migrations.
  - **Database-level parent/child isolation.** Parent/child records (e.g. a CRM contact and its notes) are kept in the same organization via `NOT DEFERRABLE` composite foreign-key constraints, replacing the fragile trigger-based check; a conformance gate verifies every composite FK.
  - **Fixed a row-level-security bug affecting pooled database connections**, and removed redundant per-statement tenant-context setup within a transaction.
  - **Public `org_scope` API.** Internal org-context primitives are retired in favor of one public `org_scope()` seam used everywhere, with a canonical `PublicSystemOrgReadMixin` for anonymous public reads and a lint gate enforcing the boundary.
  - **Operator tooling.** Admins can "view as" a specific organization from the admin panel (visible banner, one-click exit); a single audited `operator_access()` path gates cross-tenant reads and sensitive maintenance commands, logging who ran what and why.
  - **`TenantModelAdmin` base.** An orgs-owned admin base resolves the active org and scopes every admin view through `org_scope()`, fail-closed; every module admin is ported onto it, removing hand-copied scoping and inline formsets.
  - **Fail-hard configuration sweep.** Missing or invalid module settings (page sizes, rate limits, feature flags, provider/enabled flags, proxy config, storage backend, orgs mode) now raise at boot or apply time instead of silently defaulting or coercing; validators are wired into the apply path and the dead coercions they masked are removed.
  - **`QUICKSCALE_MODE` fails hard at boot** instead of defaulting to `solo`, and legacy `quickscale.yml` keys are rejected with a named-replacement error instead of being silently translated.
  - **Public-content sanitization.** Markdown-rendered links on public blog/listing pages are run through an allowlist URI-scheme sanitizer (blocking `javascript:` and obfuscated schemes per the WHATWG URL spec); the analytics template-tag payload is escaped before `mark_safe`; and orgs debug-view `next` redirects are validated same-host/scheme.
  - **Non-string public form submissions** return validation errors instead of 500ing.
  - **Client-IP and cache infrastructure.** Generated projects gain a canonical `get_client_ip()` helper with trusted-proxy config (matching DRF's proxy-count semantics) and a shared `CACHES` backend (Redis when available, else `DatabaseCache` with a deploy-script `createcachetable` step); forms/blog throttles and IP logging point at the shared seam.
  - **Cross-org account-deletion safety.** User FKs on org content are `SET_NULL`/`PROTECT` (never `CASCADE`) so deleting a user no longer destroys content across organizations, enforced by a conformance gate.
  - **Last-owner invariant.** One canonical last-owner deletion-blocking check (with `select_for_update` concurrency protection and a `pre_delete` cascade backstop) replaces three divergent copies and is enforced at the account-deletion boundary.
  - **Secrets kept out of logs and argv.** The deploy script prints per-variable set/`MISSING` status instead of raw values; Railway/DR adapter secrets are piped via stdin instead of command-line arguments, with a hard-stop rather than an insecure fallback.
  - **Billing ownership and integrity.** Subscriptions are authoritatively owned by the organization; a partial unique constraint enforces credit-ledger idempotency at the database level; account-deletion cleanup distinguishes a benign "nothing to cancel" from an anomalous subscription missing its Stripe id; and billing's state-changing endpoints move onto the DRF baseline (automatic CSRF) with a decisions-doc rule naming the two sanctioned JSON-API bases.
  - **Backups and disaster recovery.** Admin backup create/prune/restore run off the synchronous request path via background subprocess with atomic claiming; the uploaded-restore copy is crash-safe; stranded `STATUS_RESTORING` artifacts are detected and recoverable; DR media-backend resolution fails hard on real misconfiguration instead of silently falling back to local storage; and a `csrf_exempt` CI gate requires every callsite to pair with verification.
  - **Core/module boundary.** A single `quickscale_core.runtime` facade is the only public core import surface (import-linter enforced); a Django-free persistence protocol lets backups/DR inject model access; managed adapters register explicitly instead of at import time; and a bidirectional import ban keeps core from importing modules.
  - **Resilient project generation.** `apply` checkpoints progress after each step and resumes on failure; destructive or remote actions require explicit confirmation; `apply --force` stages to a backup directory with full rollback; module `pyproject` splices route through a validated TOML writer; and generator internals are split into smaller focused modules.
  - **Declarative module configuration.** The `module.yml` derivation loader is proven by round-trip tests, listings is fully migrated off imperative wiring, and a guardrail blocks any module from reintroducing it; a manifest-sync gate fails on drift between each module's `module.yml` and its core snapshot.
  - **Contract-version tracking.** Generated projects record the contract version they were built against, a compatibility gate probes each module against its claimed minimum core version, and `quickscale status` flags modules ahead of the project's contract.
  - **Retired the `showcase_html` generator theme** in favor of React-only output; existing generated projects keep their user-owned files.
  - **Generator emission mapping** is exported as a single authoritative function shared by production and its beta-migration conformance gate.
  - **Cross-module signal decoupling.** Orgs no longer reverse-imports CRM/auth: a `organization_created` signal drives CRM's default-stage seeding (self-sufficient under restricted roles), the auth adapter fails hard when the module is absent, and version-floor constraints on `required_modules` are enforced at apply time.
  - **PostgreSQL-only test infrastructure.** All modules test exclusively against PostgreSQL (SQLite removed everywhere) under a shared pre-provisioned restricted (`NOBYPASSRLS`) role; a dedicated integration gate proves tenant isolation under that role and is structured so it cannot pass by silently skipping.
  - **Parallel integration execution.** The per-module integration loop runs as a configurable parallel worker pool with isolated coverage/log state, joined failure propagation, deterministic replay, and signal-safe cleanup, matching sequential verdicts and coverage.
  - **Untracked accreting build/test artifacts** (coverage/log files, blog test-upload media) and pointed test `MEDIA_ROOT` at a temp directory.
  - **Migration squash (SA92):** All nine modules squashed to final-schema `0001_initial` (`organization_id NOT NULL` from row zero); fresh-only posture ratified with pg_policies/catalog/data hash parity against v87; Finding 8 (module-RLS-context-procedural) closed.
  - **DR persistence port (SA89a+SA89b):** Backups ORM lifecycle moved behind a fail-hard provider registry in `dr_engine/persistence.py`; bidirectional core↔module import ban enforced; Finding 1 (circular lattice) closed.
  - **Parallel integration worker pool (SA91):** Per-module integration loop runs as a configurable parallel worker pool (`QS_INTEGRATION_JOBS`) with isolated coverage/log state, joined failure propagation, and signal-safe cleanup.
  - **Per-module restricted-role gates closed under SA82 baseline:** CRM (SA84, 263 pass/21 skip/0 fail), listings (SA86), blog (SA95, non-reproducible post-SA92), forms (SA85), orgs (SA77), notifications (SA79). `make test-integration` green with empty quarantine.
  - **Pre-publish module sweeps (SA96-T1/T2):** All 12 modules re-verified green in isolation on post-SA92 v87 — no regression, empty quarantine.
  - **React-only theme retirement (SA94):** `showcase_html` generator theme retired; fail-closed `theme_validation` preflight wired into all 8 CLI callsites; generated project output is now React-only.
  - **Quality baseline established (GATE-quality):** Makefile `quality` target gates complexity/dead-code against `quality_baseline.json`; baseline reset for v87.
  - **SA97 — shared tenant test-state reset fixture.** Consolidated six divergent private copies (CRM `_reset_crm_test_state`, forms `_reset_test_state`, blog `_reset_current_org_context`, orgs `_reset_test_state`, listings `_reset_current_org_context`, social `_reset_test_state`) into one conftest-importable autouse fixture in `tests_shared/reset_state.py`. The shared fixture resets the org ContextVar, PostgreSQL GUCs (`app.current_org_id`, `app.operator_access`, `ROLE`), AF9 per-transaction priming memo, and Django cache before and after every test — preserving the full CRM/forms contract as the canonical superset (blog/orgs/listings/social reconciled upward). All Django imports are deferred until fixture execution, so the fixture is safe for both pytest-django-managed (CRM, orgs) and self-managed (forms, blog, listings, social) conftests. The commons ownership rule is recorded in [`decisions.md §SA97`](./docs/technical/decisions.md#test-commons-ownership): orgs owns org-context runtime helpers, `tests_shared/` owns cross-module test plumbing, citing `apply_force_rls`/`revert_force_rls` (SA92) as house precedent. No private copy of the reset contract remains in any module conftest. **Independent review (Adaptive-change-review pass 2) STATUS ok — CR-SA97-001 resolved.** `make test-integration`: all 12 modules pass, empty quarantine, 94.39% mean coverage. Remaining non-blocking advisories: CR-SA97-002 (low — no direct lifecycle edge-case test) and CR-SA97-003 (low — docstring at `tests_shared/reset_state.py:4-6` states three copies unified while six were migrated).
  - <a id="sa98"></a>**SA98 — shared rendered-link sanitizer closeout.** Moved the `_sanitize_href`/`_sanitize_rendered_html` implementation into the orgs-owned shared seam and switched blog/listings consumers to it; no second runtime copy remains. Evidence: orgs **874 passed / 11 skipped**, blog **206 passed**, listings **132 passed**; scoped Ruff/MyPy, sanitizer uniqueness/caller proof, diff check, and manifest sync passed. Independent review returned **STATUS ok with no findings**. No SA98-specific blockers/findings were identified. Broad `make check`/`make quality` baseline failures (coverage/dead-code/complexity) remain unrelated and outside this ticket; they were not fixed, and this closeout makes no E2E claim.
  - **SA99 — `quickscale_devtools` brought under repo ruff/mypy coverage.** arch-audit [Finding 7](docs/others/arch-audit.md) cheap sub-item. Added `quickscale_devtools` to `ruff.toml` known-first-party and to Makefile `lint`, `lint-fix`, `format`, and `typecheck` section loops (new `devtools` section with `--devtools`/`-d` flag, added to default `ACTIVE_SECTIONS` and `SRC_DIRS`). No mypy section override needed — devtools source passes strict global defaults. `make lint` and `make typecheck` cover devtools and exit 0. Finding 7 tuple-derivation remainder stays unscheduled (gated on third consumer / public update command). `DEFAULT_SECTIONS` caller-parity preservation resolved CR-SA99-001 (stale default test dispatch); CR-SA99-002 and CR-SA99-003 also resolved. **Independent change-review pass 2 STATUS ok.**
  - **SA102 — CI lint/typecheck devtools coverage (closes tech-audit TA61).** Added explicit `--devtools` to all ten scoped invocations across `ci.yml`, `publish.yml`, and `scripts/check_ci_locally.sh`; corrected the original five-site inventory discrepancy. Aggregate and CLI-scoped lint/typecheck checks passed, and a temporary devtools Ruff failure probe was rejected and removed.
  - **SA103 — blocking rendered-frontend proof gate (2026-07-19; complete).** Added the blocking `lint-frontend` workflow job with Node/pnpm/cache setup, wired the local `make ci` / `scripts/check_ci_locally.sh` frontend-lint fan-out with an absent-Node/pnpm guard that does not mask real failures, and made `publish.yml` run `frontend-proof` before downstream build/publish. A deliberate injected TypeScript error made `make ci` exit 2 with `TS2322`/`TS6133`; `Dashboard.tsx.j2` was restored byte-identically; `make frontend-proof` passed. QG1's 15 focused tests and bounded `PYTEST_XDIST_WORKERS=2 make ci` passed (4,536 core/CLI tests, 323 backups tests, 2 expected skips, 92.88% mean coverage with every file ≥80%, integrations green); QG2's 19 focused tests plus six delta tests and Ruff/MyPy/YAML/workflow-order/Bash/Make/diff checks passed. Independent change-review pass 2 returned `STATUS: ok` and resolved **SA103-REV-001**, **SA103-REV-002**, and **SA103-ADV-001**; no blocker or open decision remains. Hosted GitHub Actions were not remotely observed; this is a non-blocking residual caveat.
  - **SA104 — Frontend static-source de-specialization stage 1 (2026-07-19; complete).** 57 byte-static theme files across the `showcase_react` frontend theme/verbatim-copy surface (including the converted theme-root `frontend/index.html`) moved onto the verbatim-copy path — `.j2` suffix and `{% raw %}` wrappers removed, now routed through the generator's existing non-Jinja copy path (`_theme_non_jinja_emitted_paths`). 13 genuine active-Jinja files remain. Emitted byte/path/mode unchanged: SA90 emission-parity fixture stayed green; two-project diff of `frontend/src` for the now-static files showed zero differences; `test_generated_project_runtime.py` passed. Added `test_non_jinja_theme_root_index_html_routed_correctly` regression test. QG run 2/2: Ruff/MyPy; SA90 20/20; React 51/51; zero unexpected differences across 61 verbatim-copied frontend files and identical `frontend/index.html` SHA-256; runtime 5/5; frontend proof/lint; `make test-unit` 4,482. Independent change-review pass 2 returned **STATUS ok**; **SA104-CR-001** (blocking completeness — root `index.html.j2` was raw-only static) fixed and resolved; Caller-Parity PASS. No blocker or open decision remains. User-approved project-intake setup moved `docs/technical/project_intake.yml` to root `adaptive.intake.yml` and rewired `adaptive.rules.md` with no profile semantic changes. SA105 and SA106 are now unblocked by SA104; SA105 remains blocked by its own dormant-files maintainer decision; SA106 is assistant-executable. Root `check-json` hook exclude (`^quickscale_core/src/quickscale_core/generator/templates/themes/showcase_react/tsconfig\.json$` JSONC) reviewed `STATUS ok`; all 10 hooks pass; SHA-256 `9aaae682d5e2df06d1375bc66e738ab372a9cd42106353752f42cdda2d875d99` unchanged. **Advisory SA104-ADV-001** (severity: medium, disposition: advisory (non-blocking), category: consistency): generated `quickscale_core/src/quickscale_core/generator/templates/.pre-commit-config.yaml.j2:10` still runs strict `check-json` against emitted `frontend/tsconfig.json`; pre-existing, deferred to separate follow-up.
  - **SA100 — recovery-ledger theme exemption narrowed (complete).** TA58 now uses an explicit validator opt-in that permits only the recovery ledger's `__checkpoint__` placeholder; stale/retired recovery themes retain fail-closed remediation, all other callers remain unchanged, and TA59's dead `_RECOVERY_PROBE_PATHS` constant plus adjacent `.quickscale` comment typos were removed. Adaptive-change-review pass 1: **STATUS ok**, no findings, caller parity passed. Validation: 53 targeted tests passed; Ruff check/format and `git diff --check` passed; numeric coverage not generated. Blockers: none.
  - **TP2 — unit-suite xdist (2026-07-18; independent change-review pass 1 STATUS ok, no findings).** Added root `pytest-xdist==3.8.0`, refreshed `poetry.lock`, and installed the dev environment. `make test-unit` uses `-n auto --dist loadfile` by default; `PYTEST_XDIST_WORKERS=0` runs serially and `PYTEST_XDIST_WORKERS=N` pins workers. Serial/xdist collection sets matched exactly (core 2,354; CLI 1,993). CLI had 1,993 passed and 90.36% XML coverage in both modes; wall clock improved 56.36s → 33.44s. Core had 2,353 passed + 1 skipped and unchanged 80.39% XML coverage; wall clock improved 176.03s → 68.40s. The existing core 90% package floor remains below the unchanged 80.39% measurement, but TP2 requires parity and unchanged coverage—not that pre-existing floor to become green—so this is a waived, non-blocking baseline finding. No coverage floor or test was changed.
  - **TP2b — xdist-safe coverage phases (2026-07-19; complete).** `make test-cov` uses isolated phase `COVERAGE_FILE` paths, one explicit `coverage combine` pipeline, guarded xdist defaults, and a serial override while preserving the backups requirement behavior. Both exact commands accepted: `QS_BACKUPS_DB_USER=quickscale_test_role make test-cov` exit 0 and `QS_BACKUPS_DB_USER=quickscale_test_role make test-cov REQUIRE_BACKUPS_COVERAGE=1` exit 0 — **4,536 core/CLI passed, 323 backups passed, 2 skipped; 92.88% equal-weight package mean** (core 95.27%, CLI 90.49%); all 90 files ≥80%; default 24 auto workers, no stall. **CR-TP2B-001**, **CR-TP2B-002**, and **QG-TP2B-001** resolved. Final change-review **STATUS ok** (confidence 94). No blocking findings remain. The prior `auto`-worker Poetry-lock contention is a non-blocking advisory — it did not recur in the accepted runs. Evidence recorded in the roadmap completed task block.
  - <a id="sa93-continuation"></a>**SA93 — E2E green-gate closeout (2026-07-18).** Canonical local evidence: exact `make ci-e2e` is green (all 12 stages, Core 35, CLI 29, all 12 integration modules, 91.90% mean coverage (combined), every file at least 80%, empty per-file quarantine). The initial hosted run [29641299861](https://github.com/Experto-AI/quickscale/actions/runs/29641299861) aborted its generated-project Docker job because the workflow supplied one apply confirmation while the current CLI required two. The workflow prompt-parity fix was published in commit `667396cc` (pipe `y\ny\n`); hosted rerun [SA93-EVID-001](https://github.com/Experto-AI/quickscale/actions/runs/29641954573) verified `headBranch=v87`, `headSha=667396cc44ac7c1737c0b7825963c72963bc9fd2`, `event=workflow_dispatch`, `status=completed`, and `conclusion=success`; **Test Generated Project Docker Build** and **Full E2E Tests (PostgreSQL + Playwright)** both succeeded. Hosted evidence owns only those two green jobs and the generated-project Docker contract; it does not imply that the hosted workflow executed `make ci-e2e`. All 11 prerequisites and open decisions (zero) were resolved; SA93-REV-005 remains an unresolved advisory/out-of-scope test gap and SA93-ADV-001..004 remain advisory/non-gating.
  - **TP1 — parallel local-CI static gates.** `scripts/check_ci_locally.sh` now launches independent stages 2–9 concurrently, buffers and replays output in declaration order, attributes every failed gate, and preserves `QS_CI_PARALLEL=0` for serial debugging; database-dependent and E2E stages remain serial. The maintained `test-ci-local-parallel` target covers concurrency, lint/typecheck/manifest failure attribution, serial parity, and ownership-safe TERM/INT/HUP cleanup without stale-PID signaling, and runs in both `make ci` and GitHub CI. Same-host full-gate timing was 539.70s parallel versus 550.96s serial (2.04% faster). Final `make ci` passed with 14 TP1 tests, 4,395 core/CLI tests, 323 backups tests, all PostgreSQL integration suites, and unchanged coverage floors (91.90% equal-weight policy mean). CR-TP1-REV-001/002/003 are resolved; no blocker remains. **Independent change-review pass 3 STATUS ok.**
  - **SA96-GATE blocked checkpoint (2026-07-19; not complete):** Authored implementation was limited to five new test modules — `quickscale_core/tests/test_dr_engine_orchestration.py`, `quickscale_core/tests/test_dr_engine_orchestration_a2a.py`, `quickscale_core/tests/test_dr_engine_orchestration_a2b.py`, `quickscale_core/tests/test_dr_engine_orchestration_a3.py`, and `quickscale_core/tests/test_manifest_resolver_phase2_coverage.py`. Canonical coverage recorded **2,480 passed, 1 skipped, 90 deselected; 6,026/6,670 = 90.34%**, including orchestration **745/1,011 = 73.69%** (**+35 from 5,991**); focused authored tests passed (127), Ruff/MyPy passed, and `make check` exited 0 in **3m35.303s** with a **94.41%** integration-policy mean and empty quarantine. This supersedes/resolves the prior coverage blocker **SA96-GATE-BLK-001**. `make quality` exited 2 with **9 critical + 10 warning baseline regressions** at `.quickscale/quality_report.md` lines 32–52, so **SA96-GATE-BLK-002** remains blocking. `make ci` and `make ci-e2e` were not run; independent change-review pass 2 returned **STATUS ok** with **SA96-CR-001 (blocking)** and **SA96-CR-002 (advisory)** resolved and no new findings. This blocked checkpoint is safe to merge; SA96-GATE remains unchecked. No source, config, baseline, or threshold changes were made.

  - **SA96-GATE — green-gate join complete (2026-07-20).** All four commands exit 0 on a clean rerun at the prior synced code baseline (post-SA92 squash) with `QUARANTINE_TICKETS` **empty**: `make check` exit 0; `make quality` exit 0 with zero baseline regressions; `make ci` exit 0 (coverage mean 92.89%, all 90 files ≥80%); `make ci-e2e` exit 0 (64 E2E passed); integration: 2,262 passed, mean 94.41%. **Resolved operational finding:** initial aborted attempts left an orphan `test_test_quickscale_orgs` Django mirror DB (double-prefixed). No repository defect or threshold change was needed — no stale test workers remained, and only the disposable double-prefixed mirror database was dropped before the clean rerun passed. No open product blocker at that checkpoint. **Superseded release-path status:** later installed-context coverage work added SA111a/SA112; SA111a is complete, SA112 is next, and **SA96-PUBLISH** remains human-only and held until SA112 closes.

  - <a id="sa109"></a>**SA109 — Fix installed-wheel module discovery (2026-07-20; complete).** Module discovery previously only consulted the maintainer monorepo `quickscale_modules/` workspace and raised `ImproperlyConfigured` for any other context — the installed wheel crashed on import. Phase 1: added `discover_bundled_module_names()`, `get_bundled_manifests_path()`, `get_resolution_source()`, and the `ModuleResolutionSource` enum to `module_discovery.py`; added bundled-manifest access/discovery/fallback over pre-existing synchronized snapshots under `quickscale_core/data/manifests/*/module.yml` for all 12 shipped modules. Phase 2: `get_discovered_module_names()` in `module_catalog.py` now resolves source inventory first, then falls back to bundled inventory, then fails hard — the three-tier precedence that powers lazy discovery in `config_schema.py` (via `_validate_modules_section`) and `module_commands.py` (via `_LazyModuleChoice`), replacing the two previous import-time eager discovery sites. Phase 3 (this entry): AF7 decision recorded in `decisions.md` §Bundled Module Inventory and Source-Required Paths (AF7); SA109 roadmap item marked `- [x]`; all same-fact summary, diagram, and readiness statements updated. Source-required operations (`get_modules_base_path`, `discover_shipped_module_paths`, `load_module_manifest`, `refresh_managed_adapters`) remain fail-hard in the installed-wheel context — the AF7 fail-hard intent is preserved where it is genuinely load-bearing. **Validation:** 327 focused AF7 tests passed; 2,505 core unit tests passed; 1,997 CLI tests passed (with one pre-existing unrelated Track 2 taxonomy failure). Corrected `make install` installed-wheel smoke exit 0 from outside the source tree — `quickscale --version`, `quickscale version`, `quickscale --help`, `quickscale status` all exit 0 with exactly 12 bundled module names. Existing synchronized snapshots are verified by `make check-manifest-sync` (pre-existing manifest snapshots, not manifests added by SA109 — Phase 1 created the bundled manifest directory as a new snapshot target, but the per-module `module.yml` files were already present in `quickscale_modules/` before this phase). AF7 decision is new in the policy hub (was previously docstring-only). The next critical-path step is **SA110** (installed-artifact smoke gate, deps SA109 ✓).

  - <a id="sa110"></a>**SA110 — Installed-artifact smoke gate (2026-07-20; complete).** QG proofs confirmed SA110-REV-003 (negative SA109 regression — disposable core copy with manifests removed, gate red with ImproperlyConfigured (not ImportError)) and SA110-REV-004 (deterministic candidate ordering — 7/7 shim tests pass). Advisory SA110-ADV-001: actual 3.14/3.15 binaries unavailable; boundary tests use version-reporting shims; hosted evidence deferred. Adds `scripts/smoke_install.sh` — a gate that builds all three QuickScale wheels (quickscale_core, quickscale_cli, quickscale) from per-run staged copies (never mutates source pyproject.toml), installs them into a throwaway venv **outside the source tree** (so the monorepo `parents[4]/quickscale_modules` cannot resolve), and runs 19 probes from an external workdir with sanitized PYTHONPATH/PYTHONHOME. The gate is designed to catch the class of regression SA109 fixed — a wheel that crashes on import — before it can reach PyPI. Eighteen probes (version/help commands) assert exit 0 and no traceback: `quickscale --version`, `quickscale version`, `quickscale --help`, `--help` for every registered subcommand (version, up, down, shell, manage, logs, ps, deploy, dr, update, push, plan, apply, status, remove). The 19th probe runs `quickscale status` from outside a project and asserts exit 1, the expected "Not in a QuickScale project directory" diagnostic, and no traceback. The `quickscale status` outside-project exit-1 contract is preserved by explicit user decision — the CLI was not changed. Uses existing project conventions: `set -euo pipefail`, `_python_requirement.sh` sourcing, Poetry build with per-run staging and isolated build venvs, throwaway venv in `/tmp`, trap-based cleanup. Applies the full `>=3.13,<3.15` requires-python constraint during interpreter candidate selection (iterates past invalid candidates). **Design decisions:** staged copies eliminate the race-unsafe in-place pyproject.toml backup/restore pattern; `run_smoke_probe` sanitizes PYTHONPATH/PYTHONHOME and cds to the external workdir for every probe; `smoke_select_python` iterates candidates with the full spec; `QS_SMOKE_REGRESSION_CORE` env var allows a disposable modified core source for negative SA109 regression testing. **Implementation artifacts:** `scripts/smoke_install.sh` rewritten, `Makefile`/`publish.yml`/`roadmap.md`/`CHANGELOG.md` updated. No package API, CLI behavior, or version was changed. Advisory SA110-ADV-001 retained: actual 3.14/3.15 binaries unavailable — boundary tests use deterministic version-reporting shims; hosted Python 3.14 evidence deferred.

  - **SA108 — Frontend de-specialization migration-doc rewrite (2026-07-21; complete).** Corrected `docs/planning/beta-site-migration.md` frontend guidance to accurately reflect the project-agnostic, byte-identical `frontend/src` contract (SA105/SA106/SA107) while distinguishing legacy pre-SA105 in-place recipients from fresh current-theme generated recipients. Removed obsolete `useModules.ts` `projectName` identity-fix steps and `main.tsx` conditional-patch checklist items. Dropped overbroad per-module page-copy claims: blog/crm/listings pages are always-emitted dormant files only for fresh current-theme recipients (SA105); legacy pre-SA105 recipients have no retroactive dormant guarantee for any module surface. The shipped post-apply adoption step copies only missing forms/social surfaces; it does **not** backfill blog/crm/listings. Corrected validator name from `validateQuickScaleSeam()` to `validateQuickScaleConfig()`. Qualified main.tsx byte-identity claim (version-dependent). Clarified routing responsibility boundaries: SA107 `validateQuickScaleConfig()` validates seam fail-hard; App.tsx gates ordinary module routes; `renderQuickScaleRoot()` handles public-social dispatch; main.tsx is byte-identical only among projects generated from the same theme version. Updated file-classification table, checklists, and continuation guide consistently. Legacy compatibility finding documented: pre-SA105 in-place recipients have no retroactive dormant guarantee for any module surface; the post-apply adoption step copies only missing forms/social surfaces and preserves existing copies; blog/crm/listings absent after continuation require manual handling. Track 2 frontend de-specialization chain (arch Finding 10) is now fully closed. Roadmap SA108 marked `- [x]`; Track readiness date updated to 2026-07-21.

  - <a id="sa113"></a>**SA113 — Installed-context implication-resolver bundled-manifest fallback with fail-hard inventory validation (2026-07-21; complete).** `resolve_module_implications` (`quickscale_core/src/quickscale_core/manifest/implications.py`) previously called `get_modules_base_path()` with no fallback, crashing with `ImproperlyConfigured` in installed-wheel contexts where no source-tree `quickscale_modules/` workspace exists. Both CLI callers — `plan` (`plan_command.py:102`) and `apply` (`apply_command.py:973`) — reached the crash via `_materialize_implied_module_configs`. Phase 1 mirrors the SA109 pattern: when `get_modules_base_path()` raises `ImproperlyConfigured`, the resolver falls back to `get_bundled_manifests_path()` for bundled manifest snapshots (`quickscale_core/data/manifests/*/module.yml`). Phase 2 adds a fail-hard inventory boundary: in bundled-fallback mode, missing selected or implied module manifests raise `ImproperlyConfigured` instead of being silently skipped. The AF7 fail-hard contract is preserved — when both source-tree and bundled manifests are unavailable, `ImproperlyConfigured` is still raised. Source-required operations remain unchanged. **Validation:** 7 focused SA113 unit tests (fallback resolves billing→orgs chain, explicit path unaffected, explicit path still resolves when source tree unavailable, missing selected manifest raises, missing implied manifest raises, malformed manifest raises, both-unavailable fail-hard) passed; 30 existing implication tests unchanged; targeted implication suite 37 passed. Roadmap SA113 marked `- [x]`; all CR findings resolved.

  - **SA111a — Installed-context `plan` coverage complete (2026-07-22).** Extended `scripts/smoke_install.sh` with a 20th non-interactive `quickscale plan` probe that selects all 12 modules via scripted stdin and runs from the installed wheel outside the source tree. The probe exposed a second installed-context crash in `_load_notifications_manifest`; that loader now follows the SA113/AF7 pattern by falling back from `get_modules_base_path()` to bundled manifests on `ImproperlyConfigured`. Two regression tests cover both notifications option callers. The probe unsets inherited `PYTHONPATH`/`PYTHONHOME`; a deliberately source-contaminated `PYTHONPATH` run passed all 20 smoke probes. Ruff, MyPy, and 78 resolver tests passed; independent change-review resolved `SA111A-CR-001` and approved completion. The broad core suite retained 14 unrelated baseline failures (11 test-isolation contamination failures and 3 React-theme failures), with 2,562 passing and 90.48% coverage.

  - **SA111b — Fast in-monorepo fallback regression test (2026-07-23; complete).** Added `TestSa111bFallbackRegression` class with `test_fallback_resolves_billing_to_orgs` in `quickscale_core/tests/test_manifest_implications.py`. Monkeypatches `implications.get_modules_base_path` to raise `ImproperlyConfigured` and asserts `resolve_module_implications(["billing"])` still resolves billing → orgs via the bundled-manifest fallback (`get_bundled_manifests_path`). Runs inside `make check` as a fast early-signal companion to SA111a's authoritative installed-wheel probe. Focused test passes (1/1); existing SA113 bundled-fallback tests unchanged (7/7); targeted implication suite 38 passed. Roadmap SA111b marked `- [x]`.

  - **SA101 quality-remediation closeout (2026-07-19; complete).** Cleared all 19 SA96-GATE-BLK-002 baseline regressions (9 critical + 10 warning) against the **unchanged** `scripts/quality_baseline.json` per the Option A decision — no baseline, threshold, or quality-gate edits. Work ran in nine serial behavior-preserving phases on Track 3 (`wt-track3`, synced from `v87` at `d7ccf24`), with the Adaptive flow producing a discovery snapshot, a planner-authored nine-phase plan (`SA101-plan-r2`), and an independent plan-review that returned `STATUS: ok` after resolving `SA101-PR-001/002/003`. Per-phase quality-ledger progression: 19 → 16 → 14 → 11 → 9 → 8 → 6 → 4 → 2 → 0. Refactors were file-local extraction plus direct-identity aliases. Phase 9 compacted `quickscale_modules_crm/migrations/0001_initial.py` (605 → 413 lines) and `quickscale_modules_forms/migrations/0001_initial.py` (758 → 137 lines) with **exact parity** (SHA-256-matched). `make quality` exit 0. The v87 shrink-only quality-maxima policy was recorded in [`decisions.md`](docs/technical/decisions.md) §Migration-Squash Decision SA92 (§5): docs do not affect the gate, and these migrations were remediated, not allowlisted.
    - **`SA101-VAL-001` — RESOLVED via option (a) (2026-07-19 experiment):** the reported restricted-role failures were reproduced on **clean pre-SA101** (`d7ccf24b`) and confirmed **pre-existing, not caused by the compaction**. Same environment, restricted `quickscale_test_role` (`NOBYPASSRLS`), migration/RLS tests included: CRM **3 failed / 281 passed / 3 errors** on clean pre-SA101 vs **284 passed / 0 failed / 0 errors** at HEAD with SA101; Forms green in both states (212 passed). SA101's Phase 9 rewrite of `crm/tests/test_migrations.py` fixed the pre-existing fixture defect. No maintainer decision remains.
    - **Final closeout (2026-07-19):** `make check` exit 0 — 6,943 passed, 86 skipped, 12 deselected, zero failures; core 90.36%, CLI 90.49%, integration aggregate 94.41%. Independent full change-review returned `STATUS: ok`, confidence 94, no blocking or advisory findings. Worktree remained clean. Known BYPASSRLS-dependent skips are pre-existing. `make ci` and `make ci-e2e` were not run. SA101's feeder condition for SA96-GATE is met; the four-command join is the next pending step.
  - **TP3a implementation (completed).** The E2E harness allocates a per-run app port, exports lane-specific Compose/container scopes, and limits cleanup to that scope; CLI fixtures consume the allocated port and generated project prefix while pytest-docker PostgreSQL remains dynamically mapped. Independent review pass 2 returned **STATUS ok** and resolved CR-TP3A-001. The merged-v87 rerun passed stages 1–11 (Core E2E, unit/coverage, backups, integration, 91.90% mean coverage, every file ≥80%), but the final CLI E2E stage did not complete within the runner allowance, so integrated `make ci-e2e` green was not established — that evidence gap is carried forward as a TP3b precondition. Remaining low advisory follow-ups — CR-TP3A-002 (rare port allocation TOCTOU) and CR-TP3A-003 (unsafe custom scope leading character) — remain open and were not fixed.
  - **TP3b — concurrent Core/CLI E2E lanes (completed).** Core and CLI lanes run concurrently by default, with `QS_E2E_PARALLEL=0` for serial debugging. The harness uses isolated scopes/ports, deterministic log replay, signal-safe cleanup with active-PID handling, nonzero failure attribution, preserved `--headed`/`--no-cleanup`/`--full` flags, and no inter-suite sleep; the 47-test harness is maintained. Findings TP3B-CR-001/002/003 and QG-TP3B-001 are resolved; no open blocker remains. Final evidence: targeted registry 20 passed; harness 47 passed; coverage 91.90% with all 84 files ≥80%; integration 2,462 passed; Core E2E 35 in 317.02s; CLI E2E 29 in 816.51s; all 12 `ci-e2e` stages green. The concurrent critical path was 816.51s versus the precondition serial Core+CLI sum of 1,188.44s (~31.3% reduction); total runner wall-clock was not measured. One existing CLI deprecation warning and expected skips were non-blocking advisories.
  - **TP4 — Fast feedback loop documentation (2026-07-20; complete).** Added a "Fast feedback loop for AI-assisted / incremental development" subsection to `docs/technical/development.md` with the iterate → pre-merge → E2E-last ladder, section flags (`make lint -- --core`, `make typecheck -- --cli`, `make test-unit -- --core`), single-module reruns with concrete env var (`QS_ORGS_DB_USER=quickscale_test_role make MODULE=orgs test -- --modules`), bounded integration concurrency (`QS_INTEGRATION_JOBS=2 make test-integration`), and cross-references to the landed `QS_CI_PARALLEL` (TP1), `QS_E2E_PARALLEL` (TP3b), and `PYTEST_XDIST_WORKERS` (TP2) knobs. Five commands were verified on the clean `v87` baseline: `make lint -- --core` exit 0; `make typecheck -- --cli` exit 0; `make test-unit -- --core` exit 0 (2,489 passed); `QS_ORGS_DB_USER=quickscale_test_role make MODULE=orgs test -- --modules` exit 0 (874 passed, 11 skipped); `QS_INTEGRATION_JOBS=2 make test-integration` returned nonzero — the bounded worker pool ran all 12 modules (94.39% mean) but `orgs/test_debug.py::TestMiddlewareDebugOverride::test_debug_org_overrides_saas_session` failed with "server closed the connection unexpectedly". A serial rerun (`QS_INTEGRATION_JOBS=1 make test-integration`) subsequently passed (all 12 modules, 94.41% mean). The bounded-run failure is an unresolved non-blocking integration risk for this docs-only task. `QS_CI_PARALLEL=0` and `QS_E2E_PARALLEL=0` are documented as cross-references with their owning ticket context; their full verification is the responsibility of their respective TP1 and TP3b tickets, which are complete. Roadmap updated to mark TP4 `- [x]` and close the Track 1 TP suite. This is a docs-only change — no gate behavior, pass/fail set, baseline, or threshold was modified.

- v0.86.0 — Organizations Module (multi-tenancy with Solo/SaaS runtime modes, org-scoped RBAC and invitation flow, billing bridge with authoritative org ownership fields and `Plan.features` feature gates, `migrate_billing_to_orgs` and `promote_to_saas` management commands, React org management pages and org switcher, and billing wiring regression fix with planner/apply guard test)
- v0.85.0 — Billing Module (Stripe-backed one-time credit purchases and recurring subscriptions, credits-first Django-owned ledger, planner/apply readiness with auth-module dependency enforcement, module-owned `/billing/pricing/` and `/billing/dashboard/` pages, and starter-theme billing links)
- v0.84.0 — Backups Hardening Release (admin backup artifact download/create/integrity actions, upload-driven restore safety and compatibility checks, full-snapshot backup-contract hardening, and repo-owned runtime/tooling support alignment for the refreshed Python/frontend baseline)
- v0.83.0 — Hardening Release (repo-wide hardening across strict desired-config validation, notifications live-config placeholder rejection, generated production `SECRET_KEY` fail-hard behavior, CRM staff-only parity, forms CSV and backup artifact safety, blog upload/thumbnail/rate-limit hardening)
- v0.82.0 — Disaster Recovery & Environment Promotion Workflows (public `quickscale dr capture/plan/execute/report` workflows for local and Railway routes with `snapshot_id`-based stored snapshots, resumable capture and execute, rollback pins for production routes, conservative env-var sync, and source-side media sync)
- v0.81.0 — Beta-Site Migration Maintainer Tooling (maintainer-only Make/Python workflows for beta-site catch-up: fresh-first and in-place paths with checkpoint-first semantics)
- v0.80.0 — Analytics Module (PostHog-only website analytics with flat `QUICKSCALE_ANALYTICS_*` settings, service-style capture helpers, apply-time env-example sync, guarded forms integration, QuickScale-owned social click tracking)
- v0.79.0 — Social & Link Tree Module (curated social links and embeds, backend-owned YouTube/TikTok preview metadata, managed `/_quickscale/social/` integration endpoints, Django-owned public React pages)
- v0.78.0 — Notifications Module (transactional email foundation, Anymail-backed Resend delivery, recipient-granular tracking, signed delivery webhooks)
- v0.77.0 — internal main-branch baseline — Backups Module (private database backups, optional private remote offload, guarded CLI restore, scheduler-ready command hooks)
- v0.76.0 — Storage Module (cloud file hosting, media storage adapters, CDN-ready media infrastructure)
- v0.75.0 — Forms Module (generic customizable form builder with admin, DRF API, spam protection, GDPR anonymization, and React mount point)
- v0.74.0 — React Default Theme (showcase_react theme with Vite, TypeScript, TanStack Query, and Zustand)
- v0.73.0 — CRM Module (API-first Django CRM with 7 core models and CLI integration)
- v0.72.0 — Plan/Apply Cleanup (removed legacy init/embed commands, full transition to plan/apply)
- v0.71.0 — Module manifests & config mutability (Plan/Apply system complete)
- v0.70.0 — Existing project support (status, plan --add, plan --reconfigure)
- v0.69.0 — State management and incremental applies
- v0.68.0 — Plan/Apply System core commands (Terraform-style declarative workflow)
- v0.67.0 — Listings module with AbstractListing base model for verticals
- v0.66.0 — Blog module with Markdown, featured images, and RSS feeds
- v0.65.0 — Enhanced auth module and development tooling
- v0.64.0 — Theme rename to showcase_* (breaking change)
- v0.63.0 — Authentication Module with django-allauth and interactive embed
- v0.62.0 — Split Branch Infrastructure (module management CLI commands, GitHub Actions automation)
- v0.61.0 — Theme System Foundation (--theme CLI flag, theme abstraction layer, HTML theme)
- v0.60.0 — Railway Deployment Support (automated deployment via quickscale deploy railway)
- v0.59.0 — CLI Development Commands (Docker/Django operation wrappers)
- v0.58.0 — Comprehensive E2E testing infrastructure with Playwright and PostgreSQL
- v0.57.0 — Production-ready generator baseline
- v0.56.0 — Quality, Testing & CI/CD
- v0.55.0 — CLI implementation
- v0.54.0 — Project Generator
- v0.53.3 — Project Metadata & DevOps Templates
- v0.53.2 — Templates and Static Files
- v0.53.1 — Core Django Project Templates
- v0.52.0 — Project Foundation
- v0.51.0 — Documentation foundation
- v0.41.0 and earlier — legacy codebase (see Github repository for history)
