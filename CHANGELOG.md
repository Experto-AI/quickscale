# Changelog

`CHANGELOG.md` is the canonical QuickScale release history index. Published releases pair each version entry with a single official release note in `docs/releases/` linked from the GitHub tag and release PR. When a release note is prepared before the maintainer completes the manual tag/publish step, the changelog entry and note must say so explicitly and must not imply publication. Use `docs/technical/roadmap.md` for active or unpublished release status. Entries are version-ordered.

- v0.87.0 — unreleased / not yet tagged — Hardening release. Consolidates a large hardening pass across tenant isolation, module configuration, the generator, and disaster recovery.
  - **SA122b-4 COMPLETE — publish conformance gates reviewed and accepted (2026-08-07; Phase 2 documentation closeout; no publication).** Five standalone publish gates at `b42ddd20`; parity 0 omissions (214 tests), trigger diagnostic 4/4; DC-124 `STATUS: ok` (97, no findings). Narrow trigger invocation deferred (global coverage fail-under 0% only); `make quality` exit 2 solely on unchanged `apply_command.py` CC 56 vs 55. No tag/push/publication; SA122b + Arch Finding 11 open; SA122b-5 next, merge-gated after SA112e.
  - **Two maintainer decisions resolved — SA115 split applied, SA136d seal atomicity re-scoped (2026-08-07; documentation only).** SA115 splits into SA115a (xdist fixtures/worker count/clamp; `deps: none`; no merge gate) and SA115b (e2e.yml xdist triggers; `deps: SA112e`; merge after) — Track 2 head truly green; consolidation deferred to SA122b-5. SA136d withdraws server-transactional atomicity (window ≤ the accepted force-privileged-tag residual): seal = client check-then-act (reread branch vs SHA; tag absent or already at intended commit; single explicit refspec) + fail-closed post-push reread; window documented, not eliminated. Retires F-003 (high, security boundary). No rebalancing; open decisions: none.
  - **Roadmap cleaning pass (2026-08-07, post-SA136e; documentation only).** SA136e checked block deleted; F-003/F-004 re-homed as carry line on SA136f; SA136f's `deps: SA136a–SA136e` restated unsatisfied (SA136d open); tech-audit ref corrected to closed. No move — critical path blocked by a maintainer decision, not capacity. Tracks: T1 truly green at SA122b-4 (filler); T3 blocked at SA136d (decision 2); T2 not green. Open decisions: two.
  - **SA136d blocked checkpoint — receive atomicity retained, GitHub.com stopped (2026-08-07; planning/research only; task unchecked).** No stock-Git/GitHub.com solution for F-003 (high): no-op updates filtered pre-`send-pack`, no customer receive hook, single-ref CAS can't encode tag-absent-or-same. F-001 (high) and F-002/F-004–006 (medium) drafted, none second-review-approved. Maintainer: keep full atomicity, stop. Continuation needs an authorized hook-capable receive platform, fresh grounded plan, independent `STATUS: ok` plan review. T3/v0.87 critical path blocked at SA136d.
  - **SA136e COMPLETE — split workflow retired, publish-trigger conformance accepted (2026-08-07).** `make test-unit` green (Core 2,693/1 skip; CLI 2,060); `test_publish_trigger.py` in mandatory collection; widened-`*` trigger negative control exits 2 (two expected). DC-114 `STATUS: ok` at `b937dbc2`, F-001/F-002 resolved. T1 → SA122b-4. F-003 advisory on SA136f; F-004 normalized as serial-handoff start gate.
  - **Roadmap cleaning pass — superseded ledgers archived (2026-08-07; no ticket closed).** Closed-child narratives (SA112a, SA117a–c, SA117e-1…3, SA122b-1…3, SA136a–c, SA128, SA138), two SA117e-4 plan-review ledgers, retired auth lists, SA136e partial transcript removed; critical path restated `SA136d → SA136f → SA117e-4 → SA112b→c→d→e→f → SA96-PUBLISH`; SA122b-4 *can start* corrected to no (serial-handoff start gate — substance of F-002); delta merged `805e604c`; census → `e2e.yml`/`publish.yml` only. No move; SA136d/e truly green; one decision (split SA115).
  - **SA136e checkpoint-review addendum (2026-08-07; task unchecked).** Raised F-002 (medium): SA122 umbrella prose called SA122b-4 startable while SA136e unchecked; correction (blocked until SA136e closes) applied mechanically post-cap; F-002 deferred, not resolved; nothing waived.
  - **SA136e recorded-partial checkpoint (2026-08-07; task unchecked).** Candidate deletes `.github/workflows/split-modules.yml`, adds split-tag namespace rationale beside unchanged tag globs, one four-case conformance test; relocated after F-001 (medium — outside mandatory collection). Post-relocation QG cancelled without envelope at `qg_runs=2/2`; no final `STATUS: ok`. Record-and-merge; F-001 not waived; SA122b-4 barred.
  - **Roadmap cleaning pass — SA136e moved to Track 1 (2026-08-07; one move applied).** SA136c/SA138 closed blocks deleted; SA136e → Track 1 (`deps: none`; on T3 the one-child rule would serialize it before/after SA136d) running concurrently with SA136d; `publish.yml` co-write with SA122b-4 serialized in-track (safer than cross-track). T3 truly green at SA136d (critical path); T1 at SA136e; T2 not green. One decision: split SA115.
  - **SA136c COMPLETE — embeds consume immutable `splits/<module>-module/<core-version>` tags (2026-08-07; commit `85410b65`).** Fail before subtree when tag absent; peel to 40-hex, same SHA for subtree and provenance; maintainer-only repeatable `--split-ref MODULE=REF` with coverage checks, pre-seal warning, no fallback. 409 tests + lint/typecheck; review `STATUS: ok`; F-001/F-002 resolved; T3 → SA136d.
  - **SA138 COMPLETE — tag-path complexity baseline regression cleared (2026-08-07; commit `a8f55cbe`).** 198 git-utils tests; `make quality` exit 0, monotonicity pass, waivers empty; review `STATUS: ok`, no findings. T1 → SA122b-4; four-command green-gate milestone unclaimed.
  - **Roadmap cleaning pass (2026-08-06, fourth; one ticket opened, one placement applied).** SA122b-3 block deleted; stale `QG-SA136-QUALITY-001` prose ticketed **SA138** (T1, `deps: none`, T1 head) — `make quality` exit 2 on `git_utils.py` CC 21/13 regressions; bars editing `quality_baseline.json` ceilings. SA138 placed ahead of SA122b-4 (`git_utils.py` unowned → parallelizes with T3's chain). Decisions then: SA136e-to-T1 (later applied), SA138 placement (applied, reversible).
  - **SA122b-3 COMPLETE — hosted CI job/`needs` generation accepted (2026-08-06; commit `7fc9b5ef`).** Registry `bindings.ci_job` rename treated as regenerable drift; unowned workflow bytes preserved; job IDs/ordered `needs`/check commands strictly revalidated; `make check` runs the gate non-recursively with one `GATE_REGISTRY`. 215 tests; parity = five expected publish omissions; DC-12 `STATUS: ok`, F-005/F-006 resolved. Outside-scope blocker `QG-SA136-QUALITY-001` (pre-existing complexity drift) recorded; T1 → SA122b-4.
  - **SA136b COMPLETE — authoritative twelve-module inventory; direct publish membership guard (2026-08-06; commit `d00ffe68`).** `scripts/publish_module.py` requires inventory membership before release/origin prompts or any split/push path — `teams`/unapproved thirteenth directory rejected pre-mutation; `AUTHORITATIVE_MODULE_COUNT = 12` sole fail-hard source. 7 focused tests; version-check 12/12 at 0.87.0; `make check` Core 2,689/CLI 2,043; DC-94 `STATUS: ok`, F-002 resolved. T3 → SA136c.
  - **Roadmap cleaning pass (2026-08-06, second; no ticket closed).** SA136a block deleted; `git_utils.py` recorded unowned; no move (tenth). Decision (move SA136e to T1?) open, unapplied. T3 at SA136b (F-002); T1 at SA122b-3 (F-005/F-006); T2 closed.
  - **SA122b-3 recorded-partial checkpoint (2026-08-06; task unchecked).** Candidate keeps static 11-job graph; 204 tests + generation/Ruff/quality/diff green; parity stays five expected omissions; F-001–F-004 closed by checkpoint review. **Pending:** F-005 (medium) — registry `ci_job` rename must render new job IDs/`needs` with clean-write and unowned-byte regressions; F-006 (high) — `check-ci-gate-generation` via mandatory non-recursive gate with stale/clean proof. Record-and-merge; SA122b-4 barred.
  - **SA136a COMPLETE — tag, tree, and local-tag helpers (2026-08-06; reviewed in DC-80).** Canonical split-tag construction, exact peeled remote-tag resolution, existence checks, tree/local helpers, idempotent non-force annotated tags, single-refspec no-force push; hermetic. 198 tests; lint/typecheck/test-unit (Core 90.46%, CLI 90.78%). DC-80 `STATUS: ok`; F-001–F-004 resolved; F-005 low advisory (lock-component prevalidation parity; Git fails closed). T3 → SA136b.
  - **SA136b recorded-partial checkpoint (2026-08-06; commit `999c025b`; task unchecked).** Fail-hard twelve-module inventory landed; publish listing/version propagation/SA117 scope migrated onto it; `teams` glob path retired; 147 tests + version-check + full `make check` green; F-001 resolved. **Pending:** F-002 (medium) — direct selector still accepts an existing directory without inventory membership; reject pre-mutation. Reviews DC-87/DC-90 medium/1; record-and-merge; F-002 not waived.
  - **Loop/seal split publication ratified — SA136 opened, SA117e-4 restructured, SA119 closed (2026-08-06; planning only).** (1) Split-branch publication is an idempotent republish; only PyPI is irreversible — branches are mutable working artifacts, which is what makes verification possible. (2) Splits version-tagged in lockstep: `splits/<module>-module/X.Y.Z` resolved by identity from core's version — no mapping table; unchanged tree carries both tags; closes SA119. Ratified: `splits/…` namespace (verified outside publish triggers), deletion of `.github/workflows/split-modules.yml` (bare-`--force` 3-of-12 pusher, origin of the stale `splits/teams-module` branch). Umbrella SA136 (six children, T3, critical path); SA137 opened v88 (devtools drift). T3 truly green at SA136a.
  - **SA117e-4 plan-review-cap blocked checkpoint (2026-08-05; task unchecked).** `SA117E1-REV-001` ratified with explicit absence contract `--force-with-lease=refs/heads/<branch>:` after Git 2.43.0 research (fresh tracking can authorize overwrite; stale can spuriously reject; never-fetched fails closed); `SA117E1-REV-002` ratified as digest-bound bind-and-one-time-consume, not SA124. Blockers 2→4 at high: F-001 (interruption barriers, durable restart, replay/contender rejection), F-004 (phase-local allowlists, read-only phases), F-005 (frozen twelve-module inventory), F-006 (armed rollback to `ba34f594`); `STATUS: partial`. No implementation or push authorized; twelve-row confirmation still an execution-time gate.
  - **SA122b-1 COMPLETE — Make `check` aggregation derives from registry (2026-08-05; commit `2db57e48`).** 188 parity tests; both `make check` modes exit 0; direct checker exit 1 with exactly five publish-only omissions. DC-68 `STATUS: ok`, F-001 resolved, no new blockers. T1 → SA122b-2.
  - **SA122b-2 COMPLETE — local serial/parallel inventories derive from registry (2026-08-05; commit `cf9c3dc2`).** 24 local-CI + 190 parity + 49 worker-pool tests; gates green; parity retains only five publish omissions. DC-85 `STATUS: ok`, F-001/F-002 resolved. T1 → SA122b-3; Arch Finding 11 open.
  - **SA117e-4 recommended disposition recorded — PROPOSAL, not ratification (2026-08-05; documentation only).** Recommends ratifying REV-001 (explicit remote-absence lease — bare `--force-with-lease` can't distinguish absent from never-fetched, silent pass) and REV-002 (digest-bound bind-and-consume, explicitly not SA124). Handoff may run read-only lease research only; steps 2–4 need written ratification. Weak options would leave the irreversible push guarded by the twelve-row confirmation alone. Both open; advice, not authorization.
  - **Roadmap cleaning pass (2026-08-05, second; no ticket closed).** SA117e-3 block deleted; `SA117E3-PUBLIC-ANALYTICS-001` moved inline into SA117e-4. No move (eighth). Critical path `SA117e-4 → SA112b→c→d→e→f → SA96-PUBLISH`; T3 merge-only (REV-001/002 open); T1 truly green at SA122b-1 off-path; T2 start-only.
  - **SA117e-3 COMPLETE — exclusive-capacity `make ci-e2e` accepted (2026-08-05; no source delta).** After one transient PyPI-index timeout (cleanup/restore clean), the bounded retry exited 0: all 12 stages, Core E2E 35, CLI E2E 29, combined 93.01% (all ≥80%), integration mean 94.40%, quarantine empty; host PG restored. `QG-INFRA-001` resolved. T3 → SA117e-4 (blocked on REV-001/002, twelve-row confirmation, post-push acceptance).
  - **SA122b-1 recorded-partial checkpoint (2026-08-05; task unchecked).** Only `Makefile` + `scripts/test_gate_parity.py` changed; 187 tests; direct checker exit 1 (five publish-only omissions, zero Make diagnostics); both `make check` modes exit 0. **Pending:** F-001 (medium) — Make must select ordered non-empty `check-`-prefixed targets and add boundary fixtures; review `STATUS: partial`. Record-and-merge at cap.
  - **`SA117E3-ORDER-001` RESOLVED — option (b) (2026-08-05; documentation only).** SA117e-3 closes on `make ci-e2e` alone; the public-split installed-wheel harness gate plus `SA117E3-PUBLIC-ANALYTICS-001` (public analytics 0.80.0 vs core 0.87.0) transfer to SA117e-4 after the human-gated push — the acceptance order was circular otherwise. Option (a) declined (saved no step; pushed before proof). Nothing weakened; harness re-reviewed if SHA differs; pre-push gates untouched. T3 truly green; decisions → 2.
  - **Roadmap cleaning pass (2026-08-05; no ticket closed).** SA128/SA128d blocks and SA117e-3's consumed grant/phase-5 prose deleted; "recorded for a follow-up" became **SA135** (v88, Tier 2, `deps: SA117e-4`) — owned PostgreSQL lifecycle for the test suites, with the stage-11 banner branch redesigned in the same change, never on a run gating an irreversible publish. No move (seventh); decisions remain three.
  - **SA117e-3 fourth recorded-partial checkpoint (2026-08-05; task unchecked).** Three-file correction committed `ffd97e66` after focused proofs and `STATUS: ok` review, resolving `SA117E3-BLK-001`/`EVID-001`; stage-11 negative control green; harness re-approved at SHA-256 `6757f901…`; installed-wheel split-SHA parity 12/12, host restoration clean. **Pending:** `SA117E3-PUBLIC-ANALYTICS-001` (high, external) — installed `apply` exit 1 (analytics 0.80.0 vs 0.87.0); internal-DB acceptance and Phase 6 `make ci-e2e` not reached. `SA117E3-ORDER-001` must reconcile harness-before-push with `-4`'s push ownership. No push/tag/publication authorized.
  - **SA117e-3 correction candidate recorded (2026-08-05; pending independent review).** Restore assertion derives expected port from `connections["default"].settings_dict["PORT"]` (matches production restore; stage-11 oracle kept); sibling `localhost` expectation unchanged, out of scope. `SA117E3-EVID-001` reconciled without rewriting history: `13bca454…` = derived projection digest; `547b44b8…` = raw freeze-artifact SHA-256, byte-identical to live projection. Stage 11 and Phase 6 unproved.
  - **SA128d and SA128 COMPLETE — structural parity checker accepted (2026-08-05; commit `6fcb8c35`).** 186 parity tests; Ruff/typecheck/quality exit 0; direct checker 1 / Make wrapper 2 with the identical five publish omissions (SA122b-4's declared gap). DC-58 `STATUS: ok` (96), F-001 resolved by full-scope review. T1 → SA122b-1.
  - **SA128d `F-001` root cause diagnosed (2026-08-05; read-only).** Every bound is evaluated after the step that can exceed it, never during — producer-live overflow proof unsatisfiable as shaped. Instances: `_communicate_bounded` samples only on pipe readability; launch+wait budget reads per-file before the sum (up to 2×); worker-frame budget consumes whole files. Corrective plan recorded (time-based sampling, one shared budget, no pre-parsing, live proof); `_bash_observation_file_bytes` kept; `SA128D-EXEC-001` open as implementation-and-review round.
  - **`SA117E3-EXEC-001` GRANTED — Track 3 truly green (2026-08-05; documentation only).** One fresh execution/review budget round authorized (as `user_authorization`); approved three-file correction plan reused verbatim — prior stop was spent `plan_review_cycles=2/2`, not a design objection. Per-attempt, waives no gate: full-scope `STATUS: ok`, stage-11 control, Phase 6 `make ci-e2e` still gate. Sibling `localhost` hardcode (same defect class as BLK-001) deliberately not fixed; ticketed only if a later run redirects the host. T3 truly green, only track on critical path; decisions 4→3.
  - **Roadmap cleaning pass (2026-08-05; no ticket closed).** SA117e-3's three stacked checkpoints collapsed to one (per-phase evidence, SHA recitals, F-003–F-010 history, counter mechanics live only here); SA128d reduced to candidate `d2b3ac24` + declared gap + F-001. Contradiction fixed: continuation requirement now named decision `SA128D-EXEC-001` (open count 3→4). Track state deduplicated to the readiness table; attempts table kept (all rows rejected designs).
  - **SA117e-3 third recorded-partial checkpoint (2026-08-05; task unchecked).** Narrow three-file correction fully planned (port from `settings_dict["PORT"]`, default/nondefault proofs, digest reconciliation without changing history, reviewed patch bytes, stage-11 control + final ci-e2e); plan-review `STATUS: ok` twice (F-001/F-002/`ORCH-GATE-001` resolved) but consumed `plan_review_cycles=2/2`, so the implementation preflight was refused. Maintainer stopped; `SA117E3-EXEC-001` sole continuation. Nothing changed.
  - **SA128d recorded-partial checkpoint (2026-08-05; task unchecked; checker non-authoritative).** Two-file candidate `d2b3ac24` (containment, arbitrary DAGs, controlled failures, bounded lifecycle, adversarial matrix); sync merge `21f35506`; 183 tests + typecheck/quality/format/diff green; parity exact. **Pending:** F-001 (high) — file-backed channels + pre-parsing not one live aggregate-bounded stream; reviews medium/1 → high/1, no `STATUS: ok`. Record-and-merge at cap; SA122b barred.
  - **SA117e-3 second recorded-partial checkpoint (2026-08-05; task unchecked).** Scoped plan-review `STATUS: ok` (PG endpoint design); preflight passed at approved hashes; scratch endpoint `127.0.0.1:32773` ready; one `STOP_EFFECT_OBSERVED`, restore clean. Stage 10 stopped: backups 322 passed/2 skipped/1 failed — restore test hardcodes port 5432 vs real `QS_BACKUPS_DB_PORT=32773`; stage 11/`make ci-e2e` unreached. `SA117E3-BLK-001` (separately approved test-fix handoff) and `EVID-001` (canonical oracle) pending.
  - **Roadmap cleaning pass (2026-08-04, third; no ticket closed).** SA128c block deleted; attempts-table row 3 removed (table preserves rejected designs); closeout merge confirmed `82202674` → `2eae365a`, so SA128a–c closed **and merged**; only `SA128a/F-003` (medium advisory) survives under SA128d. No move; audits unchanged.
  - **SA117e-3 recorded-partial checkpoint (2026-08-04; task unchecked).** Harness/wrapper re-approved at exact SHA-256s `68787fd4…`/`41741154…` (F-003–F-010 resolved); Phase 3 stop-effect-before-bookkeeping proof observed, restore clean; Phase 4 verified twelve source SHAs but observed public analytics 0.80.0 vs 0.87.0 (diagnostic only → SA117e-4). **Pending:** stage-11 PG-not-available control unreachable — `make test-cov REQUIRE_BACKUPS_COVERAGE=1` exits during stage 10 without the host service; decision: alternate endpoint keeping stage 10 green, or control revision/removal.
  - **SA128c COMPLETE — hosted, publish, and E2E structural parity (2026-08-04; pre-integration closeout).** Covers hosted commands/`needs`/stage topology, all 16 publish `run` values (bounded Bash observation), uniform E2E extraction; 155 tests + five regressions green. Pass 1 found F-001/F-002; pass 2 `STATUS: ok`, both resolved. Still reports exactly five publish omissions (direct 1 / Make 2). T1 → SA128d.
  - **SA128a COMPLETE — Make-context parity rebuilt on GNU Make semantics (2026-08-04; commit `ba26cb1b`).** `_extract_makefile_targets` derives inventory via Make's own `-qp`/`--dry-run` (VAR-composed targets, `include`, delegation, `check` aggregation). QG DC-67 `STATUS: ok` (139 + 24 tests); full-scope DC-68 `STATUS: ok`, F-001/F-002 resolved; `SA128a/F-003` (advisory, `_communicate_bounded`) carried by SA128d. T1 → SA128b.
  - **SA128b COMPLETE — shell inventories observed by actual Bash (2026-08-04; commit `7e35556c`).** Controlled recorder PATH/argv with inherited errexit, explicit parallel launch/wait/order/failure proof, residual process-group cleanup, deterministic completion handshake; `/proc` a declared platform constraint. 151 tests; review `STATUS: ok`, SA128b/F-001–F-003 resolved; `SA128a/F-003` stays open under SA128d; one user-authorized continuation consumed. T1 → SA128c.
  - **Roadmap cleaning pass (2026-08-04, second; no ticket closed).** SA128b block deleted; attempts rows 3–4 and duplicate summary removed (table = rejected designs); `SA128a/F-003` sole carried finding. No move; T2 closed by rule; T3 serial umbrella; T1 chain SA128c → d → SA122b-1…5.
  - **Roadmap cleaning pass (2026-08-04; no ticket closed).** SA112a and SA117e-2 blocks deleted; cross-track edges drop 3→2 (SA122b-5 after SA112e; SA115 after SA112) with the SA112a→SA112b edge discharged; SA112b tag now `deps: SA117e-4` alone; `SA112A-RESET-001` dropped from open decisions. No move.
  - **SA117e-3 recorded-partial checkpoint (2026-08-03; task unchecked).** Plan-review `STATUS: ok` for the durable nine-phase continuation; original harness SHA `53762f9d…` verified; Phase 1 froze source tip and host PG identity. Pre-use review `STATUS: partial`: F-003/F-004 (worktree-compatible Git isolation), F-005/F-006 (immutable Docker targeting, frozen preflight), F-007/F-008 (signal-safe reconciliation, bounded deadlines), F-009/F-010 (private FIFO, canonical evidence paths). Nothing ran. Decision `SA117E3-RESET-001` (reset changed-file scope after two empty handbacks, `infra_retries=1/1` spent).
  - **SA112a COMPLETE — installed-wheel provisioner approved at `44938f75`; merged to `v87` as `1cee5823` (2026-08-03).** Four-file slice approved with SHA256s. QG DC-52 `STATUS: ok` (67 tests, two positive smoke runs with six markers/20 probes, missing-manifests negative control, expected parity 2); full-scope DC-53 `STATUS: ok`, F-005 resolved, caller parity passed; F-006 (low advisory, unreadable-directory hardening) open/out of scope. T1 → SA128a; satisfies SA112b's precondition.
  - **SA117e-2 COMPLETE — rebaselined ordered source validation approved (2026-08-03; docs-only closeout).** Five-command chain vs `SA117_BASELINE_REF=1333eb4a` already green (not rerun; no source delta); closeout review per `SA117E2-RESET-001` `STATUS: ok`, closing `SA117E-VAL-002`. Publication blockers remain SA117e-4's. T3 → SA117e-3.
  - **Roadmap cleaning pass (2026-08-03, fourth; no ticket closed).** SA112a per-attempt retrospectives collapsed to one authorization-history line + checkpoint paragraph (narratives live below); roadmap keeps merged twelve-case candidate, open F-005, F-006 out of scope, `SA112A-RESET-001`. No move (fifth). T3 truly green at SA117e-2; T1 blocked on `SA112A-RESET-001` (decision); T2 start-only. Decisions: three.
  - **SA112a recorded-partial — `SA112A-HANDOFF-004` granted and consumed; F-005 unapproved (2026-08-03; task unchecked).** DC-48 (two scripts): empty-stack guards + twelve cancellation cases; implementation-agent evidence only. F-005 (medium) unapproved — no independent QG/`STATUS: ok`; QG refused even with authorization (`qg_runs=3/2`, `review_cycles=2/2`, `authorized_retries=1/1`). Decision `SA112A-RESET-001`: supported reset clearing all three counters, then full QG + review; auth-only retries can't buy either dispatch.
  - **SA112a recorded-partial — `SA112A-HANDOFF-003` consumed, F-005 unapproved (2026-08-03; task unchecked).** DC-41 candidate (six added cases); review `STATUS: partial`; authorized QG (DC-42) returned empty — `record_failed_handback` marked infrastructure failure, `qg_runs` stayed 3/2, fresh retry refused. F-005 (medium) open: repeated/root cancellation, `link/../sibling` identity, native trap parity for `missing/../link` missing; F-006 advisory. Needs fresh authorization restoring both one QG and one review (`SA112A-HANDOFF-004`).
  - **`SA117E2-RESET-001` GRANTED — Track 3 truly green (2026-08-03; documentation only).** Control plane reset (cleared `review_cycles=3/2`, `authorized_retries=1/1`); one fresh full-scope closeout review over the two-document scope; already-green validation not re-run; `SA117E-VAL-002` closes only on `STATUS: ok`. Both critical-path legs live; decisions → 2 (SA117e-4's).
  - **Roadmap cleaning pass (2026-08-03, third; no ticket closed).** SA117e-2 narrative reduced to its blocking condition; arch Finding 12 reconciliation line → dated pointer. No move (fourth). T1 truly green at SA112a; T3 blocked on closeout review budget (`review_cycles=3/2` — a maintainer decision); T2 start-only. Decisions: three.
  - **SA117e-2 second recorded-partial (2026-08-03; task unchecked).** Five-command chain ran in exact order, each exit 0: lock-diff clean, capture 106 paths (digest `d41e698c…`), `VERIFY PASSED`, `make check` 2,620 core/2,043 CLI, `make quality` zero regressions. **Pending:** `SA117E-VAL-002` (high) — closeout review can't dispatch (`review_cycles=3/2`; refused even the user's continuation). Unchecked; SA117e-3 host work forbidden; need fresh budget or reset, never a ledger bypass.
  - **SA117e-2 recorded-partial (2026-08-03; task unchecked).** Command 1 `make sa117-lock-diff` exit 2: unauthorized lock drift, packages 91→99 vs baseline `43d9b8fc`; commands 2–5 not run. `SA117E-VAL-002` (high) open; maintainer approved `1333eb4a` (the intended dependency-upgrade lock commit) as replacement immutable baseline + full rerun without source changes — not a pass, waiver, or completion.
  - **`SA112A-HANDOFF-003` GRANTED — cap counters reset, Track 1 truly green (2026-08-03; documentation only).** One fresh implement/QG/review round over the four-file scope with counters reset; plan reused verbatim; per-attempt, waives no gate. Pre-dispatch: F-005 confirmed exploitable (`missing/../link` writes through the symlink target); the obvious fix is wrong — `mkdir -p` resolves `..` via the target, so keep the canonical `%/*` trim and recompute only the probe flag, adding `link/../sibling` as regression guard. Decisions → 2.
  - **Roadmap cleaning pass (2026-08-03, second; no ticket closed).** SA117e-1 block deleted; roadmap keeps only REV-001/-002 (SA117e-4), -004 (SA124) residue; -003/`CR-SA117E-005`/-006 noted resolved. No move (third). T3 only truly green; T1 blocked on `SA112A-HANDOFF-003`; T2 start-only. Decisions: three.
  - **SA112a second recorded partial delivery (2026-08-03; task unchecked).** Helper/test delta rejects regular-file, broken-symlink, dir/file-symlink, FIFO, socket, trailing-slash, first-dot `OUTPUT_DIR` aliases before trap arming (caller data, empty failure stdout, signal/trap behavior, smoke parity preserved); two QG chains green (38/49 tests, smoke markers, negative control, five-omission parity). Review resolved F-003. **Pending:** F-005 (medium) — probe state not restored after `..` cancels a nonexistent tail (`missing/../link` cases); F-006 advisory. `SA112A-HANDOFF-002` consumed; reviewed-tip gap recorded, not executable approval.
  - **SA117e-1 COMPLETE — full-scope documentation closeout `STATUS: ok` (2026-08-03; documentation only).** DC-14 resolved `CR-SA117E-006` and `SA117E1-REV-003` (full-value `EVIDENCE=` substitution: entire value after `EVIDENCE=` replaced by command 2's exact printed path). REV-001/-002 remain SA117e-4's; plan F-002 → SA117e-3. Checked; no source delta. T3 → SA117e-2.
  - **`SA112A-HANDOFF-002` GRANTED — Track 1 truly green (2026-08-03; documentation only).** One fresh implement/QG/review round over the unchanged four-file scope; prior stop was a consumed ledger (`qg_runs=2/2`, `review_cycles=2/2`), not a design objection; plan already carries `STATUS: ok`, reused verbatim. Per-attempt, waives no gate. Scope: fix F-005 (reject disallowed `OUTPUT_DIR` pre-trap, status 2, caller-data preservation), rerun the literal validation chain, one review closing F-003 + F-005. Decisions → 2.
  - **Roadmap cleaning pass — SA112a's blocker reclassified as a maintainer decision (2026-08-03; documentation only).** SA112a partial narrative, SA117a/b/c closure detail, SA122a registry delivery, SA132 decisions, resolved auth ledger (`SA117E-VAL-001`, `SA112A-AUTH-006`, HANDOFF-001, TRACK-003, Option A) condensed to changelog pointers. Correction: F-003/F-005 need a fresh dispatch authorization (control plane refused at `qg_runs=2/2`, `review_cycles=2/2`) — decision-blocked, not upstream. No move; T3 (SA117e-1) only green; audits unchanged.
  - **SA112a recorded partial delivery (2026-08-01; task unchecked).** Four-file installed-wheel provisioner candidate; two full service-free QG passes green (syntax, markers, 20 probes, negative control + rerun, lint, typecheck, diff, parity 2). Review closed F-001/F-002; post-cap early-output-ownership correction passed 9 targeted + 27 focused tests (8/9 fail on revert). **Pending:** F-003 (medium) lacks fresh independent approval; final review also found F-005 (medium) — disallowed `OUTPUT_DIR` shapes must be rejected pre-trap with status 2. Maintainer instructed commit+merge; preservation only.
  - **`SA112A-HANDOFF-001` AUTHORIZED; Track 1 reordering resolved as Option A (2026-08-01; documentation only).** One fresh implement attempt granted over the exact four-file scope; prior refusal was a persisted `blocked_retries=1/1` ledger, not a design objection; four-phase continuation plan reused verbatim; per-attempt, waives no gate. Option A: queue unchanged (SA112a → SA128a → … → SA122b-5); SA128a not promoted (its consumer is gated behind SA128d; queueing it delays nothing). T1 truly green; decisions 4→2 (both SA117e-4's).
  - **Roadmap cleaning pass (2026-08-01, second; no ticket closed).** SA117e-1 and SA112a checkpoint prose reduced to actionable residue; F-003 closed by adding `scripts/test_installed_wheel_venv.py` to SA112a's Files line and topology allowlist (matches the approved four-file scope). T3 only truly green; T1 blocked on `SA112A-HANDOFF-001`; T2 start-only. No move; decisions surfaced.
  - **SA117e-1 recorded-partial checkpoint (2026-08-01; task unchecked).** DC-9 full-scope historical review `STATUS: partial`; `CR-SA117E-005` resolved, `-006` open. Remapped `SA117E1-REV-*` ledger: REV-001 (high, security — bare `--force-with-lease` can't prove absence, `git_utils.py:636-680`, SA117e-4), REV-002 (medium — publication authorization not bound/consumed, `verify_sa117_publication.py`, SA117e-4), REV-003 (low — handoff exactness), REV-004 (low — deferred scope inventory, SA124). No push authorized; twelve-row confirmation still required at `-4`; SA117e-2 must not start.
  - **SA112a implementation-handoff checkpoint (2026-08-01; task unchecked).** Plan approved (`STATUS: ok` pass 2) fixing the `quickscale_provision_installed_venv REPO_ROOT OUTPUT_DIR` seam, six stderr markers, four allocation classes, 20-probe parity, terminal lines; four-phase continuation durable on the roadmap. **Pending:** `SA112A-HANDOFF-001` (medium, orchestration — dispatch refused, `blocked_retries=1/1` spent) and F-003 (medium, consistency — test file missing from Files line/allowlist). No executable work.
  - **SA117e plan-review-cap checkpoint (2026-08-01; documentation only).** Monolithic continuation rebound to baseline `43d9b8fc`; ledger high+medium/2 → medium/1. F-001 (plan-detail, SA117e-4): exact SHA/`ABSENT` pre-state frozen in the twelve-row Decision Brief; rebrief/re-authorize, never substitute a lease. F-002 (medium, resilience, SA117e-3): restoration armed before host PG stop + interruption-after-stop-effect proof. CR-005/-006 advisory on `-1`; `-1` next.
  - **SA117e split from Tier 3 into five Tier 1–2 children (2026-08-01; documentation only).** Monolith fused five risk classes (historical review, local validation, exclusive-capacity infra validation, irreversible public mutation, closeout). Children: `-1` (T2, full-scope historical review, closes CR-005/-006), `-2` (T2, rebaselined ordered source validation, no source delta), `-3` (T2, harness rematerialization + `make ci-e2e` in exclusive Docker/PG with host-PG negative control; re-reviewed if SHA ≠ `53762f9d…`), `-4` (T2, human-gated: local tag, twelve-row confirmation, twelve protected `splits/*` pushes, post-push parity + installed all-module `apply` — one unit), `-5` (T1, closeout). SA112b's precondition = `-4`'s pushed splits; `-5` leaves the critical path.
  - **Two authorization decisions resolved — both critical-path tracks unblocked (2026-08-01; documentation only).** `SA117E-VAL-001` RESOLVED: `43d9b8fc` approved as lock-diff baseline, replacing internally inconsistent `97404f3e` (0.87.0 with 60 pre-lockstep values); checker semantics not weakened; resumes at command 4. `SA112A-AUTH-006` GRANTED: review budget only — implementation still gated on plan `STATUS: ok`. Both tracks can start, in parallel; remaining gates are execution-time confirmations; Track 2 unchanged.
  - **Roadmap cleaning pass (2026-08-01; no ticket closed).** SA117e/SA112a recitals condensed to blocker + remainder; no move (T2 closed by three-worktree rule). Corrected "no decision open": `SA117E-VAL-001` + `SA112A-AUTH-006` gate can-start on the critical path. No track truly green.
  - **SA112a recorded partial delivery — scoped plan produced, review still `partial` (2026-07-31; task unchecked).** `SA112A-PLAN-002`/`-004`/`-005` resolved. **Pending:** `SA112A-PLAN-003` (medium) — parse exact complete marker lines (not substrings), assert six-marker sequence, verify all four directory classes across cleanup/signal paths. Review partial after bounded retry + authorized post-cap cycle (medium/4→2→1); implementation forbidden; needs `SA112A-AUTH-006`.
  - **SA117e recorded partial delivery — correction reviewed, lock-diff baseline blocking (2026-07-31; functional correction `b11c24c5`).** Plan + plan review `STATUS: ok`; correction `b11c24c5` reviewed ok (slice only); harness `53762f9d…` reviewed ok, never executed; validation 32 + 929 tests, version-check, manifest-sync 12/12; command 4 exit 2 — baseline `97404f3e` internally inconsistent. **`SA117E-VAL-001`** (high, blocking): reapprove baseline (recommend `43d9b8fc`) without weakening checker semantics; capture/verify/check/quality/ci-e2e not run (stop-on-first-failure). Fresh human split-push confirmation remains a separate gate. No local tag, split push, or public mutation.
  - **Roadmap cleaning pass (2026-07-31, second; no ticket closed).** SA135/SA133 sections deleted; every dependent reference reconciled (diagram, critical path, tags, green-gate paragraph, topology). TA62 retired; arch orientation → SA117e head + SA112a parallelization. No moves; T2 closed; conflict surface unchanged.
  - **SA133 COMPLETE — three Core E2E blockers fixed; Track 3 advances to SA117e (2026-07-31; commit `167ddd23`, synced `026246a5`).** Acceptance ran in the T3 worktree: `make ci-e2e` exit 0 (Core E2E 35, CLI E2E 29, all 12 stages, unit 4,717, backups 323, combined 93.01% all ≥80%, quarantine empty), `make check QUIET=1` 0, `make quality` 0. `SA133-BLK-01/02/03` + `QG-SA90-BASELINE` resolved; final review `STATUS: ok`. Carried advisories CR-SA133-001/-002 (low). Green-gate four-command join unproved (`make ci` not rerun). SA117e next.
  - **SA135 COMPLETE — three SA90 Dockerfile emission hashes refreshed (2026-07-31; commit `6a779dbb`).** Fixture delta exactly three `Dockerfile` sha256 records (`676edaa0…` → `beda7d0…`; 0 removed/added, provenance preserved); deterministic regeneration exit 0. 6 + 11 focused tests; `make check QUIET=1`/`make quality` 0; review `STATUS: ok`. `QG-SA90-BASELINE` cleared; SA133 resumes as T3 head.
  - **SA133 recorded partial delivery — delta reviewed ok; acceptance blocked by new SA135 (2026-07-31; commit `167ddd23`; task unchecked).** Fixes full-scope reviewed `STATUS: ok`; focused E2E 0 (4/27). Broad red: `make ci-e2e` 2 and `make check QUIET=1` 2 on pre-existing SA90 fixture drift from SA130's `196e6770` (reproduced with delta removed). New ticket **SA135** (T2, deps none, T3 head) owns the one-file/three-hash refresh; `QG-SA90-BASELINE` (medium, blocking) open; `CR-SA133-001` low advisory. SA133 unchecked until SA135 merges + ordered rerun + final review.
  - **SA132 COMPLETE and four decisions resolved (2026-07-31; documentation only).** `SA132-SCOPE-DEC` (b): all three `SA117E-QG-001`–`003` fixed and merged (`eac4d82e`); the `make ci-e2e` verify clause retired — remaining failures sit outside SA132's allowlist and no-scope-widening bars touching them; (a)/(c) declined. New **SA133** (T2, T3 head) owns `SA133-BLK-01/02/03`; BLK-02 strategy (a): rewrite the copied module's core dep to a path/`develop` dependency during E2E (suspect `backups/pyproject.toml:19`). `SA112A-TRACK-003` applied: SA112a → T1 ahead of SA128a. Zero open decisions.
  - **Roadmap cleaning pass (2026-07-31, second; no ticket closed).** SA130 block removed; SA132 Done compressed, live ledgers kept verbatim (440 → 417). Audits unchanged; two stale line-anchors repointed. Rebalancing: `SA112A-TRACK-003` surfaced, not applied — would delay an otherwise-mergeable Track 1 ticket; recommendation to take it unless SA132's three questions answered immediately (later taken).
  - **SA132 recorded partial delivery (2026-07-31; commit `eac4d82e`; task unchecked).** `make check QUIET=1` passes; Core E2E imports `publish_module` (35 tests); focused CLI update E2E passes; 86/4,660/29 + all pre-E2E subgates + quality + test-cov 93.01% green; two functional reviews ok for partial merge. **Pending:** `make ci-e2e` exit 1 — Core E2E 31/4 in `test_e2e_full_workflow.py`: BLK-01 (stale DRF :312), BLK-02 (lock can't resolve unpublished `quickscale-core >=0.87.0,<0.88.0` :420), BLK-03 (stale 3.14 rejections :1009); independent of `eac4d82e`. Deferred advisory: test-cov QUIET unfiltered. Unchecked.
  - **SA130 complete — Dockerfile Poetry timeout variable corrected (2026-07-31; commit `196e6770`).** `POETRY_REQUESTS_TIMEOUT=120` replaces the no-op `POETRY_HTTP_TIMEOUT` (one identifier; everything else byte-unchanged). Rendering + actual service-free emission assertions (11 passed); `make quality` 0; review `STATUS: ok`; CR-001/CR-002 resolved. Docker/network criterion waived by maintainer; the service-free emission proof is accepted. T1 → SA128a.
  - **SA129 complete — superuser sentinel behavior (2026-07-31; commit `c7e53f9d`; docs-only closeout).** `_superuser_exists_in_backend` emits/reverse-scans `QUICKSCALE_SUPERUSER=0|1` on an explicit sentinel line (Django 5.2+ banner-safe; fail-closed handling preserved). `SA129-TEST-001` resolved (literal expectations, byte-exact stdout/stderr, presence/absence across branches; 54 tests, quality 0); `SA129-DOC-002` resolved (topology re-authored). `SA129-E2E-002` low/advisory, waived-not-passed (T3 held shared infra). T1 → SA130.
  - **SA129 second recorded partial delivery (2026-07-31; commit `c7e53f9d`; task unchecked).** `SA129-TEST-001` resolved; all 54 tests pass, quality 0, review reconfirmed. **Pending:** `SA129-DOC-002` (medium) — completion candidate duplicated the `SA128a → b → c → d` node; discarded back to the synced baseline; re-author + `STATUS: ok` needed. `SA129-E2E-002` waived-not-passed. Unchecked.
  - **`SA117E-QG-DEC-001` RESOLVED — AUTHORIZED; remediation slice ticketed as SA132 (2026-07-31; documentation only).** **SA132** (T2, T3, deps none, T3 head) = fix exactly three findings: `SA117E-QG-001` (high — `make check QUIET=1` hangs in the xdist recipe), `QG-002` (high — Core E2E can't import `publish_module`), `QG-003` (high — CLI update E2E exit 1; triage test-defect vs real regression before fixing). No widening (new findings ticketed, never absorbed); no tag/split push/public mutation; separate reviewed handoff with own plan/allowlist/review/merge-back. Critical path: `SA132 → SA117e → SA112a…f → SA96-PUBLISH`; decisions → 1.
  - **Roadmap cleaning pass (2026-07-30, fourth; no ticket closed).** Done recitals removed for SA117e/SA128/SA129; pending/blocking ledgers kept verbatim; `TA1`–`TA59` recital → range line. No move (critical path on T3; T2 closed worktree; T1 in-process SA128). `SA112A-TRACK-002` carried: decline while SA129/SA130 head. Truly green: SA129/SA130/SA128a (off-path); nothing green on the critical path.
  - **SA117e validation-blocked checkpoint (2026-07-30; documentation only).** Resumed at `2466f738`; plan bound 50 commits/155 paths/twelve splits; two QG runs green except `SA117E-QG-001`/`-002`/`-003` (all high) at the two-run cap — quiet-check xdist hang, Core E2E import failure, CLI update E2E exit 1. No source change, tag, split push, publication, or installed apply claimed; separately authorized remediation slice required first.
  - **SA129 recorded partial delivery (2026-07-30; task open).** Sentinel emitted/reverse-scanned; 20 focused + 54 module tests, quality 0. **Pending:** `SA129-TEST-001` (medium) — literal expectation + byte-exact stdout/stderr + presence/absence on four branches; `SA129-E2E-002` (medium) user-waived, not passed (T3 held infra). Unchecked.
  - **`SA128-SIZE-001` RESOLVED — SA128 split by observation domain into SA128a–SA128d (2026-07-30, option (a); documentation only).** Umbrella + four Tier 2 children with disjoint slices of the stable nine-finding ledger: a (Make expansion/dry-run/delegation/`check` aggregation), b (Bash with recorder `make`), c (hosted+publish+E2E YAML + literal 16-publish/10-hosted `run` oracle), d (cross-cutting contracts + adversarial/caller matrix + closeout). No finding orphaned; the frozen 9→9 was the mechanism — one review can't find five domains proved at once (same remedy as SA117/SA122). (b)/(c) declined. SA122b `deps` → SA128d; T1 truly green off-path; `SA112A-TRACK-002` sole decision.
  - **SA128 retrospective block added; tier re-costed Tier 2 → Tier 3 (2026-07-30; documentation only).** Checkpoints recorded state, not diagnosis; ledger 9→9 across two independent attempts, no surviving candidate. Added attempts table (attempt 2's cause **UNRECORDED — needs maintainer recall**) + do-not-repeat list: green mechanical gates ≠ acceptance (132 tests + MyPy + Ruff + exit parity still rejected at full scope). Re-costed: nine high findings across five domains. Decisions: `SA128-SIZE-001`, `SA112A-TRACK-002`.
  - **Roadmap cleaning pass (2026-07-30, third; no ticket closed).** First SA128 checkpoint block removed. Applied **`SA131-ORDER-001`**: T1 order → SA129 → SA130 → SA128 → SA122b (independence verified: all deps none, allowlists disjoint, service-free, no split change; delays nothing — SA128 has no delta, consumer merge-gated behind SA112e). T1 head can-finish NO→YES; every reference re-pointed.
  - **SA128 second review-cap blocked checkpoint (2026-07-30; task unchecked).** Two-phase plan ok; candidate passed 132 tests/MyPy/Ruff/exact parity/typecheck/quality/diff — still rejected (partial ×2, 9→9 high). Restored byte-identically to `10ff5c89`; no delta; checker non-authoritative. All nine findings high/blocking. Continuation from ratified `SA128-DEC-001`–`-003` constraints; roadmap acceptance corrected to direct-1/Make-2 (SA122b fixes inventories, not exceptions).
  - **SA117c complete — exhaustive Python-binding lock-drift validation closes SA117C-CR-012 (2026-07-30; commit `46a2f186`).** Exhaustive inventory over `ast.Import`/`ImportFrom`/`TypeVar`/`ParamSpec`/`TypeVarTuple` as possible `__version__` rebindings, rejected with exit 2/no evidence; valid non-binding references tolerated. 50 + 93 tests, Ruff, version-check 12/12, quality 0, diff green; review `STATUS: ok` (97). Docs review found DOC-CR-001/-002, corrected in this entry (SA117e sole remaining child, owns published-parity proof). T3 → SA117e.
  - **Roadmap cleaning pass (2026-07-30; no ticket closed).** SA117c block removed (retains forward-binding residue: local lock-comparison gate feeds SA117e's post-push verification). Path shortens to `SA117e → SA112a…f → SA96-PUBLISH`. No move (T2 closed worktree; T1 in-process SA128). `SA112A-TRACK-002` re-costed: decline while SA128 live. Truly green: T3 only.
  - **SA128 review-cap blocked checkpoint (2026-07-29; task unchecked).** Boundary ratified: remove Bubblewrap, no text/regex fallback, delegate to GNU Make/Bash, keep six inert adversarial forms, one path contract, direct-1/Make-2 preserved, standalone until SA122b. Two rounds + post-cap re-plan all `STATUS: partial`; candidates restored byte-identically. Nine findings high/blocking; continuation needs a literal 16-publish/10-hosted `run` oracle + full-scope `STATUS: ok` before SA128/SA122b.
  - **Roadmap cleaning pass (2026-07-29, third; no ticket closed).** SA127 block (61 lines) removed; `SA122A-AUTH-001` rationale compressed to its forward-binding rule; arch F12 line → one resolved line (608 → 540; `START_HERE` refreshed, was stale). No move (T3 serial; T2 closed; T1 in-flight SA128). `SA112A-TRACK-002` decline. Truly green: T1 (off-path), T3 (critical path).
  - **Dependency upgrade to latest stable across both surfaces (2026-07-29).** Python floor 3.13 → **3.14** (`>=3.14,<3.15`; **3.13 no longer supported**); Django `>=6.0.7` with 12 module pins in lockstep (intentional drift retired); generated deps: gunicorn 26, mypy 2.3, ruff 0.16, whitenoise 6.12, pytest 9.1.1, pytest-django 4.12, django-stubs 6.0.7, pre-commit 4.6.1, Poetry 2.4.1 (virtualenv held `>=20.29,<21.0`); repo dev deps bumped; 12 stale `module.yml` dependency floors realigned and re-synced; frontend pnpm 11.18.0, eslint 10, jsdom 30. Validated on 3.14.6: 4,697 core/CLI passed, mypy clean over 92 files, theme build + 75 tests, SA90 manifests re-hashed (9 changed files).
  - **SA117c recorded partial delivery — lock-structure comparison preserved, exhaustive binding proof open (2026-07-29; task unchecked, release-blocking).** `make sa117-lock-diff` added: 55 required files, 66 canonical values, twelve allowed module-lock leaves, all else drift, malformed = controlled error; evidence deterministic/atomic/digest-bound. 33 + 76 tests, parity 55/66. Resolved `SA117-CR-006`, `SA117C-CR-007`–`-011`, `-013`. **Pending:** `SA117C-CR-012` (high) — dotted/star imports and 3.13 type-parameter bindings not exhaustively recognized (66-value oracle can false-green); `ACR-META-001` low. Record-and-merge; SA117e blocked.
  - **SA127 complete — zero-module installed `apply` fixed (2026-07-29; commit `a2eb7a6c`).** Empty `selected_modules` short-circuits managed wiring (skips base-path prep/adapters, writes empty wiring); non-empty path byte-unchanged; two files. 36/36 tests; quality/format/lint/typecheck 0; external-venv zero-module plan/apply completed; review `STATUS: ok`; REV-001 low (handoff metadata). T1 continues SA128.
  - **`SA122A-AUTH-001` RESOLVED — SA122a closed as superseded; parity checker rebuild ticketed as SA128 (2026-07-29; documentation only).** Option (b): replace the checker, keep the registry. Kept from `73c153b2`: `gate_registry.json` (7 entries; publish required for all five gates), `make check-gate-parity`, docs. Rejected: regex extractors (hand-reimplemented Make grammar, `\bmake\s+` scrapers) — false-green in both directions. (a)/(c) declined. **SA128** (T2, T1, deps none): inventories derived from source semantics; unparseable input = hard error per decisions.md §fail-hard-principle. T1: SA127 → SA128 → SA122b; `SA112A-TRACK-002` sole decision.
  - **Roadmap cleaning pass (2026-07-29, second; no ticket closed).** SA117b/SA117a blocks removed (two-line pointer carries `SA117-CR-006` → SA117c); SA122a recital compressed (507 → 489). Audits unchanged. Applied **`SA127-ORDER-001`**: T1 → SA127 → SA122a → SA122b (independence re-verified; SA122a's merged partial is itself blocked, so the release blocker goes first). `SA112A-TRACK-002` carried.
  - **SA122a recorded partial delivery (2026-07-29; commit `73c153b2`; task open).** Seven-entry registry, standalone checker/Make target, docs, focused tests; exactly five expected publish omissions; 112 tests; gates green. **Pending:** `SA122A-CR-003` (high, security — false-green-capable parsing), `-002` (high, correctness — incomplete enforcement), `QG-001` (medium — adversarial proof + two MyPy errors), `-004` low advisory. Review partial; fresh authorization needed; merge is preservation only.
  - **SA117b complete — split-publication safety and resilience (2026-07-29; not published).** Commit `efd86d21` green and approved: blank origins fail closed; verifier-owned PG cleanup across signals/timeouts; trusted Git bootstrap + filename-safe staged-index checks; sync non-continuation spy. 656 tests + Ruff/format/typecheck/quality/diff/Docker cleanup; roadmap review `STATUS: ok` resolved `SA117B-REVB-003` (one consistent chain). No blocker/decision; SA117c next; splits/tag/PyPI/SA112 open.
  - **arch-audit Finding 12 CLOSED — `quality-baseline-monotonicity-unenforced` (2026-07-29).** `check_quality_baseline_monotonicity.py` diffs baseline keys/ceilings vs the merge base, rejects positive delta/new exemption, and runs as a blocking gate (`check_quality.sh:117-138`); only escape = structured waiver in `quality_waivers.json` (present, empty). Arch-audit Option 1 — measurement separated from permission. `max_lines` half dissolved with SA125. Census row → gated; table → four findings; Arch Finding 11 now the only open ticketed finding.
  - **Roadmap cleaning pass (2026-07-29; no ticket closed).** SA121 block (~25 lines) and superseded SA117b historical checkpoint removed; SA121-DEC-001/-002 dropped from Standing decisions; `SA117B-REVB-003` substantively corrected (diagram states one `SA117b → c → e → SA112a` chain) but its review outstanding — SA117b unchecked until a fresh full-scope documentation `STATUS: ok`. No speedup; `SA127-ORDER-001` surfaced.

  - **SA117b recorded partial delivery — executable safety complete, documentation closeout open (2026-07-29; commit `efd86d21`; not published).** Blank-origin fail-close, scoped verifier-owned Compose cleanup (SIGINT/SIGTERM/timeout), publication-only trusted Git/NUL controls, five-consumer parity, sync spy; 656 tests + all gates + live Docker cleanup; executable Review A `STATUS: ok`. **Pending:** `SA117B-REVB-003` (medium) — roadmap diagram duplicates SA117e; fresh full-scope docs review needed. Exact reproducible validation command preserved for `SA117-META-001` below:

    ```bash
    PYTHONPATH=. poetry run pytest scripts/test_verify_public_module_apply.py quickscale_core/tests/test_git_utils.py quickscale_cli/tests/commands/test_module_commands.py quickscale_cli/tests/commands/test_development_commands_extended.py quickscale_cli/tests/test_apply_command_extended.py quickscale_core/tests/test_apply_embedding_steps.py -x --tb=short && poetry run ruff check scripts/verify_public_module_apply.py scripts/test_verify_public_module_apply.py scripts/publish_module.py quickscale_core/src/quickscale_core/utils/git_utils.py quickscale_core/tests/test_git_utils.py quickscale_cli/src/quickscale_cli/commands/development_commands.py quickscale_cli/tests/commands/test_development_commands_extended.py quickscale_cli/tests/test_apply_command_extended.py quickscale_cli/tests/commands/test_module_commands.py quickscale_core/tests/test_apply_embedding_steps.py && poetry run ruff format --check scripts/verify_public_module_apply.py scripts/test_verify_public_module_apply.py scripts/publish_module.py quickscale_core/src/quickscale_core/utils/git_utils.py quickscale_core/tests/test_git_utils.py quickscale_cli/src/quickscale_cli/commands/development_commands.py quickscale_cli/tests/commands/test_development_commands_extended.py quickscale_cli/tests/test_apply_command_extended.py quickscale_cli/tests/commands/test_module_commands.py quickscale_core/tests/test_apply_embedding_steps.py && make typecheck && make quality && git diff --check
    ```

    Live Docker evidence: SIGINT returned 130; SIGTERM returned 143 with forced-cleanup failure; the timeout failure was captured; the exact owned container, volume, and network were removed; unrelated controls survived; no residuals remained.
  - **SA121 closeout — quality-baseline monotonicity gate complete (2026-07-28).** `SA121-DOC-CR-002` resolved by full-scope documentation review `STATUS: ok`; topology corrected to the explicit two-edge statement (SA122b→SA112e, SA115→SA112). Delivery `9448a0f7` already reviewed green (153 tests/6.68s, 0 violations, quality 0). CR-003/CR-006 resolved; CR-007 low advisory (historical 156/156 prose, preserved). Checked; SA122a follows; green-gate milestone unclaimed.
  - **Roadmap cleaning pass (2026-07-28; no ticket closed).** SA117a closure + retired `SA117-QG-REMED` block removed (forward-binding rules kept: no CC criterion in SA117 verify lists, no line-reduction refactor on the three named files). Overview redrawn (SA122a duplicate gone; two-edge statement — the substance of DOC-CR-002, still awaiting review). Branch state re-measured; audits unchanged.
  - **SA117b plan-review-cap checkpoint (2026-07-28; task open).** Seams mapped; `SA117B-PLAN-001`–`-004` resolved (exclusive verifier-owned Compose identity with signal/timeout cleanup, publication-only Git/NUL controls, five-consumer parity, exact reviewed base/tip merge identity). **Pending:** `SA117B-PLAN-005` (medium) — Ruff check/format + typecheck gates before review, after resync, post-merge. `SA117-CR-003`, `CONT-PR-003`, `CONT-PR-006`, absorbed `SA117A-CR-002` unimplemented. Partial at cap; SA117c blocked.
  - **SA121 review-cap checkpoint (2026-07-28; task open).** `9448a0f7` (strict-key parser correction); QG2: 153 tests/6.68s, 0 violations, quality 0, baseline unchanged; functional review ok; CR-003/CR-006 resolved. **Pending:** `SA121-DOC-CR-002` (medium) — topology duplicates SA122a, denies the two merge bounds; CR-007 low. Partial; fresh docs review before close.
  - **SA117a third plan-cap checkpoint (2026-07-28; task open).** Normal implementation, validation, staging, commit, rollback-ref, abort, reset, disarm paths literalized; `SA117A-PLAN-014` resolved; proposed CR-002 spy target correct. **Pending:** PLAN-010 (high — accepted post-sync tip bound to Review B), PLAN-013 (medium — copyable shared-document conflict sequence), CR-002 (medium). Partial at cap.
  - **`SA117-DEC-005` — SA117a closed, residue folded into SA117b (2026-07-28; documentation only).** Load-bearing content verified present (canonical lockstep parser in `manifest/loader.py`; twelve source + twelve bundled versions at 0.87.0) — the entirety of what SA112b's evidence validity depends on; remaining scope was one `assert_not_called()` spy (`SA117A-CR-002`) that consumed one change-review + three plan-review caps + a doc review with zero lines. Actions: SA117a closed (chain SA117b → c → e); the spy absorbed into SA117b with its literal diff pre-written (third spy after `spy_commit`; assert after `spy_sync_state`); PLAN-010/-013 retired as generic merge ceremony. SA117b next, deps none.
  - **Roadmap cleaning pass (2026-07-28, fifth; no ticket closed).** SA126 block removed; refs re-pointed. **Verified:** `make quality` rerun direct, exit 0, 0 regressions; radon `_perform_module_embed` 18 (≤20), `_update_single_module` 13 (≤17); quarantine empty — clears `SA121-QG-001`, **Track 1 truly green for the first time**. SA117a took a fourth review cap with zero source delta — process warning: a fifth cap means the plan-gate process is the constraint, not engineering. `SA112A-TRACK-001` surfaced (decline while SA121 green — it closes arch Finding 12).
  - **SA126 complete — module-command complexity remediation (2026-07-28).** Commit `60c5d4f3`: `_perform_module_embed` 22→18 (ceiling 20), `_update_single_module` 18→13 (ceiling 17). 102 tests; quality/lint/typecheck/diff 0; no tests or ceilings changed; review `STATUS: ok`, exact behavior/caller parity. Checked; SA121 next.
  - **SA117a second plan-cap checkpoint (2026-07-28; task open).** Sync-transaction design gap resolved (`SA117A-CONT-PR-008`/`PLAN-011`); eight-case cleanup parity matrix specified (`PLAN-012`). **Pending:** PLAN-010 (high — staging incomplete), PLAN-013 (medium — non-fail-fast commands), PLAN-014 (medium — no armed rollback), CR-002 + CC 22 untouched. Partial at cap.
  - **Ticket split for parallelism — SA126 created, `SA112A-TRACK-001` resolved (2026-07-28; documentation only).** No open ticket is Tier 3. TRACK-001: repo holds at three worktrees; no Track 4 (a second review lane isn't worth one critical-path node while SA117a converges); parallelism via splitting. **`SA126-DEC-001`**: new T1 ticket holding the two `make quality` blockers (`_perform_module_embed` CC 22, `_update_single_module` CC 18) — split on empirical grounds (SA117a: three caps, zero delta); both in `module_commands.py`, one owner removes a conflict surface. T1 order SA126 → SA121 → SA122a; can-finish YES; green-gate no longer behind T3. Two tracks green and parallel; no open decision.
  - **Roadmap rebalancing pass (2026-07-28, fourth; no ticket closed).** No closeout owed; audits unchanged. **`SA112A-TRACK-001` surfaced as a decision:** SA112a is the only open ticket simultaneously on the critical path, dependency-satisfied, file-disjoint from the SA117a–c allowlist, and service-free — but no track can host it (T3 one-child, T1 holds runnable SA121, T2 closed), so it costs a fourth worktree + second review lane; **not applied**, recorded for the maintainer. Corrects the prior pass's self-inconsistency. Tracks as three verdicts: T3 only truly green (critical path); T1 can start/merge, not finish; T2 start-only.
  - **Roadmap verification pass (2026-07-28, third; documentation only).** No closeout owed, no audit finding closeable, no track gap, no move (critical path entirely T3 and causally serial; T2 closed; T1's tickets off-path). Verified against code: radon re-measured CC 22>20 and 18>17 live; `SA125-DEC-001` confirmed implemented (zero `large_files` entries, report-only retention); SA121's merged delivery present and inert (waivers empty). Dated re-verification note added.
  - **Roadmap closeout/rebalancing pass (2026-07-28, second; documentation only).** SA125 block removed (455 → 425); every reference re-pointed, stale SA121 anchor corrected to `818-831`. Audits: tech-audit zero open; arch Finding 12 **narrowed, not closed** (anchor corrected to the surviving `max_complexity` comparison; two census increases no longer gated). Tracks: T1 SA121 → SA122a; T2 SA115 heavy validation authorized (step 3), merge queued; T3 fresh scoped SA117a continuation. `make quality` red at v87 HEAD → green-gate unclaimed.
  - **SA125 — file line-ceiling quality gate retired (2026-07-28; complete).** Commit `05d0f2b8`: blocking `large_files` enforcement dropped; monotonicity helper/tests narrowed to surviving sections; large-file diagnostics retained advisory-only. 149 tests (subset 4); bash -n, lint, typecheck, diff 0; `make quality` exit 2 only on the two pre-existing CC findings (22>20, 18>17) with zero line-count blockers. `SA125-CR-ADV-001` low advisory. Review ok; SA121 next.
  - **`SA125-DEC-001` — file line ceilings retired, complexity ceilings kept (2026-07-28; documentation only).** Rationale: `max_lines` are measurement fossils (`loader.py`'s 562 = its size at the commit that happened to be HEAD when the baseline was captured — the mirror of arch Finding 12), tax documentation (the cheapest legal fix deleted ~124 of 204 doc lines incl. the fail-hard contract docstring), run against the project's own direction, and can't distinguish mandated growth from decay. Complexity ceilings kept (local, actionable, ignore docstrings). Implemented: drop `large_files` (41 entries) + blocking enforcement, narrow helper/tests, update `quality_tools.md`, record the rule in decisions.md §file-size-metric-policy; complexity/dead_code/duplication untouched. `SA117-QG-REMED` 5→2; T1: SA125 → SA121 → SA122a.
  - **Roadmap closeout/rebalancing pass (2026-07-27, second; documentation only).** Seven tickets all open; audits re-checked (tech-audit zero open; arch 11/12 live, 7/2/4 unscheduled). Gap fixed: `SA96-PUBLISH` tag → `Tier 1 · Track 3 · v87`. No move: T3 owns the causally serial critical path; T2 closed while `wt-track2` carries `5193f198`+`5b5de830` (a track merges as a branch — landing anything there drags SA115 onto `v87`); T1's file-disjoint tickets, fourth worktree buys no release time. Eleven stale `../others/` audit links fixed; `START_HERE` 375 lines; v88 assign-at-kickoff rule recorded.
  - **SA117a exact-lockstep recorded partial delivery (2026-07-27; task open).** Manifest/running-core versions validated as exact canonical three-component ASCII-decimal strings before tuple comparison (rejects padded/signed/Unicode/malformed/unequal); four-file regression coverage. 42 targeted + all-twelve JUnit proof + 4,606 core/CLI unit + 12/12 parity + lint/typecheck/diff; review resolved `SA117-CR-001`. **Pending:** `SA117A-CR-002` (medium, test gap) — update-rollback regression lacks the `_sync_module_dependencies` spy + `assert_not_called()`. Quality share now only `_perform_module_embed` CC 22>20 (line ceilings retired); with `_update_single_module` 18>17 these are the only quality blockers; no ceiling may be raised. Ledger 2→1 at cap.
  - **SA121 focused continuation recorded partial delivery (2026-07-27; task open).** Corrected `quality_tools.md` on `waiver_evaluations`; expanded strict-key tests; isolated shell success tests from line-count drift; no runtime/baseline/waiver/threshold/Make change. 156/156 focused tests, helper vs `v87`, bash -n, `make check` green. Review `STATUS: partial`: CR-003 (high — triple-colon key orphaned, malformed-envelope matrix incomplete), CR-006 (medium — broad/subset assertions), QG-001 (high — quality exit 2 on pre-existing T3 complexity). Merged by explicit maintainer instruction; not a waiver or approval.
  - **SA117 recorded partial delivery merged to `v87` (2026-07-27; task open, unapproved).** Lockstep candidate `43d9b8fc` (69 files, 8 new `scripts/` helpers) merged as blocked preservation by explicit instruction: stamps twelve modules + bundled manifests to 0.87.0, adds lockstep enforcement and lease-safe split tooling — none independently approved; no source edit or test ran (plan gate partial). Blockers: CR-001 (high — padded versions pass), CR-003 (high — blank direct-CLI origins bypass pre-mutation gate), CR-005 (medium — CLI/help/Make caller contracts), CR-006 (high — lock-drift baseline semantics); plan CONT-PR-003/006. Full-scope executable review required before checking SA117, pushing splits, or starting SA112. SA115 heavy validation frozen (step 3).
  - **Three maintainer decisions recorded (2026-07-27; documentation only).** `SA115-DEC-001` step 3 AUTHORIZED (reversing the 07-26 hold): heavy Docker/PostgreSQL validation proceeds with the `SA115-DEC-002` clamp (`E2E_XDIST_WORKERS=1` when the memory guard fires, visibly); T3 holds priority; validation ≠ merge authorization. `SA117-DEC-004` DEFER (b): scope meta-tooling + `SA117-CR-005` → v88 **SA124**; merged code stays unadvertised, reviewed by SA117e as inert. `SA122-DEC-001`: publish is a full-coverage gate context — the five gates absent from `publish.yml` are a declared-open gap, no exclusions; rule in decisions.md §publish-path-gate-coverage. Ordering tag → splits → PyPI confirmed.
  - **SA121 focused continuation authorized (2026-07-27; `SA121-DEC-001`).** Tests/docs-only cycle scoped to CR-003 (strict-parser coverage), CR-005 (`quality_tools.md` vs code/decisions), CR-006 (exact ordered records/counts/exits); preserve the reviewed runtime delta, leave `quality_baseline.json` untouched, full-scope `STATUS: ok` before close; a runtime change re-opens the decision. T1 has two runnable file-disjoint tickets, taken serially, SA121 first. SA121 open.
  - **SA117 resized from Tier 3 into five serial children (2026-07-27; `SA117-DEC-003`; documentation only).** Monolith failed the tier gate (objective not one sentence; 77 files/7,424 lines vs ~2,000 budget; coupling ≥6; two non-converged plan cycles). Split: 625 lockstep · 4,156 publication tooling · 2,586 meta-tooling · 200 docs. Findings: **a** (`CR-001`, critical path), **b** (`CR-003`/`CONT-PR-003`/`-006`), **c** (`CR-006`), **d** (`CR-005`, off-path), **e** (full-scope review + human-confirmed split push). Children are correction-and-review handoffs over already-merged unapproved code. Critical path `SA117a → b → c → e → SA112a…f → SA96-PUBLISH`; decisions.md Rule 2a (canonical parse; unparseable = hard error); SA117a startable; new decision `SA117-DEC-004`.
  - **Roadmap/audit rebalancing pass (2026-07-27; documentation only).** No ticket completed; `TA62` body/row removed from tech-audit (dated-log-line convention; closure in the SA120 entries; structural cause stays arch Finding 11). Re-homing SA122a onto T2 rejected — `wt-track2` carries `5193f198`+`5b5de830` and a track merges as a branch, so it would drag SA115 onto `v87` in violation of its merge bound; **T2 documented closed to new tickets**. Eleven stale `../others/` audit links fixed; `START_HERE` 375 lines; v88 assign-at-kickoff rule. T1 clean on SA122a; T2 validation-paused under step 3, merge-gated; T3 blocked at SA117 with six findings.
  - **Roadmap/audit closeout pass (2026-07-27; documentation only).** SA120 block removed; intake prose, diagram notes, parallelism bullets, readiness lines reconciled to the three governance tickets (SA121, SA122a, SA122b); `TA62` compacted to a closed summary pointing here; `START_HERE` 365 lines. No state/track/dependency/decision change. T1 next SA121; T2 validation-paused, merge-gated, closed; T3 blocked at SA117 with four carried findings.
  - **SA120 complete — quiet-check frontend-lint parity (2026-07-26).** Normal and quiet `make check` share one guarded `lint-frontend` target; quiet mode = collision-safe temp capture, silent on success, exact single replay + nonzero exit on failure. 79/79 policy + 4,542 unit tests; `make check QUIET=1` green first run; dry-run contains `lint-frontend`. Review `STATUS: ok`, REV-002/003/006 resolved. Checked; SA121 next.
  - **SA120 recorded partial checkpoint (2026-07-26; task open).** Normal/quiet parity with explicit failure propagation and recursive-target policy coverage; 29 focused + 79 full policy tests; QG proved dry-run order and reversible rendered-TypeScript failures. Cap `STATUS: partial`: REV-002 (medium — opposite-tool-present fixtures), REV-003 (medium — exact quiet silence/single replay, collision-safe capture), REV-006 low. No product decision open; not a waiver.
  - **SA112 plan-review non-convergence recorded; umbrella split into six serial handoffs (2026-07-26; no implementation landed).** Two plan cycles confirmed the five-path trigger tuple and `yaml.BaseLoader` contract (`SA112-CR-005` plan-level) and fixed installed `apply` stdin ("n\ny\ny\n"); highest severity stayed high (`SA112-PR-002`) — cap. Uncommitted prototype preserved as evidence in `45e37a86`, neutralized by `56b27045` (zero net delta). Monolith replaced by serial **SA112a–SA112f** (provisioner, traceback, root fix, lifecycle E2E, trigger contract, ordered acceptance), each with own plan/review/merge-back; artifact must not be reconstructed; `SA112-PR-002` discharged by per-task plans.
  - **arch-audit Finding 10 bookkeeping completed (2026-07-26; documentation only).** Closed-status stub for `frontend-source-generation-specialized` removed from live findings, restoring the dated-log-line convention; closure detail in this file (SA104–SA108) and the 07-21/07-23 log entries. No status change; no audit re-run.
  - **SA114/SA116 complete — Track 1 re-verification closed (2026-07-26).** Patch-fed full-scope review `STATUS: ok` (98, zero findings). SA114: ordered chain `make check && make quality && make ci && make ci-e2e` exit 0 (07-25); quarantine empty; 92.88%/94.41%; Core 35/CLI 29 E2E; no source fix. SA116: 1761→1749 comment/docstring-only compaction, baseline 1749 unchanged, quality 0. `SA114-CR-001` resolved by step 3 Keep-unauthorized (consistent authority surfaces); `SA116-CR-002` resolved by patch-fed review (`a58f07e0`: 2 insertions/14 deletions, comments only). SA115 stays validation-paused; T1 removed from open-work roadmap.
  - **SA114/SA116 partial re-review — SA115 authority resolved (2026-07-26).** Re-review returned `STATUS: partial` as previously recorded. Step 3 = **Keep unauthorized**: SA115 heavy validation stays unauthorized until explicit future approval — human-authorization-gated, not SA112-gated. `SA114-CR-001` (medium) resolved by the decision; `SA116-CR-002` (medium, blocking) open — one fresh patch-fed review authorized (plaintext patch or git-show-capable reviewer). Both unchecked; no source changes.
  - **SA115 committed and reconciled with `v87` (2026-07-25; steps 1–2; open, merge queued behind SA112).** Four parked files committed on `wt-track2` (`5193f198`), `v87` merged (`5b5de830`); three additive conflicts kept both sides with `v87`'s column alignment; `git diff v87...HEAD` = 274 insertions/0 deletions across exactly the four files; 31 tests, bash -n, Ruff clean. (a) memory guard demoted lanes while `E2E_XDIST_WORKERS` resolved independently — **resolved by `SA115-DEC-002` (07-25): the guard wins and clamps workers to serial when it fires**, `QS_E2E_NO_MEMORY_GUARD=1` sole opt-out, landing in the validation phase; (b) `SA115-CI-001` — `e2e.yml` trigger paths miss the two test files (same defect class as `SA112-CR-005`). Heavy validation not yet run.
  - **SA114-CR-001 documentation reconciliation (2026-07-25; roadmap cleanup).** Two same-fact scheduling sites reconciled: dependency diagram drops completed T1/T2 rows; SA115 concurrency prose no longer treats SA114's heavy legs as future contention; completed-track narrative compacted to changelog pointers (standing AF7 reference kept). Same-day deep review found three more stale sites (SA114 intro, SA116 present-tense problem statement, SA115 "parked" label) + `START_HERE` 550→280; all corrected. Finding open on its remaining half — full-scope re-review `STATUS: ok`; both unchecked.
  - **SA114/SA116 recorded partial checkpoint (2026-07-25; tasks open).** Ordered chain `make check && make quality && make ci && make ci-e2e` exit 0 from clean wt-track1 on latest v87; quarantine empty; 92.88% core+CLI / 94.41% integration; Core E2E 35, CLI E2E 29; no source fix; `SA114-ORDER-001` satisfied. **Pending:** `SA114-CR-001` (medium, blocking) — final review found stale roadmap diagram and SA115 scheduling references treating completed SA114 work as active; `STATUS: partial`. Post-cap fix/re-review needs explicit user approval.
  - **SA114/SA116 blocked checkpoint (2026-07-23; task open on `SA114-ORDER-001`).** Prior gate-drift fixes reviewed ok, merge-approved; SA116 resolved `SA114-QUALITY-001` via docs-only compaction (1761→1749; baseline unchanged; quality 0); source review ok. **Pending:** `SA114-ORDER-001` (medium, blocking) — the exact ordered chain lacks a final `make ci-e2e` exit status (30-minute agent window exceeded); identical-tree E2E 30/30 supporting only. Continuation: clean rerun with sufficient runtime, all exit 0, quarantine empty, full-scope `STATUS: ok`; then `[x]`. Checkpoint merge user-authorized — not a waiver.
  - **E2E/unit test-infra hardening (2026-07-23; landed on `v87`).** `scripts/test_e2e.sh` gains a low-memory preflight guard (serial Core/CLI fallback below `QS_E2E_MIN_AVAIL_MB` 4096 / `QS_E2E_MIN_SWAP_MB` 3072; bypass `QS_E2E_NO_MEMORY_GUARD=1`) and a `QS_E2E_HEARTBEAT_INTERVAL` (15s) progress heartbeat; `scripts/test_unit.sh` gains parallel-execution and coverage-handling enhancements; documented. Mitigates the `SA114-ORDER-001` failure mode. No product code or gate threshold changed.
  - **Generated-Dockerfile `pnpm install` network resilience (2026-07-24; landed on `v87`; targets SA114-ORDER-001).** Frontend install mirrors the pip house pattern: 3-attempt retry loop plus raised pnpm fetch timeouts (`fetch-retries=5`, `fetch-retry-maxtimeout=120000`, `fetch-timeout=300000`); `test_react_theme_integration` asserts inside the retry loop; SA90 parity fixture rebaselined (3 identical hashes). 257 generator/template/theme tests pass; full `ci-e2e` proof still pending T1 continuation.
  - **Installed-context managed-wiring traceback surface (2026-07-24; landed on `v87`; unblocks SA112 Phase-1 diagnosis).** The `apply` wiring wrapper reduced every exception to `str(exc)`, discarding the traceback — why SA112's `NameError: QUICKSCALE_BILLING_ENABLED` stayed pre-diagnostic (static passes proved the billing adapter uses dict access, which would raise `KeyError`). The wrapper now prints the full traceback to stderr under `QUICKSCALE_DEBUG`; default behavior and failure banner unchanged; SA112's capture becomes `QUICKSCALE_DEBUG=1`. Ruff clean; 65 wiring tests pass.
  - **E2E memory-guard swap-veto correction (2026-07-25; landed on `v87`).** Low `SwapFree` alone no longer vetoes concurrent lanes (idle desktop swap-out is normal, not pressure; observed 14.7 GB `MemAvailable` with 1.7 GB `SwapFree` roughly doubled E2E wall time); the swap check is gated on `MemAvailable` below `QS_E2E_COMFORT_AVAIL_MB` (default 8192); the floor still forces serial; inverted comfort clamped. Fail-hard: non-numeric threshold overrides previously disabled the guard silently; overrides now validated and abort per decisions.md §fail-hard-principle. 9 threshold scenarios exercised; bash -n clean; documented. No product change.
  - **E2E runner provenance banner and serial-cause attribution.** The runner prints a start timestamp and provenance banner (checkout path, HEAD, local changes, out-of-date warning vs `QS_E2E_INTEGRATION_REF`) before any lane; serial runs report their real cause instead of always blaming `QS_E2E_PARALLEL=0`.
  - **E2E heartbeat stuck-lane detection.** The concurrent-lane heartbeat ticks every 60s and reports per-lane silence (`running` vs `running, quiet Nm`) derived from lane-log growth, distinguishing a wedged lane from one doing quiet work.
  - **SA114 Dockerfile QG1 hardening and fixture parity (2026-07-25; landed on `v87`).** `Dockerfile.j2` gains `timeout 600` kill wrapper, `--reporter=append-only`, `rc=$?` capture with `rc=124` = timeout-killed; two Node 24-incompatible pnpm flags removed (string timeouts rejected by `AbortSignal.timeout`); three SA90 hashes refreshed. QG1 at `76f36c83`: check 0 (2,527/1,998), quality 0, ci 0 (92.88%/94.41%), ci-e2e 1 (Core 35; CLI 28 + Docker build timeout at the 300s caller timeout); quarantine empty. Sole executable delta: `Dockerfile.j2` (35 lines); QG2 canceled, its unvalidated mutations discarded. SA114 open on ORDER-001.
  - **SA112 pre-diagnostic plan cycles (2026-07-24; no implementation landed).** A disposable prototype reached installed `apply` through all 12 module embeds before `NameError: QUICKSCALE_BILLING_ENABLED` in managed-wiring regeneration; review rejected it; everything reverted to `v87`. Static passes proved the billing adapter uses dict access (`adapter.py:35`, would raise `KeyError`) — the frame needs a runtime traceback, since provided by the `QUICKSCALE_DEBUG` surface. Cycles resolved `SA112-PR-001/003/004/005`, `CR-001…004/006…008`, `CONT-001`; the authorized blocking-only cycle still partial, leaving `SA112-PR-002` (high) and `CR-005` (medium). Maintainer-authored literal continuation artifact retained on the roadmap.
  - **SA112 plan-cycle status supersession (2026-07-26; task open).** The prior statement that the monolithic artifact remains the next executable specification is superseded: two later plan cycles returned `STATUS: partial` (`SA112-PR-002` high; `SA112-CR-005` plan-level). Prototype `45e37a86` neutralized by `56b27045` (zero net delta). Active work: serial SA112a–SA112f chain, beginning with SA112a's scoped plan-review gate. Status clarification, not completion.
  - **SA115 parked implementation checkpoint (2026-07-23; merge-order-blocked, uncommitted).** On `wt-track2` at `c09e2093`: Phase 1 = session-scoped override of pytest-docker's `docker_compose_project_name` in core conftest (worker-unique name from `QS_E2E_COMPOSE_PROJECT_NAME` + `PYTEST_XDIST_WORKER`, lane prefix kept so `cleanup_scoped_containers` matches); Phase 2 = `QS_E2E_XDIST_WORKERS` in `scripts/test_e2e.sh` (RAM-derived cap, `0`/`1` serial, `>=2` → `-n <N> --dist loadscope`, documented). Harness tests in `scripts/test_e2e_parallel.py`; 16 fixture + 13 harness tests, bash -n, Ruff clean. Four files remain **uncommitted**; SA115 cannot merge until SA112 lands (both edit `scripts/test_e2e.sh`).
  - **Frontend-source de-specialization stage 2a (SA105) — complete (2026-07-20).** Removed `selected_modules`-driven frontend source omission; `App.tsx`/`main.tsx`/`App.test.tsx`/`PublicSocialPages.test.tsx` static verbatim-copy; module/public-social routes use React.lazy/Suspense with fail-closed runtime flag checks. CR-002 resolved via side-effect-free `renderQuickScaleRoot` dispatcher seam (Vitest social true/false/absent); SYNC-001 resolved with static banners on all 10 dormant module surfaces. SA90 final delta: 11 hashes + 2 entries, 0 removed. Validation: 21 targeted + 20 generated Vitest, SA90 6, core unit 2,482, core E2E 35; CLI E2E 8 with two pre-existing failures + one infra timeout (non-causal). Review `STATUS ok` (94); advisory SA105-ADV-001 (`.venv` records excluded from parity).
  - **Frontend-source de-specialization stage 2b (SA106) — identity de-spec complete (2026-07-20).** `useModules.ts`/`Dashboard.tsx`/`Sidebar.tsx` converted from Jinja `.j2` to static verbatim-copy; identity injected at runtime via `window.__QUICKSCALE__.projectName` (default empty). SA90 fixture: 3 hashes × 3 variants. Two-project proof: zero identity-bearing `frontend/src` differences. Contract-test fixes + beta-migration taxonomy updated (`renderQuickScaleRoot.tsx`/`RootDispatch.test.tsx` as `INTENTIONALLY_UNMANAGED`); frontend-proof and `make check` green. QG-SA106-001/002 resolved; advisory CR-SA106-002 (no durable single-test pin; deferred).
   - **Fail-hard runtime-seam validation (SA107) — complete (2026-07-20).** Added `src/lib/validateQuickScaleSeam.ts`: every load-bearing `window.__QUICKSCALE__` read passes `validateQuickScaleConfig()` (seam, non-empty `projectName`, strict-boolean `modules`, optional `publicPage`); silent fallbacks removed from `resolveProjectConfig()`/`renderQuickScaleRoot()`/`main.tsx`; `defaultConfig` gone. Public-social `modulePaths`/`owner`/`publicPage` enums validated. 16 seam + 3 boot tests; SA90 +2 entries/3 hashes. Pre/post-sync gates green (lint-frontend, frontend-proof, core 2,512 @ 90.46%, Vitest 35/35, isolated render+build); one post-sync frontend-proof render timeout noted, no regression. Review `STATUS ok`; CR-001/002, TEST-001, DOC-001 resolved.
  - **Tenant isolation unified.** Every tenant-scoped model inherits one shared isolation base instead of hand-copied boilerplate; a project-wide check requires each model to be explicitly tenant-scoped or excluded and is wired into generated projects' own CI.
  - **Fail-closed database access.** The app refuses to boot under a database role that bypasses row-level security (`BYPASSRLS` or `SUPERUSER`) unless explicitly overridden; production requires a restricted tenant-safe role, with the elevated role reserved for migrations.
  - **Database-level parent/child isolation.** Parent/child records (e.g. a CRM contact and its notes) are kept in the same organization via `NOT DEFERRABLE` composite foreign-key constraints, replacing the fragile trigger-based check; a conformance gate verifies every composite FK.
  - **Fixed a row-level-security bug affecting pooled database connections**, and removed redundant per-statement tenant-context setup within a transaction.
  - **Public `org_scope` API.** Internal org-context primitives are retired in favor of one public `org_scope()` seam used everywhere, with a canonical `PublicSystemOrgReadMixin` for anonymous public reads and a lint gate enforcing the boundary.
  - **Operator tooling.** Admins can "view as" a specific organization from the admin panel (visible banner, one-click exit); a single audited `operator_access()` path gates cross-tenant reads and sensitive maintenance commands, logging who ran what and why.
  - **`TenantModelAdmin` base.** An orgs-owned admin base resolves the active org and scopes every admin view through `org_scope()`, fail-closed; every module admin is ported onto it, removing hand-copied scoping and inline formsets.
  - **Fail-hard configuration sweep.** Missing or invalid module settings (page sizes, rate limits, feature flags, provider/enabled flags, proxy config, storage backend, orgs mode) now raise at boot or apply time instead of silently defaulting or coercing; validators are wired into the apply path and the dead coercions they masked are removed.
  - **`QUICKSCALE_MODE` fails hard at boot** instead of defaulting to `solo`, and legacy `quickscale.yml` keys are rejected with a named-replacement error instead of being silently translated.
  - **Public-content sanitization.** Markdown-rendered links on public blog/listing pages are run through an allowlist URI-scheme sanitizer (blocking `javascript:` and obfuscated schemes per the WHATWG URL spec); the analytics template-tag payload is escaped before `mark_safe`; and orgs debug-view `next` redirects are validated same-host/scheme.
  - **Non-string public form submissions** return validation errors instead of 500ing.
  - **Client-IP and cache infrastructure.** Generated projects gain a canonical `get_client_ip()` helper with trusted-proxy config (matching DRF's proxy-count semantics) and a shared `CACHES` backend (Redis when available, else `DatabaseCache` with a deploy-script `createcachetable` step); forms/blog throttles and IP logging point at the shared seam.
  - **Cross-org account-deletion safety.** User FKs on org content are `SET_NULL`/`PROTECT` (never `CASCADE`) so deleting a user no longer destroys content across organizations, enforced by a conformance gate.
  - **Last-owner invariant.** One canonical last-owner deletion-blocking check (with `select_for_update` concurrency protection and a `pre_delete` cascade backstop) replaces three divergent copies and is enforced at the account-deletion boundary.
  - **Secrets kept out of logs and argv.** The deploy script prints per-variable set/`MISSING` status instead of raw values; Railway/DR adapter secrets are piped via stdin instead of command-line arguments, with a hard-stop rather than an insecure fallback.
  - **Billing ownership and integrity.** Subscriptions are authoritatively owned by the organization; a partial unique constraint enforces credit-ledger idempotency at the database level; account-deletion cleanup distinguishes a benign "nothing to cancel" from an anomalous subscription missing its Stripe id; and billing's state-changing endpoints move onto the DRF baseline (automatic CSRF) with a decisions-doc rule naming the two sanctioned JSON-API bases.
  - **Backups and disaster recovery.** Admin backup create/prune/restore run off the synchronous request path via background subprocess with atomic claiming; the uploaded-restore copy is crash-safe; stranded `STATUS_RESTORING` artifacts are detected and recoverable; DR media-backend resolution fails hard on real misconfiguration instead of silently falling back to local storage; and a `csrf_exempt` CI gate requires every callsite to pair with verification.
  - **Core/module boundary.** A single `quickscale_core.runtime` facade is the only public core import surface (import-linter enforced); a Django-free persistence protocol lets backups/DR inject model access; managed adapters register explicitly instead of at import time; and a bidirectional import ban keeps core from importing modules.
  - **Resilient project generation.** `apply` checkpoints progress after each step and resumes on failure; destructive or remote actions require explicit confirmation; `apply --force` stages to a backup directory with full rollback; module `pyproject` splices route through a validated TOML writer; and generator internals are split into smaller focused modules.
  - **Declarative module configuration.** The `module.yml` derivation loader is proven by round-trip tests, listings is fully migrated off imperative wiring, and a guardrail blocks any module from reintroducing it; a manifest-sync gate fails on drift between each module's `module.yml` and its core snapshot.
  - **Contract-version tracking.** Generated projects record the contract version they were built against, a compatibility gate probes each module against its claimed minimum core version, and `quickscale status` flags modules ahead of the project's contract.
  - **Retired the `showcase_html` generator theme** in favor of React-only output; existing generated projects keep their user-owned files.
  - **Generator emission mapping** is exported as a single authoritative function shared by production and its beta-migration conformance gate.
  - **Cross-module signal decoupling.** Orgs no longer reverse-imports CRM/auth: a `organization_created` signal drives CRM's default-stage seeding (self-sufficient under restricted roles), the auth adapter fails hard when the module is absent, and version-floor constraints on `required_modules` are enforced at apply time.
  - **PostgreSQL-only test infrastructure.** All modules test exclusively against PostgreSQL (SQLite removed everywhere) under a shared pre-provisioned restricted (`NOBYPASSRLS`) role; a dedicated integration gate proves tenant isolation under that role and is structured so it cannot pass by silently skipping.
  - **Parallel integration execution.** The per-module integration loop runs as a configurable parallel worker pool with isolated coverage/log state, joined failure propagation, deterministic replay, and signal-safe cleanup, matching sequential verdicts and coverage.
  - **Untracked accreting build/test artifacts** (coverage/log files, blog test-upload media) and pointed test `MEDIA_ROOT` at a temp directory.
  - **Migration squash (SA92).** All nine modules squashed to final-schema `0001_initial` (`organization_id NOT NULL` from row zero); fresh-only posture ratified with pg_policies/catalog/data hash parity against v87; Finding 8 (module-RLS-context-procedural) closed.
  - **DR persistence port (SA89a+SA89b).** Backups ORM lifecycle moved behind a fail-hard provider registry in `dr_engine/persistence.py`; bidirectional core↔module import ban enforced; Finding 1 (circular lattice) closed.
  - **Parallel integration worker pool (SA91).** Per-module integration loop runs as a configurable parallel worker pool (`QS_INTEGRATION_JOBS`) with isolated coverage/log state, joined failure propagation, and signal-safe cleanup.
  - **Per-module restricted-role gates closed under SA82 baseline:** CRM (SA84, 263 pass/21 skip/0 fail), listings (SA86), blog (SA95, non-reproducible post-SA92), forms (SA85), orgs (SA77), notifications (SA79). `make test-integration` green with empty quarantine.
  - **Pre-publish module sweeps (SA96-T1/T2).** All 12 modules re-verified green in isolation on post-SA92 v87 — no regression, empty quarantine.
  - **React-only theme retirement (SA94).** `showcase_html` generator theme retired; fail-closed `theme_validation` preflight wired into all 8 CLI callsites; generated project output is now React-only.
  - **Quality baseline established (GATE-quality).** Makefile `quality` target gates complexity/dead-code against `quality_baseline.json`; baseline reset for v87.
  - **SA97 — shared tenant test-state reset fixture.** Consolidated six divergent private copies into one conftest-importable autouse fixture in `tests_shared/reset_state.py` (resets org ContextVar, PostgreSQL GUCs, AF9 priming memo, Django cache before/after every test; all Django imports deferred, safe for managed and self-managed conftests). Commons ownership rule recorded in decisions.md §SA97 (orgs owns runtime helpers; `tests_shared/` owns cross-module plumbing). No private copy remains. Review pass 2 `STATUS ok`, CR-SA97-001 resolved; `make test-integration` 12/12, empty quarantine, 94.39% mean. Advisories CR-SA97-002/-003 low.
  - <a id="sa98"></a>**SA98 — shared rendered-link sanitizer closeout.** `_sanitize_href`/`_sanitize_rendered_html` moved into the orgs-owned shared seam; blog/listings switched; no second runtime copy. Evidence: orgs 874/11, blog 206, listings 132; scoped Ruff/MyPy, uniqueness/caller proof, diff, manifest sync passed; review `STATUS ok`, no findings. Broad baseline failures unrelated/outside ticket; no E2E claim.
  - **SA99 — `quickscale_devtools` brought under repo ruff/mypy coverage.** Added to `ruff.toml` known-first-party and Makefile lint/lint-fix/format/typecheck loops (new `devtools` section, `--devtools`/`-d` flag, in default `ACTIVE_SECTIONS`/`SRC_DIRS`); devtools passes strict global mypy defaults. CR-SA99-001 (stale default test dispatch)/002/003 resolved; review pass 2 `STATUS ok`. Finding 7 tuple-derivation remainder stays unscheduled.
  - **SA102 — CI lint/typecheck devtools coverage (closes tech-audit TA61).** Explicit `--devtools` added to all ten scoped invocations across `ci.yml`, `publish.yml`, `scripts/check_ci_locally.sh`; original five-site inventory discrepancy corrected; aggregate and CLI-scoped checks passed; a temporary devtools Ruff failure probe was rejected and removed.
  - **SA103 — blocking rendered-frontend proof gate (2026-07-19; complete).** Added the blocking `lint-frontend` workflow job (Node/pnpm/cache), wired local `make ci`/`check_ci_locally.sh` frontend-lint fan-out with an absent-Node/pnpm guard that does not mask real failures, and made `publish.yml` run `frontend-proof` before build/publish. Injected TS error made `make ci` exit 2 (`TS2322`/`TS6133`); `Dashboard.tsx.j2` restored byte-identically. QG1 (15 tests; bounded `PYTEST_XDIST_WORKERS=2 make ci`: 4,536 core/CLI, 323 backups, 92.88% mean, all ≥80%) and QG2 (19 + six delta tests; Ruff/MyPy/YAML/Bash/Make/diff) passed. Review pass 2 `STATUS: ok`; SA103-REV-001/-002, SA103-ADV-001 resolved. Hosted GHA not remotely observed (non-blocking).
  - <a id="sa104"></a>**SA104 — Frontend static-source de-specialization stage 1 (2026-07-19; complete).** 57 byte-static theme files (incl. theme-root `frontend/index.html`) moved onto the verbatim-copy path (`.j2`/`{% raw %}` removed); 13 genuine active-Jinja files remain; emitted bytes unchanged (SA90 fixture green, two-project `frontend/src` diff zero, new routing regression test). QG 2/2: Ruff/MyPy, SA90 20/20, React 51/51, zero unexpected diffs across 61 files, runtime 5/5, frontend proof/lint, test-unit 4,482. Review pass 2 `STATUS ok`; SA104-CR-001 fixed/resolved; Caller-Parity PASS. Intake moved to root `adaptive.intake.yml`; SA105 blocked on its own decision, SA106 executable. Advisory SA104-ADV-001 (medium — generated `.pre-commit-config.yaml.j2` still runs strict `check-json` on `frontend/tsconfig.json`; deferred).
  - **SA100 — recovery-ledger theme exemption narrowed (complete).** TA58 now uses an explicit validator opt-in permitting only the recovery ledger's `__checkpoint__` placeholder; stale/retired recovery themes retain fail-closed remediation; TA59's dead `_RECOVERY_PROBE_PATHS` constant and comment typos removed. Review pass 1 `STATUS ok`, no findings, caller parity passed; 53 targeted tests, Ruff, diff green. Blockers: none.
  - **TP2 — unit-suite xdist (2026-07-18; review pass 1 STATUS ok).** Added `pytest-xdist==3.8.0`; `make test-unit` uses `-n auto --dist loadfile` by default, `PYTEST_XDIST_WORKERS=0` serial, `=N` pins. Serial/xdist collection sets identical (core 2,354; CLI 1,993); CLI 1,993 passed both modes (56.36s → 33.44s); core 2,353+1 skip (176.03s → 68.40s). Pre-existing core 90% floor below unchanged 80.39% measurement — waived, non-blocking; no floor or test changed.
  - **TP2b — xdist-safe coverage phases (2026-07-19; complete).** `make test-cov` uses isolated `COVERAGE_FILE` phases, one explicit `coverage combine` pipeline, guarded xdist defaults, serial override, preserving backups-requirement behavior; both exact commands exit 0 — 4,536 core/CLI + 323 backups passed, 92.88% equal-weight mean (core 95.27%, CLI 90.49%), all 90 files ≥80%, 24 auto workers. CR-TP2B-001/-002, QG-TP2B-001 resolved; review `STATUS ok` (94); prior auto-worker lock contention a non-blocking advisory.
  - <a id="sa93-continuation"></a>**SA93 — E2E green-gate closeout (2026-07-18).** Canonical local `make ci-e2e` green (12 stages, Core 35, CLI 29, 12 integration modules, 91.90% mean coverage, all files ≥80%, quarantine empty). Initial hosted run aborted (workflow supplied one apply confirmation, CLI required two); prompt-parity fix `667396cc` (`y\ny\n`); hosted rerun [SA93-EVID-001](https://github.com/Experto-AI/quickscale/actions/runs/29641954573) verified `conclusion=success` for Test Generated Project Docker Build and Full E2E (PostgreSQL + Playwright) at `headSha=667396cc44ac…`. Hosted evidence owns only those two jobs — does not imply hosted `make ci-e2e`. SA93-REV-005 advisory/out-of-scope; SA93-ADV-001..004 advisory.
  - **TP1 — parallel local-CI static gates.** `scripts/check_ci_locally.sh` launches stages 2–9 concurrently, buffers/replays in declaration order, attributes failed gates, preserves `QS_CI_PARALLEL=0`; DB/E2E stages serial. Maintained `test-ci-local-parallel` target (concurrency, attribution, serial parity, TERM/INT/HUP cleanup) runs in `make ci` and GitHub CI. Same-host timing 539.70s vs 550.96s serial. Final `make ci` passed (14 TP1 + 4,395 core/CLI + 323 backups + all integration suites, 91.90% floor). CR-TP1-REV-001/002/003 resolved; review pass 3 `STATUS ok`.
  - **SA96-GATE blocked checkpoint (2026-07-19; not complete).** Five new test modules only (DR orchestration a2a/a2b/a3, manifest-resolver phase-2); coverage 2,480 passed/1 skip/90 deselected = 90.34%, orchestration 745/1,011 = 73.69% (+35); 127 focused tests, Ruff/MyPy, `make check` 0 in 3m35s with 94.41% integration mean — resolves `SA96-GATE-BLK-001`. **Pending:** `SA96-GATE-BLK-002` — `make quality` exit 2, 9 critical + 10 warning regressions at `quality_report.md:32-52`. `make ci`/`ci-e2e` not run; review pass 2 `STATUS ok` (SA96-CR-001 blocking + CR-002 advisory resolved); SA96-GATE unchecked; no source/config/baseline changes.

  - **SA96-GATE — green-gate join complete (2026-07-20).** All four commands exit 0 on a clean rerun at the post-SA92 baseline with `QUARANTINE_TICKETS` empty: `make check` 0; `make quality` 0 (zero regressions); `make ci` 0 (92.89% mean, all 90 files ≥80%); `make ci-e2e` 0 (64 E2E passed); integration 2,262 passed, 94.41% mean. Resolved: an orphan double-prefixed `test_test_quickscale_orgs` mirror DB dropped; no repository defect or threshold change. Later installed-context work added SA111a/SA112 — SA111a complete, SA112 next; **SA96-PUBLISH** remains human-only, held until SA112 closes.

  - <a id="sa109"></a>**SA109 — Fix installed-wheel module discovery (2026-07-20; complete).** Discovery only consulted the monorepo workspace and raised `ImproperlyConfigured` elsewhere — wheels crashed on import. Phase 1: `discover_bundled_module_names()`, `get_bundled_manifests_path()`, `get_resolution_source()`, `ModuleResolutionSource` enum + bundled snapshots (12 modules). Phase 2: `get_discovered_module_names()` resolves source → bundled → fail hard (three tiers; replaces import-time eager discovery in `config_schema.py`/`module_commands.py`). Phase 3: AF7 decision recorded in decisions.md (was docstring-only); source-required ops stay fail-hard. Validation: 327 AF7 tests, 2,505 core, 1,997 CLI (one pre-existing failure); `make install` smoke exit 0 outside the source tree (12 bundled names). Next: **SA110** (deps SA109 ✓).

  - <a id="sa110"></a>**SA110 — Installed-artifact smoke gate (2026-07-20; complete).** QG proofs: REV-003 (negative SA109 regression — gate red with `ImproperlyConfigured`, not `ImportError`) and REV-004 (deterministic ordering, 7/7). Adds `scripts/smoke_install.sh`: builds all three wheels from per-run staged copies (never mutates source pyproject.toml), installs into a throwaway venv **outside the source tree**, runs 19 probes with sanitized PYTHONPATH/PYTHONHOME (18 version/help probes exit 0; the 19th asserts outside-project `status` exits 1 with the expected diagnostic, by explicit user decision). `QS_SMOKE_REGRESSION_CORE` enables disposable modified-core negative testing. Advisory SA110-ADV-001: actual 3.14/3.15 binaries unavailable — version-reporting shims; hosted evidence deferred. No package API, CLI behavior, or version changed.

  - **SA108 — Frontend de-specialization migration-doc rewrite (2026-07-21; complete).** Corrected `docs/planning/beta-site-migration.md` to the project-agnostic byte-identical `frontend/src` contract: removed obsolete identity-fix steps, dropped overbroad page-copy claims (dormant files are fresh-recipient-only; no retroactive guarantee), corrected the validator name to `validateQuickScaleConfig()`, qualified main.tsx byte-identity as version-dependent, clarified routing boundaries. Documented: pre-SA105 recipients have no retroactive dormant guarantee; adoption copies only missing forms/social surfaces, never backfills blog/crm/listings. Track 2 frontend de-specialization (arch Finding 10) fully closed; roadmap `- [x]`.

  - <a id="sa113"></a>**SA113 — Installed-context implication-resolver bundled-manifest fallback with fail-hard inventory validation (2026-07-21; complete).** `resolve_module_implications` crashed with `ImproperlyConfigured` in installed contexts (both callers via `_materialize_implied_module_configs`). Phase 1 mirrors SA109: falls back to `get_bundled_manifests_path()`. Phase 2: missing selected/implied manifests raise, never silently skip; both-unavailable still raises. 7 focused tests + 30 existing + targeted 37 passed; roadmap `- [x]`; all CR findings resolved.

  - **SA111a — Installed-context `plan` coverage complete (2026-07-22).** Extended `scripts/smoke_install.sh` with a 20th non-interactive `quickscale plan` probe (all 12 modules via scripted stdin, outside the source tree). Exposed a second installed crash in `_load_notifications_manifest`; the loader now follows the SA113/AF7 fallback pattern. Two regression tests cover both option callers; probe unsets inherited PYTHONPATH/PYTHONHOME; a contaminated-PYTHONPATH run passed all 20 probes. Ruff, MyPy, 78 resolver tests; review resolved SA111A-CR-001, approved completion. Broad core suite kept 14 unrelated baseline failures, 2,562 passing at 90.48%.

  - **SA111b — Fast in-monorepo fallback regression test (2026-07-23; complete).** `TestSa111bFallbackRegression::test_fallback_resolves_billing_to_orgs` monkeypatches `get_modules_base_path` to raise and asserts billing → orgs resolution via the bundled fallback; fast early-signal companion to SA111a's wheel probe inside `make check`. Focused 1/1; SA113 tests unchanged 7/7; targeted suite 38 passed; roadmap `- [x]`.

  - **SA101 quality-remediation closeout (2026-07-19; complete).** Cleared all 19 `SA96-GATE-BLK-002` regressions (9 critical + 10 warning) against the **unchanged** `quality_baseline.json` per Option A — no baseline/threshold/gate edits. Nine serial behavior-preserving phases on Track 3 from `d7ccf24`, plan `SA101-plan-r2`, plan-review `STATUS: ok` after PR-001/002/003. Ledger 19 → … → 0; refactors file-local with identity aliases; Phase 9 compacted CRM 605→413 and forms 758→137 migration lines with exact SHA-256 parity. `make quality` exit 0; shrink-only maxima policy recorded in decisions.md §SA92(§5); migrations remediated, not allowlisted.
    - **`SA101-VAL-001` — RESOLVED via option (a) (2026-07-19 experiment).** Restricted-role failures reproduced on clean pre-SA101 (`d7ccf24b`) and confirmed pre-existing: CRM 3 failed/281 passed/3 errors pre-SA101 vs 284/0/0 at HEAD; forms green both states (212). SA101's Phase 9 `crm/tests/test_migrations.py` rewrite fixed the pre-existing fixture defect. No maintainer decision remains.
    - **Final closeout (2026-07-19).** `make check` exit 0 — 6,943 passed/86 skipped/12 deselected, zero failures; core 90.36%, CLI 90.49%, integration 94.41%. Full change-review `STATUS: ok` (94), no findings; worktree clean; BYPASSRLS skips pre-existing; `make ci`/`ci-e2e` not run. SA101's feeder condition for SA96-GATE met; four-command join next.
  - **TP3a implementation (completed).** E2E harness allocates a per-run app port, exports lane-specific Compose/container scopes, limits cleanup to that scope; CLI fixtures consume the allocated port and generated-project prefix. Review pass 2 `STATUS ok`, CR-TP3A-001 resolved. Merged-v87 rerun passed stages 1–11 (91.90% mean, all ≥80%) but the final CLI E2E stage exceeded the runner allowance — integrated `make ci-e2e` green not established; carried as a TP3b precondition. Advisories CR-TP3A-002 (port TOCTOU)/-003 (scope leading char) open.
  - **TP3b — concurrent Core/CLI E2E lanes (completed).** Concurrent by default (`QS_E2E_PARALLEL=0` for serial); isolated scopes/ports, deterministic log replay, signal-safe cleanup with active-PID handling, nonzero failure attribution, preserved flags; 47-test harness maintained. TP3B-CR-001/002/003 and QG-TP3B-001 resolved. Final evidence: registry 20, harness 47, coverage 91.90% (84 files ≥80%), integration 2,462, Core E2E 35 in 317.02s, CLI E2E 29 in 816.51s, all 12 stages green; concurrent path 816.51s vs 1,188.44s serial (~31.3% faster). One deprecation warning and expected skips non-blocking.
  - **TP4 — Fast feedback loop documentation (2026-07-20; complete).** Added the iterate → pre-merge → E2E-last ladder to `docs/technical/development.md` with section flags (`make lint -- --core`, `make typecheck -- --cli`, `make test-unit -- --core`), single-module reruns (`QS_ORGS_DB_USER=quickscale_test_role make MODULE=orgs test -- --modules`), bounded integration concurrency (`QS_INTEGRATION_JOBS=2`), cross-referencing TP1/TP2/TP3b knobs. Verified: lint/typecheck/test-unit (2,489) exit 0, orgs module run 0 (874/11); `QS_INTEGRATION_JOBS=2 make test-integration` failed once (`test_debug_org_overrides_saas_session` — "server closed the connection unexpectedly") while the serial rerun passed (94.41% mean) — unresolved non-blocking integration risk. Roadmap TP4 `- [x]`; T1 TP suite closed; docs-only, no gate behavior changed.

- v0.86.0 — Organizations Module (multi-tenancy with Solo/SaaS runtime modes, org-scoped RBAC and invitation flow, billing bridge with authoritative org ownership fields and `Plan.features` feature gates, `migrate_billing_to_orgs` and `promote_to_saas` management commands, React org management pages and org switcher, and billing wiring regression fix with planner/apply guard test)
- v0.85.0 — Billing Module (Stripe-backed one-time credit purchases and recurring subscriptions, credits-first Django-owned ledger, planner/apply readiness with auth-module dependency enforcement, module-owned `/billing/pricing/` and `/billing/dashboard/` pages, and starter-theme billing links)
- v0.84.0 — Backups Hardening Release (admin backup artifact download/create/integrity actions, upload-driven restore safety and compatibility checks, full-snapshot backup-contract hardening, and repo-owned runtime/tooling support alignment for the refreshed Python/frontend baseline)
- v0.83.0 — Hardening Release (repo-wide hardening across strict desired-config validation, notifications live-config placeholder rejection, generated production `SECRET_KEY` fail-hard behavior, CRM staff-only parity, forms CSV and backup artifact safety, blog upload/thumbnail/rate-limit hardening)
- v0.82.0 — Disaster Recovery & Environment Promotion Workflows (public `quickscale dr capture/plan/execute/report` workflows for local and Railway routes with `snapshot_id`-based stored snapshots, resumable capture and execute, rollback pins for production routes, conservative env-var sync, and source-side media sync)
- v0.81.0 — Beta-Site Migration Maintainer Tooling (maintainer-only Make/Python workflows for beta-site catch-up: fresh-first and in-place paths with checkpoint-first semantics)
- v0.80.0 — Analytics Module (PostHog-only website analytics with flat `QUICKSCALE_ANALYTICS_*` settings, service-style capture helpers, apply-time env-example sync, guarded forms integration, QuickScale-owned social click tracking)
- v0.79.0 — Social & Link Tree Module (curated social links and embeds, backend-owned YouTube/TikTok preview metadata, managed `/_quickscale/social/` integration endpoints, Django-owned public React pages)
- v0.78.0 — Notifications Module (transactional email foundation, Anymail-backed Resend delivery, recipient-granular tracking, signed delivery webhooks)
- v0.77.0 — internal main-branch baseline — Backups Module (private database backups, optional private remote offload, guarded CLI restore, scheduler-ready command hooks)
- v0.76.0 — Storage Module (cloud file hosting, media storage adapters, CDN-ready media infrastructure)
- v0.75.0 — Forms Module (generic customizable form builder with admin, DRF API, spam protection, GDPR anonymization, and React mount point)
- v0.74.0 — React Default Theme (showcase_react theme with Vite, TypeScript, TanStack Query, and Zustand)
- v0.73.0 — CRM Module (API-first Django CRM with 7 core models and CLI integration)
- v0.72.0 — Plan/Apply Cleanup (removed legacy init/embed commands, full transition to plan/apply)
- v0.71.0 — Module manifests & config mutability (Plan/Apply system complete)
- v0.70.0 — Existing project support (status, plan --add, plan --reconfigure)
- v0.69.0 — State management and incremental applies
- v0.68.0 — Plan/Apply System core commands (Terraform-style declarative workflow)
- v0.67.0 — Listings module with AbstractListing base model for verticals
- v0.66.0 — Blog module with Markdown, featured images, and RSS feeds
- v0.65.0 — Enhanced auth module and development tooling
- v0.64.0 — Theme rename to showcase_* (breaking change)
- v0.63.0 — Authentication Module with django-allauth and interactive embed
- v0.62.0 — Split Branch Infrastructure (module management CLI commands, GitHub Actions automation)
- v0.61.0 — Theme System Foundation (--theme CLI flag, theme abstraction layer, HTML theme)
- v0.60.0 — Railway Deployment Support (automated deployment via quickscale deploy railway)
- v0.59.0 — CLI Development Commands (Docker/Django operation wrappers)
- v0.58.0 — Comprehensive E2E testing infrastructure with Playwright and PostgreSQL
- v0.57.0 — Production-ready generator baseline
- v0.56.0 — Quality, Testing & CI/CD
- v0.55.0 — CLI implementation
- v0.54.0 — Project Generator
- v0.53.3 — Project Metadata & DevOps Templates
- v0.53.2 — Templates and Static Files
- v0.53.1 — Core Django Project Templates
- v0.52.0 — Project Foundation
- v0.51.0 — Documentation foundation
- v0.41.0 and earlier — legacy codebase (see Github repository for history)
