# Release v0.88.0 - Request Identity and Manifest-Driven Modules

**Date:** 2026-09-12
**Status:** Prepared release artifact — the `0.88.0` tag, its GitHub release, and the PyPI distributions do not exist yet.

## Summary

v0.88.0 makes two things true that were previously only conventional. First, a generated project now decides *who a request is from* in exactly one place: one canonical client identity, computed behind validated proxy configuration and used by throttles, CSRF, and IP logging alike. Second, a module's Django wiring is declared in its manifest rather than assembled by imperative code, so what a module contributes is readable in one file and asserted against the running core.

For adopters, the consequential change is the Python floor moving to 3.14 and the fact that generated projects gain a first-position request-identity middleware. Pre-1.0 upgrades remain a clean break: regenerate and apply against a fresh database rather than migrating in place.

**Related docs:** [Changelog](../../CHANGELOG.md) | [Roadmap](../technical/roadmap.md) | [Technical Decisions](../technical/decisions.md)

## Highlights

- **One canonical request identity.** Generated projects resolve the client identity once, behind proxy configuration that is validated before use; DRF throttling, the custom blog limiter, form persistence, and CSRF all read that same seam instead of deriving it independently.
- **Modules declare their Django wiring in their manifest.** App declarations moved into `module.yml`, adapters live inside their own module packages, and the imperative wiring they replaced is gone — a large net deletion rather than a new layer.
- **Tenant isolation enrollment is idempotent and metadata-derived.** Applying FORCE RLS twice is safe, table names come from each model's own metadata, and the operator-read/tenant-write policy split is preserved and proven.
- **Backup and recovery locking is correct under contention.** The advisory lock is inode-bound with atomic stale reclamation and acquisition-bound release identity.
- **One authoritative CI database environment.** PostgreSQL provisioning is a single contract with five profiles consumed by every station, replacing four divergent hand-rolled variants.

## What's New

### Features

- **`ClientIdentityMiddleware` in generated projects**: a first-position middleware aligns ordinary DRF request identity with the canonical resolver, so a short forwarded chain cannot be trusted by one consumer and rejected by another. Proxy configuration is validated before any mutation and the production proxy count is derived rather than guessed.
- **Manifest-owned app declarations**: `module.yml` now carries each module's Django app declaration, checked by a dedicated gate. Module adapters were relocated into their own packages alongside the manifests that describe them.
- **Three-state module presence in `quickscale status`**: a module is reported as present, absent, or placeholder rather than collapsed into a true/false guess.
- **Commit-testimony gate**: any non-merge commit touching a workflow, the gate registry, or a provisioning station must carry a `vNN` roadmap reference, bare (`v88`) or dotted (`v0.88.0`); a ticket id or changelog entry does not satisfy it. A `commit-msg` hook rejects it while rewording is still free, and CI re-checks the range.
- **Security and interpreter gates**: a consolidated security static-analysis gate with recorded, expiring suppressions, and a guard that keeps repository tooling on the project interpreter.
- **Local CI stage selection and failure replay**: run part of the pipeline with `ONLY=`/`FROM=`/`SKIP_INSTALL=`, and replay just what failed. Partial runs announce themselves and never count as a full pass.

### Improvements

- **Idempotent RLS enrollment** — the forward template drops and recreates both named policies, verified by a live-PostgreSQL regression that applies it twice.
- **Frontend CSRF transport** — token handling in the React starter is hardened against malformed and duplicate cookies, with dedicated coverage.
- **E2E lifecycle** — content-addressed backend image identity with measured cold-versus-warm reuse, run-scoped resource labels, retention-aware teardown, and exact-label cleanup that leaves no stray containers, volumes, or networks.
- **Installed-wheel proof** — a source-free, all-module `plan → apply → up` lifecycle runs from an external working directory against current artifacts.
- **Quality and gate hygiene** — a merge-base monotonicity gate for the quality baseline, parallelised gate suites, and a closed-universe check that makes an unregistered CI job a red build.

## Breaking Changes

- **Python floor raised to 3.14** (`>=3.14,<3.15`) for the repository and for generated projects. Django stays at `>=6.0.7,<6.1.0` and PostgreSQL at 18.
- **Generated projects gain a first-position request-identity middleware** and recomputed proxy settings. Projects generated by an earlier version do not receive this by upgrade; regenerate to adopt it.
- **Embedded module manifests must match the running core exactly.** A version mismatch is a hard, named error instead of a downstream failure. Modules remain versioned in lockstep with the release.

## Migration Guide

1. Upgrade the toolchain to Python 3.14 before regenerating.
2. Run `quickscale plan` and review the diff, then `quickscale apply`. Manifest-derived wiring is regenerated from the module manifests during apply.
3. Apply against a fresh database. Pre-1.0 upgrades are a clean break with no in-place migration path.
4. If you configured trusted proxies by hand, re-check that configuration — it is now validated at boot and an invalid combination raises instead of being silently accepted.

## Validation

- ✅ The integrated pre-release tip passed the full twelve-stage release aggregate, including restricted-role integration, module integration coverage, and concurrent end-to-end lanes with exact-label cleanup.
- ⚠️ This release-prepared state has **not** yet been validated at release tier: the version bump changed repository bytes after that verdict, so the aggregate is owed again before publication.

## Validation Commands

```bash
QS_E2E_INTEGRATION_REF=v88 make ci-e2e
make release-gate
```

## Deferred Follow-up

- The first property portal on a project-owned extension, and the gate-parity, migration-mode, and RLS-predicate verification work, are scheduled in the [roadmap](../technical/roadmap.md).
