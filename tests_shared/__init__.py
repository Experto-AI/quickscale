"""
Shared test helpers for cross-module test reuse.

Phase 14.1 of the roadmap extracts reusable isolation assertions from
module-specific inline probes so that every tenant module can assert the
same cross-tenant contract using a single shared helper.
"""
